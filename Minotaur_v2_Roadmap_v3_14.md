# 🗺️ ROADMAP — Minotaur v2 (v3.14)

**Version 3.14 • August 18, 2026 • Forward-only file — history lives in `Minotaur_v2_Ledger_v1_0.md`**

*(v3.14, Aug 18: **DOOR + LANDING CLOSED ENTIRE at `e92f2c9`** — the signed-out root is a landing page with waitlist capture, `/auth/signup` is a deliberate closed door with **no form**, and `/api/waitlist` writes through the `SECURITY DEFINER` RPC on the anon key with **no service-role client in `src/`**. Four rounds folded into one commit (11 files, 757 insertions, 163 deletions, no trailers). Suite 1611/105 → **1639/106**; visual **28/28 at zero tolerance on two consecutive rounds**, **zero baselines written and nothing declared**; migration ledger **50**, no new schema in the unit. **Three further rulings — R-S/R-T/R-U** — the last replacing the landing copy wholesale on Daniel's word. **DOOR-SEO's propagation is DISCHARGED**, and the propagation list itself was wrong: it named five documents, but two entries are the same file and one is immutable — **three mutable files**, corrected 2026-08-18. **R-V** schedules the harness line behind the box unit, the v1 import/export work and SHARE; **R-W** deletes and rewrites the Comms Prompt rather than amending it. New: **GATE-THEMERACE · GATE-COUNTWAIT · GATE-FIXDIRTY · GATE-LOADPROXY · VIS-ENVFILE · DOOR-RATE · DOOR-FLOOR · DOOR-V1LINK · COMMS-REWRITE**. Four Arch corrections — instances **fifteen through eighteen**, two of them CC-caught. **The pen returns to the main line. Retires Roadmap v3.13.**)*

*(v3.13, Aug 16: **HARNESS-H4 CLOSED ENTIRE at `13a8233`** — the visual gate judges the moved set itself at **zero pixel tolerance** (`CHANNEL_THRESHOLD` untouched at 24), the `GATE_DIFF_THRESHOLD_PCT=0` workaround is **retired**, `screenshots/diff-exceptions.json` is checked in with **four sites, two provisional**, and rule 9 is **per-dimension at FACTOR 1.75 / ADD 8** with the area constants retired in the same step. Suite 1594/105 → **1611/105**; visual **28/28 at the new default across four runs**; **zero baselines written**. **DOOR's waitlist migration APPLIED AND SMOKED from the main line (R-R)** — ledger **49 → 50**, RLS on with zero policies, `anon`/`authenticated` absent from the table entirely, privilege-level smoke 5/5, census to zero. **Four DOOR rulings taken — R-O/R-P/R-Q/R-R** — and **the pen handed to a dedicated DOOR+LANDING build seat**, which owns v3.14. **Both stranded HOLD sheets folded in**: the Aug-15 Comms merge and the Aug-16 parallel-Arch DOOR merge. New: **DOOR-SEO · DOOR-MW · DOOR-DBLAUTH · DIFF-EXC-BOUND**. Two Arch corrections — instances **thirteen and fourteen**, both new shapes. **Retires Roadmap v3.12.**)*

*(v3.12, Aug 15: **S4-3 LEG 4 CLOSED ENTIRE at `23a34d6`** — the Box Types admin exists (new route on the Methods precedent, six v1 columns in layout-194 order + Weight + a derived Count, both entry points, R-J/R-K/R-L/R-N), **IMP-SPINE CLOSED** in a corrected shape CC found and Arch had got wrong, **DIALOG-BLANK CLOSED**, and an **app-wide autocomplete defect** root-caused and fixed — `onBlur` never closed the menu, so portalled menus accumulated one per row hop. Suite 1548/102 → **1594/105**; visual **28/28 @ 0.0000%** across a 10-moved + 2-added declared event; migration ledger **49, no schema**. **HARNESS-H4 RULED AS ITS OWN UNIT BEFORE LEG 5** — CC's OQ4 proved the threshold hole is live: nine of ten moved frames measured 0.0389–0.0504% and printed PASSED. **H3's step-1 control ran free and answered its question** — the drift wanders in PRESENCE and is stable in POSITION; row renamed **SUBPIXEL-WANDER**. **ACCOUNTS registered and routed to Strat** — twelve auth accounts, six consistent with automated signup, four real third parties. New: **ADMIN-NAV · BOXTYPE-CA · BOXTYPE-BTN · IMP-SPINE-2 · UNITS · ACCOUNTS**. Three Arch corrections — instances ten, eleven and twelve — two of them new shapes. **Retires Roadmap v3.11.**)*

*(v3.10, Aug 14: **ARCH PROMPT v30 INSTALLED** (KNOW-HYG closes) · **HARNESS H1 CLOSED at `e55269c`** — the gate report prints measured truth on every line, combined flags hard-error, the load gate refuses above 5.0, `fail()` reaps the server; it caught its first real sub-threshold event the same day · **S4-3 LEG 3 CLOSED ENTIRE at `254ec1c`** — P4's Packed cascade built from v1 script 690 with the R-G consolidated Remaining prompt, B1's find engine ported from the cable list, R-E strips the panel's identity fields, R-I's ⌥⇧I cycle, B5 delivered as a REPORT, two catalog corrections. **R-F re-scopes P5 to leg 6** off script 804's own header. Suite 1495 → **1514/101**; visual **26/26 @ 0.0000%** across an 8-frame measured declared event; migration ledger **49**, no schema. **BOX-FIND · BOX-AC-AUDIT · KNOW-HYG · COWORK-REV · ZERO-CLEAR · PROJ-CLEAN · EVID-LAND all CLOSE.** New: **BOX-SORT** (R-H retires R-46) · **SLOT-AUDIT**. Two Arch premise errors CC-caught — standing-correction instances six and seven.)*

**Migration of record (Daniel-confirmed fold path, Aug 9):** v3.0 folds **Roadmap v2.46 + Sync Sheet #1 + Sync Sheet #2 directly.** This file carries only what is live (status ≤10 lines · sprint plan · ONE unified docket · live open questions); everything historical exits to **Ledger v1.0**. Completed docket items EXIT to the Ledger; there is deliberately no DONE status. Structure rule RM-5 carries forward; **RM-6: every Roadmap delivery opens with the 🧭 Next Sessions block.**

---

## 🧭 Next Sessions (RM-6 — plain English, kept current)

- **The pen is back with the main line.** The DOOR+LANDING build seat closed its unit at `e92f2c9` and wrote this Roadmap. There is no parallel writer, and the one-writer rule is satisfied.
- **Next: the `ADMIN-NAV` ruling, then S4-3 leg 5 — Contents/Assignments.** In that order, because leg 5 builds another list surface and inherits whichever idiom is ruled.
- **Leg 5 is not the unit its one-line name implies.** v1 has **no on-screen box-contents surface** (layout 179 is the sole browse surface; layout 180 `Boxes` is an empty 120-character stub), so **"Contents" has no pedigree to port and is Trigger A** — halt and take Daniel's scoping ruling. **"Assignments" is largely already built**; establish what remains before assuming a unit's worth of work exists.
- **Leg 5 is the first unit to declare under H4's defaults.** Emit and write with plain `--gate --emit-declaration` / `--update-baselines --declare` — no env override, and any handoff still carrying one is stale. Rule 9 is per-dimension at 1.75/8. **Frame 11's registry site is n=1**: if a rule-3 refusal hits frame 11, check whether that 1×1 registration was adequate first.
- **The gate has a known intermittent failure — do not let it stall a leg, and do not mask it.** `01/02-light` at **Δ42 over the topbar theme-toggle region**, position-identical across the four runs where it appeared and absent in the two where it did not. Registered as **`GATE-THEMERACE`**. It is above channel-24, so unlike the registered Δ1 wander sites it reaches the judged verdict. **Re-run once; if it clears, proceed. Never register it as a diff-exception** — a Δ42 entry is exactly `DIFF-EXC-BOUND`'s trigger.
- **`npm run test:visual` does not work.** Use `node --env-file=.env.local scripts/visual-capture.mjs --gate`. Registered as `VIS-ENVFILE`.
- **The harness line is scheduled, not idle (R-V):** H3 plus the five new gate rows run **after** the box unit, the v1 import/export work and SHARE — in that order, Daniel's ruling.
- **The post-private-beta box cluster is still SIX rows of one shape:** TYPE-HOLD · PANEL-NAV · AC-ENTER · BOX-DUP · BUNDLE-HOLD · BOX-SORT. `ADMIN-NAV` is adjacent but spans the Methods admin too.
- **Print is CLOSED and stays closed.** PRINT-REDO opens only on Daniel's word.
- **At your desk:** the **R8 drill is OVERDUE** (it unblocks PORT-READY's restore-proven leg, feeds the attorney addendum's retention number, and gates the account/project cleanup) · the four CAND-4 texts · **the hero's bid-to-opening line goes plural when Vape! Chicago opens** — `HERO_BID_TO_OPENING` in `src/app/page.tsx`, a one-line edit as designed · confirm Vercel READY at `e92f2c9` (`node scripts/vercel-preopen.mjs`) · TOK-VC expires ~**Nov 8** regardless · the housekeeping strays under GIT-CFG.

## Current Status (August 18, 2026)

Live at `https://minotaur.app` at **`e92f2c9`**, pushed and refs-verified (`13a8233..e92f2c9`, read from the repo's own ref files and origin's reflog, git never invoked from this seat). **DOOR + LANDING is closed entire** — four rounds folded into one commit (11 files, 757 insertions, 163 deletions, no trailers). The signed-out root is a landing page carrying **Daniel's own copy (R-U)**; `/auth/signup` is a closed door with no form, so the GoTrue vendor string is unreachable **by construction** rather than handled; `/api/waitlist` writes through `waitlist_join_v1` on the anon key with **no service-role client in `src/`** and no new secret. Counts: suite **1639/106** · frozen **3/3 vs `be0769de`** · visual **28/28 at zero tolerance across two consecutive rounds**, **zero baselines written**, `declared-event.json` and `diff-exceptions.json` untouched · **migration ledger 50**, no new schema in this unit. Governing set: **Arch Prompt v30** · Strategy v14 · **Roadmap v3.14** + Ledger v1.0 (Aug-18 append). Session Log at **6 entries**, rollover clear. **Vercel READY at `e92f2c9` is NOT seat-verifiable** — run `node scripts/vercel-preopen.mjs`. **Next action:** install this package → the `ADMIN-NAV` ruling → leg 5 and its Trigger A halt → **R8-DRILL, overdue**.

## 🏗 Sprint Plan (scope of record)

**Sprints 0–3: CLOSED.** **Sprint 4 — Boxes + the basket:** **S4-1** decks-clear — COMPLETE Aug 10 → **S4-2** S4-AUTO — COMPLETE Aug 10–11 at `1f06092` → **MMM-CAT** — established NEVER-RUN (Aug 12); handoff stale; re-issue on scheduling → **S4-3 the box unit family — IN BUILD**: legs 0–1 COMPLETE Aug 11 · leg 2 COMPLETE Aug 11–13 at `6d52921` · leg 3 COMPLETE Aug 13–14 (`8461c2a` + `254ec1c`) · leg 4 COMPLETE Aug 15 at `23a34d6` (IMP-SPINE + DIALOG-BLANK closed with it; H3 step-1 ran) · **leg 5 Contents/Assignments — NEXT** (Trigger A on Contents; DOOR closed at `e92f2c9`, Aug 18) · leg 6 the three PDFs **+ P5 (R-F)**; the three at-desk print gates stay non-blocking → **HARNESS: H1 `e55269c` · H2 `ddcf929` · H4 CLOSED `13a8233` — the line's only open work is H3, still unscheduled** (the wander is tolerated by the registry, not explained) → **S4-mid** D-2b + BASE-EVT · FOCUS-MP in headroom → **S4-late** GS-C (SPL-C, (mmmm)/(nnnn)/(oooo), + MULT-DUP) → LINE-STECK (+STECK-COLOR) → S3-G-2 (+UNFLAG-WARN) → ALLOC → **S4-close** EXPORT+GBD (**PORT-COV's v2 build lands here; its v1 side is done**) → WF1A-SUNSET + auto-mode-rider review + COWORK-PORT + the (rrrr) box-⊕ adjudication + PRINT-REDO if Daniel opens it → KKK-PASS after. Standing constraints carry; auto-mode rider EXPIRES at Sprint 4 close. **Post-Sprint-4 / pre-beta:** SHARE build (design RULED Aug 12; REV-B → build-prep) → ADM-1 · AUTH-DOMAIN (gates SHARE phase 2) · 3TL · DEPS · CL-HILITE. **Post-beta:** GCM completion · Maintenance · Spreadsheet Export · RF/Walkies/Circuits · IAS Frequency Import · real-time collaboration · licensing (~Nov) · PWA/offline.

## 📌 Unified Docket (the ONE register of open items)

| ID | Item (one line) | Status | Owner | Trigger |
|----|-----------------|--------|-------|---------|
| S4-3-LEGS | Box family legs 5–6: **leg 5 Contents/Assignments — NEXT, after DOOR; Contents is Trigger A** (v1 has no on-screen contents surface) · leg 6 the three PDFs + P5 (R-F). Legs 0–4 CLOSED (`6d52921` · `8461c2a` · `254ec1c` · `23a34d6`) | IN-BUILD | Arch | Leg 5, after DOOR + the ADMIN-NAV ruling |
| ADMIN-NAV | **⌥⇧↑↓ moves the SELECTION but not FOCUS on the admin grids.** On the Box List the pair moves together (K-2), preserving the column; on the **Box Types admin** and on the **Methods admin** it was built from (`MethodsAdminClient.tsx:251-266` calls `setCurrentRowId` alone) the highlight moves while focus stays put, so cursor and highlighted row disagree after one chord. A pre-existing divergence between two house idioms, now on **three surfaces**. Fits none of AC-ENTER/PANEL-NAV/FOCUS-MP/BOX-DUP/SLOT-AUDIT/AC-HANDLED | OPEN | Arch | **Ruling wanted before leg 5** |
| DIFF-EXC-BOUND | **NEW from H4.** Nothing bounds a diff-exception site's size; the Δ ceiling, the `maxPixels` hard error and the checked-in git history hold it back, none of them a bound. **Trigger: the first site registered above Δ1** — the point at which the registry could mask a delta the judged gate would notice. The harness prints a warning naming this row, and `_futureBound` sits in the registry file itself. **Design note of record:** bound **area × `maxPixels`**, not the bbox alone — a large bbox with a small `maxPixels` is the honest shape for a wandering site, and bounding the box alone would repeat the mistake the area metric made in rule 9 | OPEN | Arch | On its trigger |
| GATE-THEMERACE | **NEW from DOOR.** An intermittent **Δ42** site over the topbar theme-toggle region failing `01-project-menu-light` and `02-equipment-list-category-light`. **Position-identical across the four runs where it appeared** (`172 px, bbox 1273,14–1307,32` on frame 01) and **absent in the two where it did not** — the presence-wanders / position-holds signature of SUBPIXEL-WANDER, an order of magnitude larger and **above channel-24, so it reaches the judged verdict** rather than being absorbed. Likely mechanism: the first light-pass frames captured mid theme-transition. **The best bridge case H3 has** — H3's model was built entirely on Δ1 sites. **Never register it as a diff-exception** | Arch | Harness line, per R-V |
| GATE-COUNTWAIT | **NEW from DOOR, root-caused at source.** `visual-capture.mjs:426` decides element presence with `locator.count()`, which **does not auto-wait** — unlike every other Playwright call in the harness — so a slow-rendering equipment list fail-hards with no retry. **This is what capture-flake (3) actually is**; it killed three runs on 2026-08-17. Fix is one line; `:460`'s `count()` is an optional click and benign. **Propagation owed:** flake (3)'s Aug-13 amendment names dev-server AGE as the trigger — a run against a **277ms-old** server failed identically, so age was one sufficient cause, not the cause | Arch | Harness line, per R-V |
| GATE-FIXDIRTY | **NEW from DOOR, CC's find.** The equipment sort restore at `visual-capture.mjs:432` is a fire-and-forget `.click().catch(() => {})`, so a fail-hard after it leaves `projects.equipment_sort_mode = 'method'` on the fixture — and the **next** run then fails frame 02 for a reason unrelated to the tree under test. Cost a full run on 2026-08-17 | Arch | Harness line, per R-V |
| GATE-LOADPROXY | **NEW from DOOR.** The harness's 5.00 load ceiling never fired during any failing run. Measured across six runs: passes at **4.97** and **3.72**, failures at **2.84–3.87** — one pass above the failure band and one inside it, so **load does not track the failure mode in either direction**. The gate's own guard measures a quantity unrelated to what it guards: a check that reads as satisfied while doing nothing, the same class as H2's rule-3 containment and H4's threshold hole | Arch | Harness line, per R-V |
| VIS-ENVFILE | **NEW from DOOR.** `npm run test:visual` is `node scripts/visual-capture.mjs --gate` with no `--env-file` (`package.json:13`), so it dies at preflight on missing `TEST_USER_EMAIL`/`TEST_USER_PASSWORD`. CLAUDE.md documents the working invocation; the two disagree | Arch | Harness line, per R-V |
| DOOR-RATE | **NEW.** `waitlist_join_v1`'s per-IP brake is **5/hour** on a hashed address. This audience sits behind shared university and shop NATs — Carnegie Mellon, Pace, DePaul, a Broadway shop — so five real people from one address in an hour is realistic on an announcement day, not hypothetical. **R-S(a) makes that failure visible rather than silent**; raising the cap is schema and stays with the main line | Arch | If waitlist volume warrants |
| DOOR-FLOOR | **NEW.** The submit floor is **400ms measured from first interaction** with the email field (AMEND-1 moved it off the mount clock, which measured time-since-hydration and could silently drop a real person on a slow device). Residual, named in the constant's own doc comment: a browser that autofills on load with no focus event starts the clock at `input`, so a submit within 400ms of that is still dropped silently | Arch | Opportunistic |
| DOOR-V1LINK | **NEW, a consequence of R-U.** The deleted “v1 is at work today.” section carried the only direct link to the v1 product page (`lundbergsound.com/?page_id=656`); the footer's `lundbergsound.com` now points at the site root. The Aug-15 Strat close listed **“keep selling v1”** as part of this page's job. R-U is Daniel's own copy ruling, so this is **registered rather than contested** — the round-trip trust signal itself survives inside hero paragraph 2 | Daniel | His call, any time |
| COMMS-REWRITE | **NEW — R-W (Daniel, 2026-08-18): Comms Prompt v9 is DELETED and rewritten from scratch** before the next Comms deliverable. Not amended. The two amendments banked at the Aug-15 close — point at Writing Prompt v1 §Daniel's Voice rather than duplicating it, and give the Comms tier its own propagation check — become **inputs to the rewrite**, not edits to a document that will not exist. **Third input, from R-U:** the landing copy was logged as Daniel-approved through three rounds and the build reproduced it verbatim, yet the assembled page read to him as “inauthentic and generic softwarese.” The miss was in the copy file, not the build — a fact about the Comms process and the strongest argument for the rewrite | Daniel | Before the next Comms deliverable |
| CAND-4 | The four uninvited accounts (`abrion@pace.edu` · `josh.jpr@gmail.com` · `soundcooper@gmail.com` · `jhaggerty97@me.com`) are **named beta candidates and their accounts STAY LIVE.** Four personal texts from Daniel this week — one message doing triple duty (courtesy on the closing door · "you're on the list" · whatever signal they volunteer). **No product conclusions drawn at n=4:** uninvited, unonboarded pokes are curiosity behaving as expected, not churn. Tripwire: substantive pre-terms use → kind disable per addendum §B. Invitations only after September beta terms exist | OPEN — RULED | Daniel | Texts this week; invites post-terms |
| IMP-SPINE-2 | Two secondary type-unscoped reads in the workbook importer (`workbook/fetchCurrentState.ts:21,26`). The all-types **count is CORRECT as is** — it matches the RPC's guard, and narrowing it would break the workbook fingerprint the same way the naive IMP-SPINE fix would have. The `max(sort_order)` across all types means a cable's sort_order can raise the base imported rows append after — **cosmetic, cannot delete anything.** Left alone deliberately | OPEN — LOW | Arch | Opportunistic |
| BOXTYPE-CA | Change All on the Box Types admin. v1 layout 194 carries it (script 693) and v2's Box List has it; the Methods admin does not. A real gap, deliberately not built at leg 4 | OPEN | Arch | With the post-beta box cluster |
| BOXTYPE-BTN | The Box List's new **EDIT BOX TYPES** button renders as a bespoke white outline `Link` rather than in the toolbar palette; its treatment is uncited. **Daniel saw it at the gate and accepted it as-is** — cosmetic, registered not built | OPEN — COSMETIC | Arch | Opportunistic |
| UNITS | **R-M (Daniel, Aug 15):** v1's Units global switches the Volume header `ft³`/`m³` and the `Box Size` calc between `W"xD"xH"` and `W cm x D cm x H cm`; v2 has no units global at all, and `boxRollups` divides in ft³. A project-setting-shaped feature touching print, rollups and the create dialog | OPEN — RULED | Arch | **Private beta, before public beta** |
| HARNESS | **H1 `e55269c` · H2 `ddcf929` · H4 CLOSED `13a8233`. Scheduled behind the box unit, the v1 import/export work and SHARE (R-V), with five new gate rows joining the line: GATE-THEMERACE · GATE-COUNTWAIT · GATE-FIXDIRTY · GATE-LOADPROXY · VIS-ENVFILE.** H4 retired the `GATE_DIFF_THRESHOLD_PCT=0` workaround: the gate judges the moved set at zero pixel tolerance, `CHANNEL_THRESHOLD` untouched at 24. `screenshots/diff-exceptions.json` masks Δ1 wander sites out of the **zero-tolerance aggregates only** — the judged verdict is structurally outside the mask — bounded by a per-site Δ ceiling and a `maxPixels` hard error. Rule 9 per-dimension at **1.75 / 8**; area constants retired. **The line's only open work is H3, unscheduled.** Plus (qqqq)/scroll-state frames, which ride the legs that stage them | OPEN — H3 ONLY | Arch | H3 unscheduled |
| SUBPIXEL-WANDER | Presence wanders, position reproduces exactly. **Now TOLERATED by H4's registry rather than worked around**: four sites, two provisional, each provisional one carrying a `refineBy`. Site 2 (`262,107–299,108`) is **measured three times identically** — the strongest-evidenced entry and H3's best anchor. Site 3 (`597,107–723,108`) is approximate from the record. **Site 4 (`184,358`, dark-only, `maxPixels` 1) is n=1** — if a rule-3 refusal hits frame 11, check whether that registration was adequate; a second observation upgrades it on the spot. **Diagnosis is H3's; H3 stays unscheduled** | OPEN | Arch | H3, unscheduled |
| PORT-COV | Import/export coverage to equipment+cable+box. **The v1 side is DONE** — thirteen-sheet digest, script 566 read whole, identity design, corpus scan, all in `handoffs/parallel/` (`260814-1909_*`, `260814-1930_*`); all seven rulings taken (partition by detail-table name · non-UUID historyIDs null-and-hashed · **recompute** as a third import class · hash inputs = script 567's own field map · Groups' Notes/Start Date deprecated · equip `Box` emits the name · the `rf city list.xlsx` correction). Coverage measured: **4 imported of 13 unconditional + 9 conditional register-only sheets** (re-based from "4 of 17"). What remains is the v2 build | OPEN — V1 SIDE DONE | Arch | S4-close, inside EXPORT |
| EXPORT-ORDER | **v1's re-ingest is POSITIONAL** — script 567 maps `Source field N → Table::Field` — so column ORDER is part of the contract, not just names. 567's boxes map runs to field **38** where 566 writes **36**. **Box Types datapoint added Aug 15:** script 777 exports `Box Types.xlsx` in the order **Box Count · Box Type · Depth · Height · Volume · Width** (alphabetical, count first), UTF-16, field names as headers, sorted by Box Type asc — confirmed byte-for-byte against both JOY and BVSC exports, 61 rows each. Any v2-emitted v1-compatible export must match 567's order | OPEN | Arch | With EXPORT / PORT-COV build |
| WIRE-TYPE | `cable.xlsx` exports `Wire Type` and `Wire Type Quantity`; script 567 maps field 60 → `Cable::Wire Type`; **v2 has no column for either** in `cable_details` or `cable_types`. Confirmed three ways rather than inferred from absence — rule whether it ports or is a deliberate omission | OPEN | Arch | Before PORT-COV's v2 build |
| BOX-SORT | **R-H (Aug 14): R-46 retires** — the Box List's per-column header sort buttons give way to the cable list's toolbar sort idiom, in a pass of its own. A **deliberate v1 deviation** of record. Until the pass runs, the current header stands | OPEN — RULED | Arch | With the post-beta box cluster, or sooner on Daniel's word |
| SLOT-AUDIT | **Shortcut-registration sweep.** ⌥⇧J was silently dead on the Box List — an unregistered shared-slot chord no-ops by design. Per route, list which of onNew/onDelete/onFind/onDisplayAll/onSplit/onClone it registers vs what the surface can do. **Leg 4 audited the Box Types admin route only** (clean); the rest of the app is unswept | OPEN — PARTIAL | Arch | Opportunistic; rides any list-surface unit |
| AC-HANDLED | `AutocompleteInput.handledRef` latches on first commit and never clears, so the (kk) blur-fallback is inert per mount, app-wide. Fix direction is **"clear the latch when a new edit begins"** (onChange or focus) — never removal; every seam depends on it being true between a commit and its blur | OPEN | Arch | Own small pass — no longer trivial |
| TYPE-HOLD | Store an unmade box-type name the way v1 does (OQ2 reversed; Trigger B mapped; SEAT-MIG authorized; TH-R1 automatic healing · TH-R2 the name prints · TH-R3 Change All + the flag travels). **Leg 4 did NOT foreclose it** — R-K's "what happens to the boxes" sits behind its own named function in `boxTypeAdmin.ts`, so TH can make delete write the name into the hold column instead of nulling. **R-J's DB uniqueness constraint rides this migration** | OPEN — RULED, GATED | Arch | Sprint 4 close / private-beta prep |
| PANEL-NAV | The footer panel navigates records the way the row does: (a) panel Enter → next record's same panel field (multi-line exempt); (b) ⌥⇧↑/↓ from a panel field keeps the field. R-I landed the route INTO the panel and `setDetailFocus` is live with its first caller | OPEN | Arch | After private-beta start |
| AC-ENTER | Two-stage Enter in autocompletes (pick ≠ leave). B5's report is the design input. **First design question: is a HOVER-set highlight a commit target at all, or a visual affordance?** `highlightFromHoverRef` already tracks the distinction; making `commitTarget` consult it would resolve (bbb)+(eee) together without touching `handledRef` | OPEN | Arch | After private-beta start |
| BOX-DUP | No Duplicate on the Box List (v1 has none either — deliberate improvement if built). Needs a ruling on what a duplicate carries | OPEN | Arch | After private-beta start |
| BUNDLE-HOLD | The cable Bundle cell holds a new name the way the box Type cell will; sequence with TYPE-HOLD | OPEN | Arch | After private-beta start, with TYPE-HOLD |
| (fff) | No path to edit `remaining_quantity` on an EXISTING row (creation has one — the New Defaults "Mark All As Remaining" seeds it) | OPEN | Arch | After private-beta start (Daniel, Aug 14) |
| SHARE | Sharing-UI + membership layer — design RULED Aug 12 (four roles); RLS delta + ADM-1 need REV-B at build-prep. **Build-prep design input (Strat, Aug 15, Trigger A route): with public signup closed, phase-1 invites need an account-creation path that does not route through the public form** (admin-issued / invite-created). Intent ruled; mechanism is Arch's at build-prep | OPEN — DESIGN RULED | Arch | Post-Sprint-4, private-beta prerequisite |
| ADM-1 | Beta admin panel — sketch delivered; REV-B owed | OPEN | Arch | Post-SHARE, pre-October |
| CL-HILITE | Cover-letter editor highlight control | OPEN | Arch | Post-Sprint-4, pre-public-beta |
| PAL-UNIFY | `MINOTAUR_PALETTE` vs `MINOTAUR_COLORS` — unify or bless the split | OPEN | Arch | Unscheduled |
| PRINT-REDO | Equipment-list print parity re-attempt. Daniel's word only; measurement harness first | GATED | Daniel | Sprint 4 close at the earliest |
| PARITY-PAIR | v1 preliminary + v2 revision-mode prints, both missing | OPEN | Daniel | Before PRINT-REDO |
| MMM-CAT | Category sub-band editable under method/box/group sorts incl. ⌥⇧I — never ran; re-issue fresh on scheduling | OPEN | Arch | On scheduling; band machinery warm |
| MULT-DUP | Mult duplicate doesn't offer lines/tails; needs the v1 dialog verbatim | OPEN | Arch | → GS-C, S4-late |
| UNALLOC | Unallocate may not return a cable/tails to generic | OPEN | Arch | Needs a reproduction from Daniel |
| METHOD-FIX | Fixture debt: no method band on the printed fixture; no changed+note-bearing fixture row | OPEN | Arch | Next fixture-touching declared event |
| GATE-PDF | Three-PDF interactive Save-as-PDF measurement packet | GATED | Daniel | At printer/show |
| GATE-OL25WR | OL25WR die-cut stock re-gate | GATED | Daniel | At a printer |
| GATE-C4 | C4 pagination confirm — superseded in practice; re-scope if PRINT-REDO opens | GATED | Daniel | — |
| R8-DRILL | Backup restore drill per the R8 runbook. PORT-READY's restore-proven leg still unmet. **Also feeds the addendum §B retention-number placeholder, and gates the account/project cleanup** | OPEN | Daniel | **Early week of Aug 17 (Daniel, Aug 15); hard stop before the September attorney engagement** |
| PORT-COV-V2 | *(tracked inside PORT-COV; retained here as a pointer only)* | — | — | — |
| COWORK-P2b | Boxes evidence digest fold-in — retained as reference | OPEN | Daniel | Confirm retention at convenience |
| COWORK-PORT | Memorialize the Cowork workflow portable across projects. **Carries the §Seats one-writer amendment proposal** — the rule that earns its keep is "exactly one WRITER at a time across all seats and CC," not "no Cowork session while CC is open" | OPEN | Daniel | Sprint 4 close |
| TOK-VC | Vercel token rotation + renewal. Reminder fires at private-beta start — but the token expires ~**Nov 8** regardless; if beta hasn't started by end of October, rotate anyway | OPEN | Daniel | Private-beta start; hard stop ~Nov 8 |
| D-2b | Bundle Clone/Ext linkage | OPEN | Arch | S4-mid, cable-side leg 1 |
| S504 | Residual s504 delete-variant frames | BLOCKED | Daniel | When v1 cooperates |
| GS-C | Cable completeness: SPL-C · INS-1 · (bbbb) · (qqq)/(sss) · (mmmm) · (nnnn) · (oooo) Method-0 design gate · MULT-DUP | OPEN | Arch | S4-late, cable-side leg 2 |
| S3-G-2 | Residual v1 outputs (Shop Orders + remaining reports) | OPEN | Arch | S4-late, after LINE-STECK |
| FOCUS-MP | Focus/nav mini-pass. ⌥⇧↑/↓ is not dialog-guarded (only R-I's ⌥⇧I is). **MEASURED at leg 4 on BOTH box surfaces** — the record moves behind an open modal on the Box List and the Box Types admin. **Mitigating fact of record: no wrong-row delete** — both paths snapshot their target when the dialog opens (`deleteTargetId` · `deletePlan`), so confirming deletes the row the copy names. The defect is that the operator's current row silently changes underneath an open modal | OPEN | Arch | S4 between-units headroom |
| UNFLAG-WARN | Unflagged-cables print warning | OPEN | Arch | Rides S3-G-2 |
| EFFGRP | effective_group / grouped-bundle-group view defect | AWAITING-RULING | Daniel | Ruling → four-step migration |
| LINE-STECK | Line/steck label unit | OPEN | Arch | S4-late, after GS-C |
| STECK-COLOR | Steck-color verify | OPEN | Arch | Rides LINE-STECK |
| CMDK | ⌥⇧K command palette | OPEN | Arch | Post-Sprint-4 |
| LAT | Latency/optimistic unit — the box-create observation is MEASURED as server cold start (+743ms cold vs +89ms warm). Residual: ~89ms first-write warm-up, untested | OPEN | Arch | Post-basket |
| DEPS | Dependency maintenance | OPEN | Arch | Pre-October beta |
| AUTH-DOMAIN | Custom-domain auth email — gates SHARE phase 2 | OPEN | Arch | Pre-public-beta |
| 3TL | Three-tier library architecture — inherits the SHARE flag | OPEN | Arch | Pre-beta |
| BASE-EVT | Declared baseline-event session — **substantially retired by H2**; what remains is fixture/frame curation, not mechanism | OPEN | Arch | S4-mid, rides D-2b |
| ALLOC | Allocate as type-conversion (D9) | OPEN | Arch | S4-late |
| CONCEPT-A | Print-header unit | BLOCKED | Daniel | Gated on the a-1 uploads |
| LIB-RENDER | Library-grid render pass (+ (pppp)) | OPEN | Arch | Post-Sprint-4 |
| GCM-LAND | GCM→list focus landing | OPEN | Arch | Post-Sprint-4 |
| EST-AUDIT | Estimator-audit / measured pagination | OPEN | Arch | Unscheduled |
| UNDO | Session Undo/Redo | OPEN | Arch | Unscheduled |
| VERHIST | Durable Version History | OPEN | Arch | ~November |
| TRASH | Trash/Archive unit | OPEN | Arch | Post-Sprint-4 |
| BINGO | Bingo disposition | OPEN | Daniel | Post-private-beta |
| RTL | Hook-test + RTL/component-test infra — the extract-to-pure-modules pattern held a third time (`declaredEvent.mjs`); a component harness remains the real fix | OPEN | Arch | Someday-maybe |
| (rrrr) | New Box ⊕ UX on the EQUIPMENT list's Box column | GATED | Arch | End of Sprint 4 |
| (ppp) | Settings page: project menu bar joins the fixed header — rides SHARE | OPEN | Arch | SHARE build |
| TYPE-TOG | Equipment-list type-visibility toggle | OPEN | Arch | Post-Sprint-4 / EP |
| SEED | Fresh-project seeding — v1's Box Types catalog port. **`box types.xlsx` is in the JOY and BVSC corpora, 61 rows each, byte-identical**; the catalog's "56 seeded rows" is the DDR-time count and is now dated as such. The admin surface to receive them exists as of leg 4 | OPEN | Arch | Unscheduled — unblocked |
| KKK-PASS | Narrow-window responsive pass | OPEN | Arch | Immediately after Sprint 4 |
| NOTES-NL | Library-page notes fields strip newlines | OPEN | Arch | Unscheduled |
| GROUP-RES | Equipment grouping/sort engine residue | OPEN | Arch | Unscheduled |
| REV-STAMP | Revision-stamp labels deliberately screen-sized | AWAITING-RULING | Daniel | Unscheduled |
| MPP-COS | Multi-page print preview cosmetic whitespace | OPEN | Arch | Unscheduled |
| REV-SPRINT2 | Equipment Change All cross-surface constraints inheritance check | OPEN | Arch | Opportunistic |
| (q) | Consolidate Zite source into ONE reference folder | OPEN | Daniel | Unscheduled |
| (u) | Snapshot-fetch failure surfaces console-only | OPEN | Arch | Unscheduled |
| (ff) | Tab strip renders no selected button on revisions/settings | AWAITING-RULING | Daniel | Unscheduled |
| (yy) | Methods admin non-rename fields swallow DB errors | OPEN | Arch | Unscheduled |
| (aaa) | ⌥⇧F find-seeding misses Model | OPEN | Arch | Unscheduled |
| (bbb) | Autocomplete inline-completion Enter commit — a hover-set highlight still outranks the completion branch. Design resolution lives in AC-ENTER | OPEN | Arch | With AC-ENTER |
| (ccc) | Quantity Escape-revert missing | OPEN | Arch | Unscheduled |
| (eee) | Exact-match autocomplete suppression — `hasExact` never outranks a highlight. Design resolution lives in AC-ENTER | OPEN | Arch | With AC-ENTER |
| (hhh) | Editing a category with its band header off-screen | OPEN | Arch | Unscheduled |
| (jjj) | Remaining-cell save-error-flash gap | OPEN | Arch | Unscheduled |
| (vvv) | Inverted-extension end DISPLAY may be un-swapped | OPEN | Arch | Unscheduled |
| (dddd) | `# ` autoformat headings print literally | AWAITING-RULING | Daniel | If it chafes |
| (ffff) | New Defaults Method/Group inputs render DARK in light mode | OPEN | Arch | Unscheduled |
| SNAP-CHK | Production snapshots lacking `items`? — 12 surveyed, all carry; fallback stays | OPEN | Arch | Opportunistic |
| CONTRAST | Contrast recon | OPEN | Arch | Unscheduled |
| DASH-FRAME | /dashboard capture frame | OPEN | Arch | Future baseline event |
| TAB-BRIDGE | Cross-band Tab bridge design question | OPEN | Arch | Unscheduled |
| GIT-CFG | Real `git config user.name/email` · Next 16 `middleware`→`proxy` · `next-env.d.ts` dev/build flip-flop (benign, never committed) · **plus two stray trees to delete: `Minotaur/{src/` (unquoted brace expansion, empty but for `.DS_Store`) and the empty `Minotaur/_to_delete/`** | OPEN | Daniel | Housekeeping |
| LINT | NO ESLint config exists anywhere; standing one up is config authoring | OPEN | Arch | Post-Sprint-4 chore |
| ST-RACE | Minor `setTimeout` race in error-flag cleanup | OPEN | Arch | Low |
| CTF-SEED | CTF edit-dialog seed helper unused by the grid | OPEN | CC | Opportunistic |
| IMP-SORT | Import sheet order → sort_order | AWAITING-RULING | Daniel | Unscheduled |
| SRGRANT | `service_role` GRANT restore sweep | OPEN | Arch | Housekeeping |
| BRIEF-12 | Brief v1.2 pass. **Carries the Aug-15 trigger restatement:** terms must exist before **any** third-party access, paid or unpaid — replacing reliance on "before first paid commercial user" alone, which survived this window on luck rather than design. Also fold in the signup-state history (open ~Jul 17 – Aug 15, closed of record) under §IP & Legal / §Beta Strategy | OPEN | Daniel | Strat scheduling |
| COMMS-Q | Comms queue. **RULED Aug 15: no announcement without a way in.** **The landing-copy session RAN Aug 15 and its copy is delivered and Daniel-approved through three rounds** — `handoffs/260815_landing_copy_minotaur-app.md`. Remaining: the October announcement to the v1 list, carrying the beta path. **The v10 amendment path is SUPERSEDED by R-W — see COMMS-REWRITE:** the prompt is deleted and rewritten from scratch, and the two banked amendments become inputs to that rewrite | OPEN | Daniel | Announcement October |
| LEGAL-SEP | September attorney engagement. **Scope amended Aug 15: addendum §F added** — pre-terms account exposure (click-through attachment of terms at next sign-in), the notice-obligation question, and the IMP-SPINE disclosure of record. Amended addendum installed to project knowledge by the Strat seat same day; `drafts/` copy in this repo | OPEN | Daniel | September 2026 |
| LIC-NOV | Licensing subsystem design — ownership-transfer design lands here | OPEN | Daniel | ~November 2026 |
| WF1A-SUNSET | WF-1a commit pilot + auto-mode rider sunset review. Datapoints now include: the overnight commit-authority question · the Aug-11 push-instruction correction · the Aug-12 stream-edit pattern · the Aug-13 disclosed heredoc · the Aug-13 index.lock halt + "Everything up-to-date" read · the Aug-14 scratchpad-`perl` disclosure · the attested-but-unpushed push caught from the refs · the Aug-14 declined stage against a handoff contradicting CLAUDE.md · **NEW Aug 15: CC refuted Arch's stated root-cause hypothesis by probe rather than building on it; caught the fingerprint coupling that would have disabled the importer; DISCLOSED the `git checkout --` rule tension and asked Arch to rule instead of proceeding; inverted its own FOCUS-MP conclusion after spotting a confound (second self-caught confound in two days); and self-disclosed two `sed -n` repo reads against the standing Read-tool instruction — reads, not writes, self-caught mid-run** | GATED | Daniel | Sprint 4 close |

*(Exited to the Ledger at this close: **DOOR** (closed entire at `e92f2c9`) · **DOOR-SEO** (ruled R-O(a) and its propagation discharged across the three mutable documents) · **DOOR-MW** and **DOOR-DBLAUTH** (both closed inside the build — the allowlist line proven by a signed-out POST returning 200 with zero redirects, and `/` now prerendering as static with no auth call). **Registered new:** GATE-THEMERACE · GATE-COUNTWAIT · GATE-FIXDIRTY · GATE-LOADPROXY · VIS-ENVFILE · DOOR-RATE · DOOR-FLOOR · DOOR-V1LINK · COMMS-REWRITE. Previously exited at v3.13: **HARNESS-H4** (closed entire at `13a8233`; the HARNESS row retains H3 plus the new gate rows), and both stranded HOLD sheets, now spent.)*

## ❓ Open Questions (live only)

*(This close moves **R-O · R-P · R-Q · R-R · R-S · R-T · R-U · R-V · R-W** to the Ledger, ruled, together with DOOR-SEO's correction and its discharged propagation. **Two former unknowns are now characterised and scheduled rather than open:** the Δ42 gate site (`GATE-THEMERACE`) and the true cause of capture-flake (3) (`GATE-COUNTWAIT`). The Aug-15 Strat and Comms merges remain folded in per v3.13.)*

- **Is `ADMIN-NAV` a defect or an idiom?** ⌥⇧↑↓ moving selection-without-focus is consistent across BOTH admin grids and inconsistent with the Box List. Rule whether the admin grids adopt the row surfaces' behaviour or whether an admin grid is deliberately a different kind of surface — the answer decides whether leg 5 inherits it or diverges from it.
- **`box_default_large_labels` defaults to 5; the v1 panel reads `#Big Labels 2`.** OQ4 ruled 5; 2 may be one show's value. Flagged, not acted on.
- **The categorized-box print case is unexercised** — the fixture's boxes carry no category; needs a category added as its own declared event (cheap under H2).
- **The v1 continuation header** (category repeated on continued pages) → PRINT-REDO.
- **The cover page** reads small relative to the list (8.25pt body) → PRINT-REDO.
- **Substring vs prefix narrowing** (B5): v2's autocompletes narrow on substring where v1's value lists are prefix-matched — unruled divergence, app-wide; rule it inside AC-ENTER's design.
- **`Wire Type`'s disposition** — port or deliberate omission (WIRE-TYPE).
- **RESOLVED in H4 — rule 9's metric.** Per-dimension at FACTOR **1.75** / ADD **8**. FACTOR moved from the ruled 3.0 because **a per-dimension factor squares in area** (1.75² = 3.0625 preserves the ruled area strictness); ADD was set from the binding bound — a 2px extent tolerating zero shrink at ADD 4 would fail a *correct* declaration. `√3 = 1.7321` would zero the ~2.08% residual and was deliberately not taken. Retained here only as the pointer; detail is in the Ledger's Aug-16 block.
- Script 551 extraction · Cross-surface checkbox eligibility for CABLE Change All · House-wide Cancel semantics · Used∩Ignored semantics · Empty-string note decision · Link `field_name` CHECK backstop · Cable-type RENAME affordance · CL a-1 evidence uploads · d2_01 red Members count · CL multi-letter-per-project · Stale-category-text healing · CC credit-budget standing policy · Comms Prompt structure · OQ#2b own-band pendency · Band-cascade harmonization · **Seat consolidation** (rule at or after COWORK-PORT; **new Aug 16: a third scoped Supabase-MCP exception was AVOIDED by ruling R-R to the main line rather than the build seat, deliberately keeping schema authority at one seat; and the parallel Arch seat held read/write on both mounts by folder grant and confined itself to reads on `Minotaur` by DISCIPLINE, not by mechanism — the same asymmetry logged Aug 14, now observed a second time, while the git prohibition held as a mechanism**; datapoints also include the Cowork-seat Supabase capability with two scoped exceptions granted against the migrations-from-claude.ai clause, SEAT-MIG #1–2, project-knowledge deletion, the mount's deny-unlink making the no-git rule a mechanism, and the one-writer amendment proposed Aug 14 — plus the honest asymmetry that claude.ai's read-only-ness is enforced by a missing write mount while a read-only Cowork session is a stated discipline).

---

*End of Roadmap v3.14 — August 18, 2026. **Retires Roadmap v3.13.** **DOOR + LANDING closed entire at `e92f2c9`** (`13a8233..e92f2c9`); handoffs of record `to-cc/260817-0758_door-landing.md` + `260817-0831_door-landing-amend1.md` + `260817-0947_door-landing-amend1-resume.md` + `260818-0725_door-landing-amend2.md` + `260818-0743_door-landing-amend3.md`; returns `from-cc/260817-0821_door-landing.md` + `260817-1158_door-landing-amend1.md` + `260818-0738_door-landing-amend2.md` + `260818-0753_door-landing-amend3.md`; build opener `handoffs/260816-1249_arch_opener_door-landing-BUILD-AUTHORITATIVE.md`; design of record `handoffs/parallel/260816-1210_door-landing-design-block.md`; close running notes `drafts/260818_DOOR_Close_Running_Notes.md`. Landing copy of record is **R-U (Daniel, 2026-08-18)**, in `src/app/page.tsx`; `handoffs/260815_landing_copy_minotaur-app.md` is superseded on body copy and corrected on SEO. History: `Minotaur_v2_Ledger_v1_0.md` (Aug-18 append). Prior units: **HARNESS-H4 at `13a8233`; S4-3 leg 4 at `23a34d6`; H2 at `ddcf929`; leg 3 at `8461c2a` + `254ec1c`; H1 at `e55269c`.** Print findings: `docs/reference/Minotaur_v1_Print_Spec_Measured.md` + `Minotaur_Print_Parity_Findings_2026-08-11.md`.*
