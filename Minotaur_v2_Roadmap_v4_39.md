# 🗺️ ROADMAP — Minotaur v2 (v4.39)

## ▶ NOW

The front door is finished and live. The beta terms are in force, the mail sender
is our own, and the recovery code works for the first time. Jamie Tippett and Mike
Tracey are in — the first two people outside this office ever to use v2.

Nothing is building. What stands between here and the other fourteen is sharing:
five permission migrations and one roster screen.

Jamie's first night produced eight findings, one of them urgent, and a design
session found a backup that would have silently dropped every cable on a shared
show. Both are below.

## ⏭ NEXT — the next three sessions

1. **Sharing.** A show belongs to one person today, and nobody else can open it.
   Five database permission changes, one at a time with your confirm, then one
   screen where a lead types a colleague's address and that person appears on the
   show. Nobody is added to any show until the second of the five is in.
2. **BATCH-COHORT-1.** Jamie's urgent bug and everything near it, pulled forward.
3. **The invite.** The accounts, the note, the cohort brief.

**Yours:**

- The sixteen accounts, minus the two that exist.
- The invite note and the one-page cohort brief.
- A cable label print on real stock, the 5167 sheet with it, and the READY check.
- Which methods palette is correct — v1's, not ours (METHODS-PALETTE).
- CABLEDEL-FAILMSG's wording, and YES or KEEP on the sign-in placeholders.

## 🏗 THE SEQUENCE — the road, in order

- **≈2026-09-18 — PRIVATE BETA.** Sixteen leads plus their teams, 30–40 people.
  Still needed: **SHARE-ROSTER**, **AUTH-POLISH**, **BATCH-PREBETA**,
  **RLS-HELPER-COST**, **APPLY-TIMEOUT**, the fourteen accounts, the invite, a label
  print on stock, and the READY check. Terms are in force; sharing's permissions are
  in (migrations 65–69, 2026-09-15); BATCH-COHORT-1 shipped `9e581be`.
- **The four weeks after — fix what the cohort finds.** **CONTRAST-PASS** ·
  **BATCH-HOUSEKEEPING**'s remainder · **VIEWER-ROLE** · **VERHIST-COUNTER** ·
  **ENTITLE-V1** scoped and built. PROCESS opens as a parallel once week-one fixes ship.
- **Target 2026-10-15, floating — V1-HOLDER BETA.** Free with an active v1 licence —
  any tier except trial, expiring on or after 2026-04-15 — gated on the terms,
  ENTITLE-V1 and **BETA-SIGNUP-GATE**, without which ~185 people have no way in.
  The announcement is the drop; no tease.
- **~November — LICENSING.** v2 checkout on the REGISTRY (payments and subscriptions are the next
  migrations after the sharing set — unnumbered until they are written), with an attorney review before
  the first charge and the New York auto-renewal rules built in (**GBL-527A**).
  **SEAT-CAP** before anyone is charged.
- **RELEASE — $199.** v1 included on request, none sold à la carte; v1 Personal
  holders free to the end of their year; v1 Production holders free during the beta,
  ~$100 after. Production tiers unchanged (NfP $349, Commercial $599, perpetual per
  project). Named-user accounts; one active session per Individual.
- **Stop condition: paid beta by ~March 2027.**
- **Off the road, released by trigger and not by date:** the fail-open controls,
  FIELD-CONTRACT leg 3, BATCH-HOUSEKEEPING's remainder, `duplicate_item_v1`, META's
  Tier 2 and 3, ORG-TIER, MONTHLY-HOLD, CATCOUNT-BREAKDOWN.

---
## 📌 THE DOCKET

Three tables. **▶ ACTIVE** is being worked, dated, or owed now. **⏸ HELD** waits on
a named unit or trigger. **📐 STANDING** binds indefinitely and never closes. A row
is one line: what is owed and who owns it. Reasoning lives in the Ledger; row text
as it stood before v4.39 is in `Minotaur_v2_Roadmap_v4_38.md`, kept in the repo.

### ▶ ACTIVE — units, not items

| ID | One line | When / trigger |
|---|---|---|
| **BATCH-PREBETA** | ⚠ **The next session's FIRST CC handoff (SESSION-SHAPE), and it bundles everything ruled and CC-able so the seat is free to design while CC runs.** Members: **CATCOUNT-COLUMN** (ruled 2026-09-15 — every record in a category regardless of list; the number is already computed and passed to the tab, so this is one column and no new read) · **AUTOCOMPLETE-NOPORTAL** (cause corrected, below) · **TYPES-REGEN** (now stale by **eleven** migrations; 65–69 changed no table shape, so zero-diff is expected — run it anyway) · **the five sharing migration files into `supabase/migrations/`** · **FONT-FALLBACK** (must precede CONTRAST-PASS) · **FENCE-INTERP + COMMITMSG-PATTERN** · **FROZENCHECK-FAILOPEN** · **LINT-DEAD** · **REPO-STRAY-UNTRACKED** · **CLAUDEMD-RIDERS** · **RPC-COUNTNOTERROR**'s sentence · **DEFINER-ANONEXEC**'s one real item (`handle_new_user` has no `search_path`) · **LINECOLOR-ZERO** · **ADMIN-NEWDUP** · **CREATEMETHOD-BLINDERR** · **DEADEXPORT-GRIDCOL** · **SRGRANT** sweep · **ST-RACE** · **CTF-SEED** · **SLOT-AUDIT** residue · **REPO-WEIGHT** · **BOX-TYPES-DIM-DROP** · **GIT-CFG** · **PROJ-CLEAN-2**'s last project. · **AUTH-POLISH's input half** (ruled 2026-09-15 — cut the ghost text) · **CABLE-CHANGEALL-METHODNULL** (ruled 2026-09-15 — pulled forward). **CABLEDEL-FAILMSG joins only if Daniel gives the wording before the handoff is written.** | CC, first handoff of the next session |
| **SHARE-ROSTER** | The sharing panel — CC's, and **the largest remaining build before the invite**. A lead types an address; if it has an account, a membership row is written and access is immediate. Every accepted member sees the roster (D-3). ⚠ **Zite built sharing too — study its interaction options in a PARALLEL before the handoff is written, not during the build. Parity is NOT wanted; the ideas are.** **WHOCOL-RLS's database half is DONE** — `project_member_names()` applied at 69 returns `(user_id, display_name)` for one project including past snapshot authors, and `profiles` stays self-only; what remains is the surface that calls it. | Arch parallel design next session, then CC; gate ≈Sept 18 |
| **RLS-HELPER-COST** | ⚠ **Gate item, measured 2026-09-15, not a prediction.** Migration 67's helper-based policies cost, per one show, settled and warm over three runs: equipment list **1.8→43 ms**, library **6→48 ms**, box details **16→455 ms**. Cause: the three helpers are `SECURITY DEFINER`, which blocks inlining, so every row pays a function call instead of one flattened join. Both helper branches are already index-served, so indexes are not the fix. **Scales with row count, and the cohort will import larger shows than the one measured.** ⚠ **Dropping SECURITY DEFINER is RULED OUT — 69 puts policies on `project_members` and an invoker-rights helper reading it would recurse.** A set-returning candidate measured no better but was measured inside a DDL transaction and proves nothing. Needs one settled round of its own. | Arch parallel, before the invite |
| **AUTH-POLISH** | **RULED 2026-09-15: cut the ghost text.** The sign-in page's two inputs adopt `AUTH_INPUT` like every page built after them — `border-surface-300`, and the placeholders `you@example.com` and `••••••••` go with it. Closes the four remaining dead `surface` steps on that page, so DEADCLASS-SURFACE's sign-in clause retires and its row narrows to the other 101. Still owed from Daniel: **CABLEDEL-FAILMSG's wording** — the text IS the deliverable, so the member cannot be written without it. ⚠ When it runs, the dead-step positive control at `resetSurface.test.ts:68-70` must move to a file that still carries one BEFORE the login assertion goes to `[]`. | Input half rides BATCH-PREBETA; the wording waits on Daniel |
| **AUTOCOMPLETE-NOPORTAL** | ⚠ **The row's old cause is FALSE and was corrected 2026-09-15.** The equipment list's six call sites DO portal — they opted in at FIELD-CONTRACT leg 3a on 2026-09-08 and `src/lib/autocompletePortalScope.test.ts` pins the census (33 sites, 30 portalled, 3 registered). **The real candidate, measured:** the three un-portalled sites are the print-options filter row (`PrintEquipmentListClient.tsx:427` Method, `:437` Category) and the revisions grid (`PreviousRevisionsClient.tsx:1017`). They were excluded because neither has a clipping ancestor — the wrong test: the menu is `absolute` (`AutocompleteInput.tsx:666`) so it does **not** extend the document's scroll height, and the flip-upward geometry is gated on `portalDropdown` (`:328`), so the sites denied the prop are denied the fix. Matches Daniel's own CDMX words. **No screenshot from Jamie exists and none is needed.** | Rides BATCH-PREBETA |
| **CABLE-CHANGEALL-METHODNULL** | ⚠ **RULED 2026-09-15: PULLED FORWARD — rides BATCH-PREBETA.** `CableListClient.tsx:3499-3508` — a cable Change All on Method resolves the typed name and falls to `?? null`, so **a typed `0` OR ANY UNMATCHED NAME writes `method_id: null` across the whole cable found set.** Same defect shape fixed on the equipment side 2026-08-31 (CHANGEALL-METHODNEW), still live on cables, on a surface 30–40 cohort members are about to use. The equipment-side pattern to copy already exists. | CC, in BATCH-PREBETA |
| **SYSROW-REPORTMSG** | Does the operator need the report to say a system row was protected? `guard_system_method` refuses the deletion silently today. A wording question, not a mechanism one — Daniel's ruling, then it rides whichever equipment messaging unit is open. | Daniel; then the next messaging unit |
| **OQ-UNPLACED** | ⚠ **The questions that have no owning unit, held in ONE row so that unplaced is visible rather than invisible** (QUESTIONS-IN-ROWS): script 551 extraction · cable Change All checkbox eligibility · house-wide Cancel semantics · Used∩Ignored · the empty-string note · the link `field_name` CHECK backstop · the cable-type RENAME affordance · CL a-1 uploads · d2_01's red Members count · CL multi-letter-per-project · stale-category healing · CC credit-budget policy · OQ#2b own-band pendency · band-cascade harmonization. **Fourteen items. Each is either placed into a unit's row or closed; none stays here past the sweep.** | Arch, one parallel sweep after the invite |
| **METHODS-PALETTE** | ⚠ **RULED 2026-09-15: methods use the shared palette — the one that already governs project, cable and box colour.** That palette is `src/theme/minotaurColors.ts`, whose own header calls it the single source of truth; its fourteen named hexes already drive the project-colour picker, the cable and box colour lists, `ColorSwatchPicker`, the tail-label print geometry and the v1 import's colour domain. **So the "pale" values WERE the palette all along** — Pink `#F6C6D8`, Aqua `#94E3FE`, Chartreuse `#B8D586` are its own entries, and the saturated values are not in it at all. The seed was never the drift; `global_default_methods` is. **Measured 2026-09-15: EIGHT of its NINE rows are off-palette** (only `House #000000` is legal), across **seven** distinct off-palette hexes; and **54 of 162 coloured methods are off-palette in live data** across six projects — Test 16, Comet Fall '26 8, Matched Pair Fixture 8, Memoirs 8, tesr 8, Liberation 6, of which only the 22 on Memoirs, Liberation and Comet Fall '26 are real shows. **BVSC, the US Tour, the Cable Parity Fixture and Vape! are clean.** **Mapping RULED by Daniel 2026-09-15, each stray to its nearest palette colour by name and by eye:** `#80FF00`→Chartreuse `#B8D586` · `#FF69B4`→Pink `#F6C6D8` · `#00FFFF`→Aqua `#94E3FE` · `#FFA500`→Orange `#EEA036` · `#FFE800`→Yellow `#FFF002` · `#808080`→Grey `#C2C3C3` · `#C2B280`→Sand `#E4BF8F`. No method changes what it means. Two halves, both Arch's: the defaults table, then the live rows. Trigger B. | Arch data migration, before the invite |
| **APPLY-TIMEOUT** | **Phase 1 returned 2026-09-15; Phase 2 and 3 were held by Arch until 65–69 were applied, and that hold is now RELEASED.** Measured: full size is ≈7,400 spine rows over thirteen tables + ≈3,100 identity-map rows + a 160 kB blob — small for a database. ⚠ **The import is ONE 8-second budget, not 59** — 59 write statements, no loops, clock never resets, proved live; **chunking inside the procedure is eliminated**, a split must cross an RPC boundary. ⚠ **The clean abort has one loose end:** the version-history snapshot is a separate committed call ahead of the import, so a timed-out import leaves a backup for a gesture that never happened. ⚠ **No failed import has ever been recorded and no import has ever been timed** — `import_runs` logs inside the same transaction and its start/finish stamps are identical by construction. `import_apply_v1` carries no function-level timeout while two siblings in its schema carry `5min`. **Absorbed question — is there an HTTP or gateway request ceiling shorter than 8 seconds?** If there is, raising the database ceiling accomplishes nothing; Phase 2 answers it. **Absorbed question — should a failed import be recorded at all?** It cannot be today; Arch proposes, Daniel rules. Trigger B on any change. | Parallel, resume now; gate ≈Sept 18 |
| **BETA-INVITE** | The invite note (Email Prompt) and the one-page cohort brief (Writing Prompt). Required: the two ruled sentences (forgot your password → email me; v1 stays your source of truth), the three-step sign-in instruction, the two-sentence terms fragment, one sentence on GLOBAL-LIB. RULED 2026-09-13: no confidentiality ask anywhere. CAND-4's four personal texts fold in. | Daniel, before ≈Sept 18; MAILGATE per send |
| **COHORT-ACCOUNTS** | Fourteen of sixteen still to create by hand (create user, auto-confirm, no password — no mail sent). ⚠ **Tippett `jtippettsound@gmail.com` and Tracey `mike@mtsounddesign.com` exist** — Tracey's is at his sending address, not the `mtracey@me.com` COHORT-EMAILS ruled; the row is corrected here so no future session "fixes" a working account back to a dead one. Haggerty needs a membership row, not an account (CAND-4). Then **PROFILE-REALNAMES**: Arch writes real names into `profiles.display_name` in one confirmed pass — Jamie's currently reads `jtippettsound`. ⚠ **Now doubly load-bearing: `project_member_names()` is live, so display_name is what every member sees of every other member.** **Absorbed question — should first and last name become separate columns, and does `registry.people` own them?** `registry.people` has **only** `display_name` today and is the right eventual home; Daniel rules when the names are written. | Daniel, before the invite; names then Arch's |
| **CONTRAST-PASS** | ⚠ **The unlock.** Jamie: raise contrast everywhere in both themes and assess type that is too small; the proposal is five renderings, or a temporary switcher between five mockups. DEADCLASS-SURFACE's 105 dead `text-surface-400..700` classes have sat blocked on "which should be dim is not machine-decidable" — five mockups make it decidable by looking. **Absorbs DEADCLASS-SURFACE · the bounced auth link colour · CONFIRMBTN-FLIP · CONTRAST-PAIR (`ImportV1Client.tsx:1105`) · the type-size assessment · and NEW: SORTBAND-DARKGROUND.** ⚠ **SORTBAND-DARKGROUND (Daniel, 2026-09-15, measured):** the outer sort band's ground is built per theme in `src/theme/hcTheme.ts` — dark at **4.5% lightness** (`:90`, stops 9%/7%) against light at **14%** (`:144`, stops 15%/12%). `.sort-band` (`globals.css:214-230`) was specified to stay dark in BOTH themes, Zite's group header verbatim, which reads as a strong band on a light page and vanishes on a dark one. **It is band-versus-ground separation, not text on a ground, so the target is a non-text boundary contrast**; report APCA Lc for the band text beside it. ⚠ **Declared baseline event — the band renders in the gate frames.** APCA reported beside WCAG. ⚠ **Runs AFTER FONT-FALLBACK.** Acceptance is Daniel picking one rendering. | Week one |
| **VIEWER-ROLE** | **Now buildable — `viewer` and `admin` are legal roles as of migration 69.** D-1: the beta ships **editor-only**, so nothing in it is read-only; viewer and its whole-app control inventory move here together — every edit control on every surface must be found and hidden, because a control that would be refused must not be rendered. ⚠ **Reinforced by measurement 2026-09-15: RLS on UPDATE is SILENT.** A member editing a show's settings affects zero rows and raises nothing — proven for editor and viewer, while the owner affects one. So an un-hidden control does not fail loudly; it fails invisibly. Re-reads **D-5** (`private_notes` readable by any member) with the role in hand. | Week one |
| **BATCH-HOUSEKEEPING** | What does NOT ride BATCH-PREBETA: **PRINT-RULES runs after this** (ruled 2026-09-14) — housekeeping repairs the fence and the frozen-check hook, and PRINT-RULES is measured by them. Remainder after BATCH-PREBETA takes its share: CCSED/SEDRULE items belong to PROCESS, not here. | Week one |
| **PRINT-RULES** | One bundled handoff, ruled 2026-09-13: solid row rules (EQUIPLIST-DOTRULE) with the preview clip measured in the same pass (PREVIEW-CLIP), methods without a sort override printing after those with one (METHOD-SORT-ORDER), and the ruled hint line (PRINT-HINT-EQUIP, `PrintEquipmentListClient.tsx:951`). ⚠ **NEW — EQUIPSORT-NODESC-PRINT:** the on-screen list now sorts a blank description FIRST (shipped `9e581be`) while `printEngine.ts:909` and `:959` still sort it LAST through the shared comparator. **Screen and paper deliberately disagree until this unit rules it against a real v1 print.** **AFTER BATCH-HOUSEKEEPING, never bundled with it.** Frame 07 may move (baseline event). **Absorbed questions, all v1 measurements this unit takes as it goes:** does the SMALL box layout's Box Name carry a Conditional Formatting rule (OQ-3) · the dashed grey cut rules are fully specified and the build is pending (OQ-4) · which cell edge is the parity target (OQ-5) · the v1 continuation header · the cover-page body size. | After BATCH-HOUSEKEEPING |
| **VERHIST-REST** | **VERHIST-TRUNCATE is CLOSED by migration 66.** What remains, in order: **VERHIST-COUNTER** (makes the truncation class impossible rather than merely unreachable — D-6; the proposed shape, unnumbered until written: a definer function returning true per-table counts, called by `snapshot_project_v1` after the payload is built, raising when any array length disagrees) · **VERHIST-FORK** ("open as new production" — a migration, rehearsal-first, design at `drafts/260911-1600_verhist-fork_DESIGN.md`; ~4,000 rows against the 8-second ceiling is the open measurement, and APPLY-TIMEOUT's Phase 1 now says that budget is shared, not per statement; Trigger B; DEFERRED past the invite) carrying **VERHIST-REVBLOB** · **VERHIST-GESTURECLASS** + **VERHIST-680** · **VERHIST-BLOBWATCH**. | COUNTER week one; the rest after the invite |
| **BATCH-LAYOUTS** | The nine queued v1 layouts, designed 2026-08-28/29 against real v1 prints, **none built, all greyed out**: LAYOUT-FINDSCREEN (six land on a find-or-options screen v2 has never built; four want the same `Add Blank Records` skip-N control — **design the screen and the offset ONCE**) · GROUPPARTS · DEVICEPATCH (the largest, 1,919 rows / 384 devices) · EQUIPLABELS · BUNDLESUMMARY · LINELIST · BUNDLELABELS · STECKLABELS · TAILFOLDER. Arbiter filenames, measured counts and per-layout estimates in the Ledger's twenty-first append. Blocked on MARKER-OVERHANG for real stock. | After the invite |
| **BATCH-BOX** | The box residuals, one family: **BOX-DOC-PARITY** (⚠ its trigger fired 2026-08-30 and the row sat in HELD until the 2026-09-14 audit — the Box List and Contents List have never had a v1 parity round) · **BOX-PRINT-CLIP** · **BOX-REPRINT** (Daniel's desk) · **EQUIPLIST-ESTIMATE-PAGINATOR** · SMALLNAME-STEP · BOXLABEL-ZERO · BOX-REPARENT · BOX-NAME-UNIQ · BOX-ALPHA-UNSAMPLED · BOX-TYPE-VOL · BOX-LABEL-COUNTS · BOX-EXPORT-ORDER. **Absorbed questions:** does Box-in-Box suppress fields other than the Stack (OQ-B) · do the 39 boxes whose types BOX-TYPE-LIB created want dimensions (Daniel's ruling, when the unit reaches them). | After the invite |
| **FIELD-CONTRACT-3** | Leg 3 and its dependants, one unit: CABLEGROUP-EDITOR · CABLE-FINDDEFAULT (`CableListClient.tsx:476` reads `[data-b2-field]` only) · FINDFLAG-CHECKBOX · CHANGEALL-METHODDOCTRINE · **CDMX-DEFERRED**'s four (CABLE-NAME-COMMIT, CABLE-CHANGEALL-BOX, CABLE-TAB-BUNDLE-METHOD, BOXBAND-DOCTRINE) · FIELDCONTRACT-3B (scope from the Ledger's tenth append first). | After the invite |
| **EXPORT-INTEGRITY** | One export unit rather than six: EXPORT-HISTID-DROP (**the 852-of-1,119 figure is contradicted and must be re-measured before design**) · EXPORT-CARRIES-STATE · CATNOTE-EXPORT-LF · RENAME-HASHDRIFT · APPLY-PROMISE-GAP · CABLE-AUDIT-TIME · LINECOLOR-ZERO · ROUNDTRIP-INMEM · LINKJOIN-FENCE · **IDMAP-ORPHAN-REIMPORT** (⚠ **823 orphan identity rows on BVSC Mexico `cf780353…`, re-confirmed exactly 2026-09-15**; what a re-import does against that residue is unmeasured) · BUNDLE-LINK-IMPORT. ⚠ **FIXTURE-NUMLABELS-GONE first — look at what ran before re-importing; a re-import overwrites the evidence.** **Absorbed question — what selects the 596 rows in v1's `cable clone links.xlsx`?** (CLONELINK-ROWSET, a v1 measurement this unit takes.) | After the invite; IDMAP before any import unit |
| **CABLE-SPLIT** | v2 has split columns and no split feature. Trigger A. Carries SPLITEND-DAISY and SPLITOF-NO-ARM. UNBLOCKED 2026-09-13: Paradise Square (New York) at `~/Minotaur_v1_exports/Paradise_Square_NY/` — 24 cables with splits, 146 `Split of` rows. Ruled: no upload to minotaur.app until the unit is designed and built; the upload is then its test. | Arch design, after the invite |
| **BATCH-HARNESS** | HARNESS-SIGNOUT · HARNESS-THEMEWRITE · GATE-METHODRACE · GATE-SETTINGS-FOLD · GATE-DEVCHECK · GATE-PORT · GATE-LOAD-AUDIO · CAPFLAKE-LIBLIGHT (**must not enter `diff-exceptions.json`**) · DIFF-EXC-BOUND · HARNESS-WAIT · GATE-THEMEGLYPH (Δ42, light only, reproduced twice — **never register it**) · REPRO-SUSPECT · SENT-BUNDLE · HARNESS-READ's fixture half. | H3, after the invite |
| **SECURITY-PREPUBLIC** | One pass before public beta: GRANT-TRUNCATE (residual `service_role` TRUNCATE on 35 objects; TRUNCATE ignores RLS — ⚠ **and `service_role` holds no SELECT/INSERT/UPDATE/DELETE on the cable family, measured 2026-09-15, so TRUNCATE is its only reach there**) · HARNESS-READ (a read-only role; today no harness can read the database) · RLS-INITPLAN (`auth.uid()` unwrapped, plus 38 unindexed FKs — **read together with RLS-HELPER-COST, they are the same surface**) · **REPO-MIGRATIONS-ABSENT (two files against SIXTY-NINE applied — the database cannot be rebuilt from the repo; BATCH-PREBETA adds five of them)**. All Trigger B. | Before public beta |
| **LIBRARY-DEFAULTS** | **Design unit, Trigger A — three of Jamie's asks, one surface.** On new project creation the user chooses whether to seed from their own global library (refer to Zite); the same choice for default methods; and an inspector at the foot of the equipment list to edit library defaults — default category, default note, rack spaces — per v1's graphics. ⚠ **Needs a per-user library tier that does not exist:** `equipment_library_items` holds **zero** rows with an `owner_id` and **zero** unscoped, re-confirmed 2026-09-15 — and migration 67 deliberately PRESERVED the personal `owner_id` arm for exactly this. Supersedes GLOBAL-LIB's "not a gate item"; one sentence about it still goes in the invite. Schema work, so Trigger B follows Trigger A. | Design after the invite |
| **NOTES-MULTILINE** | Jamie: a multi-line note shows as one line on an unfocused row with a subtle more-than-one indicator, and expands when the row or the notes field has focus. ⚠ **Kept out of the cohort batches deliberately** — it changes row heights, and row heights are measured against v1 and feed the paginator. Pairs with EQUIPLIST-WHOEST, whose estimator models a layout the renderer no longer draws and has no wrap model. | Its own scoped round |
| **METHOD-STRAYS** | ⚠ **Registered 2026-09-15, deprioritised by Daniel the same day ("not important right now").** The create-on-any-unmatched-text path mints a permanent method from any typed string. In live data: **four methods named `0`** (Cable Parity Fixture, BVSC US Tour, BVSC, Versioning Test — all `is_system = false`, **0 items use any of them**), and Daniel's own picker shows `i` and `BVSC` as strays, `i` being a single keystroke. **Removal is a destructive write on real projects, so it is Arch's hand on Daniel's confirm, never CC's.** The minting path is closed for `0` only (`9e581be`); any other typo still mints. | Daniel, when he wants them gone |
| **REGISTRY-REST** | The registry's remaining legs: **REGISTRY-2** (the admin surface; ⚠ `registry.admins` holds 0 rows, so `is_admin()` returns false for everyone including Daniel — its first job) · **REGISTRY-IMPORT** (dry-run the LICDB workbook in a rolled-back transaction and publish the reconciliation — 747 people · 778 spine rows · **181 live serials, the figure that must not move** · 477 invoices · $96,503.42 · 425 unmatched · 241 eligible; the 425 import as a worklist, not a graveyard) · **REGISTRY-3** (payments, after Strat picks the provider) · **ENTITLE-V1** (answered and half built — the gate reads `my_entitlement()->>'v1_live_until'`, applied at migration 64; what is left is the data) · **BETA-SIGNUP-GATE** (`v1_beta_eligible(email)` applied and granted to `authenticated` only; the `anon` grant is withheld until the rate-limited route exists) · **V1-ISSUE**'s admin screen · **SEAT-CAP**'s mechanism. | Arch parallels |
| **LEGAL** | **LEGAL-SELFDRAFT** (terms adopted and in force; still to draft: the IP assignment and **PRIVACY-POLICY**, promised to the cohort in §12 before the v1-holder beta — its own parallel on the beta-terms pattern) · **TERMS-60DAY** (the post-licensing window returns to 60 days; four places, all in `drafts/`, none in the code repo — ⚠ **it was clean only while `terms_acceptances` held 0 rows, and it now holds two; this is a different act than it was**) · **LEGAL-SEP** (attorney review of the self-drafted documents plus the subscription agreement before v2 charges anyone; nineteen packet items, plus D5 and D9) · **GBL-527A** (a build requirement, not only an attorney question; `registry.subscriptions` carries a column per requirement, unapplied) · **DEEP-ARCHIVE** (⚠ no row existed until the 2026-09-14 audit; the R8 drill closed with **F9: NO COVERAGE TODAY**; carries the attorney's 7-day persistence window and goes on the agenda **before** the archive is built). | LEGAL-SEP scoping, week of Oct 5 |
| **STRAT-OWED** | **BRIEF-13** (carries PRICE-199, BENCH-FIX — Lightwright is $399/year, not $149 — COMPETE-FIX, the sharing rationale; the RELEASE line above is operative until then) · **COMMS-Q** · **COMMS-REWRITE** (Comms Prompt v9 deleted and rewritten from scratch) · **REGISTRY-DESKWORK** (the eight unreachable live v1 holders — Weston $898, Potts $495, Barsky $433, Sonnabend, Proctor, Leach, D'Amore, Schloegel; the fifteen paid B-list names; the Square Customers export, the highest-leverage missing input) · **BUNDLESUM-MARKS** · **METRIC-UNRUN** · **TOK-VC** (Vercel token rotation, hard ~Nov 8) · **HERO-PLURAL**. | Daniel and Strat |
| **PROCESS** | Everything harness- and document-shaped, in ONE parallel and never on the main line (ruled 2026-09-12): PROCESS-REFACTOR's remainder · CCSED-260911 and SEDRULE-REMAINDER (`perl -i` / `gawk -i` / `ruby -i` can edit any repo file with no guard) · WF1A-SUNSET and WF1A-PHRASE · UNATTENDED-MEASURE (three measurements before any unattended-CC build — **never a build first**) · CORPUSBOUND-AUDIT · FENCE-COUNTS · **NEW: DOC-RETIRE** (⚠ **Daniel 2026-09-15 — project knowledge carried both Roadmap v4.38 and v4.37; an install must retire its predecessor in the same step. Audit the whole doc list for the same fault on the Session Log, the Brief, the Architecture Prompt and the Strategy Prompt.**). ⚠ **A parallel that starts proposing process which costs build time has failed.** | Sprint 4 close; release trigger is week-one fixes shipped |

### ⏸ HELD — waiting on a named unit or trigger

| ID | One line | Trigger |
|---|---|---|
| **CATCOUNT-BREAKDOWN** | A per-kind breakdown of the category count — equipment-list items, cables, tails and boxes shown separately rather than as one total. **Daniel 2026-09-15: wanted, but much later.** Widens the column and turns an admin tab into a small report. | After public beta |
| **ORG-TIER** | An organisation account — the theatre or shop owns the projects, an admin manages seats. Lightwright's $375/seat is the ceiling. **Not precluded and not built:** `registry.license_seats` and `licenses.seats` applied at 62, so an org licence later is one table and a nullable column. | Strat, after the first paid quarter |
| **MONTHLY-HOLD** | A monthly Individual only if LICDB's usage distribution shows the few-shows-a-year segment is small, priced so annual wins by month seven. Becomes a query against `registry.payments` once payments land. | Strat, after the first paid quarter |
| **INSPECTOR-ARM** | Change All did nothing from a Method field with no row selected; **the recorded cause does not match the code** (`method_name` is an ordinary row cell). Arch measures on the running app before proposing a fix. | Arch, measurement first |
| **CAT-MERGE** | No `merge_categories` function, so a category rename collision refuses and reverts. Trigger B. | Its own later unit |
| **REVGROUP-SPLIT** | Revision-mode print groups by model alone while the plain list groups by (model, note); changing it means deciding how new rows pair against old snapshots. **Pairs with EQUIPSORT-NODESC-PRINT now that screen and paper disagree.** | Its own unit |
| **CATBAND-CASEDUP** | ⚠ **New, from CP3's return.** The category band editor's pristine check compares `match.name === label`, so two *distinct* category rows differing only in case and sharing a rank are now one band labelled with the first spelling, and picking the other spelling is a silent no-op. Needs duplicate case-variant category rows in one project to reach. | Its own unit, or the next category work |
| **MENU-DEPRECATE** | Remove Wire Types and Wiring Schedule from the main menu. Likely a declared baseline event. | Rides a UX unit with a working gate |
| **SPENDCAP-BETA** | Keep the Supabase spend cap on. PITR is outside it and bills regardless. | Before public beta, or at half any quota |
| **LINKEDBOX-LIVEPROOF** | A live browser proof that a cleared Box on a linked cable fans out to its pair. Pinned by test at source 2026-09-15; `gateLinked` is a component closure, so no live write was driven. | Rides the next cable unit |
| **DEVICE-FK-LINK** · **CATSORT-NATURAL** · **EQUIPVIEW-UNSAFE** · **EQUIPLIST-QTYCOL** · **SKIP-LABELS** · **BUNDLE-NA-UNTESTED** · **FIXTURE-NUMLABELS-GONE** · **HISTID-HASH-COST** · **SMOKE-LOGIN-HYDRATION** · **IMPRPT-COPY** · **BUILDSHEET-CMT** · **DEV-SERVER-AGE** · **SHOP-ORDER-NOTES** · **BUNDLE-FIND** · **SED-GUARD** | unchanged; each waits on the unit named in v4.37 | Their units |
| METHOD-FIX · GATE-OL25WR · GATE-C4 · S504 · CONCEPT-A · UNALLOC · EFFGRP · DOOR-V1LINK · CABLE-LOADER · RULINGS-WANTED | unchanged from v4.6 | Icebox sweeps |

### 📐 STANDING — binds indefinitely; never closes

| ID | One line |
|---|---|
| **MAILGATE** | Nothing sends email to any address that is not Daniel's own until he approves that send in a message whose only content is the approval. One approval, one send. CC never sends mail or provisions accounts. |
| **SESSION-SHAPE** | ⚠ **Daniel, 2026-09-15.** A session opens with a **CC handoff first** — bundled as large as the ruled material allows. Only once Daniel confirms the handoff is in CC's hands does the seat spin off whatever **parallel Arch designs** the next units need. Arch never idles waiting on CC. |
| **HANDOFF-BUNDLE** | Small fixes ride as few, long handoffs as possible. |
| **QUESTIONS-IN-ROWS** | ⚠ **Daniel, 2026-09-15 — the Open Questions section is ABOLISHED.** A question may not be recorded anywhere except inside the row of the unit that will answer it, or as its own row with an owner and a trigger. The Roadmap has four sections and no others; a fifth list inherits none of the Docket's discipline, which is why questions sat unowned since July. |
| **HIST-BLIND** | v1's field-level audit lives entirely in OnObjectSave triggers, which FileMaker's `Replace Field Contents` does not fire. **Any scripted mass edit over a found set is invisible to v1's `history` table.** This bounds what that table can ever prove, and no question about intent in v1 data can be closed from it. |
| **POLICY-CAPTURE-FIRST** | ⚠ **Added 2026-09-15 after migration 67.** Any migration that drops or replaces a policy captures `pg_policies` for every affected table into the return **before** the apply, so a rollback is from record and never from reconstruction. |
| **HELD-IS-NOT-FORGOTTEN** | A HELD row without a named release trigger is illegal; every close checks whether any trigger has fired. |
| **SWEEPLINE-NOT-DURABLE** | Anything that must outlive one session goes in a row, never a summary line; every propagation claim is a search before it is a sentence. |
| **APCA-NOT-WCAG** | For dark-mode text the WCAG ratio cannot see polarity; report APCA Lc alongside it. |
| **CONTRAST-PAIR-RULE** | No white text on yellow. A static ground wants static text; a flipping token over a static ground is the defect class. |
| **FONT-IS-NOT-SHIPPED** | Any measurement of a printed or laid-out surface states which typeface rendered it. The app does not ship Century Gothic. |
| **GATE-SETTLE-BETWEEN** | A reliability measurement lets the machine settle between runs; a refusal captures nothing and is never counted. ⚠ **Second citation 2026-09-15: timings taken inside a DDL transaction produced a phantom 28× regression and nearly rewrote a rehearsed migration. A measurement sharing a transaction with schema changes is not a measurement.** |
| **GATE-REPORTING** | A green machine gate is never a substitute for Daniel's browser gate. |
| **GATE-NOGREP** | Never pipe a gate run through a filter; write it to a file and read the file. |
| **TOAST-ON-WRITE** | `pg_column_size` on a computed value reports the uncompressed datum. Any storage estimate cites a stored measurement or says it is an expectation. |
| **PROJECT-NAME-NOT-UNIQUE** | Two productions of one musical share a show name and that is a feature. Never add a uniqueness constraint; every citation names the project id. |
| **DDR-IS-THE-SOURCE** | Go to the source that made the ink: `docs/reference/v1_ddr.xml` (UTF-16LE, read via `iconv`). |
| **GRAPH-BEATS-RERUN** | An import graph says whether a frame could ever have seen the change; two re-runs is the limit. |
| **FOUNDSET-NOT-COUNT** | A count that matches is not a found set that matches. |
| **TENSE-IS-A-CLAIM** | An expectation written in the past tense reads as a measurement. |
| **DEAD-CODE-IS-NOT-SAFE** | A symbol with no production callers is dead code, not low blast radius. |
| **BROWSER-NOPRINT** · **PDF-CHROME-PATH** | The built-in browser has no print; a parity print is valid only through Chrome's own Save-as-PDF. |
| **HARNESS-RLS-CORPUS** | The parity fixtures are invisible to the test user; every harness count is a labelled reproduction on a throwaway. |
| **FENCE-ONEUNIT** | The run fence carries one unit, not a history; retired fence text goes to the Ledger. |
| **PARALLEL-CLOCK** | `YYMMDD-hhmm` in a filename is the ordering key; write it at the time of writing. |
| **PORT-COV-REIMPORT** | A v1 re-import path is a product requirement; acceptance is round-trip, not column order. |
| **ARCH-OUTPUTS-MIRROR** | Files written through the Cowork output path mirror into the code repo. Intermediates go to the scratchpad. |
| **CONTCAT-DIVERGE** · **PAGEBREAK-MODEL** · **CATNOTE-TYPE** · **IMPORT-LINEBREAK-CR** · **LABELSDONE-RESIDUE** · **EQUIPLIST-ROWH-BIMODAL** · **R-7 LABEL SETS** · **LEN-SCALE** · **HISTENTRY-NOTID** · **JOY-NAME-HYGIENE** · **LINES-NOFK** · **CLONELINK-ROWSET** · **BVSC-8POWER-DRIFT** · **PRINTPROBE-MEDIA** | unchanged from v4.37; ruled divergences and standing cautions. Do not "fix" any of them back by citing the artifact. |

### ✅ RETIRED TO THE LEDGER — index only

| ID | Closed at | Ledger |
|---|---|---|
| **SHARE-MIGRATIONS** — 65 `share_revb_helpers` · 66 `share_revb_cable_family` · 67 `share_revb_sweep` · 68 `share_revb_settings_features_storage` · 69 `share_revb_roster`, all applied 2026-09-15, each rehearsed under forced rollback with a residue check and Daniel's confirm | v4.39, 2026-09-15 | twenty-second |
| **VERHIST-TRUNCATE** — closed by migration 66; a member's backup now carries its cable system | v4.39, 2026-09-15 | twenty-second |
| **WHOCOL-RLS** (database half) — `project_member_names()` applied at 69; `profiles` stays self-only | v4.39, 2026-09-15 | twenty-second |
| **BATCH-COHORT-1** — shipped `9e581be`, live 2026-09-15: GROUPCELL-BLANK (and blank Circuit and IP Address with it), EQUIPSORT-NODESC (screen), BOXBAND-DUPKEY (five sites), CHANGEALL-NAALIAS (planner + both bulk arms); the linked-cable Box blank verified, not rebuilt | v4.39, 2026-09-15 | twenty-second |
| **AUTH-FRONTDOOR** — CP1+CP2 shipped `3b98ce9`, live 2026-09-14 | v4.38, 2026-09-14 | twenty-first |
| **AUTH-SMTP** · **AUTH-DASHBOARD** — Porkbun `smtp.porkbun.com:587` as `info@minotaur.app`, sender name **Minotaur** | v4.38, 2026-09-14 | twenty-first |
| **SHARING** REV-B design · **COHORT-EMAILS** · **OTPLEN-MISMATCH** (b) | v4.38, 2026-09-14 | twenty-first |
| **TERMS-ACCEPT** — shipped `1f82072`; the beta terms are IN FORCE | v4.37, 2026-09-14 | twentieth |
| Everything earlier | v4.36 and before | by append |

---

*Roadmap v4.39 · 2026-09-15 · retires v4.38. Cap: **40,000** characters. Code **`9e581be`** live. **Migration ledger 69**; suite 3,355 / 175; frozen 3/3; visual 28/28; no baseline event; `CLAUDE.md` 110,968 characters. One CC unit shipped and five migrations applied this session; one parallel returned Phase 1. **The ❓ Open Questions section is abolished at this version (Daniel, 2026-09-15) — every question now lives in a row with an owner and a trigger, per QUESTIONS-IN-ROWS; Q-1, the v1 label counts, was closed off the list unremembered and its consequence recorded in the Ledger.** ⚠ **NOW and ⏭ NEXT are carried VERBATIM from v4.38 and are stale — Arch never writes them; the proposed replacement was given in chat for Daniel to edit.***
