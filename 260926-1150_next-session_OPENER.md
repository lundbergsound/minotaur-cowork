# NEXT SESSION — opener

*Written at the close of the 2026-09-24/26 Cowork-Arch main line, under **Architecture Prompt v39**. Read the
governing set first, in the prompt's order: Brief v1.2 · Roadmap **v4.51** · the Ledger on citation · the Session
Log.*

## State — read from the files at the close, 2026-09-26 11:50 ET. Verify it anyway.

- Code **`cef0949`** = `origin/main` in both ref files (CHANGEALL-REFUSAL, pushed 2026-09-26). **Never run `git` in
  either repo folder.** Three stale lock files sit in `Minotaur/.git/`; none blocks git; Daniel's to remove.
- **Migration ledger 83** (live-read 2026-09-24; no migration since). ⚠ Live-read it. `supabase/migrations/` holds
  22 files, 72–83 with a file of record.
- Session Log at **6** entries. Governing set: Architecture Prompt v39 · Roadmap v4.51 · Brief v1.2 · the log.
- **No CC session is live.** Nothing is uncommitted in the code repo.
- **The licence-management parallel (opened 2026-09-20) has not returned.** SEED-DEFAULTS waits on it.
- Rulings made this session are in the Ledger's 2026-09-24/26 append and `drafts/260924-1805_session_RULINGS.md`.

## First turn — one bundled CC handoff (SESSION-SHAPE, Daniel 2026-09-26)

Daniel: *"plan to have Arch issue the largest, most bundled handoff possible in its first turn. once I confirm CC is
running, have it spin off parallel sessions for any open arch design work."*

After the seat check, write **one** handoff, unit name **WILL-A**, carrying:

1. **METHOD-CREATE-CABLE** — on the cable list, a method name that doesn't exist is created and applied, on Enter
   and on Change All, replacing the refusal shipped in `cef0949`. Daniel, 2026-09-26: *"Adding a method called
   Nutes should make that a method that appears in GCM."* Mirror the equipment list's CHANGEALL-METHODNEW path.
2. **TAB-AFTER-SCOPE** — after the equipment list's "just this one or all" question, focus returns to the cell so
   Tab moves on. Measured: `resolveConfirm`, `EquipmentListClient.tsx` ~`:2022` at `3809860` — **re-measure at
   `cef0949`**.
3. **CABLETYPE-ADD** — spec in `handoffs/parallel/260922-1237_cabletype-add_TO-MAINLINE.md`.
4. **LIBRARY-DELETE** — only if Daniel has said delete may ship before any restore exists. Ask him in one line
   **in the same message as the handoff**; if he hasn't answered, it stays out.
5. **The gate for MMM-CAT and the equipment Category refusal**, carried in this unit's gate steps.

⚠ **PROPOSAL-ASKS-FIRST.** Any user-visible choice in these four that is neither Daniel's words nor v1 evidence
(for example a new method's colour) is **left out of the handoff** and asked in plain words — it never reaches
the gate as a surprise. Census the bundle before writing it (the prompt's rule).

⚠ **JOY-READ-ONLY.** Every gate step that writes data names the Cable Parity Fixture or a `daniel+test01` show.
Never JOY. Write every step as what Daniel clicks and what he should see.

**SHORTCUT-HELPER** ships alone afterwards (it re-baselines the top bar).

## Once Daniel says CC is running

Write three **parallel openers** (`handoffs/parallel/`, with `drafts/` copies, real-clock stamps):

- **STALE-WRITE** — the cheap fix: a write that changes no rows says so and refreshes. Design only.
- **LIBRARY-PANEL** — the read path (the LIBRARY return's one open call).
- **PRINT-REPARITY-BOX** — measure the JOY box pair against v2 and design the box family's round.

The main line meanwhile:

- **IP-ONE-FIELD** — rehearse the migration for Daniel's Trigger B.
- **Bundle clone/extend R4–R8** with Daniel, one screen at a time, each quoting the return's text.
- **Correct the remaining-layouts design §4.2** ("the device join is exact"; it is case-insensitive). Search first
  (O-8): the same claim is in the Ledger and in `devicePatch.ts`'s header, which is CC's once the repo is free.

## Traps

- ⚠ **Don't let one unit sprawl.** CHANGEALL ran five inbound handoffs over four days and Daniel lost the thread
  (*"I don't know what we're working on or why"*). If a gate fails, say what failed in a sentence and re-scope; do
  not chain addenda.
- ⚠ **Write to Daniel, not to yourself.** Plain words first; jargon belongs only in CC handoffs.
- ⚠ **Arch does not drive the app.** CC reproduces on a throwaway; Daniel gates.
- ⚠ **A citation inherited is not a citation read.** Re-measure line numbers at `cef0949`.
- **MAILGATE holds.** Notes to Will, Josh and Aaron are Daniel's to send.

---

## For Daniel

**What shipped:** Change All can no longer overwrite a whole list with a value you never typed, and Enter no
longer quietly creates a cable type. **What you rejected is first:** typing a new method will create it, as v1
does.

**First thing next session:** one handoff to CC with the new-method fix, the Tab fix, adding cable types, and your
check of the category line on a test show — never JOY. I'll ask you one question with it: can library delete ship
before there's any way to restore a version?

**While CC works:** three side sessions design the silent-loss fix, the library panel and the box family, and I
rehearse the IP address change with you.

**Yours, when you have a moment:** the words for the length message · five bundle rulings and ten minutes in v1 ·
a second test account · a word to Aaron about editing one show together.
