# 📒 LEDGER — Minotaur v2 (v1.0)

**Ledger v1.0 • August 9, 2026 • Split at the v3.0 migration from Roadmap v2.46 + Sync Sheets #1–2 (2026-08-09)**

Write-mostly; read on citation. Sessions APPEND at close (the close package's Docket Sweep names what was appended); prior entries are immutable in the Session Log manner — supersessions annotate, never delete. This file may grow without limit; its size is not a defect. **Cross-reference of record:** the forward Roadmap is `Minotaur_v2_Roadmap_v3_0.md` (and successors); nothing OPEN lives in this file — open items live only in the Roadmap's Unified Docket. Any citation of Roadmap v2.17–v2.46 registers resolves here. The frozen v2.46 file is archived byte-exact alongside the Session Log archives.

---

## 📐 Rulings of Record

*(Carried WHOLE from Roadmap v2.46 at the migration — every ruling ID unchanged; then the Aug 7–9 and Aug-9 additions appended. Append per close from here.)*

## 📐 Rulings of Record (register; narrative in Session Log archives)

**Import/identity (July 14–15):** IMP-1..8 + amendments 1–4 (dictionary identity: HistoryID primary / row-hash fallback / `assigned_history_id` minted; mirror re-runs w/ mandatory dry-run + auto-snapshot; no DB staging; staged table scope; provenance tables only; spec-of-record discipline; Category Start Number → rank-based version-sort; Area = IGNORE(computed); **method '0' is LOAD-BEARING**) · IMP-CD text-epoch-dates variance-and-drop permanent · 2B-1 upgrade-in-place · **2B-DUP N-to-N stable pairing** (hash contract FROZEN in spec.ts) · 2C-1..4 · (jj)-OQ1 tie order `created_at DESC, id DESC` (dissolved below group by the sort spec; stands as the stable underlay) · (jj)-OQ2 usage = MODEL match · (jj)-OQ3 throw-loud fetches.

**Export (July 15 PM; unit deferred but rulings stand):** EXP-0..7 · RC-1..10 (**HistoryID emits UPPERCASE**) · D-EXP-3 exact numerics · type='generic' export scope. **S-12 rider (July 19): at revival, re-align export order with the ruled chains** (see Build Order item 2).

**Category-fill on model change (July 20 PM; CAT-1..CAT-4, all Daniel-confirmed — matches v1 D1 step 2, "fill only if blank"):** CAT-1 category is NEVER overwritten on a model change — a record that already carries a category keeps it (v1 functional truth; the "coverage extension" is a per-record blank-fill, not an overwrite). CAT-2 the fill is SILENT — no prompt (both the edited record's own fill and the sibling blank-fill). CAT-3 on a "Change All" commit the new model's default category fills every AFFECTED sibling whose category is blank, and only those; "Just this one" writes no siblings (the edited record still gets its own blank-fill). CAT-4 no new dialog. **Architecture of record:** the new-model default category (`newCat`) is resolved ONCE from the library, independent of the edited record's blankness; the sibling blank set is read fresh (`equipment_items.category_id` for the sibling ids) and filled via a SEPARATE scoped `propagateFieldToIds('category_id', …)` — category no longer rides the unconditional model+note sibling write. Guardrail candidate: a future cascade pass must not re-fold category into the model+note sibling write (that was the clobber-or-skip bug).

**Model-band key is category-aware (July 20 PM):** a model band's identity is the natural triple **`outerLabel␟category␟model`** (U+001F separators), produced by ONE shared pure helper `modelBandKey(outerLabel, catLabel, model, fallbackId)` and used at BOTH derivation sites (the render closure's `groupKey` and `resolveModelGroupKeyFor`) so nav-derived and render-derived keys never diverge. Blank model → falls back to the first record's id (unchanged — preserves `wasPending`/`newRowId` new-row anchor semantics). The three `data-model-group` `querySelector` consumers escape the key with `CSS.escape()`. Guardrail candidate: never derive a model-band key from model text alone — it collides when one model straddles two category (or outer-group) bands, which is now normal under CAT-1.

**Focus-follow on existing-record model commit (July 21 AM; FF-1..FF-5; punch (rr) closed; commit `c23f92d`):**
- **FF-1** plain commit (blur / outside-click / unmount net) on a CHANGED model → re-open the SAME record's model band editor at its post-resort position, text selected, center-scrolled — **orphan-guarded:** the reclaim fires only if `document.activeElement` fell to body/documentElement; a click that landed on a real interactive target wins (no focus steal, no scroll yank).
- **FF-2** typed Enter → open the model band editor of the record BELOW in the **POST-sort** order (supersedes the pre-sort synchronous advance); end-of-list clamps to the edited record itself. Mechanically retires the fragile single-rAF locator on this path — the 5-retry `pendingBandFocusRef` locator is the mechanism.
- **FF-3** Tab / arrow-highlight+Enter / dropdown mouse-pick → Quantity of the SAME record ("next field"), selected, center-scrolled — semantics unchanged from slot 2. **Amendment of record:** the landing is DEFERRED past cascade dialogs when a changed-model cascade is in flight (immediate landing collided with Change All / note prompts stealing focus after it); pristine path keeps the immediate `landInQuantity`. No orphan guard on this arm (keyboard gesture — land unconditionally).
- **FF-4** pristine or blank commits: NO follow, any gesture (nothing moved; nothing to chase). This is FF-1's loop-breaker — the reclaim happens once per actual change; a second click-away commits pristine and the editor stays closed. Enter on a pristine band keeps today's immediate band advance.
- **FF-5 (architecture of record):** the follow rides the cascade `finally` as an added existing-record branch AFTER `releasePin()` and the untouched `wasPending` new-record line — S-7 ordering extended, never reordered. Gesture identity reaches the cascade via `modelCascadeInFlightRef` + `modelCommitGestureRef` set by the band editor's advance callbacks (which fire synchronously AFTER `commitValue→onCommit` — recon-proven at every gesture) — **`AutocompleteInput`'s commit contract is unchanged and the component stays byte-untouched.** Landing resolution happens post-settle in a `pendingBandFollow` effect against fresh `grouped`/`orderedIds` (never the handler's stale closure), through `resolveModelGroupKeyFor` → the triple band key. `ConfirmDialog` verified safe: focuses on open only, restores nothing on close — the deferred landing is the last focus action and wins.

**(kk) bridge build rulings (KK-0..KK-8, July 21; all Daniel-confirmed — "all approved"):**
- **KK-0** four gated legs A (core wiring + read-mostly pages) → B (equipment list) → C ((ss)) → D (images, Trigger B at open). **Storage-cleanup is not separately buildable** — recon proved zero storage existed, so it folded into leg D as the IMG-4 rider.
- **KK-1** architecture of record: pages convert from server-component fetch to thin server shells rendering `'use client'` loaders that run `useQuery` per read (browser supabase client + unchanged `fetchAllRows`; identical RLS/session — security-neutral; layout server auth gates stay). First visit fetches as before; every later visit paints instantly from cache with silent background revalidate. (Next-native-prefetch-only alternative presented and rejected: no freshness story, speculative full-fetch per hover.)
- **KK-2** `src/lib/queryKeys.ts` is the ONE key source (pure, tested). Family prefix `['project', projectId]` = the invalidation handle. Shared reads share keys — `v_equipment_list` per project is ONE cache entry read by equipment, print, and revisions.
- **KK-3** invalidation doctrine: on converted pages `router.refresh()`-as-data-refresh retires → scoped `invalidateQueries`; `router.refresh()` is retained ONLY where server-rendered chrome depends on it (settings save → layout project name/color). Cross-page writes invalidate by family. Call sites classified by recon table, never pattern-replaced.
- **KK-4** equipment-list contract: the `initialItems` seam and resync effect are PRESERVED (query data feeds the same prop). (a) **Unmount write-back** — leaving the list snapshots `itemsRef.current` into the cache so returning paints exactly what was left, never a stale flash. (b) **Guarded resync** — while any in-flight signal is active (`editingModelGroupKey`, cascade in flight, open dialog, pending new record) an incoming resync is STASHED and applied when signals clear; **latest-stash-wins** (CC OQ-2 disposition — newest server truth applied once, no merge/conflict resolver). Transient sub-tiebreak reorder on write-back paint accepted (CC OQ-3 — self-heals on the immediate refetch; mirrors the standing (jj) tie-order caveat).
- **KK-5** preload: project-menu mount prefetches the library reads (leg A) + equipment reads (leg B). Print/revisions inherit equipment's warm cache through shared keys.
- **KK-6** declared visual outcome per leg: 16/16 at 0.0000%, any diff = stop-and-report (held at every checkpoint of every pass). First-visit loading state = existing chrome, render-null while loading.
- **KK-7** the vestigial `?noMethod=1` becomes a real consumer via (ss)'s param machinery: Show Me → equipment list with method filter FILTER_NONE. `landModel` wins if both params present.
- **KK-8** DB surface: zero for legs A–C (ruled at open and held); leg D = the Trigger B surface (bucket + policies migration, Arch-applied + smoked).

**(ss) build rulings (SS-Q1 + SS-1..SS-3, July 21, leg C; spec locked July 20 — both directions, model field selected+focused):**
- **SS-Q1 = A** — context-aware nav chords: ⌥⇧W on the list with a focused row lands on that model in the library; ⌥⇧L on the library with a focused row lands on the matching list record (model band editor opened). No focused row / blank model / no match → plain navigation, silently. Zite has NO matching-jump precedent (header pills are plain nav) — this is a v2 extension, chord-only, zero new chrome.
- **SS-1** match = exact model text, then case-insensitive fallback. **SS-2** multiple matches → first in current DISPLAY order (list: grouped/orderedIds; library: `sorted`). **SS-3** the `?land=` param is consumed ONCE and scrubbed via `history.replaceState` — refresh/bookmark never re-lands; the scrub PRESERVES `window.history.state` (A-rider hardening — never null it).
- **Mechanism of record:** capture-phase window keydown listeners in each client claim their one chord ahead of `useNavShortcuts`'s bubble listener (`preventDefault` + `stopImmediatePropagation`; all other chords flow through; the shared hook is byte-untouched). List chord runs the K-5 flush before navigating; library chord blurs (its writes are fire-and-forget, matching existing edit-then-navigate behavior — CC OQ-2 accepted). `landTarget.ts` (`findLandTarget`) is the ONE matcher for both sides. Landing effects: list → editing-key + `pendingBandFocusRef` (FF idiom, no new locator); library → `data-lib-row-id` + local 5-retry rAF locator → model field focus+select, center-scrolled. Post-landing the open band editor is a KK-4b in-flight signal, so the first resync stashes until it closes — benign, on record.

**Project images build rulings (IMG-1..IMG-6 + A1..A5, July 21, leg D; product rulings (a) scope = the three settings zones (b) PNG/JPEG ≤5MB (c) replace-in-place, no version history):**
- **IMG-1** ONE private bucket `project-images`; server-enforced 5,242,880-byte cap + `image/png`/`image/jpeg` allowlist; object path `{projectId}/{slot}` (slot = `top_left` · `top_right` · `box_logo`), contentType set on upload.
- **IMG-2** four storage policies (select/insert/update/delete, authenticated) mirror the house ownership pattern: allowed iff the path's first folder is a project with `owner_id = auth.uid()`. One security doctrine, applied to storage. Migration `storage_project_images_bucket_and_policies` (Arch-applied via MCP; smoked read-only: bucket + all four policies verified).
- **IMG-3** replace-in-place: fixed path per slot + `upsert: true` — no versions, no orphans on replace. **Columns store the storage PATH**; display resolves via short-lived signed URLs at mount. Legacy branch of record: a stored value starting with `http` renders directly and ✕ nulls the column only.
- **IMG-4** delete-project cleanup: `removeAllProjectImages` (list folder + remove; NEVER throws) — **ordered BEFORE the row delete (A1, ruling of record): the policies verify ownership against the live projects row, so a post-delete remove is denied and orphans everything.** Best-effort: the delete proceeds regardless of cleanup `{ok}`.
- **IMG-5** print boundary: recon found `top_right_image_url` is print's only image consumer (rendered directly as src). **Fold-in ruled (A3):** print resolves a non-http path to a signed URL in its data mapping (cancelled-guarded effect; unresolved renders no image, exactly as before). Top-left render → registered onto the Print header line2 + autosize unit; box-logo render → Sprint-4 box labels. Print needs no cache-buster (per-mount resolve — CC verdict accepted).
- **IMG-6** sequencing held: migration drafted → Daniel confirmed → Arch applied + smoked → CC client pass.
- **A2** image-column writes are DIRECT single-column updates + `projectSingle` invalidation — never the shared debounced autoSave (a superseded debounce silently dropped the column write while the storage upload succeeded).
- **A4** drag-and-drop through the same validate→upload path as the picker (the zone's own label promised it); no at-rest render change (frame 06 byte-identical).
- **A5** replace re-resolve: root = the resolve effect keyed on the (stable) path never re-fired on a same-path replace. Fix = `resolveNonce` bumped per successful upload (effect dep) + a display-only `&cb=` cache-buster on the signed URL (the stored value stays the bare path). ✕-remove needs no equivalent (null is a real dep change).

**Keyboard semantics (July 17–18, slot 2 — full detail in CLAUDE.md, standing):** K-2 one navigation action sets `currentRowId` + `rowFocusRequest` together · K-3 ⌥⇧I toggle · K-4 ⌥⇧J unified Show All — fully landed July 19 · K-5 commit-flush via `trackedUpdateItem` · K-6 Enter = next record, same field, Zite-verbatim · K-7 named delete dialogs + scroll · U2-1 select-on-entry keyboard-only · U2-2 useLayoutEffect double-paint fix · U2-4 ⌥⇧I blur-first · U2-5 Enter-in-band advance-and-follow · U3-3 pristine band commit = total no-op · U3-4 post-delete focus continuity · U3-5 render-anchored new-record placement · U4-1/2/3 pending-record focus lifecycle · U5-1 follow-after-settle · U5-2 centered keyboard scroll · U5-3(+AMENDED) arrow-select Enter = Tab path · U7-1 band-context ⌥⇧↑/↓ · ⌥⇧E = active record always. **July-31 extension (S3F-FIX-2): K-6 Enter-advance now wired on the four cable end fields (source/destination device/connection) via bubbled-Enter + wrapper `endCellKeyDown` — the AutocompleteInput branch only fired inside `data-equip-field` wrappers; cable cells wear `data-b2-field` and the advance was never wired (root cause; the CP-A funnel was innocent). Commit fires, advance is immediate, async patch resolution interrupts from the new focus.**

**Sort spec (July 17, supersedes (qq)) — IMPLEMENTED July 18–19 (sort unit):** by category: category→model→method→description→group · by method: method→category→model→description→group · by box: box→category→model→method→description→group. After group: stable internal tiebreak, engineering hygiene only. Chains apply to SCREEN and PRINT identically; all model comparisons use `compareModels` (SRT-D1..D4).

**Sort-unit build rulings (S-1..S-12, July 18–19; all Daniel-confirmed):**
- **S-1** chain module `src/lib/equipment/sortChains.ts` — `makeEquipmentChainComparator(mode, methodRankByName)`; category term = `category_sort` rank nulls-last then label; method term = `sort_order` rank nulls-last then label; model = `compareModels`; box/description/group = `compareLabels`; returns 0 below the last term (JS stable sort ⇒ the Layer-1 server chain persists as the don't-care tiebreak; server chain itself untouched). Screen sorts `filtered` before grouping. **(kk)-era note (July 21): the Layer-1 underlay sort now applies inside the equipment query fn — one place, verbatim semantics.**
- **S-2** group mode (fourth sortMode) included: group→category→model→method→description.
- **S-3** ONE shared label comparator everywhere: `compareLabels` — empty/null LAST, otherwise numeric + case-insensitive.
- **S-4** print convergence: `buildSections` both branches on the full ruled chains; revision-mode modelRows → `compareModels`; orphan-header merge → `compareLabels`; **`nat` DELETED — never reintroduce**; `revisionMutations.ts:241` migrated. Zite-verbatim option lists (print method dropdown, revisions panel dropdowns) deliberately untouched.
- **S-5** library grid model term → `compareModels` (category term untouched by S-5). **July-24 supersession note: the library page's FULL comparator is now ruled by O2-1..4 (`librarySort.ts`) — S-5's model-term ruling is honored inside it (compareModels imported unmodified); the category term is now the equipment list's rank-then-label shape over the same `categories.sort_order` source.**
- **S-6** FLT-2 pulldown band order (see Closed list).
- **S-7** (ddd) branch behavior: cascade path releases pin + sets follow AFTER the awaited cascade in one batched render (try/finally — release on every exit path incl. rejection); blank/pristine/no-target/cancel release immediately.
- **S-8** clock-pin = option (c), pure-harness DOM overwrite before capture, fail-hard if the node isn't matched; covers BOTH live-date surfaces (frame 01 menu stamp; frame 07 print footer). B-2 drift rule retired. **July-23 rider: frame-01 bounded wait (3rd flake tripwire). July-24 extension: fixture locator waits for the NAMED link + fail-hard (`efb176c`).**
- **S-9** print-stamp frame staging = option (b), **ruled fixture amendment:** permanent revision snapshot "Baseline Rev" (`89e87927-9591-45eb-981b-4ab4e5062482`) on Visual Gate Fixture. Channel-threshold-24 ratified as the diff definition of record.
- **S-10** bundle scope correction of record: packed checkbox was already live; bundle = ✕ removal + toolbar rework. **July-24 extension: S-10's Delete All/Delete Found semantics are the LICENSED TEMPLATE mirrored onto the cable header (a ruled v1 deviation — v1 cable has no bulk delete).**
- **S-11** frames 12 → **16**; `EXPECTED_CAPTURE_COUNT` 16; missing baselines report PENDING without failing.
- **S-12** export drift rider registered (Build Order item 2).

**FLT-batch build rulings (D-1..D-8, July 19; all Daniel-confirmed):**
- **D-1** filter-bar "Display All (⌥⇧J)" button renders ONLY when a column filter is active; fourth sibling after the Group dropdown; renders nothing at rest.
- **D-2** FLT-1 precedence, per field: concrete filter beats the New Defaults bar (name-resolved, null on no-match — never invented); FILTER_NONE seeds null overriding a set default; FILTER_HAS / FILTER_ANY fall through; `remaining_quantity` never filter-affected. **July-23 extension: the cable surface mirrors D-2 for Group/Method AND Bundle (bundle-filter-born membership); the cable header filters are free-text autocompletes with NO sentinels — the sentinel clause reduces to concrete/empty.**
- **D-3** inheritance fires on filter state alone — find activity neither enables nor suppresses it.
- **D-4** `showAll` = `displayAll` + clear three filters, a NEW wrapper — never folded into `displayAll`. Consumers: ⌥⇧J (find-or-filter gate), FOUND-strip Display All, the D-1 button. Wire: `onDisplayAll={findActive ? showAll : displayAll}`, FindPanel byte-untouched.
- **D-5** exemption widening: `createNewItem` AND `duplicateItem` grant the visibility exemption on find-or-filter-active; any filter change clears `exemptNewIds`; duplicate = verbatim copy; Library "+ Add" untouched. **July-24 note: cable's 680 post-write does NOT use the exemption (CP-B4 moves the filters instead — see the cable rulings below).**
- **D-6** seeding logic extracted to pure `buildNewItemSeed` (`newItemSeed.ts`).
- **D-7** defaults-override affordance: Method/Group default fields dim opacity-50 + native title when `isDefaultOverridden(filterX)`; dim only, never disabled; Mark All As Remaining never dims.
- **D-8** filter-pulldown type-ahead = punch (iii), rides Sprint 4.

**Split-unit build rulings (SP-1..SP-9 + SP-1a/SP-7a, July 20; all Daniel-confirmed; standing detail in CLAUDE.md "Split architecture of record"):**
- **SP-1** one Zite-styled `SplitDialog` — **amended by SP-1a** for the over-split path.
- **SP-1a** over-split (n ≥ Q) is a SEPARATE second dialog — `OverSplitDialog`, v1 prompt shape, Zite copy verbatim, `brand-600` accent; its Cancel/Escape aborts entirely. NOT an in-place OK→"Add Instead" flip.
- **SP-2** gate: row glyph + ⌥⇧S enabled on `quantity > 1`; type guard (`generic` only) enforced in the pure module.
- **SP-3** split glyph on the current row only, absolutely-positioned leading-edge overlay, zero layout shift, one `useStableCallback` id-as-argument.
- **SP-4** new-row payload = `duplicateItem` copy shape + dialog overrides; exemption under find-or-filter; NO anchor pin.
- **SP-5** remaining = v1 proportional, CONSERVED: `newRem = min(n, max(0, round(R·n/Q)))`, source = R − newRem; null stays null. Add-instead: source untouched, new remaining = `markAllRemaining ? n : null`.
- **SP-6** write order = insert-first then decrement; decrement via the `propagateFieldToIds` idiom (NOT `trackedUpdateItem`); on decrement failure the clone is deleted (compensation). **Observable-programmatic-write pattern of record.**
- **SP-7/7a** post-split AND post-add focus lands in DESCRIPTION, selected, centered — via the K-2 seam.
- **SP-8** ⌥⇧S = a `KeyS` case in `useNavShortcuts` dispatching a per-route `onSplit` target.
- **SP-9** `SplitDialog` on the ConfirmDialog idiom; `splitTarget` stage machine; both confirm paths run one `executeSplit` primitive.

**Add-to-List build rulings (AL-0..AL-11, July 20; all Daniel-confirmed; standing detail in CLAUDE.md "Add-to-List architecture of record"):**
- **AL-0 = A**: build the v1 dialog and supersede the (jj)-OQ2 follow-on.
- **AL-1** the library "+ Add" opens a modal on the ConfirmDialog idiom; button appearance unchanged. New `AddToListDialog.tsx`.
- **AL-2** 8-field v1-parity seed map: Category · Model · Notes (← library `public_note`) · Quantity (1, focused+selected) · Description · Method · Group · Private Notes · Star.
- **AL-3** [corrected mid-session] Method is an **add-time OVERRIDE** ("ADD WITH METHOD", action bar; `addWithMethod`, "—" = project default, sticky); the grid method filter REMOVED. Seed = `override || projectDefault`, never `method_hint`.
- **AL-4** plain single `equipment_items` insert; `buildAddToListInsert` the single source. NOT the SP-6 pattern.
- **AL-5** quantity N → ONE row with quantity N.
- **AL-6** "Hide Window on OK" DROPPED.
- **AL-7** the default-note prompt stays OUT (Model band cascade owns it).
- **AL-8** [reversed → AL-11].
- **AL-9** star/private-note homed; all 8 fields built.
- **AL-10** no-baseline-event declared for the build pass (held); the amendment produced a deliberate frame-04 event, refreshed separately.
- **AL-11** ⌥⇧S in the library opens the add dialog for the last-focused grid row via `currentLibraryRowIdRef` (ref, not state); shares the SP-8 `onSplit` slot by per-route dispatch. **Cross-route scroll-to-matching — BUILT July 21 ((kk) leg C).** **July-24 note: the AL-11 onFocusCapture ref pattern is the licensed mechanism reused for cable Change All focused-field tracking.**

**Hoisting / row-render architecture (July 18, slot 3 — standing detail in CLAUDE.md B-6..B-9):** H-1 module-scope editors · H-2 `memo(EquipmentItemRow)` default shallow, no custom comparator ever · H-3a memoized supabase client · H-3b `useStableCallback` boundary callbacks; per-row data as arguments · H-4 all unmount-while-dirty nets retained (Quantity net load-bearing → (ccc)) · H-5 dead-code slate. Expected render footprint: plain hop = 2 rows; single edit = 1 row. Rows re-rendering on genuine order changes = the memo working correctly. FilterDropdown onChange inline arrows sit outside the row system — not a B-6..B-9 violation.

**Print/revisions (July 17):** D-1 symmetric per-item old-side find filtering · D-2 nowrap stamp · D-3(a) estimator-audit unit registered · D-3(b) free-text stamp width limitation accepted · legacy no-items fallback retained pending the one-query check · notes do NOT narrow with the find. *(The print D-series and the FLT D-series are distinct registers; cite with unit context.)*

**Workbook import (July 15–16):** WB-1(c) hybrid matching · WB-2..11 as ruled.

**Other rulings:** DEL-1..6 (DEL-4 amended: hard delete now; Trash/Archive = post-Sprint-4 unit, Trigger B) · MRG-1..6 · XX-1(a) · FLT-1/2/3 (ALL LANDED July 19) · Tab OQ-1/OQ-2 · JOY retained · **Parts Schedule + Cost Estimate DEPRECATED** · methods "Delete All"→"Delete Unused" · Library grid sort — **superseded July 24 by O2-1..4** · Cost Date blank render · CR/LF import normalization = registered hash-contract event · Used∩Ignored: **still awaiting Daniel** (Arch: keep-current).

---

- **S3A-1..32 + amendments (July 22, Sprint 3 open):** the cable decision block of record — architecture (extension shape confirmed; UID/FK linkage; one linkage primitive; derived device registries; per-cable length units; method-0 = v1-identical magic value; mult-line TEXT; flags declaration incl. manual is_mult override + STORED is_extension; merged cable_types catalog; bundles rebuild; patch points as stored FK joins; tail counts numeric), ledger rulings Q1–Q21 (Panel Line Chart deprecated → 15 outputs; variants dead; "Cables" label; header-cluster Delete; empty-band literal table — the browse literal AMENDED July 23 by fresh evidence, see the B2 rulings below; 14-name lowercase color canon), Allocate as type-conversion on SP-6, sequencing S3-A→G, S3A-31 single baseline event, Pass-0 recon shape. Narrative: Session Log 2026-07-22/23 entry.
- **MIG-1..9 + MIG-Q1/Q2 (July 22):** the Sprint-3 schema of record — see the migrations line under Security; `v_equipment_list` recreated character-identical (verified by re-extraction); `v_cable_list` carries the §1.2 coalesce + derived is_mult + rollups.
- **DL-1..7 (July 23, DL-6 amended twice; final state = WF-1, July 24):** delegation rearchitecture — unit-scale handoffs, Parity Rule (uncited = unbuilt; CC bounce license), evidence judged at CC's desk, response-quality standard, throwaway-smoke standing. **WF-1 (Daniel ruling, July 24) SUPERSEDES the DL-6 commit-execution grant: CC NEVER stages, commits, or pushes — every unit ends at green gates + the handoff file, and Arch delivers Daniel ONE exact pastable terminal block per commit point (`cd` · stage by explicit filename · `git status --short` verify with the expected staged set spelled out · `git commit` · `git push`). The July-23 per-commit CC-execution authority is RESCINDED; push remains human-only (Strat invariant 1). Enforcement rides `.claude/settings.json` (commit joins push in the deny set).** Canonical text: Arch Prompt v27 + the CC Operating Contract inside CLAUDE.md.
- **WF-2 (Daniel-confirmed, July 24) — per-unit CC model tier:** every handoff carries a `Tier:` line (Arch's call per unit; Daniel overrides at will). **Opus = the default** for fully-ruled builds, recon passes, test-writing, mechanical follow-ups. **Fable = by exception** for units judging ambiguous evidence at CC's desk, frozen-adjacent work, or where OPEN QUESTIONS quality is the product. Escalation tripwire: weak OQs, evidence misreads, or a missed halt on an Opus run → that unit's remainder returns to Fable (+ a Corrections entry if the tier call was Arch's). No mid-unit tier switches — changes land at unit boundaries. Datapoints on record: CTF (held) · B4 (one invention defect, weighed, no escalation) · R7 + S3-F (held; S3-F's FIX-1/FIX-2 root-cause work incl. falsifying two Arch hypotheses with evidence — the tier performing above its brief). **July-29 addition (S3-F): mid-unit SESSION handovers on CC's own context-budget flag are licensed and encouraged** — CC stopping at a checkpoint boundary rather than opening the hardest checkpoint on a thinning budget is the correct call; sessions are disposable, CLAUDE.md + the handoff files are the durable memory; the resume paste declares the expected dirty set so a fresh session doesn't misread mid-unit work as drift.
- **Grants rule of record (July 23, from the outage):** every table-recreating migration ships its grant set (per-project tables: full DML to authenticated; global catalogs: SELECT only; no anon); every migration smoke includes a `SET LOCAL ROLE authenticated` probe — service_role smoke is blind to grants.
- **Flags-2/3(a) — the ATOMIC SELF-CLEANING THROWAWAY DB SMOKE, licensed as a named pattern (Daniel ruling July 29, R7; folded into CLAUDE.md at `5b2a689`):** execute_sql writes permitted ONLY under throwaway-rows-only · one atomic call · rollback-on-any-error · census-to-zero in RAW OUTPUT · browser smoke still owed where drivable. Outside the pattern: SELECT-only, unchanged. Exercised across R7 (one smoke) + S3-F (five smokes + the amend-pass flag smoke), every census verbatim all-zero.
- **B2 micro-migration + B2-R1 (July 23):** `b2_cable_micromigration` — `projects.cable_model_before_name` (R-37 one shared global, browse + print, NOT NULL default false) · `projects.cable_default_big/small_label` (nullable booleans; null = app seed big-off/small-on; **B2-R1: the defaults panel gains Big/Small Label checkboxes — R-20 killed the numeric pair counts, not boolean seeds**) · `cable_details.allocated_from_id` (uuid FK → equipment_items, ON DELETE SET NULL, `split_of_id` sibling shape; **FENCED consumer-less until the Allocate unit**) · `v_cable_list` +1 appended column, `security_invoker` re-specified, grants restored.
- **Cable B2-line rulings (July 23–24; architecture detail standing in CLAUDE.md "Cable B2-line architecture of record"):** empty-group band renders **bare "Group:"** (b2a_05 functional evidence overrules the frame-091 layout literal) · R-36 pill renders ONLY while true; when the last unflagged cable gains a flag under pill-narrowing, the narrowing releases (non-trap) · Tab order ruled: Name→Description→Bundle→Model→Length→SrcDevice→SrcConn→DestDevice→DestConn→inspector fields; Steck counts click-entry only · bundle cell = join-EXISTING autocomplete; **no-op-revert on no-match is the pattern of record for typed edits to existing records' resolved fields** (bundle + inspector Category/Method/Box); null-on-no-match stays a create-seed rule · bundle creation (the "+" affordance, b2a_03) + Update Bundle dialog (R-33, b2a_02) = **B3** · bundle-filter-born membership (D-2 mirror) · script-680 flow verbatim incl. the empty-case dialog; **CP-B4: post-write the found set MOVES to the new group (display→All, Group filter→written name); empty-input Yes writes nothing; failed write reverts, view untouched** · **inspector = permanent bottom-anchored chrome** (pulled forward from B5, which dissolves); frame-030 anatomy; ⌥⇧I = Color↔Name focus cycle; disabled-empty on no current row; Box = box-type-records-only autocomplete, free text illegal, registry legitimately empty until Sprint 4 · **Delete All/Found on the cable header = a ruled v1 DEVIATION, S-10 the licensed template** · chord parity ⌥⇧N/D/E/I/↑↓ (licensed patterns; `useNavShortcuts` byte-untouched) · delete/unallocate focus continuity (same field, next record; field captured at request time) · **CROSS-SURFACE (1): Change All acts on the FOCUSED field on every Minotaur surface** · **CROSS-SURFACE (2): every bulk-delete confirm on every list defaults to No** (`ConfirmDialog.defaultCancel`; single-record deletes keep the original default) · **Unallocate All** on the bulk dialog (flip-first batch, B1-g orphan doctrine) · **palette-publication rule: any `--equip-*` consumer surface must publish the palette or every var falls back to :root light literals both themes; active rows ride `--equip-row-selected`, never hardcoded** · row zebra stays on surface tokens · MBN default false (name-first) stands.
- **(ooo)-2 / O2-1..4 (July 24, Daniel-confirmed; BUILT `6a7e297`) — the library sort of record, superseding the first (ooo) fence:** **O2-1** categories order numerically as a human reads them (1.0 < 9.0 < 11.0 < 13.0 < 14.5) via the equipment list's OWN ordering source — `categories.sort_order` rank ASC nulls-last, then `compareLabels` on the name (one truth; rank is the PRIMARY term — if a rank ever disagrees with the numeric name, the library follows the equipment list, documented in a test). **O2-2** models within a category use `compareModels` (imported unmodified; byte-agreement proven on Daniel's capture vector). **O2-3** the sort key IS the displayed value: stored category text resolves by the select's own exact-match doctrine; no-match/empty → rank +∞ → the trailing uncategorized block — a row rendering "—" sorts uncategorized regardless of stale text underneath. **O2-4** `librarySort.ts` (`makeLibraryComparator(categories)` factory, useMemo-keyed on the live category list) is THE library comparator. **Defect mechanism of record:** the page sorted on raw stored text while displaying the resolved select value — library categories are text references (not FKs), so deletes/renames orphan text; orphaned rows rendered "—" but sorted mid-list under invisible text, and the plain localeCompare put "11.0" before "9.0". Scope fence held: `sortChains`/`cableSortChains`/`modelSort` and every equipment/cable consumer byte-untouched (imports only).
- **CTF unit rulings (July 24; BUILT `c1d7ae6` — CT-1..4 ruled July 24 AM + the session's amendments; the dialog is REUSABLE — S3-F wired edit mode July 29–31, then D3-AMEND-2 superseded row-click-edit with the inline grid; the dialog remains the CREATE path):**
  - **CT-1..4 (spec):** v1's nine-modal cable-type cascade (evidence ctf_01..09, COMMITTED) consolidates to **ONE dialog, four sections**: ①Ends & Lines (source/dest end, mult line count, auto-add-lines ☐ → `add_lines`) ②Labels (**Big ☐/Small ☐ → `default_label_size` big/small/both/none** via a pure tested mapping fn; Branch Cable Type autocomplete) ③Specifications (OD in / weight lbs-ft / reference) ④Tails (auto-add ☐, source/dest tail models, tail method, five tail label COUNTS numeric — **CT-3: collected now, behavior INERT until S3-D**). **CT-1 trigger:** unmatched Model commit (both prior-state branches — create-seed new record and existing-record edit converge on `handleCommitModel`, dispatched via pure `classifyModelCommit`) opens "New Cable Type: name"; Save creates + the triggering commit resolves; Cancel creates nothing + reverts (existing: no-op revert per the B2 pattern; new: back to prior/empty). Blank explicit commit still clears a set type (intent, kept). **The interim silent clear is RETIRED — no path silently clears.** All fields optional; Save-with-blanks = v1's bare-type path. Structural divergences ruled deliberate: wizard→single dialog (CT-1); v1's numeric pairs→checkboxes (CT-4; schema stores only the enum).
  - **R1 (migration `ctf_widen_default_label_size_check`, Arch-applied + smoked July 24):** the live CHECK admitted only big/small — widened to the four values; existing rows untouched; NULL legal; invalid values proven refused; authenticated grants proven; smoke rolled back clean. *(Generated TS types carry no CHECK domains — regen produced zero diff, expected.)*
  - **CTF-G1 (as amended) — the creation queue, BRANCH ONLY:** unrecognized names never stack dialogs mid-flight — queued FIFO, opened after the current Save, recursively; parent Cancel kills its queue; dequeue-resolve guard (a queued name that now resolves never re-creates — and a resolving BRANCH back-patches to the existing id). **R2 (FK arm):** parent Save stores `branch_cable_type_id` NULL on unresolved; the queued Save BACK-PATCHES the saved parent (loud save-error on failure, never silent); queued Cancel leaves the NULL. **F4+F8 SPEC CORRECTION (Daniel gate): tails are NOT cable types** — source/destination tail models are PLAIN TEXT fields of the cable_types record (the columns' actual shape); typing a tail name never creates a type and never queues; the tail arm was REMOVED from the queue. `UNIQUE (project_id, model_name)` = the DB backstop; duplicate-name creation surfaces a readable error, dialog kept open.
  - **R3 + F3 (prefills, per ctf_03/ctf_08 v1 functional truth + Daniel riders):** dialog opens Small ☑ (untouched Save writes 'small'), Sets of Line Labels = 1, **Small Tail Block Labels = 1**, **smart mult-line count** (`parseSmartMultLineCount`: a leading integer + pair/pr/ch/channel token in the NAME — "19-Pair"/"19pr"/"19ch" — seeds Number of Mult Lines; word-boundary guarded; no match → blank), tail-model placeholders "e.g., 19-Pair G3M-XLRF Tails, NL4 AB-Y" (source) / "…G3F-XLRM…" (destination); all else blank.
  - **F5–F7, F9 (usability, Daniel gate):** Tab traverses every field including OUT of an empty Tail Method (optional; empty commits nothing; `onAdvance` + capture-phase shift ref) · Tail Method hint "(if blank, new tails use the project's default method)" — **REGISTERED S3-D RIDER: honored at S3-D** · sticky dialog header · **Tail Method = autocomplete text input (the Sprint-3 constraint enforced; a methods FK, set-existing-only, null on no-match — correctly NOT in the queue; blur-commit/Save race guarded by a synchronous ref mirror)** · F6: the row Model field verified already the v1-parity type-to-narrow autocomplete (no change needed) · F7: Description = fixed v1-proportion width (176px, CC's delegated judgment) + trailing flex-1 spacer BOTH header and row — surplus collects FAR RIGHT; wide windows scroll-free; the <~1508px intrinsic residual joins (kkk) · F9: the dialog's Tail Method strips the inherited `--equip-sel-input-bg` project tint → neutral input styling (dialog context only).
  - **F10 (model display, root-caused):** the Model cell bound to the phantom `v_cable_list.model` (a real column, EMPTY for cables) — fresh loads rendered typed cables blank; in-session edits masked it via optimistic writes. **Pre-existing latent at `6a7e297` (byte-proven), surfaced by CTF making typed cables reachable.** Fix: `cableModelDisplay(row)` = `cable_type_name ?? ''` — the view-truth field — used by the Model cell and the Change-All preview; regression-guarded (the phantom column is never read). Phantom writes retirement = the registered punch item.
  - **Accepted dispositions:** post-Save cascade onto the triggering row = MODEL ONLY (the other dialog fields are type-level metadata with no cable-row columns; auto-adds inert till S3-D) · field shows prior value behind the modal (cosmetic, hidden) · dequeue back-patch to a resolved existing id.
- **B3 unit rulings (July 25; BUILT `03b9753` — B3-R1/R2 ruled at open, R1a + A1–A7 from Daniel's gates; the dialog is REUSABLE, B4 wires the band-name trigger):**
  - **B3-R1** bundle creation lives on the "+" affordance ONLY; the row Bundle cell keeps B2 join-existing / no-op-revert byte-for-byte. **B3-R1a (A4):** while the "+" is visible, Enter/Tab in the Bundle cell ACTIVATE it (identical code path; Tab additionally advances — silent-create path only; the no-length path returns focus to the cell on Cancel). "+"-hidden commits keep B2 semantics untouched.
  - **B3-R2** ONE Update Bundle dialog, two modes: CREATE (the no-length "+" path, b3_01) + UPDATE (R-33, anatomy b2a_02; **triggered in B3 by clicking a BUNDLED cable's Length cell — v1 220; unbundled Length keeps inline editing byte-for-byte**). Band-name trigger + full update wire = **B4 riders**. Keyboard entry into a bundled Length cell: none exists (bundled rows skip Length in the ruled Tab order) — mouse-only stands, no Tab-order change.
  - **A1** the "+" renders only on a genuinely NEW name (non-empty AND no exact-existing match, findExactBundle doctrine, case-insensitive/trimmed); hidden on blank and on existing (join stays the autocomplete's job); the "Bundle" default-name path REMOVED.
  - **A3 (ruled v2 DEVIATION from v1 script 157):** the no-length create DEFERS THE INSERT TO SAVE — opening the dialog creates nothing; Cancel leaves NO bundle, NO join, typed text retained, "+" still visible; Save = create + join, patching effective faces from the INSERT data with no re-read (the clobber-fix doctrine — the B2 join-EXISTING path still re-reads, correctly); blank-length Save LEGAL (a length-less bundle — the registered S3-G print guard watches that state); a join failure compensates by deleting the orphan bundle (SP-6). The silent-create path (adoptable length) is UNCHANGED per 157: create + join immediately, no dialog.
  - **Membership at creation = the anchoring cable ONLY** (b3_01; bulk enrollment = Change All, Daniel ruling — no found-set membership built).
  - **A5** dark control chrome (cable flag checkboxes + steck radios rendering light-mode boxes in dark theme): PRE-EXISTING at `745e447` (B2-line), line-proven — `accent-color` never drove the native box chrome; fix = cable-scoped `color-scheme` keyed off the `.dark` scope (palette-publication rule; no literals; equipment controls untouched).
  - **A6 (dialog idiom of record):** select-on-entry (U2-1) in both modes — update focuses+selects length, create focuses+selects the carried name. Root cause: the persisted dialog instance reset the caret on reconcile; **remount-per-open via a keyed seq** (the NewCableTypeDialog precedent) is the dialog idiom of record.
  - **A7 (portal doctrine of record):** an OPEN autocomplete menu is ALWAYS topmost. Mechanism: opt-in `portalDropdown` on the shared AutocompleteInput — portal to body, fixed at the input rect, viewport-clamped, flips UPWARD when no room below (the last-row-above-the-inspector case); threaded to all nine cable-surface call sites; equipment callers take the unchanged absolute branch (byte-identical; 16/16 warm-run fence proof); the commit contract untouched. Clip root pre-existing (B2): the list wrapper's `overflow-x-auto` + the fixed bottom inspector. Box (empty registry until Sprint 4) + CTF Branch (native datalist, browser-topmost) covered by mechanism.
  - **Schema finding of record:** `bundles` carries NO `UNIQUE(project_id, bundle_name)` — duplicate names are DB-legal; A1's UI gating is the current guard; whether a backstop constraint is wanted = a future Arch schema question.
  - **S3-G print guard (Daniel feature ruling, with A3):** before rendering the cable checklist or bundle sheet, any cable whose bundle lacks a length is flagged to the user before print proceeds. Definition pinned at S3-G design.
- **B4 unit rulings (July 25 PM; BUILT `a066155`):** carried verbatim from v2.41 — B4-Q1/Q2/Q3 (collector banners; band read-only except inline end-label/note fields; the ONE `openBundleUpdateById` opener) · mid-run rulings (FULL-NEST; Ready per 571 one-way + Packed; per-band static headers) · A4 + FIX2-3 hierarchy (approved 1–5) · FIX-2 sort control (the dropdown struck — **never reintroduce**) · FIX2-1 fixed-viewport chrome · FIX2-2 shell sticky · FIX3-1 (the phantom-model chain defect — see the punch row) · FIX3-2 center landings · the sticky-region finding (CC, corrective).
- **B6 unit rulings (July 26; BUILT `4dd575d`):** carried verbatim from v2.41 — OQ-B6-1 (linkFields vocabulary doctrine — tokens = exact v2 column names, ONE exported module, never free-typed; no DB CHECK on `field_name`, a registered future schema question) · OQ-B6-2 (the 19-ENROLL + 4-QUEUED Clone set; Link Mult/Line classified OUT — mult-line remote coupling) · OQ-B6-3 (Ext = Clone minus {cable_name, length_value, bundle_id}, census-proven) · enrollment semantics per 400/301/603 (join-or-create-both; SP-6 compensation; B6 = enrollment ONLY) · gesture rulings (header cluster; ⌥⇧D/⌥⇧G/⌥⇧X; flags reset B6-R9; Ext bundle NULL; suffix defaults; zero mult-line copies; invert gated on `projects.bundle_invert_extensions`; Invert checkbox on the Defaults panel) · FIX-1 (the dialog keyboard contract — standing rule) · FIX-2 (ext remaining seeds purely from Set-Remaining per v1 301) · governance (the execute_sql ask-gate retired; the CLAUDE.md SELECT-only rule is the standing guard — **superseded in part July 29 by flags-2/3(a)** which licenses the named smoke pattern).
- **B7 unit rulings (July 26 PM; BUILT `18d09ce`):** carried verbatim from v2.41 — R1 atomic per-field fan-out (the equipment_items/cable_details split correction on record) · R2 badges from ONE live-id-filtered membership index · R3 per-field break only, ONE `executeBreak` · R4 ≤1 dissolution · R5 the edit-time "Update Clone <Field>s?" gate on EVERY commit path (script-verified; ext caveat) · R6 bulk link-awareness (a ruled v2 INVENTION — closure check, straddle dialog, out-of-scope preview, whole-op Cancel) · D1-R gate-before-create · the CABLE_LINK_FIELD read-key sweep · namespaced remount keys · the idle-editor resync fix.
- **S3-D unit rulings (July 27; BUILT `dfd95c9`):** carried verbatim from v2.41 — S3D-R1 (UID linkage both children; the update-members chain never built) · S3D-R2 (five label CHECKBOXES; migration `s3d_tail_label_counts_to_checkboxes`; whole-record fallback) · S3D-R3 (auto-add per 354 all-or-nothing; the own-records guard after the derived-is_mult defect) · S3D-R4 ("Not in a Mult" collector) · D2/D3/D4/D6 + A1–A10 (A4v2/v3 band header + the `--tail-label-fg` fork; A6 both directions; A8 ⌥⇧T; A9 chips; A10 non-vacuous landing proof) · the harness limitation of record (React-19 programmatic-toggle defeat; the M4 Enter-bleed → (qqq)/(sss)).
- **S3-E unit rulings (July 27 PM; BUILT `670849d`):** carried verbatim from v2.41 — S3E-R1 (derived-only is_mult + the ONE `+ New Mult` stored write) · R2 (no mult checkbox) · R3 (bands for every effective mult incl. zero-line note) · R4 (auto-add lines: executor 26, trigger 354, inputs 330/331; own-records guard; tails-then-lines) · R5 (delete-time child re-homing; the `cable_id` CASCADE hazard) · R6 (NO mult_line link groups; Link Mult/Line = remote-coupling FKs) · R7 (patch DEFERRED → R7 pass, since BUILT) · R8 (fences: branch → S3-F ✅ discharged; split + the live split-name sync → the split unit) · R9 (delete-path membership purge) · R10 (Add Pairs; s3e_01; residuals registered) · R11 (single-stage Auto-Fill; the FileMaker-limit retirement doctrine) · AMEND-1 fixes (planAutoFill unification; the `cable_color` domain) · OQ dispositions (no Delete All/Found on Lines; per-band headers; **the `print_list` affordance OWED BY THE PRINT UNIT**; the nameless-mult dialog retired; Unallocate does not re-home).
- **(uuu) unit rulings (July 28; BUILT `8505484` + `1786211`):** carried verbatim from v2.41 — the adjudication of record (H2 confirmed / H1 not observed; all three v1 nav scripts SELECT then scroll, no flash primitive) · UUU-R1 (select-first-match, flash retired) · R1a (landing keyboard focus = the first field of each surface's Tab order via the licensed rowFocusRequest seam) · R2 (subsumed) · R3/R3a (widen-once; plain selectNone terminal; consume-last) · R4 (Lines a real consumer; all triangle doors) · R5 (+ New Mult stashes-before-push) · R6 (non-cable origins OUT, registered) · D1 (Daniel's armed-tracer gate = the proof of record) · D3 (filters don't persist — v1-correct) · D4 (tracer in-tree armed-off — **RETIRED at R7, rider discharged**) · D5 (long handoffs travel as saved `.txt` — standing) · the plain-terminal scroll disposition.
- **R7 unit rulings (July 28–29; BUILT `3acbee8` — R7-1..6 + OQ-R7-1..3 + R7-A1..A5; design gate first; fourteen scripts extracted; standing architecture in the CLAUDE.md R7 fold at `5b2a689`):**
  - **R7-1** the Make Patch? dialog KEPT v1-verbatim (668's anatomy) — no auto-resolve. **R7-A1 (amendment of record, Daniel's gate):** the default is the OPPOSITE end of the edited one (668 source-side defaults Destination; 678 destination-side defaults Source) — the build's uniform default was the OQ-R7-3 symmetry error, Arch-owned. **R7-A2:** origin-identifying dialog copy.
  - **R7-2** BIDIRECTIONAL atomic FK-pair writes; **R7-3** `_end` = the PARTNER's end (the reciprocity invariant); full back-write per 668 (device_text / •connection / description / line_color).
  - **R7-4 (FINAL on 686/687):** fresh/re-patch/steal + SILENT unpatch — victims' patched-end TEXT blanked; description/color untouched at unpatch; the INITIATOR never swept (ruled mechanism divergence, outcome parity — v1's token loop degenerates to pairwise under FKs). Delete-path partner `_end`+text sweep on BOTH delete paths (ON DELETE SET NULL only nulls the `_id`).
  - **R7-5** Change All routes per-occurrence through the same funnel (the B4 escape valve correctly not taken — no bulk-patch policy invented; **superseded in part by the S3-F FIX-1 sequencing rule for mixed batches**, see below).
  - **R7-6** the 2 patch tokens in linkFields are **enroll:false PERMANENT** — never enrolled, never copied on Duplicate/Clone/Ext, never mint groups; the 2 split tokens stay QUEUED.
  - **Transitive propagation (item-3 amendment):** linked DESCRIPTION propagation per 616's engine — BFS over patch neighbours, visited + convergence (Omit-on-equal) terminators, 2-/3-cycle tests. **R7-A5 (deliberate v1 divergence, Daniel: "may have been a v1 oversight"):** COLOR rides the same engine — 615's entry gate has no hasPatch term, so v1 never linked it. Gated by the **Update Patches dialog** per 688 verbatim minus the dissolved-clones clause (S3E-R6 mechanical omission); **Cancel skips propagation only — the initiator's edit STANDS** (a ruled divergence from v1's Cancel Halt). **R7-A3b:** the device autocomplete corpus += effective-mult names (Daniel v1 attestation).
  - **R7-AMEND OQ#2 (Daniel ruling → the S3-F bulk rider, since BUILT):** a bulk op touching patched records raises ONE Update Patches dialog for the whole op, affected count in the copy; OK propagates for all; Cancel skips propagation only.
- **S3-F unit rulings (July 29–31; BUILT `327c100` — SR-1 + S3F-R1..R9 + R6-AMEND-1/2 + R3-AMEND + R7-AMEND-1 + D3-AMEND-1/2 + FIX-1/2/3 + W1–W3; design gate first; seven scripts extracted (609/665/421–424/681/683; 668/678 re-supplied); one mid-unit session handover on CC's context flag; three Daniel browser-gate rounds; standing architecture in the CLAUDE.md S3-F fold):**
  - **SR-1 (scope):** S3-F ABSORBS the cable↔line patch follow-on (OQ-R7-2 discharged) — the Link Cable column IS that family's surface.
  - **STRUCTURAL FINDING OF RECORD:** v1 has NO cable-side patch fields — a cable patched/branched to a line is held there entirely by clone-token enrollment (609 enrolls the branch cable's fields in the line's groups: Name↔Description, near/far device/connection, mult-name, line-number, color). **S3F-R1 (mechanism ruling):** v2 = the `cable_details` FK pair (`source/destination_patch_line_id/_end` → `cable_mult_lines`, ON DELETE SET NULL) + the R7 propagation engine; NO link groups minted (R7-6 and S3E-R6 stand). Ruled mechanism divergence, outcome parity. The Link Cable column = a reverse lookup over the FKs; many cables per line side legal (branch).
  - **S3F-R2:** script 665 (cable-form branch — Clone + wipe Group/Bundle/Length/Box + wild-line default Category/Method + end shuffle) REGISTERED, DEFERRED to its own follow-on with the wild-line defaults surface.
  - **S3F-R3 (as AMENDED):** empty cell = 📎 (branch-create); populated = **➡ ALONE** (R3-AMEND per s3f_01 — the "LINK badge" half resolved to B7's field-level badges, a different idiom) firing seamLanding onto the branch cable (selected + scrolled + Name focused; first-in-sort-order among multiple; v1 pedigree = script 41's link-token walk).
  - **S3F-R4:** `projects.branch_cable_type` (text, nullable; migration `s3f_branch_cable_type_default`, Arch-applied + verified) surfaced as an autocomplete "Branch Cable Type" field on the Cable Defaults panel; drives the 📎 model (empty → empty model, no dialog — v1 Default Ends behavior).
  - **S3F-R5:** line delete → cable FK nulls by schema + patched-end TEXT sweep on BOTH delete paths; cable delete owes the line nothing (it holds no FK; back-written text stands). Manual unpatch = the silent-unpatch shape (text-clear IS the unpatch; no invented control).
  - **S3F-R6 (the coupling map, as AMENDED):** couplings born at patch time, engine-carried, Update-Patches-gated: **(1)** line description ↔ cable NAME (the 616 rename inheritance, both directions — 421–424 prove the reverse) · **(2) FORWARD ONLY:** mult rename (the mult's OWN name field) → branch cables' near-end device text · **(3) FORWARD ONLY:** line renumber (the line's OWN number field) → branch cables' near-end connection (R7 dot convention) · **(4)+(5)** line chosen-end device/connection ↔ cable far-end device/connection, BOTH directions. **R6-AMEND-1 (Daniel-confirmed; CC's OQ recommendation adopted):** the REVERSE legs of 2/3 are STRUCK — a branch cable's near device/connection edit means CP-A patch RE-RESOLUTION (fresh/re-patch/steal/silent-unpatch), not rename/renumber; one gesture cannot mean both, and the referent's own field serves the rename. **R6-AMEND-2:** couplings 4/5 wired with RESOLUTION-WINS precedence — resolution runs first; only resolution-NO-OP ids get 4/5 propagation; never Make Patch AND Update Patches stacked on one edit.
  - **S3F-R7 (Daniel's colour rule, as AMENDED):** a patched/branch cable's colour links to the LINE's own colour if set, else the MULT's colour — evaluated at PROPAGATION TIME (dynamic; a deliberate divergence from both 681/683's no-colour-coupling and 609's frozen-at-enrollment choice). **R7-AMEND-1 (Daniel ruling, reversing CP-B's "colour never flows cable→line"):** a colour edit on a patched non-broken cable raises the B7 Update/Break/Cancel gate anatomy in colour copy — line-coupled → default UPDATE (the line's colour follows, then fans; Break/Cancel offered); mult-coupled → default BREAK (Update/Cancel offered; **Update repaints the MULT itself** — Daniel's registered ruling upheld over CC's uniform-line reading — then fans to non-broken cables on colourless lines, NO line write). Break persists via **`cable_details.patch_color_break`** (boolean NOT NULL default false; migration `s3f_patch_color_break` + the `v_cable_list` recreate w/ `security_invoker` explicitly re-specified and the column appended LAST — the only position CREATE OR REPLACE VIEW permits; Arch-applied + verified); broken = colour excluded from the coupling BOTH directions; any fresh patch/re-point RESETS the flag. **The mult-coupled Update leg ships known-defective in the browser = punch (www).** The dialog copy = CC's proposal, Daniel-approved (line body as written; mult body names the mult-repaint effect).
  - **S3F-R8:** cable-side re-patch/steal unified to R7 semantics (fresh/re-patch/steal + silent unpatch; victims' patched-end text blanked; initiator never swept) — v1's missing victim sweep (W3) NOT ported.
  - **S3F-R9 (end-exclusivity, Daniel-confirmed from the gate):** when a line end gains a line↔line patch, any cable patched into that SAME end is SILENTLY unpatched (the S3F-R5 sweep shape) — one end, one feed; all creation paths (single / Change All / Auto-Fill) via the one `confirmPatch` funnel, both ends of the new pair swept.
  - **FIX-1 (defect of record, root-caused + browser-verified):** Auto-Fill's per-line values make MIXED batches (some ids queue Make Patch, some owe coupling Update Patches) — the coupling bulk dialog painted OVER the Make Patch dialog (same z, mounted later), reading as "resolution not running"; uniform-value paths (single, Change All) can never mix, which is why only Auto-Fill failed. FIX: the coupling bulk DEFERS behind the Make Patch queue drain — **the two dialog systems never coexist** (the sequencing rule of record).
  - **FIX-2:** the cable end-field Enter-advance (see the K-6 extension above) — the CP-A funnel hypothesis FALSIFIED by evidence (the advance was never wired); browser-verified 4/4 in lieu of a unit test (the RTL hard-stop honored).
  - **FIX-3:** the mult-coupled reverse-colour plan targeted the LINE in both couplings (the OQ3 uniform implementation) — retargeted to the mult per the ruling; **the browser residual = (www)**.
  - **D3-AMEND-1/2 (Daniel anatomy rulings):** the Cable Types admin at `/cable/types` = a FULL-FIELD grid (every CTF field as readable columns, AMEND-1) grown to **INLINE-EDITABLE** (AMEND-2, four parts): (a) every field edits in-cell (direct write + save-error idiom; autocomplete text cells, no selects); the CTF dialog remains the CREATE path only; row-click-to-edit RETIRED; (b) ONE "Labels" column with Big + Small checkboxes (⇄ `default_label_size` via the pure mapping pair); (c) the add-lines header reads "Add Lines at Creation"; (d) a vertical divider between cable-attribute and tail columns. Horizontally scrollable. Disclosed residuals accepted: no type-rename field (a future anatomy addition needing its own pedigree); unrecognized Branch Type on edit resolves to null (no CTF-G1 queue on the edit path); the D2 dialog-seed helper unused (punch-listed).
  - **DISSOLVED:** 609's "More than one mult to link color" collision dialog — cannot arise under ID-based linking (ruled mechanism divergence).
  - **WARTS registered, not ported:** W1 = 683's dialog message reads the SOURCE relationship under a destination condition · W2 = 681's condition/message table-occurrence mismatch (both superseded mechanically by R7-A2 copy) · W3 = the missing victim sweep (→ S3F-R8).
  - **Step-0.4 verification of record (discharges the 421–424 "Inverted Cables" clause):** v2 stores device/connection UN-SWAPPED (`flagged_end` a pure flag), so a link group's field_name always names the SAME physical column on every member and B7's R1 fan-out reaches all members, inverted or not — v1's opposite-column token fan-out was an artifact of v1's physical end-swap storage, eliminated in v2 (the S3F-R1 divergence pattern). Residual display question = punch (vvv).
- **Governance rulings (July 23–29):** **Auto-mode rider** — CC in auto permission mode through Sprint 4 close; behavioral contract rules ALL unchanged; WF-1 moots the approval channel · **Sed Disclosure Rule** — stream edits permitted under per-command ledger; fence blocks in-place editors on frozen files only; single-file stream edits = Edit tool, no exceptions (the fence caught and correctly blocked a heredoc append + a `perl -i` attempt during S3-F — both routed through Edit) · **hook execute-bit rule** — `chmod +x` after any hook placement; a "Permission denied" hook error = the fence is offline, flag immediately · **flags-2/3(a)** as itemized above · **UUU-D5** (handoffs as saved `.txt`) extended in practice at S3-F: the resume paste for a mid-unit session handover declares the expected dirty set.
- **Hash remap (July 10 rewrite):** carried verbatim (`7f36ed9a→be0769de` et al.).

---

### Aug 1–3 rulings (V-1 gate · D-2 · DEV-1 · CL · DEV-2 · the registration sweep)

- **V-1 DESIGN GATE — CLOSED (Sheet #1, Daniel, Aug 1):** **V1-R1** gated-never-silent (a mult's own colour edit triggers the ask — the 615/R7-A5 question resolved by ruling, not inheritance) · **V1-R2** ONE dialog, THREE choices: Cancel (edit stands, propagation skipped — R7 Cancel-divergence semantics) / Update / **Review, a ruled v2 INVENTION** (read-only preview: every affected cable, current→incoming colour, grouped by mult under Change All; Update/Cancel inside; ONE dialog total) · **V1-R3** `patch_color_break` cables excluded (never listed). Riders: Review V-1-only for now (the shared 688-verbatim dialog untouched); read-only for now (jump-to-cable = a future seamLanding rider). Substrate banked (260731-1853): `planReverseCableColor`'s mult branch; the missing piece = the cable-list forward trigger + the R2 dialog; existing-data recon clean.
- **D-2 rulings (Sheet #2 §2, of record):** D2-R1-AMEND (Group-sort-only bands) · D2-R2..R8 · **D2-R9** (Duplicate resets Bundled/Packed/Tested — a ruled divergence from s361, script in hand) · **D2-R10** (Change All excludes Bundle Name — UNIQUE) · gate-ruled divergences: full-unwind Cancel · unpatched line copies (R7-3 reciprocity) · in-dialog UNIQUE rejection over v1's silent " Copy" · bulk delete two-way vs the d2_03 three-way single · footer = found-set sum · timestamp-only audit stamps (attribution = a sharing-era question) · chords KeyB active / **KeyA reserved-unbound** (no chord routes to a 404) · the `bundles` landSeam destination · **the warm-up-capture-before-`--gate` harness rule (standing discipline)**.
- **DEV-1 rulings (Daniel, Aug 2):** **R1** Additional Model target + per-model row expansion (shared fields copy; emitted rows consecutive at source position) · **R2** Ignore Blanks (superseded — see R11-AMEND) · **R3** quantity as copy-line-only (recon proved the RPC coalesce; no checkbox — an Arch simpler-alternative accepted) · **R4** Description mapping optional, blanks import `''`, validation ON the import attempt (Daniel-amended: no pre-disabled button) · **R5/R7** value hygiene; verbatim values incl. parenthesized models · **R6** Notes first-emitted-row-only · **R8 + R8-AMEND** the workbook file fence (five files + the two glue files retroactively admitted — `fetchCurrentState.ts` R9-named, `runWorkbook.ts` structurally required; the shared engine + `hashContract.test.ts` untouchable, byte-proven) · **R9** sheet-order via `import_sort_order` (sequence base = live max + 10, mirroring the RPC append convention; the fingerprint guard makes the max-fetch race-safe) · **R10** equipment-modal-only scoping (library modal byte-identical, pin-tested) · **R11 + R11-AMEND (Aug 3):** the two blank-handling rules collapse into ONE checkbox — **"Ignore rows with no model," DEFAULT CHECKED** (checked: any row blank across Model + all Additional Model columns is skipped — covers spacer rows AND description-only rows; unchecked: everything imports as built; the R2 checkbox retired; the ~39 Vape description-only rows not importing = Daniel-confirmed intent) · **Private Notes = first-emitted-row-only** (joins R6; Daniel, Aug 3). **Migration of record: `dev1_import_apply_equipment_sort_order`** (proof-run w/ rollback + authenticated probe; the Arch transcription defect self-caught pre-apply — see Corrections; post-apply ACL byte-identical).
- **CL rulings (Daniel, Aug 2–3):** **R1** Concept B is the unit (the Zite structured letter; Concept-A print rendering = the separately registered print-header unit) — **amended of record: v1 HAD a simpler Concept-B letter (cl_v1_01)**; settings and cover letter were always separate surfaces · **R2** Zite anatomy = the build target (Daniel-confirmed with the v1 evidence in hand); menu colour VERIFIED gold per Parity Spec R5 · **R3** one letter per project; autosave UPSERT (covers pre-seeding projects) · **R4 + R4-AMEND** Json-native boundary, no schema change except the ONE Arch column: **`cl_cover_letters_signature_data`** (`signature_data jsonb NOT NULL DEFAULT '["","","","",""]'`) · **R5** print un-stub + the live Show/Don't-Show toggle (persistence = DEV-2's) · **R6** the v1 five-line Signature block wins over Zite's absence (option (b); the signature guard surfaced the conflict exactly as designed) · **R7** signature prints BOTTOM-RIGHT, traditional-letter (supersedes the plain-left judgment call) · **R8** add-row inserts below the focused row — Who/When/NOTES all (Daniel-confirmed) · **R9** rich intro toolbar, **option (b): build to the evidenced affordance set** (cl_zite_01: B·I·S | bullet·ordered | undo·redo) with the **Arch-approved dependency set of record: `@tiptap/react@2.27.2` + `@tiptap/starter-kit@2.27.2` + `tiptap-markdown@0.8.10` — the first dependency addition since the sprint began; the Zite platform export mined first (bundled/transpiled; stack confirmed)**; storage stays markdown text; StarterKit at DEFAULT per Zite parity ((dddd) registered) · **R10 + R10-ENC** the v1 columnar Who print (four columns; role labels wrap; blank-role continuation rows; NO org-line semantics — a company is an ordinary name-only row; pedigree cl_vape_01, Arch-rasterized) · **R11** an EXISTING-but-empty letter prints ONE blank cover page (Zite-verbatim CONFIRMED by ruling — the auto-seed consequence accepted) · **R12** a MISSING-row project prints the SAME blank page (missing == empty; suppression removed). **Assessment closures:** Who/When/Where/Notes header underline KEPT (v2 emphasis over v1's plain) · Where venue bold/underline KEPT · inter-group spacing = manual blank row ONLY (no automatic gap) · letter title needs no change (print-options settable).
- **DEV-2 rulings (Daniel, Aug 2–3; the pass-bundle was FULLY RULED — Phase 1 since BUILT `7fad0f8`; Phase 2 = DEV-2p2 open):** **DEV2-R1** ⌥⇧J = abort find AND clear filters (full v1 ⌘J Show All semantics) · **DEV2-R2** sort + print-options persistence is **DB-BACKED, per-project** (document-level v1 semantics — one collaborator's change is everyone's; a small Arch persistence migration at the DEV-2 open) · GCM method-name Enter/arrow commit parity · (cccc) Notes multi-line fix · the R11-AMEND checkbox · Private-Notes first-row.
- **Aug-2–3 registration sweep (Daniel production notes #1–#14):** **P-1** Change All dead in production (button + ⌥⇧\) — the production-defect unit, sequenced CL-landing→P-1, diagnostics deliberately held until CC is off the tree (probes: console/network readout; local-vs-prod repro) · **#14** Change All found-set scope confirmation = a REQUIRED P-1 finding (v1 truth: the found set — the 941-records frame) · **#13** Delete All → Delete Found under narrowing (label + scope together; v1 pedigree; joins the found-set cluster) · **the FOUND-SET SEMANTICS CLUSTER** (#4 stale sets stay until find/filter changes + #13 + #14): one design gate ruling what "the found set" means to every bulk op — v1's pinned-set truth · **the LATENCY/OPTIMISTIC UNIT** (#8 model→note→change-all chain (UI stands, "zippier" — a (pp)-class latency recon) + #11 Duplicate/Change-All slow on spotty connections + New Item slow in production): recon → design gate → build, **designed AROUND the frozen `updateItem` region — any frozen contact is a declared event, never a casual fix**; absorbs the punch (pp)/item-13 remainder; P-1's findings feed it · **the FIELD-EXIT AUDIT (#10)**: every field's Return/Tab/arrows/autocomplete/no-focus exit behavior audited v1/FileMaker-first → a parity matrix → a ruled fix batch (generalizes the GCM item) · **the DEPENDENCY-MAINTENANCE UNIT** (the 7-advisory adjudication; pre-October) · **WF-2 datapoints:** Sheet #2's five Fable-on-Opus-lines passes + the CL-0 INVERSE (designated Fable, ran Opus 4.8, quality held) — selector-side, Daniel's call, no doc change.


### Aug 3–4 rulings (REC recon · found-set · field-exit grammar · EP-P1 · the AIR2 window · CL landing)

- **REC-1/REC-2 findings of record (Aug 3, `e5a7ecd` — read-only recon; legs 1–3 Opus, leg 4 Sonnet, WF-2 datapoint):** v1's print aggregation lives in the LAYOUT — purge-and-refill is 1:1; the collapse is sub-summary over the BrowseSortView sort spine (verbatim-extracted); the Model(composed) break level is INFERRED from name-only DDR layouts (accepted residual; live-file inspector = escalation only if a theatre diff ever bothers) · **v2 had NO pinned found set** (live per-keystroke drop — the #4 mechanism; since closed by `0c0792c`) · **v1's keystroke grammar is EVIDENCE-SILENT** (zero OnObjectKeystroke) — the exit grammar must be RULED, not extracted · **W-2's lines/tails Enter-advance = NEVER WIRED** (not regressed) · the B6 cable-Duplicate cascade gap source-confirmed; `buildTailCopy`/`buildLineCopy` exist, consumed only by the bundle ride — **D-2b extends the two-phase pattern, never forks a third copy family**. Recon self-corrections (CC, of record): "Shop Order scripts absent" RETRACTED (present in the DDR as "Basic Shop Order"/"Detailed Shop Order"/"Shop Order Setup"); the field-21 composition gate is `Length for Summary > 0`, not raw Length. **Arch correction (of record):** the D-2b docket's "Tails?/Lines? dialogs have no screenshots" premise was FALSE — d2_04/05/06 cover them. **Genuinely missing frames (gate D-2b's design close):** "Enter Clone Bundle Name" (s441) · "Add Cable to Cloned Bundles?" (s704) · the four s504 Delete-Cable-with-Clones confirm variants.
- **FS-R1..R4 — the found-set design gate CLOSED (Daniel, Aug 3) + BUILT `0c0792c`:** FS-R1 find OR any filter change derives and PINS the visible id-set (member edits never eject — v1 snapshot semantics; re-derive only on find/filter CHANGE; ⌥⇧J dissolves per DEV2-R1; new records join the pin, exemptNewIds kept) · FS-R2 Change All / Delete Found / counts scope to the PIN (v1 `Get(FoundCount)` truth — #14's design half closed; the live-set race dies) · FS-R3 Delete All ⇄ Delete Found label+scope move together under ANY narrowing (#13; the equipment find-only label-branch inconsistency dies) · FS-R4 attach at `useFindController` extended over consumer filter layers — final shape landed by CC judgment as the composable sibling hook `foundSetPin.ts`, the controller byte-untouched · **FS-R2-EXT (Daniel, Aug 3):** the find strip's count reads the PINNED set, same source as the toolbar readout — supersedes the live-find-only interim · **Lines DEFERRAL (Daniel, Aug 4, accepted as its own unit → GS-A Leg 2):** pin the displayed-line id-set, derive `foundLinesByCable`/bands from the pin; Add Pairs + Auto-Fill keep reading the FULL set (the Find-engine structural-consumer rule).
- **The FIELD-EXIT GRAMMAR — RULED product-wide (Daniel, Aug 3); W-2 DISSOLVES IN:** Enter commits-and-advances on EVERY row field including autocompletes (the cable end-cell wrapper pattern applied everywhere) · Escape reverts · delete lands successor-same-field focus · post-action focus always defined. The matrix's 10 flagged cells resolve mechanically under it. W-2's TailListClient NULL-clear rider rides along (keep-separate flag available at the unit gate). **v1 keystroke silence [?-U1]: the RULING IS the pedigree, on record.** The exit-grammar unit = GS-A Leg 1.
- **EP1-R1..R6 + AMEND — the EP-P1 design gate CLOSED (Daniel, Aug 3) + BUILT `40aa945`:** as itemized in the Complete entry (print-path derivation · v1-verbatim composition cables-only · `projects.units` first consumer, no conversion ever · the evidence-spine aggregation · quantity-1-rows-summed confirmed as the gate's last fact — FULLY RULED, zero build OQs). **Registered follow-ons:** (a) **mixed-unit per-cable lengths** — live DDL confirms `cable_details.length_unit` / `v_cable_list.effective_length_unit` exist ('ft'/null today); EP-P1 ships project-unit-only by ruling; a per-cable unit surface = its own small Trigger B when opened; (b) **non-generic gear print projection (boxes · walkies · RF)** = an explicit rider on each of those sprints — Sprint 4 boxes inherit the print question on arrival.
- **The AIRBORNE-BUILD LICENSE (Sheet #1 §5 principle, of record):** machine gates (full suite · tsc · `next build` · frozen 3/3) run OFFLINE — airborne builds are licensed CL-style: code + machine gates green, parked for ground; every leg's file footprint DISJOINT from parked work and sibling legs, or commits become inseparable. **The AIR2 multi-leg session pattern is the LICENSED TEMPLATE** for the consolidated map (Daniel directive, Aug 3): disjoint-footprint legs, full gates after every leg, per-leg commit manifests.
- **CL-R7-AMEND (Daniel gate, Aug 4, evidence-ruled off the live Vape! v1 print):** the print signature renders IN THE CONTENT FLOW after the last printed section (Notes), closing gap above, block left edge ~2/3 content width (`SIGNATURE_LEFT_FRACTION`), text left-aligned in-block — never page-bottom-pinned, never right-flush. **Supersedes CL-R7 bottom-right.** Accepted rider: near-full Notes pages can push the in-flow block toward page bottom (signature not in pagination math — matches the prior model). **CL-0 evidence errand DISSOLVED (Daniel, Aug 4):** the v1 signature "artifact" was a mockup screenshot, not a real print — the amendment stands on pedigree class (a), no artifact committed.
- **B.1 FINAL:** "Ignore rows with no model" ships default-checked and drops description-only/no-model rows — the veto window CLOSED Aug 3. · **GCM arrow-commit ADJUDICATED NO-SURFACE** (plain input; CC's correct call, of record).
- **Fixture-state ruling:** GATE-CABLE + the "6-Pair" cable type are PERMANENT pristine fixture data (the frame-07 event; the fixture-write license remains declaration-only, S-9 class). · **08-restore ruling:** undeclared byte-refreshes of untouched baselines are RESTORED, never committed — scope = the declared frames only.
- **N-R2 ambiguity resolved of record:** menu digit chords navigate-to-surface, context-dependent per the N-R1/N-R2 ruling structure (CC's Parity bounce — ruling text not in hand — honored; the resolution now cited here).


### Aug 4–7 rulings (the GS-A window · Strat ADM-1 · the compression · the S3-G gate)

- **GS-A in-flight rulings:** G1 fork-salvage accepted + the STANDING FORK RULE adopted (recon forks are read-only; a fork that writes is reverted by default — salvage only with full hunk review, full re-gate, disclosure, Daniel sign-off) · G2 bundle delete-successor ratified · G3 Auto-Fill focus = trigger-line ≡, selected (SP-7) · G4 tail ⌥⇧↑↓ field-preserve (sibling-cited) · G5a inspector ⌥⇧↑↓ record-step, focus stays in-field · G5b inspector Escape = "stay" · G7 CTF full-close lands the ACTIVE record's Model field, selected, queue-drain covered · G6-R tail Model clear commits '' (A6) · L0-R1 `print_list_settings` is the print persistence of record (the DEV2-R2 print column was redundant — Arch recon miss owned — dropped by corrective migration, empty-proven 19/0) · L0-R2 fixture `equipment_sort_mode='category'` declared pristine, self-maintained by the gate run · **V1-R2-AMEND four-way** (Cancel=FULL REVERT zero writes / **Break=PERMANENT patch_color_break per V1-Q1** / Review / Update; the V1-Q2 yellow→brown→Cancel→yellow scenario of record) · **V1-R4** single + Change All both route the gate, ONE dialog per bulk op, Change All DEFERS-then-gates (the D-2 full-unwind precedent) · GS-A2/Leg-2b rider confirmed ((gggg)).
- **E-CA rulings:** ECA-1 mechanism (cable architecture verbatim; propagateFieldToIds; zero frozen contact) · ECA-2 extraction-first · ECA-3 library-in (Daniel #3) · ECA-F1 quantity ELIGIBLE · ECA-F2 checkboxes ELIGIBLE v1-verbatim (Daniel testimony) · ECA-F3 category PLAIN ("just what you see — no siblings, no library") · **ECA-F4 commit-draft-first**, landing as **CAPTURE-AND-THREAD** (the architecture of record: DOM draft = ActiveFieldContents · field-doctrine resolvers · editor closes WITHOUT its single-edit commit · override-row threading · buttons onMouseDown-preventDefault; the flushSync failure PROVEN — React-Query notify = setTimeout(0) macrotask) · **F4-No v1-LITERAL** (No/Escape commits the draft to the ACTIVE record only, same silent per-record path) · E1 fresh-session build · E2 Model Change All never touches notes (Daniel v1 attestation; 551 unextracted, optional-future) · E3 the declared button baseline event, gate-proven 8-frame scope (frames 02/03/04/08 both themes) · 694 transcribed to the corpus (`9121338`); the "Update All Models" hypothesis dead — 694 = the per-record 551 walk.
- **THE PROBE RULE (registered from the Amend-4 incident):** mutating-capable browser probes NEVER run against the Visual Gate Fixture — throwaway projects only. A probe's safety must be RE-DERIVED whenever cancel/No-path semantics change (the Amend-3 probe was safe only while No discarded). The Aug-7 corruption (one record + one library row) was contained, SELECT-verified, and restored under a **declared one-time Arch execute_sql write exception** (guarded single-transaction, row-count-asserted); the SELECT-only standing constraint otherwise stands.
- **STRAT-ADM-R1..R8 (parallel Strat session, Aug 5 — Daniel-confirmed, registered by fold-in):** R1 ADM-1 = metadata-only beta admin (user list + activity + project metadata depth, NEVER content; per-user notes; signup count; no dashboard; feedback → Comms/support@) · R2 private beta = manual provisioning + ONE bare allowlist-entry field (retired at public beta); public beta open self-serve w/ email confirmation; the custom-domain auth email = LOAD-BEARING public-beta prerequisite; closed-door screen + copy = Comms deliverables · R3 disable/enable only, no admin delete; deletion = manual support (~30-day placeholder pending R8) · R4 support access = per-incident, read-only, USER-INITIATED via project sharing — no admin content surface exists; the SHARING UNIT is now load-bearing · R5 attorney addendum delivered (in project files) · R6 license admin → the ~Nov licensing design; ADM-1 slot post-sharing/pre-October + the DECOUPLING CONTINGENCY (ADM-1's true minimum has zero sharing dependency; decouple to protect October if sharing slips) · R7 student honor system stands · R8 the backup posture upgrade — the pre-port drill (THIS WEEK; shop prep Aug 10) + a documented standing posture before public beta; the runbook is delivered (project files), execution owed.
- **FR-1 registered (Daniel, Aug 6):** every find panel gains a find-and-replace mode for found text; slot post-Sprint-4, post-ADM-1; design gate banked (substring-vs-whole-field · panel scope · likely v2-invention [?-U1] class · preview-before-apply on the V-1 Review chassis).
- **THE AUG-7 COMPRESSION + THE S3-G GATE:** as itemized in the In-flight bullet — both of record.

---


### Aug 7–9 rulings (the S3-G window — Sync Sheet #1 §1, carried at migration)

- **S3-G ruling register additions (all Daniel, Aug 7–9):** Leg-1 round (method-0 exclusion extends to the Bundle Sheet · Labeled: = ordinary find field · unflagged-cables warning → S4 (docket UNFLAG-WARN) · Clear-Print-List = DDR-573 verbatim w/ Clear default + the once-per-arrival latch · line-gesture deferral RULED, trio 606/607/608 evidenced → docket LINE-STECK) · the Aug-7 gate-notes triage (#1/#2/#3/#10–#14 amended in; #4–#9 registered → docket FOCUS-MP) · A3 fixed Source|Destination checklist order (Daniel paper-confirmed; flagged side tinted only) · Aug-8 round (#2 band scoping: bundle band only under bundle-aware sorts; Quick Print bandless+modelless · Skip Labels only on By-Cable · folder labels keyed to sort w/ the No-Group trail) · **PRINT FONT PARITY (standing rule, product-wide):** every print output renders point-true sizes matching its v1 exemplar, proven by measurement at its gate — all nine S3-G outputs + equipment, and joins the gate set for every future print surface · P7/OQ3 **Max Label Sets RETIRED** · P9 phantom-model retirement steps 1+2 (view-column DROP stays Trigger-B → docket P9-DROP) · A5 mult-delete: DDR-224-extracted, the mult's own tails DELETE, over-delete proofs on both cascade consumers · A6 nav doors · P10 🖨/green-→/Clear-Print-List frame-verbatim, 🖨 = toggle (OQ5) · Quick Print one-click · folder-by-group sort.
- **New standing doctrine (S3-G):** `@page` uses EXPLICIT DIMENSIONS (`11in 8.5in` / `8.5in 11in`), never the landscape/portrait keyword (Chrome interactive-path drop, root-caused twice) · print-orientation/size verification is NEVER evidenced by Playwright `pdf()` or headless `--print-to-pdf` alone — the interactive Save-as-PDF dialog is a different renderer and **Daniel's desk is the renderer of record** · evidence commits STAGE BY DIRECTORY (exotic filenames never travel in pasteable blocks — the Aug-7 U+202F clipboard failure).
- **Defect of record (S3-G Amend Pass 1):** all 13 print-settings saves were silently dead lazy builders — proven by network capture, fixed; spawns the DWV docket row (verify-by-capture before fixing the class).

### Aug-9 rulings (the Strat retrospective + housekeeping program — Sync Sheet #2 §1–§2/§5, carried at migration)

- **The workflow-housekeeping program, all Daniel-confirmed:** GitHub connector installed (repo `.md` + CLAUDE.md; evidence media excluded — 1631% capacity overrun; "Sync now" joins the pre-open, rides Arch Prompt v28) · **Cowork Phase 2** (canonical governing-doc home = `~/Developer/Minotaur-Cowork/`, its own git repo; diff-verify acceptance; CLAUDE.md upload-and-return; the ONE-WRITER-ON-THE-REPO rule standing; pilot 2–3 doc sessions then review) · **WF-1a commit pilot** (CC stages by explicit filename and commits ONLY on Daniel's terminal confirm phrase `COMMIT APPROVED: <first words of the subject>`, subject-bound; `git push` remains Daniel's paste; no-trailer rule moves to the tested commit-msg hook; hooksPath wiring after an empty-.git/hooks check; fence + deny list lift `git commit` in ONE pass, push blocked in both; sunset review at Sprint 4 close w/ the auto-mode rider) · the **Multi-Leg Run Pattern memorialized** · the **Evidence Intake Protocol** + the **PDF evidence-class rule** (all v1↔v2 output parity = print-to-PDF, 100% scale, matching paper both sides; screenshots = on-screen anatomy only) · the prompt slate (plain-language-first · bullets for 3+ enumerations · model check state-and-proceed · the Docket Sweep at every close · Strategy Prompt v14 delivered · Arch v28 held for housekeeping install · Comms v10 rides its open question) · **the Roadmap v3.0 split** (this migration; status vocabulary OPEN·GATED·AWAITING-RULING·IN-BUILD·BLOCKED, no DONE; Ledger starts v1.0 per ruling #11; UX Reference Catalog → Ledger w/ forward pointer) · **CLAUDE.md relocation** (arch-record extraction + the Guarded Architectures Index; byte-accounting proof; its own gated errand) · the project-knowledge prune EXECUTED (12 files + the two Aug-5 drafts).
- **Registered Aug 9:** the **⌥⇧K command palette unit** (→ docket CMDK); **the other four Aug-9 UI proposals (print-list drawer · selection action bar · collapsible inspector · saved find sets) DENIED of record.** · **Latency-unit scope amendment:** mandatory measurement recon first; the candidate menu in evidence order: query collapse into RPCs · prefetch expansion ((kk) idiom) · index/view audit + the Vercel↔Supabase region pairing check (one session) · row virtualization (HIGH-CAUTION — sticky bands/focus/scroll) · payload trimming · TanStack cache persistence · bundle audit/code-splitting · streaming shell/skeletons (baseline-event interaction) · write coalescing (only if measured) · compute tier (after the audit, never instead); realtime push deliberately SHELVED to the collaboration unit.
- **The five Aug-9 one-word confirms (Daniel, in-session):** S3-G-2 → Sprint-4 basket · R8 deferred-to-desk, deadline pre-September (attorney F4) · **Aug-25 formally RETIRED** (shop runs v1; import "when v2 is in a good spot"; port criterion + cable-data sequencing ruled at the Sprint-4 map → docket PORT-CRIT) · fold path = v3.0 direct, no v2.47 · Sprint-4 planning after housekeeping, written once into the v3.0 docket.
- **Aug-9 late amendments (Daniel, housekeeping open):** the standalone owed-gates session DISSOLVED; the CLAUDE.md fold errand moved into the housekeeping session (executed `cc96e82`); all remaining S3-G verification items → the v3.0 docket as early-Sprint-4 rows with triggers (GATE-VIS · GATE-PDF · GATE-OL25WR · GATE-C4); R8 re-triggered to before the September attorney engagement. The gate-before-build invariant holds — GATE-VIS is the first action of the first Sprint-4 BUILD session.
- **Post-delivery migration rulings (Daniel, Aug 9):** OQ3 = CLOSED (the Max Label Sets retirement is the whole record; the "OQ3 for Sprint 4" carry was a memory phantom, struck) · the four accepted-risk watch items RETIRED from the docket to this Ledger (below) · BINGO trigger = post-private-beta, pre-public-beta · v2.46 archived byte-exact (done) · migration CONFIRMED and installed.
- **Accepted dispositions retired at migration (no action owed; revive by new registration only):**
  | (dd) | `frozen-check.sh` fires on seeded-failure proofs — sanctioned proof mode; "no action owed" | OPEN | Arch | Unscheduled (retire candidate — batch Q2) |
  | RTID | `resolveTargetId` null-guard asymmetry (unreachable only by DB invariant) | OPEN | Arch | Watch (retire candidate — batch Q2) |
  | STOR-ORPH | Storage-cleanup orphan residue on delete-cleanup failure (accepted risk; no action unless observed) | OPEN | Arch | Watch (retire candidate — batch Q2) |
  | DEV-WATCH | Dev-server blank-on-reload watch item (+ the rm-rf `.next` protocol of record) | OPEN | Arch | Watch (retire candidate — batch Q2) |
- **RM-6 (Daniel, Aug 9):** every Roadmap from v3.0 forward opens with a 🧭 Next Sessions block — a few succinct plain-English bullets on what the next few sessions do, kept current at every delivery.

### Aug 9–10 rulings (the housekeeping session execution — appended at close)

- **The Session line (Daniel, Aug 9, standing):** every Arch CC handoff carries `Tier:` + `Session: CONTINUE | FRESH` (FRESH states why; absent → CC asks). Folded into CLAUDE.md Standing instructions (`d5637e2`) and Arch Prompt v28.
- **WF-1a execution rulings (Arch, Daniel-gated):** the bash-fence `git commit -a/-am/--all` block RE-ARMED (the switch-on's lift was over-broad — CC-flagged); **`Bash(git add *)` removed from the settings deny on Daniel's explicit authorization** (the lift program's omission — CC hard-stopped rather than self-serve); final enforcement wording of record: the deny lifts `git commit` + explicit `git add` only, bulk staging stays fence-blocked, `git push` denied in both layers. Heading merges to single-line `###` accepted (Index precedent). The completion ran `Session: FRESH`, proving deny-list changes live post-reload.
- **Noted sharp edge (note-and-wrap, no registration):** the bash-fence text-matches command *strings*, not shell tokens — observed false-positives: `>` inside a quoted argument read as a redirect; the literal phrase `commit -a` inside a commit BODY matching the re-armed block (worked around correctly via `git commit -F <file>`; all five files explicitly staged). Register a fence-tokenizer tweak only if it recurs.
- **Arch Prompt v28 INSTALLED (Aug 10)** with the five touch-ups: DRAFT line removed · the WF-1a commit-authority paragraph (as finally ruled) · the Session line · the Roadmap+Ledger two-file source of truth (RM-6 named) · Sync-now in the pre-open.

### Aug 10 rulings (the Sprint-4 planning session — appended at close)

- **The Sprint-4 map CONFIRMED (Daniel, Aug 10) — written ONCE into the Roadmap v3.2 docket; sequence of record:** **S4-1** decks-clear (GATE-VIS first action → DWV gated pass → P9-DROP Arch micro-migration → VERCEL-CLI setup) → **S4-2** S4-AUTO (one idiom, one declared baseline event; sequenced before any box surface, forced by the method/box autocomplete-from-day-one constraint) → **S4-3…n** the box unit family (Box List → Box Types → Contents/Assignments → three PDFs; GATE-PDF/GATE-OL25WR/GATE-C4 adjudicate as Daniel's packets arrive, non-blocking on the family's open) → **S4-mid** D-2b (Trigger B linkage schema at open) + BASE-EVT as the sanctioned frames session · FOCUS-MP in between-units headroom → **S4-late** GS-C (SPL-C cable split rides here, answered of record) → LINE-STECK (+STECK-COLOR rider) → S3-G-2 (+UNFLAG-WARN riding its print work) → ALLOC → **S4-close** the EXPORT+GBD deadline pair (EXPORT carries PORT-COV's export half) → WF1A-SUNSET + auto-mode-rider sunset review → KKK-PASS immediately after close. Confirmed off the plain-definitions rewrite Daniel requested — the confirm form of record.
- **PORT-CRIT RULED (Daniel, Aug 10):** the v1↔v2 port WAITS until the box unit family is finished; then ONE coverage unit (**PORT-COV**, registered below) extends BOTH the import engine and the export to carry equipment + cable + box data; ports open only after it. Aug-25 remains retired; no date pegged. Arch flagged the consequence of record: the end-of-sprint EXPORT slot gets heavier (v1 cable-side DDR extraction is net-new work) — accepted by ruling.
- **PORT-READY criterion NAMED (Daniel-confirmed, Aug 10 — five legs; resolves the port-readiness-gate-criteria OQ):** a v1→v2 port may be offered when ALL hold: (1) the import engine runs green on the target show's real v1 file (DEV-1-class dry-run first) · (2) the equipment-list PDF renders at parity, gated by M0-WIN · (3) the v2→v1 export round-trip is proven · (4) R8 restore-proven-yes is standing (storage-backup addendum included) · (5) PORT-COV complete.
- **OQ #3 RULED — RIDE (Daniel, Aug 10):** the method-field-in-list autocomplete (Aug-2 production note #3) rides S4-AUTO; no pull-forward. One idiom, one baseline event; S4-AUTO's S4-2 slot makes the wait days at most.
- **VERCEL-CLI SLOTTED (Daniel, Aug 10):** rides S4-1 as a setup step — read-scoped token; CC pre-open Vercel verification moves from Daniel's screenshot to the terminal, every session.
- **CMDK DECLINED from Sprint 4 (Daniel, Aug 10):** trigger rewritten Post-Sprint-4. The chord-registry verify rider + the own-Parity-ruling-block requirement stand unchanged; rationale of record: net-new UX surface in the fattest sprint, and palette contents get richer once box surfaces exist.

### Aug 10 rulings (the S4-1 decks-clear session — appended at close)

- **GATE-VIS ADJUDICATED — PASS (ON RERUN) ACCEPTED as the official Sprint-4 visual gate (Daniel, Aug 10):** run 2 = 16/16 @ 0.0000% on a verified-quiet machine (load 3.96/10 cores); run-1's two nonzero frames (01/02-light, 19px/1.6M = 0.0012% each) matched registered flake surface #4 exactly and cleared to zero on the one licensed re-run; same surfaces 0.0000% in dark mode same run; HEAD unchanged. Two environment hard stops preceded the pass (Dropbox-sync load; the post-reboot storm at load 218) — both the STEP-0 quiet-machine clause working as designed. Frozen 3/3 vs `be0769de` in the same pass. The row exits the docket at this close.
- **DWV CLOSED — ZERO DEAD SITES (Aug 10):** the S3-G dead-lazy-builder class has no surviving instance in src/. Static sweep: every mutation builder subscribed; the only two bare-looking builders are the two founding suspects and both chain `.then()`. Both suspects capture-proven LIVE: TopBar theme write → PATCH /rest/v1/profiles 204 (correct bodies both directions); EquipmentListClient.tsx:276 sort-mode write → PATCH /rest/v1/projects 204, gesture-landing proven on a scratch project (created + destroyed in-run, cleanup SELECT-verified 0 rows). Fixture pristine re-proven by SELECT. No commit — the zero-dead verification record (CC handoff 260810-0916) is the deliverable of record. **Belt-and-suspenders capture of the ~90 awaited builders DECLINED (Arch, of record):** statically live suffices; no defect motivates the larger pass.
- **P9-DROP APPLIED (Arch, Aug 10 — Trigger-B micro-migration, Daniel-confirmed pre-apply):** migration 40 `p9_step3_drop_phantom_model_from_v_cable_list`. Live-DDL introspection first; four-step sequence honored (proof-run DO block with forced-rollback + in-block verifies → residue check proved DB untouched → apply → post-apply verify). `v_cable_list` 61→60 columns, `model` gone; `security_invoker=true` re-specified explicitly; grants made deterministic (REVOKE ALL → GRANT SELECT to authenticated — today's effective rights preserved; `service_role` SELECT deliberately NOT added, stays docket SRGRANT); `SET LOCAL ROLE authenticated` probe passed (permission granted, 0 rows — RLS-blind as expected). No dependent objects existed. **Types-regen rider (Daniel-ruled in-session):** `database.generated.ts` regenerated same session — one-field diff proven (`0 added / 1 removed`), tsc clean (zero surviving consumers), committed `281958f` on Daniel's phrase, pushed, Vercel Ready.
- **VERCEL-CLI LIVE (Aug 10, `1426e03`) — design divergence of record (Arch, Daniel-confirmed):** NO Vercel CLI installed; `scripts/vercel-preopen.mjs` calls Vercel's REST deployments endpoint directly (the machine's npm prefix is read-only — CC auto-update failure observed same day; the CLI also wants linking state the check doesn't need). Token facts of record: Vercel offers NO read-only token; **project-scoped** (July 2026 feature) is the tightest — scoped to minotaur-v2 only, 90-day expiry, stored at `~/.config/minotaur/vercel_token` chmod 600, outside the repo, never printed on any script path; the single sanctioned call is the read endpoint. Live-proven READY @ `281958f`; missing-token path proven by env-override without touching the real file. CLAUDE.md gains the standing pre-open line (CC runs the script at every pre-open; READY required before build work; exit 2 → Daniel recreates). **Visual-gate skips of record (Arch ruling, both units):** the types-regen and VERCEL-CLI commits ran suite+frozen+typecheck but not the visual gate — types-only and tooling/docs diffs cannot alter pixels; the gate was green twice that day at the same tree.
- **COWORK-P2 AMENDED — "B with seatbelt" (Daniel, Aug 10):** target end-state registered as ALL Arch work in Cowork. Pilot structure with teeth: **P2a** = the Session Log rollover (owed anyway — entry 10 landed at this close, rollover DUE) runs as Cowork's maiden act and shakedown (cold-start from governing docs, mounts, complete-file delivery, byte-exact archive); **P2a clean → S4-2 (S4-AUTO) proceeds IN COWORK in the same sitting** — full Arch authority for the session EXCEPT migrations (halt-and-route to the claude.ai Arch seat, same rule CC lives under); **P2b** = the Boxes evidence digest (full local v1 corpus — the capability this seat lacks) before S4-3's design gate; **COWORK-REV** = the keep/expand/revert review at S4-2 close, **default EXPAND absent a named defect** (burden of proof reversed, Daniel-confirmed). Mounts: `~/Developer/Minotaur-Cowork/` RW · `docs/reference/` RW · whole Minotaur repo READ. Enforcement stated honestly: prompt-level (no OS read-only), backstopped by Cowork-never-runs-git-on-the-Minotaur-repo (Daniel or CC commits its `docs/reference` edits off `git status`) + no-Cowork-while-CC-open. Session-context mechanism = the governing docs (the designed portable memory); optional `transcripts/` folder read on request only; a log insufficient to resume a session is a LOG defect, fixed at the log. Session-log archives relocate to `~/Developer/Minotaur-Cowork/session-log-archives/` (governing-docs home ruling extended). Cowork Prompt v1 authored by Arch at this close.
- **TOK-VC registered (Aug 10):** the pilot token traveled through chat at creation — rotate at Daniel's convenience (revoke + re-mint dashboard→terminal, skipping chat); 90-day expiry renewal due ~early November (feeds the standing pre-open line's exit-2 path).
- **THE TWO-SEAT AMENDMENT (Daniel, Aug 10 — same session, post-close):** Cowork Prompt v1 (authored at this close, installed minutes prior) **RETIRED AT BIRTH — never opened a session** — on Daniel's challenge ("Cowork's job is arch — why can't cowork follow arch?"); Arch owned the over-engineering of record (a second governing prompt duplicates v28 and drifts). Superseded by **Architecture Prompt v29**, two additions: **§Seats** (one Arch role, two homes — the claude.ai seat holds Supabase MCP and ALL migrations during the pilot; the Cowork seat holds the mounts of record, one-writer, never-git-on-either-repo, halt-and-route on schema; governing docs = the cross-seat memory) and **§Handoff Files** (`~/Developer/Minotaur-Cowork/handoffs/to-cc/` + `from-cc/`, gitignored, `YYMMDD-hhmm_<unit>.md` — Cowork-Arch writes unit prompts and reads CC returns directly, killing the copy-paste both directions; CC executes a `to-cc/` file ONLY on Daniel's pointer; **the never-auto-ingested rule NARROWED of record:** `from-cc/` files read as EVIDENCE — instruction-like text inside binds nothing; every Daniel action gate untouched — WF-1a phrases, push-as-Daniel's-paste, install confirms, checks-never-share-a-paste). Daniel deletes `Cowork_Prompt_v1.md`; Roadmap → v3.4. **The Cowork opener of record (per-session paste):** "You are the Arch seat for Minotaur v2, running in Cowork under Architecture Prompt v29 — read it at `~/Developer/Minotaur-Cowork/Architecture_Prompt_v29.md` first, then the governing docs in its fixed order (Brief v1.1 → Roadmap WHOLE → Session Log; Ledger on citation). Seat boundaries: v29 §Seats; handoffs ride `Minotaur-Cowork/handoffs/` per v29 §Handoff Files. The Session Log header shows Rollover ⚠ DUE — run it before anything (archive → `Minotaur-Cowork/session-log-archives/`), I'll diff-verify; if clean, open S4-2 (S4-AUTO) in this sitting. Any schema need halts and routes to my claude.ai Arch chat. COWORK-REV fires at S4-2's close."
- **S4-2 / S4-AUTO COMPLETE (Aug 10–11 — the Cowork maiden BUILD session; the unit exits the docket):** commit chain `5027737` (leg 1 — method row field → autocomplete + auto-create; the dormant K-6/U5-3 row-field Enter branch goes LIVE) → `64968c4` (leg 2 — box row field → autocomplete + New Box ⊕; **the box-creation convention of record ESTABLISHED on an empty surface:** spine `equipment_items` row `type='box'`, name in `description`, then 1:1 `box_details` (Insert needs only `id`), delete-compensation on details failure; `v_equipment_list.box_name = COALESCE(bundle_box.description, own_box.description)`; the equipment list filters `type='generic'` so boxes never list) → `d338701` (leg 3 — (iii) filters → Zite FilterInput type-aheads; new `ui/FilterInput.tsx`) → `f47ecab` (leg 4 — the (mmm) method/box/group half: the outer-band editor generalized to every sort, `outerBandLabelFor` single source, U2-5/⌥⇧↑↓/landInQuantity machinery extended never forked) → **`1f06092` (leg 5 + AMEND-1 + AMEND-2, one combined commit):** declared baseline event frames 02/03/08 ×l/d (scope-proven twice, zero undeclared drift) · box ⊕ REBUILT on the CableRow A4 bundle-"+" idiom after the gate FAIL (root cause: AutocompleteInput's document-level outside-click handler unmounted the sibling ⊕ before its click — create now on `onMouseDown`+preventDefault, Enter/Tab activate while visible) · the (llll) blink fixed (root cause: display patch applied AFTER the async insert; now synchronous patch-then-reconcile, `createMethod` primitive shared with All-N) · the sticky chain (root cause of the re-gate regression: leg 4 added `relative` alongside `sticky` and Tailwind emits `.relative` later, so `position:relative` won — the band NEVER pinned; fix = drop `relative`, sticky already provides the ⊕'s containing block) · the band scope dialog · the Showing count. **OQ #3 discharged by leg 1. P2a shakedown CLEAN (the rollover: split proven byte-identical, archive+retain = original entries byte-for-byte, machine-verified).** Suite 1250/89 · frozen 3/3 · visual 16/16 @ 0.0000% of record at close.
- **Gate rulings registered (Daniel, Aug 10, at the S4-AUTO gates):** filter matching = **Zite-verbatim live substring per keystroke** (supersedes exact-equality; sentinels KEPT on all three filters per gate amendment J, Zite-style italic+divider render; internal FILTER_* constants retained so D-2/D-7 logic is untouched; the D-5 per-keystroke exemption drop accepted at gate) · **box fields are existing-records-only with the v1 New Box ⊕ affordance** (Daniel: "follow v1 — a new box creates an add icon", the bundle-"+" family; unresolved text without ⊕ = no-op-revert; empty = unlink) · **method auto-create = Grey via the GCM insert convention** (row field, band editor, and composing with All-N) · **the band scope dialog (supersedes the handoff's single-record-only ruling):** a method/box/group band edit that changes the value prompts "All N Items / Just This One" (Just-This-One = Enter default; model-change-dialog exemplar), All-N through the widened `propagateFieldToIds` (type-only widening; the dialog IS the propagation guard, doctrine-consistent), category cascade unchanged, row fields dialog-free · **the sticky band spec of record (Daniel-confirmed):** column headers always pinned; category sort pins category band + model band; group/method/box sorts pin sort band + category sub-band + model band; model bands pin in ALL sorts; incoming same-kind bands push off outgoing; rows always scroll under · **Showing X of Y beside Display All in every narrowed view** (filters now; find already had FOUND n of N).
- **Process/incident record (Aug 10–11):** **the PROBE RULE gains a clause: probes address inputs by EXPLICIT selector only, never positional matchers** — an AMEND-2 scroll-probe `.first()` grabbed the New Defaults Method field and committed "Rent-Main" to the fixture; caught by CC's own 6-frame gate FAIL, diff-artifact root-caused, UI-restored to null (execute_sql correctly not used), SELECT-proven, re-gated 16/16, disclosed unprompted · **(qqqq) registered from a caught-in-run flake:** the DEV2-R2 persisted `equipment_sort_mode` raced the harness's category reset and a baseline wrote in METHOD sort — detected at 28% FAIL, PNG-read, re-captured correct · **WF-2 datapoints:** the whole unit + both amend passes held on **Opus 4.8** (Daniel-designated at session open, credit economics; the handoff's Fable line amended live; feeds the CC credit-budget Strat question) · localhost login "Failed to fetch" adjudicated ENVIRONMENT-NOT-CODE (spotty link + stale dev server; auth-js network internals; no registration) · **evidence intake OWED as files** (inline pastes carry no bytes): `s4auto_v1_01_newbox_bundlelist` · `s4auto_v1_02_newbox_equiprow` · `s4auto_gate_01_modeldialog_exemplar` · `s4auto_gate_02_band_stack` → `docs/reference/evidence/equipment/`, landed by Arch after the CC session closes (one-writer).
- **Workflow rulings (Daniel, Aug 10–11 — the Cowork expansion set):** **the parallel-session framework + `handoffs/parallel/`** (third folder, gitignored with the rest): parallel Cowork-Arch seats are read-only on governing docs, the repo, and Supabase; deliverables land as FILES in `parallel/`; proposed rulings return by fold-in to the ONE principal writer session; no CC interaction — **P2b launched same night as the live pilot** (opener of record: `to-cc/../260811-0155_p2b_parallel_opener.md` content) · **S4-3 = the overnight/long-autonomous pilot** (structure of record in Roadmap v3.5: P2b digest → Daniel's design gate → MMM-CAT closes the band work → overnight build with per-leg machine gates, self-captured new-surface frames, a running GATE-QUEUE, and NO commits — morning = one batch browser sweep + serial WF-1a phrases; the UX-surface rework risk named and accepted) · **commit authority UNCHANGED** (per-commit phrases; any loosening belongs to WF1A-SUNSET) · **COWORK-PORT registered** (portable memorialization of seats/folders/parallel framework, CC-Contract-style canonical text; assess at Sprint 4 close) · the **evidence-exemption proposal** (untracked `docs/reference/evidence/` files excluded from CC footprint counts) rides COWORK-REV · **COWORK-REV: Arch seat-report delivered = EXPAND, no named defect; Daniel confirm OWED** (the fact of record for the eventual seat-consolidation question: the Cowork seat holds Supabase MCP too — the migration split is now discipline, not capability).
- **MMM-CAT amended + SEQUENCED (Daniel, Aug 10):** spec now = the category sub-band under method/box/group sorts gets the SAME edit behavior as under category sort **including ⌥⇧I**; CC's leg-4 flag (⌥⇧I on the OUTER bands stayed category-only, unruled) joins its design gate. Scheduled immediately after S4-AUTO's close — the band machinery is warm.

### Aug 11 rulings (the S4-3 DESIGN GATE + MMM-CAT design session — Cowork principal seat)

- **COWORK-REV CONFIRMED — EXPAND (Daniel, Aug 11).** The Cowork pilot expands; no named defect. Three riders confirmed with it: **(1)** the CC evidence-footprint exemption — untracked files under `docs/reference/evidence/` are excluded from CC's diff-count expectations; **(2)** the parallel-session framework ratified as **standing**, not pilot (`handoffs/parallel/`, read-only seats, fold-in to the one principal writer); **(3)** the S4-3 overnight structure as written in Roadmap v3.5. **Migrations continue to route to the claude.ai seat** until a seat-consolidation ruling. The docket row exits.
- **MMM-CAT design gate (Daniel, Aug 11) — two rulings, unit handoff issued to `to-cc/260811-0216_mmm-cat.md`:** **MMM-CAT-R1** — ⌥⇧I toggles **Model ↔ Category in EVERY sort mode** (top-level band in category sort, the category sub-band elsewhere); the outer method/box/group sort band **stays click-only**, as it already is — no regression, and CC's unruled leg-4 flag is hereby ruled. **MMM-CAT-R2** — a category **sub-band** edit cascades to **that visible sub-band's records only**; the same category under other outer bands is untouched; dialog-free, consistent with the Aug-10 "category cascade unchanged" ruling. Arch source-grounded the design in the live file (sub-band is display-only at `EquipmentListClient.tsx:3122`; category sort collapses categories to the `'_'` bucket so the sub-band never renders there — the unit is purely additive). **No declared baseline expected**; the July-13 band amendment already gave the sub-band the same `.category-header` class as the top-level band.
- **S4-3 DESIGN GATE COMPLETE (Daniel, Aug 11)** — the full P2b decision block adjudicated ahead of the overnight run, so overnight CC never waits on a decision.
  - **Batch CONFIRMED as proposed: P1 · P2 · P4 · P5 · P6 · P7 · P8 · P9 · P10 · P11 · P13 · P14.** Texts of record in `handoffs/parallel/260811-0156_p2b_decision_block.md`; all evidence-cited, none costs schema.
  - **P3 RULED — box rename ships silently with a passive toast** ("Renamed for N items"). v1's *"Update Members?"* dialog **dies**: it exists in v1 only because membership is free-text and the cascade is a six-table Replace; under v2 FKs the rename is intrinsic and lossless, so the dialog would ask permission for an operation that no longer happens. Registered as a deliberate v1 deviation.
  - **P12 / M0-WIN RULED — the "0" convention becomes an explicit state on a PROTECTED SYSTEM ROW.** Strings: method **`N/A - Labels Only`** · box **`N/A - No Box Needed`** (the `N/A - <reason>` family — one idiom, field-specific tails because what is excluded differs). Renders at the **bottom of every relevant pulldown, below a divider, italic** (the gate-amendment-J sentinel idiom); method badge **grey + italic**, not a coloured pill; the N/A band **sorts last**. **`0` survives as an INPUT and SEARCH alias only, never a stored value** — typed it commits the state, searched it returns those items, and the v1 importer maps incoming `"0"` strings onto it, **through ONE shared predicate** consumed by the commit, find, and import paths. Daniel's design contribution of record: the pulldown placement and the interchangeability of `0` with the label for old-school users. Arch's shape contribution: the **protected** system row (not renamable, not deletable, seeded per project) — v1's magic record with guardrails, so a user cannot break the convention for a whole show. **Windowing ruled:** method-N/A items are **VISIBLE and editable on the on-screen equipment list** (banded last) and **EXCLUDED from the printed/exported equipment list, cable checklist, tail checklist, and Bundle Sheets**; box-N/A items are excluded from Box Contents output and do **not** trip the P5 unboxed check. This is the PORT-READY leg-2 criterion — **M0-WIN closes when built and gated**.
  - **s3g_census §7 Q1 CLOSED (Daniel, Aug 11):** the Method-0 exclusion **extends to Bundle Sheets** — three sites, matching what v1's `Layout-Bundle Sheets` actually does, rather than the two named in the 2026-07-21 rule of record.
  - **OQ1 RULED — "Show boxes within boxes" works under EVERY sort**, and the control is **greyed out** where it genuinely cannot apply. v1's `scripts/789` guards it to by-truck only while Box-cat §5.3 documents it as unconditional: the intent was general, the implementation partial.
  - **OQ2 RULED BY NEW EVIDENCE — R-51 CORRECTED** (see Corrections). Daniel supplied the first box output PDF in the corpus.
  - **OQ3 RULED — Box Types Volume is STORED and manually entered.** Daniel's reason of record: **volume is the box's INTERIOR capacity; W/D/H are EXTERIOR dimensions** — different measurements, so deriving one from the other is a category error, not a simplification. Consequence registered: nothing computes with W/D/H, so they stay **display-only text** and v1's fractional entry (`14 1/2`) survives. Both catalogs corrected (see Corrections).
  - **OQ4 RULED — ship the per-project Box Defaults panel**, seeded with v1's template constants: **5 big / 0 small labels**, category `11.0 Racks and Boxes`, method/group/header blank, Set-Remaining off. Storage follows the house precedent (`box_default_*` columns on `projects`, alongside the existing `equip_default_*` / `cable_default_*` / `tail_default_*` / `bundle_default_*`) — no new table.
  - **OQ5 RULED — Warn/Over volume thresholds are HARD-CODED at 23 / 27**, not per-project; they drop off the v1 Box Defaults panel (two fields removed, deliberately). Daniel's reason of record: percent-full measures nominal material volume against interior capacity, so a well-packed box reads ~23% once air and connector bodies are accounted for, and past 27% it is too cramped to work in. **Disclosed asymmetry:** `projects.bundle_warn_area_in2` / `bundle_over_area_in2` remain per-project on the bundle side; flagged, no action.
  - **OQ6 RULED — nesting cycles forbidden at write time, depth uncapped (R-50 stands).** UX on an attempted loop: **inline message beside the field, entry reverts, field stays open, no modal.** Database trigger is the backstop; the client message is the UX. *(Arch note of record: the first framing of this question was jargon-heavy and Daniel could not act on it — re-asked in plain language per the v28/v29 plain-language rule, which is working as a defect signal, not a favour.)*
  - **OQ7 RULED — keep the cat easter egg, with a visible render** (small cat mark on the box row). R-39 flourish family; the deprecated Picture container stays unported (R-48).
  - **OQ8 RULED — stub the box-logo slot and build the labels now**; do not chain S4-3 print work to the blocked CONCEPT-A uploads. `projects.box_logo_url` already exists, so the stub is nearly free.
  - **OQ9 RULED — ADOPT (a) live-narrowing results under the find fields and (b) Truck as a box find field; DECLINE (c) and (d).** v1's **three separate labels menu entries stand** (no one-page type radio — R-56 normalized the FIND, not the menu count), and **Box Contents gets no options screen** (v1 has find only; the Zite screen is an invention).
  - **N4 RULED — v1's box Cost is a BUG and v2 fixes it.** v1 sums equipment members + `additional_cost` and **omits cable member cost**, so a cable-packed box reads $0. v2 includes it. Deliberate divergence registered: **box costs on cable-heavy shows will not match v1 output**, and that is the intended outcome. Weight keeps v1's shape exactly.
  - **N6 RULED — Star is a manual flag** the user ticks, and a find field on the box and label searches. The extraction seam closes **by Daniel's ruling, not by evidence** (the DDR shows no Star field among Box List's 45). No schema needed: `equipment_items.starred` already exists and boxes inherit it as spine rows.
- **Tonight's sequence RULED (Daniel, Aug 11):** blocking migrations applied at the claude.ai seat **first**, then the **full-scope** overnight run (Box List → Box Types → Contents → the three PDFs). MMM-CAT defers to the next sitting.
- **S4-3 schema block AUTHORED (Arch, Cowork seat) — `drafts/260811_S4-3_Schema_Block.md`, REV B.** Design only; **no migrations applied from Cowork**. Carries: the blocking `box_details` field set · the `projects` box-default columns · the `box_types` W/D/H + stored volume extension and the 56-row global seed (D-9; awaits Daniel's `Box Types.xlsx` export) · the nesting cycle guard + resolve-up on the existing `within_box_id` · the P12 system-row shape · the rollup cost correction · the N1 Brief reconciliation. **Finding of record: the S4-3 schema is a PREREQUISITE for the overnight run's FIRST leg, not a follow-on** — Box Number, Location, Stack, Colour, Header, Label/Return Notes, Additional/Actual Weight and Additional Cost have no home in v2 at `1f06092`.
- **S4-3 OVERNIGHT HANDOFF ISSUED — `to-cc/260811-0300_s4-3-boxes-overnight.md`.** Six legs, full machine-gate set per leg, self-captured frames of every new surface, a running GATE-QUEUE, **NO COMMITS** (WF-1a intact) with a **per-leg COMMIT MANIFEST** in the return so Daniel can reconstruct the chain by serial phrases in the morning. **Declared as a baseline ADDITION event only:** new frames sanctioned, the existing 16 must hold at 0.0000%, and any movement on an existing frame is undeclared drift that halts the leg.

### Aug 11 rulings, second block (the SCOPED MIGRATION EXCEPTION — migrations 41–44 applied FROM the Cowork seat)

- **SEAT-MIG — scoped one-time exception RULED (Daniel, Aug 11), with a mandatory reassessment.** Daniel asked why the schema block could not be applied from the Cowork seat. Answer of record: **nothing prevented it** — the split is discipline from the COWORK-P2 "B with seatbelt" pilot, not capability, exactly as the Aug-10 fact of record states. **Verified live before ruling:** Supabase MCP answers from this seat, `apply_migration` is in the toolset, and the ledger read **40** with `p9_step3_drop_phantom_model_from_v_cable_list` last — matching the Roadmap, so the freshness check moved from attested to machine-confirmed. Arch's position of record: the argument for keeping the split was never *which window the call comes from* — it was that this seat had applied zero migrations against the sprint's largest schema change at 3am; and the argument against was that routing it out meant a tired context handoff into a second seat, which is its own error surface. **Daniel ruled: apply here as a one-time exception, and queue reassessing a permanent lift no later than the end of Sprint 4** (→ docket SEAT-MIG, riding WF1A-SUNSET / COWORK-PORT). v29 §Seats' "any schema need = halt and route to the claude.ai seat" backstop therefore stands as written, with this single registered exception against it — **not** silently rewritten at 3am.
- **Migrations 41–44 APPLIED (Arch, Cowork seat, Aug 11). Ledger 40 → 44.** The claude.ai runbook was run verbatim for every one: live introspection → proof-run with forced rollback → residue check → apply → post-apply verification.
  - **41 `s4_3_box_details_field_set`** — the ten blocking box fields + `cat_photo`: `box_number` (NUMERIC, per the Taco Truck PDF's `8.5`) · `location` · `stack` · `color_name` · `header` · `label_notes` · `return_notes` · `additional_weight` · `actual_weight` · `additional_cost` · `cat_photo`.
  - **42 `s4_3_projects_box_defaults`** — the seven `box_default_*` columns, house precedent followed; backfill verified on all 21 projects (5 large / 0 small / set-remaining false / category null). Warn/Over deliberately absent per OQ5.
  - **43 `s4_3_box_types_dimensions_and_stored_volume`** — `width`/`depth`/`height` (TEXT, fractional entry preserved) + `volume` (NUMERIC, stored, no generated expression, no trigger). Legacy `dimensions` **RETAINED not dropped** (0 rows; dropping is destructive and needs its own confirmation).
  - **44 `s4_3_p12_na_system_rows`** — `methods.is_system` + `box_details.is_system`; `seed_project_na_rows()` (SECURITY DEFINER, `search_path` pinned, EXECUTE revoked from public/anon/authenticated); backfill of all 21 projects; a `seed_na_rows` AFTER INSERT trigger on `projects`; and the `guard_system_method` / `guard_system_box` protection triggers.
  - **Proof discipline of record:** proof-run created 11+7+4 columns then rolled back, residue verified 0/0/0 with row counts unchanged; the P12 proof-run produced exactly 21 N/A methods + 21 N/A boxes then rolled back, residue verified back to 189/1924/21. Post-apply counts matched the proof exactly (210 methods · 1945 equipment_items · 24 boxes). Grants intact for `authenticated` on all altered tables. The `SET LOCAL ROLE authenticated` probe returned **zero rows, which is the RLS gate working** — a grant failure raises `permission denied`, a clean empty result is correct for a role with no user context. **Security advisors unchanged at 9 before and after**; the two new SECURITY DEFINER functions do not appear, the REVOKE + pinned `search_path` having pre-empted both lints.
  - **Negative probes, all six correct and all rolled back:** rename system method → RAISES · delete system method → RAISES · rename system box → RAISES · delete system box → RAISES · rename a NORMAL box → ALLOWED · delete a NORMAL method → ALLOWED. Plus the cascade test that actually mattered: **a new project auto-seeds its N/A pair, and project DELETE still cascades cleanly with no orphans** — the `pg_trigger_depth() <= 1` guard distinguishes a direct user action from a cascade, so protecting the rows did not break project deletion.
- **P12 seeding shape RULED — EAGER, both fields (Daniel, Aug 11).** All 21 existing projects backfilled now (42 rows), and new projects seeded on INSERT. **Architectural note of record: the insert seeding lives in the DATABASE, not app code.** There is no project-insert trigger in v2 today — method/category seeding is app-side — but "every project has its N/A rows" is a system invariant rather than a user-facing default, and the import path creates projects too, so no client can be allowed to miss it. Consequence for CC: **do not add N/A seeding to the app's project-create path — it would double-seed.**
- **Findings surfaced by the introspection (registered, not acted on):** **(1)** the database now holds **three stray gate-test boxes** — `Salmon`, `Moose`, `Tryout` in project **JOY**, created 01:20–01:22 Aug 11 with 1–2 members each — the S4-AUTO New Box ⊕ tests. This is exactly the "stray-test-box check when Box List lands" that punch **(rrrr)** anticipated; keep-or-clear is Daniel's morning call. **(2)** the security advisory count has moved **7 → 9** since DEPS was written (two new SECURITY DEFINER lint rules), so that docket row's number is stale. **(3)** `service_role` holds only REFERENCES/TRIGGER/TRUNCATE on `box_details`, `box_types` and `projects` — **live corroboration for the SRGRANT docket row**, pre-existing and not caused by this session.

### Registration texts of record (units OPEN in the v3.0 docket — cited by docket ID; text carried verbatim from v2.46 at migration)

- **665 cable-form branch follow-on — NEW REGISTRATION (July 29, S3F-R2):** the Clone-based cable branch gesture + the WILD-LINE DEFAULTS surface it depends on (`projects` wild-line default category/method columns = its Trigger B; a Defaults-panel cluster). Small unit; verbatim 665 extraction on record in the S3-F design gate.
- **(lll)/(nnn) focus mini-pass** — two banked pre-existing focus defects (root trace banked, 260721-1201). Queued behind Sprint 3 (order ruled July 22).
- **EQUIPMENT PROJECTION UNIT (registered July 28–29; sequenced after S3-G, Daniel-approved).** The cable/tail → equipment-list bridge; the BVSC "equipment-list PDF renders from real cable data" criterion stands on it. Recon of record: `equipment_items` carries NO provenance today — the projection is net-new. **Rulings of record:** **EP-R1** projected rows (cables + tails): Name/Description + Model READ-ONLY; Method, Group, Box EDITABLE; Description = the mult name · **EP-R2** Model = "[Cable Model], [Length]" derived read-only · **EP-R3** editor toggle cluster Manually Added / Cables / Tails / Boxes, default Manually-Added-only; VISIBILITY ONLY (print scope stays with the existing search fields); Boxes ships empty until Sprint 4 · **EP-R4** cross-table warning on BULK ops only (R6-straddle anatomy); single-row edits warn nothing · **EP-R5** MATERIALIZED architecture — real `equipment_items` rows w/ provenance, synced by the cable/tail write paths; the frozen layer untouched by construction; provenance schema = Trigger B at the unit's open · **EP-R6 (divergence of record):** v1 never shows cables in the equipment EDITOR — only the PRINTED list, purge-and-refill per print; v2 = a live editor projection, Daniel-ruled. **Design-gate residue:** projected quantity/checkbox semantics · the sync-trigger inventory · the provenance migration · extraction of the v1 purge-and-refill print scripts.
- **Library-grid render pass** — post-Sprint-4: hoist/memo the library row on B-6..B-9.
- **GCM→list focus landing** — post-Sprint-4, Daniel-scoped: reuses (ss)'s param-landing machinery.
- **Guarded bulk-delete** (carries Library Delete Found) — deadline end of Sprint 4, alongside the deferred export unit (+S-12 rider). The No-default constraint inherited.
- **Estimator-audit / measured-pagination** (D-3(a)).
- **Method-0 windowing** — gates the port-readiness "equipment-list PDF renders" criterion. Pre-port.
- **Sharing-UI + membership layer** — private-beta prerequisite (owner-only `projects` UPDATE policy ⇒ editor New-Defaults writes silently no-op — fix here). The IMG-2 storage policies extend alongside.
- **Three-tier library architecture** (Strat July 11). Pre-BVSC registration.
- **Method field → autocomplete with auto-create** (Sprint 4, paired with box autocomplete + box editability + (iii) + the (mmm) method/box/group band-editability half) · **Print header line2 + autosize** (+ top-left image render) · **Library-page notes fields strip newlines** · **Equipment grouping/sort engine residue** (re-scope at open) · **Session Undo/Redo** (⚠ the note-propagation prompt is the sole guard on an unrecoverable destructive write) · **Durable Version History** (~Nov; the cable inspector History stub joins its consumers) · **Trash/Archive** (post-Sprint 4, Trigger B) · **Custom-domain auth email** · **Bingo disposition** (post-BVSC) · **Allocate as type-conversion (D9)** — `allocated_from_id` landed and FENCED consumer-less; this unit is its consumer · **Hook-test infra + RTL/component-test infra** (someday-maybe; repeatedly disclosed hard-stop) · **(mmm) category-band-under-other-sorts half** (own pass, unscheduled) · **(ppp) settings fixed-header pass** (declared baseline event, frame 06) · **Equipment-list type-visibility toggle** (post-Sprint-4) · **Fresh-project cable-catalog seeding** (mitigated July 24 by CTF in-flow creation) · **Steck-color verify** (rides the print-labels unit) · **Harness settle-wait** (readiness settle beyond the clock pin; the full signature family in the punch row) · **CTF-dialog Enter hardening** ((qqq)+(sss), own small pass) · **(rrr)/(ttt) + (www) baseline/colour window** (bundle at the next sanctioned opportunity).
- **ZA-2 rider (standing):** the Equipment Projection unit's Trigger B deliberately revisits equipment-surface type visibility (the EP-R3 toggles + provenance); the (zzz) list-only client filter is the interim truth. Box-type rows stay off the equipment surface until EP rules otherwise.
- **OQ#2b (future option, unscheduled):** the pending new record renders as its OWN band until first model commit — supersedes the ZA-3-accepted shared-band pendency visuals if they annoy in practice (CC's banked alternative, handoff 260801-1029).
- **Latency/optimistic unit (registered Aug 2–3; recon → design gate → build):** #8 + #11 + New Item; frozen-region caution of record; P-1-informed.
- **Dependency-maintenance unit (registered Aug 3):** the gated Next/postcss/sharp upgrade pass; pre-October; feeds the security review.
- **S3-G-2 — residual v1 outputs (Shop Orders Basic/Detailed + remaining reports):** Arch-recommended Sprint-4 home; **Daniel's one-word confirm owed.**
- **GS-C — cable completeness (SPRINT-4 BASKET per the compression):** SPL-C split (evidence banked; the 661-wart adjudication owed) · INS-1 inspector tail/line summary (Daniel's Aug-5 registration; the v1 detail-form screenshot = anchor evidence, naming/commit owed at its gate) · (bbbb) Show-Me routing · candidate (qqq)/(sss) CTF hardening.
- **ADM-1 — beta admin panel (STRAT-ADM-R1/R2/R3/R6; post-sharing, pre-October; the R6 decoupling contingency registered).** License admin = a named deliverable of the ~Nov licensing design.
- **FR-1 — find-and-replace mode on every find panel (Daniel Aug 6; post-Sprint-4, post-ADM-1; design-gate questions banked in the Aug 4–7 rulings block).**
- **Concept-A print-header unit (CL-R1 consequence):** the remaining v1 cover/branding print rendering (top-left image · box logo · line2+autosize · units · live preview) — largely existing roadmap lines, now gathered; **gated on the a-1 uploads**.
- **EQUIPMENT PROJECTION UNIT (registered July 28–29; sequenced after S3-G, Daniel-approved).** The cable/tail → equipment-list bridge; the BVSC "equipment-list PDF renders from real cable data" criterion stands on it. Recon of record: `equipment_items` carries NO provenance today — the projection is net-new. **Rulings of record:** **EP-R1** projected rows (cables + tails): Name/Description + Model READ-ONLY; Method, Group, Box EDITABLE; Description = the mult name · **EP-R2** Model = "[Cable Model], [Length]" derived read-only · **EP-R3** editor toggle cluster Manually Added / Cables / Tails / Boxes, default Manually-Added-only; VISIBILITY ONLY (print scope stays with the existing search fields); Boxes ships empty until Sprint 4 · **EP-R4** cross-table warning on BULK ops only (R6-straddle anatomy); single-row edits warn nothing · **EP-R5** MATERIALIZED architecture — real `equipment_items` rows w/ provenance, synced by the cable/tail write paths; the frozen layer untouched by construction; provenance schema = Trigger B at the unit's open · **EP-R6 (divergence of record):** v1 never shows cables in the equipment EDITOR — only the PRINTED list, purge-and-refill per print; v2 = a live editor projection, Daniel-ruled. **Design-gate residue:** projected quantity/checkbox semantics · the sync-trigger inventory · the provenance migration · extraction of the v1 purge-and-refill print scripts.

- **PORT-COV — import/export coverage extension (NEW REGISTRATION, Aug 10; the PORT-CRIT ruling's consequence unit):** ONE unit extending the v1 import engine to parse cable-side data (cable/tail/bundle/lines — registered-but-never-parsed in v1; the DDR-format extraction is net-new) plus box data, AND the v2→v1 export v1-leg to emit the same three classes. Sequenced after the box unit family; merged into the end-of-Sprint-4 EXPORT slot (the export half already lives there). Ports open only after PORT-COV, per PORT-READY leg 5.

---

## 🧾 Corrections of Record

*(Carried WHOLE from Roadmap v2.46 at the migration; entries through July 14 remain retired to the v2.17 archive. Append per close from here.)*


- **Arch (Aug 2, DEV-1 handoff):** the "standalone (expected)" premise on the workbook importer was FALSE — it shares the frozen §2 hash engine + the `import_apply_v1` RPC with the v1 show importer. CC's Step-0 hard stop was the gate working; resolved by the R8 fence, not by premise-patching.
- **Arch (Aug 2, DEV1-R1 as first written):** "extend the existing sort_order assignment scheme" — NO such scheme existed for imported equipment rows (neither client plan nor RPC assigned it; imports landed effectively unordered under the DESC tiebreak). CC's HS-2 recon exposed it; R9 built the scheme instead of extending a phantom.
- **Arch (Aug 2, the DEV1-R9 proof run):** transcription defect in the reproduced RPC body — `{identity,deleteIds}` for the live `{ops,equipment_items,deleteIds}` in section 7. **Self-caught in the pre-apply diff; the rollback-proof pattern contained it by design; the applied migration verified defect-absent.** The lesson stands: every reproduced-body migration gets a targeted post-apply key check.
- **CC → evidence (Aug 2, CL-0):** "v1 has no Concept-B cover letter" — an honest absence finding (no artifact in-repo) AMENDED by Daniel's cl_v1_01: v1 had a simpler letter (Shop Order body + five signature lines); settings and cover letter were always separate v1 surfaces. Recon absence-claims are provisional until Daniel corroborates.
- **CC (Aug 2, CL-0):** "cover_letters has no updated_at trigger" — read from generated types; live DDL showed `set_updated_at` PRESENT. The live-DDL-over-code-reading rule vindicated again.
- **CC self-correction (Aug 3, AMEND-3):** the first audit report implied the 7 vulnerabilities came from the tiptap tree; the corrected report proved ALL 7 pre-existing and the added tree clean. Unprompted self-correction; the adjudication ran on the corrected detail.
- **Import identity-matcher case bug (LIVE, predated the unit, July 15):** fixed at the comparison boundary; the round-trip harness's biggest save.
- **Arch (July 15):** `import_identity_key_present` CHECK never widened for amendment 1 · D-EXP-3 misdiagnosed as TS coercion.
- **CC (July 15):** Pass A headline contradicted its own raw output; Raw Output Rule vindicated.
- **Arch (July 16):** (vv-b) hazard retracted · PreviousRevisions inversion crossed one palette.
- **Identity (July 16):** "Foobar" = "Elephant Shoes / Two River Theater 2026" (deleted). `38acd35` over-claims; `16b29de` corrects.
- **Arch (July 17):** corrected CC's method-only-finds recon conclusion · push-without-commit caught pre-shop-print.
- **Arch (July 17–18, slot 2):** flat-array placement superseded by U3-5 · original U5-3 amended.
- **Slot 3 (July 18):** CC's "B-1..B-5 missing" flag resolved · frame-01 midnight-rollover diagnosed benign.
- **Arch (July 18, consolidation):** (rr)/(ss)/(zz) letter collisions resolved by sequential re-lettering.
- **Sort unit (July 18–19):** zero Arch design corrections. Daniel-side staging misfire (fixture RLS-invisible from his dashboard) resolved via test-account login.
- **FLT batch (July 19):** zero Arch design corrections.
- **Split unit (July 20):** one Arch error, owned — stale harness name in the handoff; CC ran the correct harness and flagged it.
- **Add-to-List (July 20):** one Arch error, owned — AL-3 speced filter-aware; Daniel's gate corrected to add-time override. Harness two-flag + all-16-rewrite limitations registered.
- **Category-fill / band-key (July 20 PM):** one Arch miss, owned — CAT-1's straddle→collision consequence unflagged; caught by Daniel's gate, fixed same session.
- **Focus-follow (July 21 AM):** one Arch design miss, owned — the Tab-family landing was left immediate against cascade dialogs; Daniel's gates 5–6 surfaced it; amendment fixed same session.
- **(kk) bridge (July 21):** one Arch design error, owned — A1 ordering (storage cleanup after the row delete); **caught by CC's OPEN QUESTIONS pre-commit**; fixed in amendment 1. Two Daniel-gate-caught gaps fixed in amendment 2 (A4 drag-drop unwired; A5 replace re-resolve). NOT corrections: IMG-5 recon-caught pre-ship; (lll) fix-hold correct contract behavior; OQ-2/OQ-3 flagged dispositions accepted; the leg-A classification table held.
- **Sprint 3 open (July 22–23): four Arch errors, all owned.** (1) **The grants outage** (production down until CC's Step-0 caught it; standing grants rule adopted). (2) The corrective migration's first-apply collision with the pre-existing `cable_default_category_id`. (3–4) **Two invented UX features in the B1 handoff** — the direct impetus for the DL rearchitecture and the Parity Rule. Recurring defect logged: four plain-language re-asks in one session.
- **B2 line (July 23–24): the first full run under the delegation contract — the contract HELD.** Zero invented features shipped. **Arch, owned:** (1) the B2-B handoff's parked-commit premise overtaken by the already-pushed `daa928c`; (2) two commit-message accuracy amendments. **Delivery-mechanics incident, Arch-owned:** `bash-fence.sh` execute bit (chmod rule adopted). **Process finding:** chat-relayed commit approval never reaches CC — approvals were terminal-only (mooted by WF-1). **CC noted:** two self-flagged stream edits (rule firmed); sound unprompted calls on record. **Daniel gates produced three defect rounds, all fixed same-session pre-commit.**
- **(ooo)-2 + CTF session (July 24): three Arch errors + one sequencing miss, all owned; two contract-vindication halts; the first WF-2 Opus run held.** (1) **Schema recon miss, domain vs column:** the `default_label_size` CHECK sat in live DDL; CC's Step-0 HALT caught it; R1 fixed it. (2) **OQ5 — the Sprint-3 autocomplete constraint not carried into the CTF handoff's section ④** — CC built the ruled-illegal select and flagged it for the wrong reason; corrected pre-commit. (3) **F10 misattribution:** Arch's first Amendment-3 named METHOD as the refresh-blank field; Daniel's correction (MODEL) redirected pre-cost. (4) **Sequencing miss:** the amended CTF resume issued before the (ooo) commit landed — CC's precondition HALT refused to stack units on the uncommitted tree. **NOT corrections:** the ruled divergences correctly built + flagged; CC's blur-commit race guard and F10 root-cause-first discipline; the 15/16 flake correctly bounded. **Daniel gates produced two defect rounds + the four (ooo)-2 captures that dissolved a two-week open question.**
- **B3 session (July 25): ZERO Arch design errors — the third consecutive clean unit run.** Daniel gates produced three finding rounds — all fixed same-session pre-commit; **A5 and A7 both byte-proven PRE-EXISTING (B2-line).** **CC process note (self-reported, recovered clean):** a `git stash push -u` round-trip partially applied on pop; recovered via checkout-from-stash + full-gate verification. **CC sound unprompted work:** the SP-6 orphan-compensation; the A6 instrumentation-first root cause; the smoke-caught create-then-type length clobber.
- **B4 session (July 25 PM): one CC invention defect + two Arch errors, all owned; four gate rounds, all fixed same-session, ONE commit.** (1) **CC invention defect:** the B4-AMEND sort control shipped as a dropdown of CC's own design — struck at Daniel's gate, v1 toggle anatomy built in FIX; the WF-2 tripwire weighed, no escalation. (2) **Arch, owned:** ratify-lean on that dropdown issued before checking its pedigree. (3) **Arch, owned:** the sticky "regression" framing was a mis-diagnosis — CC's FIX-1 finding corrected it. (4) **Process catch (Arch desk):** a stale handoff file identified before review. **NOT corrections:** FIX3-1 root-caused as the phantom column's second bite; CC's sticky-region finding; Daniel's mid-run direct-to-CC rulings registered clean.
- **B6 session (July 26): one CC-introduced build defect, one gate round, ONE commit; the Step-0.5 hard stop was the contract's finest hour.** (1) **Contract vindication:** CC's Step-0.5 HARD STOP on the link field-set — resolved by b6_01 + three rulings. (2) **CC build defect, owned + root-caused (FIX-1):** the ExtensionDialog value-coupled focus effect — class-eliminated; the keyboard contract ruled standing. (3) Checkpoint-merge disclosure accepted. (4) **Governance:** the MCP ask-gate retired by ruling.
- **B7 session (July 26 PM): two contract-vindication hard stops + one Arch process miss + one Arch imprecision, all owned; four gate defects + one Daniel overrule across two amend passes, all fixed same-session, ONE commit.** (1) **Arch process miss:** the b7_01 staging instruction rode prose, not the handoff — CC's missing-artifact HARD STOP correct. (2) **Contract vindication:** the bulk-path HARD STOP; Arch's EXEMPT lean OVERRULED by Daniel's R6 invention. (3) **Arch imprecision:** R1's "all 19 tokens are cables-table columns" corrected by CC's enumeration. (4) **Daniel gates:** AMEND-1 four defects; AMEND-2 = the D1-R overrule + the vocabulary sweep. **NOT corrections:** CC's Step-0.2 script verification; the idle-editor resync fix; two exemplary sed ledgers.
- **S3-D session (July 27): one Arch process miss owned + one CC guard defect root-caused; three gated amend passes, ONE commit; the Parity Rule's first evidence-bounce of an ARCH fallback.** (1) **CC divergence vindicated:** the per-end auto-add skip was an Arch invention-as-fallback; script 354 unambiguous; CC built the cited truth. (2) **CC build defect root-caused (AMEND-1):** the derived-is_mult guard. (3) **Arch process miss, owned:** AMEND-2's A1 accepted on a VACUOUS harness proof; the non-vacuous proof ordered at AMEND-3; the field residue → punch (uuu), instrumentation-first ruled. (4) **Stop-and-punch honored.** **NOT corrections:** the R2 conformance scare; the M4 isolation; the scope-fence discipline.
- **S3-E session (July 27 PM): one Arch evidence-citation error owned + one Arch citation corrected + two CC build defects gate-caught; one Step-0 hard stop honored + one gated amend pass, ONE commit; the read-back loop's first premise dissolution.** (1) **Arch, owned:** frames ordered as evidence without verifying disk presence — the fourth consecutive Step-0 catch of an Arch premise. (2) The 330/331 citation corrected per the pre-authorized rule. (3) **Premise dissolution:** the clone-lockstep proposal dissolved by Daniel's correction + the read-back loop, pre-build. (4) Two CC build defects gate-caught, class-eliminated. (5) Ratified residuals on record.
- **(uuu) session (July 28): ZERO Arch design errors; one transmission incident process-fixed (UUU-D5); two CC hard stops honored; zero gate-defect rounds — the cleanest unit run on record.**
- **R7 session (July 28–29): two Arch errors owned + one CC build defect gate-caught; one gate round + one amend, ONE commit; the smoke pattern licensed.** (1) **Arch, owned:** the uniform dialog default — the OQ-R7-3 symmetry inference applied to the scripts' logic but not their DEFAULTS; Daniel's gate caught it (→ R7-A1). (2) **Arch, owned:** description linkage missed at the design gate — 668 was in evidence, 610 was not; the extraction request came one gate late. (3) CC's missed Auto-Fill path — a build defect vs its own spec, gate-caught, funnel-fixed w/ tests. (4) **NOT corrections:** CC's OQ#2 refusal to invent a bulk-propagation policy (ruled next turn → the S3-F rider) · the RTL hard-stop honored twice · the 688 mechanical omission (Step-0-sanctioned, flagged anyway) · the flags-2/3(a) licensing arising from CC's honest disclosure of an unlicensed-but-hygienic smoke.
- **S3-F session (July 29–31): one Arch map-drafting error owned (gate-caught by CC) + two Arch hypotheses falsified by CC evidence + one Arch recommendation withdrawn on evidence + two CC build defects root-caused + one implementation-ambiguity resolved against CC's reading; three Daniel browser-gate rounds + two amend passes + one mid-unit session handover, ONE commit; (www) ships known-open by ruling.** (1) **Arch, owned (the unit's headline catch):** the S3F-R6 coupling map assigned the SAME gesture (a branch cable's near-field edit) to BOTH patch re-resolution (Checkpoint A, accepted) and mult-rename/line-renumber (couplings 2/3 reverse) — CC's CP-B HARD STOP identified the contradiction, refused to silently pick, and recommended the resolution Daniel adopted (R6-AMEND-1). Amendment procedure, not silent folding. (2) **Arch recommendation withdrawn on evidence:** the OQ-2 "gesture-only, v1-true" lean — 421–424's opening `Check Cable … Connected to Mult` calls proved v1 parses typed end text on every save; the v1-true build includes the parse. (3) **Arch hypothesis falsified (FIX-1):** "the acted-id refactor dropped the queue" — the truth was dialog STACKING on mixed Auto-Fill batches, CC browser-proven pre/post. (4) **Arch hypothesis falsified (FIX-2):** "the CP-A funnel swallowed the advance" — the advance was never wired (the `data-equip-field`-only branch); CC evidence-corrected and fixed per the Tab precedent. (5) **CC build defect (FIX-3):** the reverse-colour plan targeted the line in both couplings — the OQ3 ambiguity CC correctly flagged; Daniel resolved AGAINST the uniform reading (mult-coupled Update repaints the MULT); the browser residual = (www), Daniel-ruled ship-and-log. (6) **Contract vindications:** the Step-0.2 evidence hard stop (s3f_01 absent; whole-unit gate held on Daniel's scoping call) · the CP-B coupling hard stop · the mid-unit context-budget handover (CC's own flag; licensed as WF-2 practice) · the couplings-4/5 wiring deferral (an uncited double-dialog risk correctly not invented around — ruled next turn as resolution-wins). (7) **NOT corrections:** the Step-0.4 inverted-enrollment verification (dissolving 421–424's opposite-column machinery as a v1 storage artifact — architecture of record) · the evidence-faithful ➡-alone cell (R3-AMEND — evidence beat Arch's paraphrase) · five licensed smokes censused to zero · the fence catching a heredoc + a `perl -i` attempt · the D3-AMEND disclosures (no rename field; no edit-path queue) accepted as correctly-not-invented.
- **Hash remap (July 10 rewrite):** carried verbatim (`7f36ed9a→be0769de` et al.).
- **Find session (July 31): one Arch scoping error owned (CP-4 bundle-list rollout bounced by CC — no surface exists; Parity vindication; → unit D-2) + one Arch naming error owned (the base handoff's "Opus 5" tier — nonexistent; D-4 fixes the tier of record at Opus 4.8, Fable 5 by exception) + the D-3 fence evasion closed same-session.** Process adoptions: the fresh-server re-gate preface at every Daniel browser gate (three stale-chunk observations); D-6 widen-once clears BOTH narrowing layers; V-3 debris (11 S3-F smoke projects + "Test Cable") Daniel-deletes at leisure.
- **(zzz) session (Aug 1): two Arch errors owned, both design-gate premises, one caught by CC's contract and one by Daniel's browser gate; two amend rounds, ONE commit.** (1) **Arch, owned:** the Z-2 "ONE shared query fn" placement premise — the KK-2 doctrine cited as implemented reality without ordering the read-path inventory first; CC's CP-A HARD STOP delivered the six-site truth (print's own GO read feeding print-time snapshots; the method-names read; three import pre-snapshot reads). Contract vindication. (2) **Arch, owned (the session's headline catch):** the ZA-1 view-side uniform filter violated **EP-R6's own second clause** — v1 shows cables on the PRINTED list (the shop order); Arch cited the evidence's first half as Z-1's pedigree while recommending against its second. Daniel's print gate caught it pre-commit ("we can't order any cable for the project"); resolved by Z-1-AMEND/ZA-1-AMEND (list-editor-only client filter; the view WHERE unwound same-hour, data-side). (3) **Process adoption:** commit-block annotation lines carry `#` comment prefixes — the July-31 close block's bare `(expected: …)` lines executed as commands and errored harmlessly. (4) **WF-2 datapoints:** both (zzz) CC passes ran on Fable against a Tier: Opus 4.8 line (Daniel's session selector; flagged per the tier rule both times, proceeded; no action). (5) **NOT corrections:** CC's reader/writer cache-consumer distinction (KK-4(a) correctly classed a writer) · the regen-as-drift-check (+1 line diff proving live-DDL fidelity) · the run-1 orphaned-dev-server infrastructure failure (port swept, kill hardened, orphan censused) · the ZA-3 residual behaving exactly as ruled at Daniel's gate (transient highlight).

- **Aug 3–4 window (REC · AIR2 · the ground gates): three Arch errors owned + CC correct calls of record.** (1) **Arch, owned:** §0.2 of the AIR2 handoff described the five REC-1 files as STAGED — Sheet #1's commit-time picture carried as current tree state; actual = untracked; CC's Step-0 hard stop correct. (2) **Arch, owned:** the v2.44 lineage line omitted `bfd75ca` (corrected in this file's Current Status). (3) **Arch, owned:** Ground Session 2 issued before the Move-1 pastes had run; CC's precondition hard stop caught it — the guard exists for exactly this. (4) **Arch, owned (document defect):** the v2.44 footer misread "End of Roadmap v2.43" (corrected here). (5) **CC correct calls, of record:** the arrow-commit no-surface adjudication · the Lines structural deferral · the N-R2 Parity bounce (ruling text not in hand) · both hard stops · the 08-restore recommendation. (6) **NOT corrections:** the REC self-corrections (Shop-Order retraction; the field-21 gate precision) — the recon's own read-back discipline working.

- **Aug 4–7 window (GS-A · P-1 · E-CA): five Arch errors owned + one CC incident contained.** (1) **Arch:** P-1 registered as a production DEFECT without cross-checking the Roadmap's own Sprint-Plan line stating equipment Change All was the unbuilt guarded unit — the answer was in our own document (CC's static diagnosis caught it). (2) **Arch:** the DEV2-R2 migration designed without inventorying existing print persistence — `print_list_settings` was live all along; corrected same-hour by the L0-R1 drop (CC's hard stop caught it). (3) **Arch:** the AMEND-1 cross-surface recon ENUMERATED surfaces from memory ("cable and lines") instead of instructing derivation from code — tails were omitted; the AMEND-2 STEP-A derive-from-code rule is the permanent fix. (4) **Arch:** AMEND-2's flush-then-re-read deviated from the specified capture-and-thread and was RATIFIED without flagging the deviation — Daniel's gate exposed the race; AMEND-3 implemented the spec. (5) **Arch:** the AMEND-3 spec's close-without-commit created the F4-No divergence from v1 — recommended, overruled by Daniel's v1-literal ruling. (6) **CC (owned, contained):** the AMEND-4 fixture-probe corruption — a probe whose safety was derived under discard-No semantics ran after F4-No began writing; disclosed immediately, SELECT-assessed, Arch-restored under a declared exception; the PROBE RULE registered. (7) **Transport:** three CC handoff uploads rendered EMPTY in-context (disk copies intact) — the disk-read fallback is now the standing routine. (8) **CC correct calls, of record:** the P-1 static diagnosis + repro-skip deviation (ratified) · the 694 hard stop · the E-CA fresh-session handover · the Leg-1 fork disclosure/salvage · both Amend-4 verdicts incl. the Item-2 not-reproducible proof.

---

### Aug 7–9 corrections (the S3-G window — Sync Sheet #1 §4, carried at migration)

**Arch:** propagated 1a's stale HEAD citation into the 1b opener unverified · the "21 expected lines" staging miscount (was 20) · the bundle-sheet "trailing numeric pair" pdftotext misread (the Pulled/Labeled boxes) · the per-filename commit-block convention failed on exotic filenames (replaced by directory staging, standing).
**CC:** originated the stale HEAD in the 1a state map · amend-2 B9(ii) false "structure matched" parity claim (overlaid the Playwright path, missed the Description column and the interactive rotate/scale) · amend-2's landscape "verification" via the disqualified path.

### Aug-9 corrections (Strat — Sync Sheet #2 §4, carried at migration)

- **Strat (owned):** the roadmap-restructure need (v2.46's four overlapping registers, the 2,000-word Status paragraph) was flagged by Daniel before Strat raised it — register hygiene was Strat's to own earlier; the v3.0 split + the Docket Sweep are the structural fix.
- **Strat (self-caught):** the S3-G close rider retired before delivery when Daniel serialized the close through the Strat session.
- **Not corrections:** the drafting session's fold-in surfaced an 11th open question (Ledger versioning) beyond its numbered list — caught at Strat adjudication; the hook shipped pre-tested (four cases).

### Aug-9 corrections (the housekeeping session)

- **Arch (owned):** the step-2 CLAUDE.md fold handoff instructed "keep the heading" on the run fence — leaving the stale `SPRINT 3 CABLE (opened July 23…)` heading above a body reading "No run open." Contradiction authored by the instruction, not CC's execution; fix rides the relocation errand (heading becomes bare `## Current run fence`).

- **Arch (owned, ×2 — the WF-1a switch-on program):** STEP 4's lift said "`git commit` only," blind to the separate `Bash(git add *)` deny — the pilot's own stage-by-filename flow was unexecutable as instructed; and "lift the fence's git commit block" mechanically lifted the `-a` variants block, a real defense-in-depth reduction. Both CC-caught (one hard stop, one disclosure), both remediated at `d5637e2`.
- **Arch (owned — the sweep defect):** step-2's Placement C interleaved the Cable S3-G section between ambient doctrine and the Desktop Handoff Protocol; the relocation's cut-at-next-`###` rule then swept four standing-doctrine bullets into `gs-a-e-ca.md` (`4a1ee1b`). Caught by CC's Fold-1c anchor discipline; remediated at `d5637e2` by restoring the four under their own contract heading `### Standing gates and guarded surfaces` — extraction-proof by construction. The relocation's byte proof was honest about its limit: it proved no bytes lost, not correct filing.

### Aug-10 corrections (the S4-1 session)

- **Arch (owned — stale invocation citation):** the GATE-VIS handoff spec'd the bare `node scripts/visual-capture.mjs --gate`; the standing harness invocation per CLAUDE.md is `node --env-file=.env.local scripts/visual-capture.mjs --gate` (the bare form exits 1 demanding credentials). CC caught it and ran the documented command; the handoff's citation was stale.
- **Arch (owned — remembered-structure edit instruction):** the VERCEL-CLI handoff instructed "replace the Daniel-screenshot Vercel line" in CLAUDE.md's "pre-open verification section" — neither existed (CC grep-proven: zero vercel/pre-open matches). The same never-design-against-a-remembered-schema discipline applies to CLAUDE.md; CC's placement judgment (Standing instructions list, after the frozen byte-gate bullet) accepted as made.

---

### Aug-11 corrections (the S4-3 design gate session)

- **R-51 CORRECTED by new evidence.** As registered, R-51 said nested boxes carry an "Inside ⟨parent number⟩ ⟨parent name⟩" annotation in the by-truck Box List's **Notes** column. Daniel supplied the first box output PDF in the corpus (landed as `docs/reference/evidence/print/parity/v1_elephantshoes_boxlist_bytruck.pdf`) and it shows otherwise: the **Notes column is EMPTY** under by-truck — the `Combined` calc's suppression of both annotations is correct — and nested boxes instead render as **indented, italic, grey child rows in the NAME column, prefixed `Inside:`, carrying their OWN number and name**, immediately beneath their parent, which is never named because the parent-first sort guarantees it is the row above. The DDR extraction had predicted exactly this and could not resolve it ("five Text objects conditionally formatted on `nested AND by-truck` — consistent with a hide/indent treatment"). P2b's OQ2 is closed by artifact rather than by recall. **Also confirmed from the same PDF:** the column set `# · Name · Type · Location · Size (WxDxH) · Weight · Notes`; **decimal box numbers** (`8.5` slotting a parent between 8 and 9); unnumbered boxes sorting first as their own block; and **blank-beats-zero** rendering (no weight prints empty, never `0 lbs`). Partially closes digest gap **G2**.
- **Box-cat §3 and DDR-cat §1.7 CORRECTED.** Both state that Box Types' Volume "computes from W×D×H". **Both are wrong.** The DDR extraction is right — Volume is a plain editable Number, no extracted script computes it, and it travels as its own column in the 776/777 interchange. Daniel's ruling supplies the *reason* the catalogs could not: volume is INTERIOR capacity, W/D/H are EXTERIOR dimensions. The evidentiary hierarchy (DDR over screenshot-leg) resolved this correctly on the mechanics; the catalogs' error was semantic.
- **Arch self-correction — S4-3 schema block REV A withdrawn and replaced by REV B, same session.** REV A proposed adding `box_details.starred` and stated that "nothing in this block is required for the overnight run's first legs." Both were wrong, caught by reading `src/types/database.generated.ts` directly rather than trusting the P2b digest's NOTES: `equipment_items.starred` already exists and boxes inherit it, and the missing `box_details` field set **blocks the run's first leg**. Corrected before Daniel carried the file to the claude.ai seat. Standing lesson restated: the reference corpus describes v1; only the generated types describe v2.
- **Arch process correction — OQ6 asked badly.** The first framing ("forbid cycles, depth uncapped — the UX-on-cycle question") drew *"I don't understand the question"*. Re-asked in plain language against Daniel's own data (FOH Racks / FOH CPU Rack, from the PDF he had just supplied) and answered immediately. Logged per the v28 rule that treats an "explain plainly" response as a defect signal in the question, not a favour to the reader.

## 🔗 Lineage

*(Commit chains and per-unit build narratives, by window; append-only. The two v2.46 Current-Status bullets below are carried VERBATIM at migration — the full July–August lineage of record.)*

- **Live:** v2 at `https://minotaur.app` (Vercel prod on `main`; repo `lundbergsound/minotaur-v2`; local `~/Developer/Minotaur`). HEAD **`b09178e`** (GS-A Leg 6 — Change All on all five list surfaces). July 31–Aug 1 lineage: `327c100` (S3-F) → `b572fd4` (**Find Engine** feat + amend 1) → `d6a44a4` (D-3 fence patch) → `19104e7` (Find CLAUDE.md fold) → **`2720607` ((zzz) — pending-aware model band key + list-only generic scope; 5 files +85/−19; one design gate + one correct CC hard stop + one amend pass + two Daniel gate rounds incl. the print-truth catch, ONE commit)**. Prior anchor: July 23–31 lineage: `3b0b175` → `a63e03b` → `51290df` (**B2 unit**) → `efb176c` → `2c85769` (**B2-A**) → `daa928c` (**B2-B**) → `023cb10` → `8329881` → `72919e3` → `6a7e297` (**(ooo)-2**) → `c1d7ae6` (**CTF**) → `745e447` → `03b9753` (**B3**) → `e92deaf` → `a066155` (**B4**) → `d049b41` → `4dd575d` (**B6**) → `1d4a142` → `18d09ce` (**B7**) → `2e90707` → `dfd95c9` (**S3-D**) → `603c9c7` → `861b4f9` (frames 031–035) → `670849d` (**S3-E**) → `864c506` → `8505484` (**(uuu) Phase 1**) → `1786211` (**(uuu) Phase 2 + AMEND-1**) → `c1fbdb5` ((uuu) CLAUDE.md fold) → **`3acbee8` (R7 — line→line patch pass + AMEND; 13 files +1175/−341)** → `5b2a689` (R7 CLAUDE.md fold) → **`327c100` (S3-F — Link Cable column + cable↔line patch family; 18 files +2512/−87; one design gate + three Daniel browser-gate rounds + two amend passes + one mid-unit session handover on CC's own context-budget flag, ONE commit)**. Prior lineage: `757e3e4` → `9d9e7d8` → `5cf9c61` (B1) → `3b0b175`. Aug 1–4 lineage: `2720607` → **`bfd75ca`** ((zzz) CLAUDE.md fold — **the v2.44 lineage-line omission corrected of record**) → **`de1dda9` (D-2 — Bundle List + find map + AMENDs 1–4; 37 files +3992/−87, ONE commit)** → **`35e23c3` (DEV-1 — multi-model expansion, blank handling, sheet-order import; 12 files +439/−51; RPC migration `dev1_import_apply_equipment_sort_order`)** → **`5ddf30b` (CL UNIT — the Cover Letter landed; 21 files +2297/−122, ONE commit incl. AMEND-4/CL-R7-AMEND + the deps, the two frame-01 baselines, and the print evidence set + the Zite excerpt)** → `5230b4b` (docs(cc) — the D-2/DEV-1/CL CLAUDE.md fold) → `e5a7ecd` (docs(evidence) — the REC-1/REC-2 five files, +2054) → **`40aa945` (EP-P1 — engine + `printProjection.ts` + the client fetch-wire + the two adopted frame-07 baselines; 6 files +420/−8)** → **`7fad0f8` (DEV-2 PHASE 1; 7 files +111/−49)** → **`0c0792c` (FOUND-SET PIN + strip count; 5 files +377/−11)** → **`97b0377` (N-1 tests; 1 file +29)** → **`81f6541` (docs(cc) — the AIR2 + fixture-event CLAUDE.md fold; 1 file +81/−12, executed at the Aug-4 open)**. **Aug 4–7 lineage (the GS-A window, all Daniel-committed):** `81f6541` → `f949d8e` (Leg 1 exit-grammar) → `04752c6` (Leg 1b+G7 riders) → `fe49514` (Leg 1c tail-Model '') → `5b4afe3` (Leg 2 Lines pin) → `322ddb9` (Leg 2b tail-Model autocomplete, (gggg)) → `3ffd371` (docs d2b_01–03 evidence) → `2279937` (Leg 3 N-R2 chords) → `1c1d6db` (Leg 0 sort-mode persistence) → `7e8014b` (Leg 4 (eeee) model band) → `4ebb9ba` (Leg 4b category band) → `cd043f4` (Leg 5 core planMultColorForward) → `618ffc8` (Leg 5 V-1 four-way gate) → `9121338` (docs 694 transcription) → **`b09178e` (Leg 6 E-CA — Change All live on equipment/library/cable/lines/tails; 19 files +1034/−67)**. **Migrations applied (Arch, standing sequence):** `dev2_r2_project_sort_and_print_options_persistence` (equipment_sort_mode + print_options) → `l0_r1_drop_redundant_print_options_column` (the print half was ALREADY satisfied by `print_list_settings` — L0-R1; the redundant column dropped, empty-proven). **Fixture state of record:** sort-mode `'category'` declared pristine (self-maintained by the gate run, L0-R2); the Aug-7 probe corruption (one record + one library row) RESTORED by a declared one-time Arch execute_sql write exception, SELECT-verified before and after. Vercel Ready/Production verified through **`9121338`**; `b09178e` Building at this close's capture — **Ready enters the record at the next pre-open**. **Working tree: CLEAN. No CC session open.** The standing `next-env.d.ts` tidy stands (regenerates on every local build; `git checkout -- next-env.d.ts`; never commit).
- **Gates:** suite **1184 tests green (83 files) at HEAD** (…1159/80 `97b0377` → 1164/81 Leg 1 → 1166 Leg 3 → 1170 Leg 5 → 1178/82 Leg 6 → 1184/83 AMEND-3) · visual baselines — **16 frames**, clock-pinned; **THREE declared baseline events since `5cf9c61`, every one scope-held by Daniel ruling:** (3) the **E3 button event** (committed `b09178e` — the enabled Change All button restyling frames 02/03/04/08 BOTH themes, 8 files; the gate run PROVED the set, 01/05/06/07 byte-identical, zero restores owed; re-gate 16/16) + the prior two:  (1) the **frames-01 CL event** (committed `5ddf30b` — the enabled Cover Letter menu face, Daniel's Aug-3 declaration, scope-proven) and (2) the **frame-07 fixture-cable event** (committed `40aa945` — **GATE-CABLE / type "6-Pair" / 25 ft / unbundled added to the Visual Gate Fixture** via the production UI path, dry-run on a throwaway first, census-bracketed +1 cable +1 type zero tails/lines; frame 07 now photographs the EP-P1 projection "6-Pair, 25'" w/ the green revision-diff row, deterministic 3.8269% delta at adoption). **The 08-restore ruling holds:** the `--update-baselines` all-16 rewrite also byte-refreshed the 08 pair (persistent, sub-threshold, visually 0.0000% — B1-era PNGs vs current Turbopack antialiasing); Daniel RESTORED the pair rather than adopt an undeclared write — undeclared byte-refreshes are restored, never committed. 16/16 @ 0.0000% on the adopted set; the flake registry lives on the settle-wait punch row (incl. the Aug-3–4 additions) · frozen reference **`be0769de`**, 3/3 at every checkpoint via `scripts/frozen-gate.mjs`. The two hashes serve different gates and advance independently. Harness hardening of record: fixture locator waits for the named link and FAILS HARD (`efb176c`).

### Aug 7–9 — S3-G (Sprint 3 close) + the Aug-9 housekeeping fold

**S3-G COMPLETE:** `55d27e9` (GS-A CLAUDE.md fold) → `769b84e` (Leg 0 types regen) → `ff6c25d` (Leg 1 census + parity evidence) → `5616b89` (Leg 2 scope plumbing) → **`1055e76`** (Legs 3+4 + Amends 1a/1b/2/3; 44 source files + `docs/reference/evidence/print/parity/` — all nine cable/tail print surfaces + the 🖨/Quick Print system + the A5 mult-delete tail cascade + equipment print point-true fonts). Counts at close: suite 1250/89 · frozen 3/3 vs `be0769de` · migrations unchanged (`l0_r1_…` last) · fixture pristine all window · three baseline events total (none new). Vercel Ready verified for `5616b89` + `1055e76` (Aug-9 screenshots). → **`cc96e82`** (docs(claude): the S3-G close fold — run-fence reset, Print doctrines standing, the S3-G architecture record; the Aug-9 housekeeping session's first commit).


### Aug 9–10 — the housekeeping close (appended)

`cc96e82` → **`4a1ee1b`** (docs(cc): the 20 arch-record relocations + the Guarded Architectures Index + the run-fence heading fix; 21 files, byte-accounted to 69,257; CLAUDE.md 139,166→69,909 bytes) → **`d5637e2`** (docs(wf1a): **the WF-1a pilot's maiden commit, landed on Daniel's typed phrase** — hook + hooksPath + paired lift + the CLAUDE.md folds (1a/1b/1c/1d + Multi-Leg + Evidence classes + amendments a–e) + the four-bullet restoration + the fence re-arm; push Daniel's paste, `4a1ee1b..d5637e2`). Interim test commit `330d8d9` created and unwound byte-exact within the STEP-5 proof (never pushed). Vercel Ready verified for `cc96e82`/`4a1ee1b`; `d5637e2` Building at close.

### Aug 10 — S4-1 decks-clear (appended at close)

`d5637e2` → **`281958f`** (chore(types): P9-DROP regen — `v_cable_list.model` dropped, migration 40, one-field diff `0/1`, WF-1a phrase-gated, push Daniel's paste; Vercel Ready 53s) → **`1426e03`** (feat(tooling): VERCEL-CLI pre-open — `scripts/vercel-preopen.mjs` + the CLAUDE.md standing pre-open line; 2 files, 116 insertions; WF-1a phrase-gated, push Daniel's paste; Vercel Building at close, verified by the script itself at next pre-open). Database: migration ledger 39→**40** (`p9_step3_drop_phantom_model_from_v_cable_list` last). No-commit units of the same window: GATE-VIS (verify-only by design) · DWV (zero dead sites — the record is the deliverable).

---

## ✅ Completed Units (retained for citation)

*(The v2.46 Complete bullet + both Registered-Units citation sets, carried VERBATIM at migration; Sprint histories and the frozen punch register follow. Completed docket rows append here at each close.)*

### The v2.46 "Complete" line (verbatim)

- **Complete:** Print positions 1–5 · R-3 Visual Parity (July 12) · Equipment-List Adoption (July 13) · unit (aa) Live Find (July 14) · **v1 Import Engine** (July 14–15, live-proven: run `950e9b00…`) · **v2→v1 Export** (July 15 PM: file-level half-loop ROUND-TRIP CLEAN; Pass D v1-leg deferred — see Build Order) · **Zite-parity Workbook Import** (July 15 PM–16 AM) · (ll) Delete Project (live-fired) · Methods merge-on-rename (`merge_methods_rpc`) · (xx)+(xx-b) theme flash · July-16 production sprint (**Matchbook Festival**) · Print-defect emergency unit (July 17) · **Slot 2 keyboard/data-entry batch** (July 17–18) · **Slot 3 Hoisting Unit** (July 18) · **Sort Unit** (July 18–19: S-1..S-12) · **FLT batch** (July 19: D-1..D-8, commit `1d1cf1a`) · **Split unit** (July 20: SP-1..SP-9 + SP-1a/SP-7a, commit `98cbf67`) · **Library Add-to-List** (July 20: AL-0..AL-11, commits `4ddde18` + `7cdc698`) · **Category-fill correction + category-aware band key** (July 20 PM: CAT-1..CAT-4, commit `859e44e`) · **#1/#2 focus-follow pass** (July 21 AM: FF-1..FF-5, punch (rr) closed, commit `c23f92d`) · **(kk) BRIDGE** (July 21: four gated legs `3914090`/`5c8a16d`/`d5036e8`/`cc3b299`, KK-0..KK-8 + SS + IMG rulings, migration `storage_project_images_bucket_and_policies`) · **SPRINT 3 B1** (July 22–23: `5cf9c61`) · **SPRINT 3 B2 LINE** (July 23–24, six commits `a63e03b`→`8329881` — see v2.33 narrative retirement per RM-5; architecture standing in CLAUDE.md) · **(ooo)-2 LIBRARY SORT** (July 24: `6a7e297` — O2-1..4; the raw-stored-text-vs-displayed-category defect root-caused and fixed; `librarySort.ts` is THE library comparator) · **CTF — CABLE-TYPE CREATION FLOW** (July 24: `c1d7ae6` — CT-1..4 + CTF-G1 branch queue + R1 migration + R2 back-patch + R3/F3 prefills + gate-round findings F4–F10; the set-existing-only silent-clear interim RETIRED; first unit run on the WF-2 Opus tier; first unit closed under WF-1 human-only commits) · **B3 — BUNDLE LIST** (July 25: `03b9753` — B3-R1/R1a/R2 + Amendments A1–A7: new-name-only "+" creation with length adoption + silent create; the no-length create DEFERS INSERT TO SAVE, Cancel leaves nothing (ruled v2 deviation from v1 157); dual-mode Update Bundle dialog incl. the bundled-Length-click update trigger; Enter/Tab "+"-activation; dark control chrome fix (pre-existing, B2); select-on-entry both modes via remount-per-open; topmost-autocomplete portal doctrine (opt-in on the shared AutocompleteInput, equipment byte-identical, 16/16 fence-proven); b3_01 committed) · **B4 — BANDS + BANNERS + CHROME** (July 25 PM: `a066155` — B4-Q1/Q2/Q3 + mid-run OQ rulings + A1–A4 + FIX1–4 + FIX2-1..4 + FIX3-1..3 across four gated amend passes: FULL-NEST bundle/box/group bands over the verbatim chains; "Not in Bundles"/"Not in Box" collector banners (box banner = ruled v2 deviation); inline end-label + note band fields; Ready (571: pulled+labeled+Remaining=0, one-way) + Packed checkboxes; band-name + bundled-Length → ONE Update Bundle opener; v1-anatomy toggle Sort control (a CC dropdown invention struck); fixed-viewport pinned chrome (defaults/filters/column header; inspector → flex child, spacer retired); sticky shell tab bar (fence-proven one-liner); sort-band hierarchy; **FIX3-1 defect of record: the chain Model term read the phantom `model` column — corrected to `cable_type_name` via `cableModelDisplay`; latent since B2, the phantom column's second bite**; b4_01/b4_02 committed) · **B6 — ROW-ACTION GESTURES** (July 26: `4dd575d` — B6-R1..R9 (design ruled in the parallel B6 Arch session; text of record in that session's entry + the CC unit order) + OQ-B6-1/2/3 field-set rulings on Daniel's live v1 Clone Links capture (b6_01): Duplicate (header button + ⌥⇧D converged; B6-R9 pulled/labeled/tested reset; bundle rides) · Clone (⌥⇧G via SP-8 dispatch; the b6_01 vocabulary pinned in `linkFields.ts` — 19 ENROLL + 4 QUEUED tokens, exact v2 column names; join-or-create-both enrollment, SP-6 compensation; DB census proved the set field-by-field) · Ext (inspector ⊕ + ⌥⇧X; ExtensionDialog with the ruled keyboard contract after the FIX-1 root cause — value-coupled focus effect; name = prefix+parent+suffix; is_extension; bundle NULL; flags reset; zero mult-line copies; flagged-end invert gated on `projects.bundle_invert_extensions`; **remaining seeds PURELY from the Set-Remaining default per v1 301, never the parent — FIX-2**) · Invert Extensions checkbox on the Cable Defaults panel · the execute_sql ask-gate retired from `.claude/settings.json` (the CLAUDE.md SELECT-only rule is the standing guard); b6_01 committed) · **B7 — LINKAGE UI** (July 26 PM: `18d09ce` — B7-R1..R6 + D1-R + AMEND-1/2: the R5 edit-time "Update Clone <Field>s?" gate on EVERY linked-field commit path (v1-verbatim per-field copy, script-verified all-fields; ext caveat for Name/Length/Bundle) · R1 atomic per-field fan-out via propagateFieldToIds · R2 LINK badges on row cells + inspector fields from ONE live-id-filtered membership index (Length badge LEFT, overlap-proven) · R3 badge popover + inspector-rail Edit Links dialog, per-field break only, ONE shared executeBreak · R4 ≤1-member dissolution · **R6 bulk link-awareness — a ruled v2 INVENTION**: per-(group,field) closure check, straddle dialog with out-of-scope preview, Cancel aborts the entire bulk op · D1-R gate-before-create (Cancel leaves nothing, the B3 precedent) · CABLE_LINK_FIELD read-key sweep (zero free-typed literals) · namespaced dialog remount keys · the idle-editor resync fix; two Step-0 hard stops honored (missing b7_01; the undesigned bulk path); b7_01 committed) · **S3-D — TAILS** (July 27: `dfd95c9` — S3D-R1..R4 + D2/D3/D4/D6 + A1–A10 across three gated amend passes, one commit: the Tails sibling surface at `/cable/tails` (mult-grouped bands + the "Not in a Mult" collector (S3D-R4) + per-band headers, the sticky top header removed (A5)); tail rows w/ the UID re-parent autocomplete (S3D-R1 — Daniel-extended to BOTH children: tails AND mult lines follow by UUID FK; the v1 update-members name-propagation chain NEVER built) · the FIVE LABEL CHECKBOXES (S3D-R2, Daniel's ruling reversing the numeric lean — sets-of-line-labels semantic; **migration `s3d_tail_label_counts_to_checkboxes`**, int→boolean w/ rename across tail_details/cable_types/global_cable_types/projects, recon-proven lossless) · full Source/Destination radios (A2) + the "Labels" cluster (A3) + the A6 pulled⇄labeled implication BOTH directions; the Tail Defaults panel (D4 — no Manage control, that's S3-F; the R-14 radio = creation-time group auto-fill per D2); + Source/Destination Tails creation via pure `tailSeed` (whole-record type-vs-panel fallback; **the tail-method-default rider KEPT** — F5's hint honored); **auto-add tails ACTIVE per v1 script 354 all-or-nothing** (CC correctly bounced Arch's per-end fallback on unambiguous evidence; AMEND-1 guard defect root-caused: the type-DERIVED `v_cable_list.is_mult` skipped bare cables of mult-capable types — the guard now reads the cable's OWN tails + mult lines); the CTF dialog's five tail numerics → checkboxes (CP-A, production-restoring); ⌥⇧T global nav chord (A8); `landSeam` focus continuity + scroll-to-record (A10, non-vacuously proven — the cable→tails direction remained punch (uuu), CLOSED July 28); A4v2/v3 v1-true fixed-column band header w/ the tail-scoped label tier; A7 dark trio; A9 colorless-vs-black chip distinction in dark) · **S3-E — MULT LINES** (July 27 PM: `670849d` — S3E-R1..R11 + AMEND-1, one commit: the Lines surface at `/cable/lines` (bands for EVERY effective-mult cable per R3 incl. the zero-line note — a ruled divergence from the ≥1-row convention; anatomy per frames 032–035; sticky band headers on the model-band idiom; per-band headers only per the A5 sibling doctrine); R1 derived-only is_mult + the ONE licensed `+ New Mult` stored write (then Cable-List navigation per v1 182); R2 no mult checkbox; R4 auto-add lines LIVE (executor 26 + trigger 354, count inputs 330/331; own-records guard; tails-then-lines at both trigger sites); R5 delete-time child re-homing (lines AND tails to the oldest same-named survivor outside the delete set — v1 name-key semantics restored under UID; the `cable_id` CASCADE is the guarded hazard); R6 NO `entity_type='mult_line'` link groups (the clone-lines lockstep premise DISSOLVED — interchangeable trunks share ONE line set; Link Mult/Line = remote-coupling FKs, CC-confirmed on b6_01); R9 the delete-path membership purge (the B7 punch CLOSED); R10 Add Pairs dialog (s3e_01; clamp 19/19→20/20 smoke-proven); R11 single-stage Auto-Fill (Suffix field four; the FileMaker 3-field limit retired) + letter/letter-range fills through the unified `planAutoFill` + the Color `cable_color`-domain fix; Enter/⌥⇧↑↓ same-field-selected idioms; ⌥⇧N/E/| route-local + the ⌥⇧M v2-wide nav chord; **R7 line-patch parse DEFERRED by ruling to its own pass**; R8 fences held (no Split? field, 📎 inert until S3-F); frames 031–035 committed `861b4f9`, s3e_01 committed in-unit) · **(uuu) — CROSS-VIEW FOCUS CONTINUITY** (July 28: `8505484` + `1786211` — the instrumentation-first pass ruled at the S3-E close, run start-to-finish in ONE session: Phase 1 = the read-only `[uuu]` tracer + verbatim extraction of v1 nav scripts 41/34/25 with the landing-semantics table (headline fact: ALL THREE v1 scripts SELECT the matched record via Go to Record inside a find-loop, then scroll — no flash primitive exists in v1); Daniel's field captures on JOY ADJUDICATED same-session: **H2 confirmed, H1 not observed** — a spec gap, not a mechanism gap; Phase 2 = UUU-R1..R5 + R1a + R3a built: the shared `seamLanding` primitive (select-first-match · widen-once · plain selectNone terminal · consume-last), landing FOCUS via each surface's licensed rowFocusRequest seam, the band flash RETIRED, Lines a real consumer, all eight triangle doors claimed, + New Mult stashes-before-push; field-verified on minotaur.app production, both phases, zero gate-defect rounds) · **R7 + AMEND — LINE→LINE PATCH PASS** (July 28–29: `3acbee8`, 13 files +1175/−341 — R7-1..6 + OQ-R7-1..3 + R7-A1..A5 + flags-2/3(a); design gate first, fourteen v1 scripts extracted (611/612/668/677 + 686/687 + 610/615 + 616/652/656–661 Daniel-uploaded; 688 CC-in-corpus): `patchResolve.ts` = the ONE resolver/plan module, single commit funnel incl. the ≡ Auto-Fill path (the gate-caught miss); Make Patch? dialog v1-verbatim per 668 w/ the OPPOSITE-END default (R7-A1 — the uniform default struck as the OQ-R7-3 symmetry error) + origin-identifying copy (R7-A2); bidirectional atomic FK-pair writes (`_end` = the PARTNER's end; reciprocity invariant) w/ full back-write (device_text/•connection/description/line_color per 668); fresh/re-patch/steal + silent unpatch — victims' patched-end TEXT blanked per 686/687, description/color untouched at unpatch, the INITIATOR never swept (ruled mechanism divergence, outcome parity); delete-path partner `_end`+text sweep on BOTH delete paths (ON DELETE SET NULL only nulls the `_id`); Change All per-occurrence routing (B4 — no bulk-patch policy invented); **TRANSITIVE linked description propagation** per 616's engine (BFS over patch neighbours, visited + convergence terminators, 2-/3-cycle tests) + **COLOR propagation as a DELIBERATE v1 DIVERGENCE** (R7-A5 — 615's entry gate has no hasPatch term; Daniel: "may have been a v1 oversight"), gated by the Update Patches dialog per 688 verbatim minus the dissolved-clones clause (S3E-R6 mechanical omission; Cancel skips propagation only — the initiator's edit STANDS, a ruled divergence from v1's Cancel Halt); the device autocomplete corpus += effective-mult names (R7-A3b, Daniel v1 attestation); linkFields: the 2 patch tokens **enroll:false PERMANENT** (R7-6 — never enrolled, never copied on Duplicate/Clone/Ext, never mint groups), the 2 split tokens stay QUEUED; RIDER i: the uuu_01 mult-line color render; RIDER ii: the [uuu] tracer RETIRED) · **S3-F — LINK CABLE COLUMN + CABLE↔LINE PATCH FAMILY** (July 29–31: `327c100`, 18 files +2512/−87 — SR-1 absorption + S3F-R1..R9 + amendments; see the S3-F rulings block below for the full register. Design gate first: seven v1 scripts extracted (609/665 + 421–424 + 681/683; 668/678 re-supplied matching the R7 record); **the structural finding of record: v1 has NO cable-side patch fields — cable↔line coupling was clone-token enrollment (609); v2's FK-pair mechanism = a ruled divergence, outcome parity (S3F-R1)**; typed-parse patch on every cable end commit path (681/683, opposite-end defaults now evidence-cited cable-side); the S3F-R6 five-coupling map on the unified cross-entity propagation engine (cables BRIDGE line descriptions in ONE BFS, mixed-cycle tested); S3F-R7 dynamic colour (line-own-colour else mult) + the R7-AMEND-1 gated REVERSE colour flow w/ `patch_color_break` persistence; couplings 4/5 wired both directions resolution-wins (R6-AMEND-2); coupling 2/3 REVERSE legs STRUCK (R6-AMEND-1 — near-field edits mean CP-A re-resolution; rename/renumber live on the referent's own field); S3F-R9 end-exclusivity sweep (a line end gaining a line↔line patch silently unpatches cables feeding that end); 609 branch-create on 📎 (no dialog; the v1 mult-name-collision colour dialog DISSOLVED under ID linkage) + ➡ seamLanding navigate; the Branch Cable Type default (`projects.branch_cable_type`); the CTF edit-wire + the INLINE-EDITABLE full-field Cable Types admin grid (D3-AMEND-2 — the create dialog remains create-only); FIX-1 Auto-Fill Make-Patch dialog-stacking root-caused browser-verified; FIX-2 cable end-field Enter-advance wired (the equipment K-6 idiom; the advance was never wired, not swallowed); FIX-3 mult-coupled reverse-colour plan retargeted to the mult; two migrations Arch-applied (`s3f_branch_cable_type_default`; `s3f_patch_color_break` + the `v_cable_list` recreate w/ `security_invoker` explicitly re-specified); s3f_01 committed in-unit; **punch (www) ships known-open** — CLOSED at the Find close, see the punch row) · **FIND ENGINE** (July 31: `b572fd4` + fold `19104e7` — F-1..F-8 + AM-1..AM-5 + OQ-F1: ONE surface-agnostic engine `findEngine.ts` w/ the F-2 FieldMap contract + **`applyFindWithFields`, the F-7 pure headless apply = the S3-G print Which-X scope substrate (R-23)**; `useFindController` extraction; FindPanel moved to `components/find` byte-identical **+ the AM-1 Zite open-focus effect (never ported in (aa) — focus was dead on ALL FOUR surfaces pre-fix)**; consumers equipment/library/methods refactored indistinguishable + cable (find-before-bands; exemption one-set idiom; narrowingActive includes findActive) + tails + lines (zero-hit bands drop only under an active find; NO delete affordance per F-5); AM-3 band-header find seeding via the find-only `data-find-field` attribute; AM-5 FK hypothesis refuted; CP-4 bundle-list rollout BOUNCED by CC — correct, no surface exists (Parity vindication; Arch scoping error owned; superseded by unit D-2); suite 973→980; D-3 fence patch `d6a44a4` (in-place matcher catches bundled flags, `perl -pi` et al.); D-4 tier of record = Opus 4.8) · **(zzz) — MIXED-PROJECT NEW + GENERIC LIST SCOPE** (Aug 1: `2720607` — Z-1..Z-6 + ZA-1..ZA-5 + Z-1-AMEND/ZA-1-AMEND: the blank-band `records[0]?.id` vs `editingModelGroupKey` mismatch fixed via the **pending-aware fallback at BOTH derivation sites through the ONE shared `modelBandKey` helper** (the never-diverge guarantee held by construction; the "never key from model text alone" guardrail stands; the shared-"—"-band RENAME editor unreachable from the pending path; committed-transition clean — gate-proven transient highlight); pending commits target+cascade the pending record ONLY; **equipment list EDITOR scoped to `type='generic'` at `fetchEquipmentList`, the sole cache read** — print, `fetchPrintMethodNames`, and ALL snapshot paths keep FULL type visibility per **EP-R6's printed-list clause** (the printed equipment list is the shop order; Daniel's gate caught the uniform-filter error — see Corrections); consumer-set verification of record: the cache entry's only row-data readers = the list page + the KK-5 prefetch (KK-4(a) is a writer; cable/tail/line surfaces invalidate only; import "equipmentListRows" are site-d locals); **three Arch view migrations** `zzz_expose_type_on_v_equipment_list` (`type` appended LAST, ordinal 25) + `zzz_generic_scope_v_equipment_list` + `zzz_amend_unscope_v_equipment_list_print_truth` (net: type exposed, NO view WHERE) each proof-run/rollback-proven w/ authenticated probes; types regen +1 line; suite 980→984; OQ#2a shared-band pendency visuals ACCEPTED AS-BUILT (ZA-3; OQ#2b own-band shape = the registered future option); one correct CC hard stop (the Z-2 single-read-path premise false — six-site inventory delivered); ZA-2 EP unwind rider registered) · **D-2 — BUNDLE LIST + FIND MAP** (Aug 1–2: `de1dda9` — the full browse surface at `/cable/bundles`; D2-R1-AMEND..R10 + gate-ruled divergences; migration `d2_bundle_list_view_and_name_unique`; the answers-first two-phase ride; the A6 latent NULL-clear defect fixed; KeyB/KeyA chord registry; full register in Sync Sheet #2 + the Session Log) · **DEV-1 — EQUIPMENT IMPORT DIALOG** (Aug 2: `35e23c3` — off-roadmap deviation, rulings DEV1-R1..R10: the Additional Model mapping target w/ per-model row expansion (notes first-emitted-row-only per R6); Ignore Blanks; description no longer hard-required (blank → `''`); value hygiene incl. numeric stringification; **DEV1-R9 sheet-order import** via the NEW `import_sort_order` plan key + the Arch-applied RPC migration `dev1_import_apply_equipment_sort_order` (v1-importer plans never carry the key — provably inert; today's scrambled-order defect died with it); TWO correct CC Step-0 hard stops honored (the shared frozen engine finding; the no-sort-order-scheme finding) — see Corrections; carried the real Vape! show import same-day) · **CL — COVER LETTER** (Aug 2–4: **`5ddf30b`** — CL-R1..R12 + R10-ENC + **CL-R7-AMEND** (Daniel gate, evidence-ruled off the live Vape! v1 print: the print signature renders IN THE CONTENT FLOW after the last printed section (Notes), closing gap above, block left edge ~2/3 content width via the named const `SIGNATURE_LEFT_FRACTION`, text left-aligned in-block — never page-bottom-pinned, never right-flush; SUPERSEDES CL-R7 bottom-right; accepted rider: near-full Notes pages can push the in-flow block toward page bottom): the Concept-B structured letter — editor at `/project/[projectId]/cover-letter` w/ the Zite anatomy + the v1 five-line Signature block (migration `cl_cover_letters_signature_data`) + the tiptap rich intro toolbar (the approved 3-package set) + insert-below-focused rows + the v1 columnar Who print (R10/R10-ENC) + blank-page for empty AND missing letters (R11/R12) + the enabled gold menu entry + the frames-01 scoped baseline event; 21 files +2297/−122, ONE commit; **the CL-0 evidence errand DISSOLVED by ruling Aug 4** — the v1 signature "artifact" was a mockup screenshot, not a real print; CL-R7-AMEND stands on pedigree class (a), no artifact committed) · **REC-1/REC-2 — AIRBORNE RECON PASS** (Aug 3: **`e5a7ecd`**, read-only, five evidence files + one addendum at `docs/reference/evidence/` — `print/ep1_print_projection_recon.md` (650 lines incl. the REC-2 addendum) · `foundset/foundset_semantics_recon.md` · `audit/field_exit_matrix.md` · `cable/w2_return_grammar.md` · `cable/d2b_clone_bundle_recon.md`; headline findings in the Aug 3–4 rulings block) · **EP-P1 — PRINT CABLE-MODEL PROJECTION** (Aug 3–4: **`40aa945`** — EP1-R1..R6+AMEND fully ruled + built: PRINT-PATH derivation (no schema, no view recreate, no editor coupling; dies cleanly under future EP materialization); composition v1-verbatim `"[Model], [Length][unit]"` w/ the field-21 gate (length>0) + fallbacks, **CABLES-ONLY** (tails have no lengths), bundled cables inherit bundle length, unit suffix reads **`projects.units`** (its FIRST consumer — `'` imperial / `m` metric, imperial default, **NO CONVERSION EVER**); aggregation on the evidence spine (Method → Category → numeric length pre-sort → collapse at Model(composed), quantities summed — individual cable rows are quantity 1, ten identical cables → one line qty 10 — notes via the engine's `notes[]` merge; Description/Group trailing sort terms, not breaks); tails unchanged (EP1-R4), bundles never project (EP1-R5), the (zzz) editor scope untouched (EP1-R6); + the frame-07 fixture-cable baseline event; **the Aug-25 "cables project on the printed equipment list" criterion is MET**) · **DEV-2 PHASE 1** (Aug 3–4: **`7fad0f8`** — the R11-AMEND "Ignore rows with no model" checkbox **default-checked FINAL, veto window closed**; GCM method-name Enter-advance (**arrow adjudicated no-surface** — plain input); **(cccc) CLOSED** (effect-dep root cause); Private-Notes first-emitted-row; **Phase 2 = DEV2-R2 DB-backed persistence remains open**, opens w/ the Arch migration) · **FOUND-SET PIN + ⌥⇧J** (Aug 3–4: **`0c0792c`** — FS-R1..R4 + **FS-R2-EXT** + DEV2-R1: find OR any filter change derives and PINS the visible id-set, member edits never eject (v1 snapshot semantics), re-derives only on find/filter CHANGE, new records join the pin; Change All / Delete Found / counts **and the find strip's count** scope to the pin (v1 `Get(FoundCount)` truth — closes #14's design half; the live-set race dies); Delete All ⇄ Delete Found label+scope move together under ANY narrowing (#13); ⌥⇧J dissolves the pin AND clears filters (full v1 ⌘J); plumbing of record: composable sibling hook `foundSetPin.ts` (reducePin/pinnedRows/foundCount/narrowKeyOf + useFoundSetPin), **`useFindController` BYTE-UNTOUCHED** (FS-R4 shape by CC judgment); consumers equipment/cable/tail; **LINES DEFERRED BY RULING (Daniel, Aug 4) → GS-A Leg 2**) · **N-1 (HALF)** (Aug 4: **`97b0377`** — N-R1 verified + tested; digit-chord collision + reserved-namespace (Digit1-only) pinned test-side; **N-R2 build OPEN → GS-A Leg 3**, the CL-dirty MenuPageLoader collision now unblocked; ambiguity resolved of record: navigate-to-surface, context-dependent per the N-R1/N-R2 ruling structure) · **GS-A — GRAMMAR + SMALL UNITS (Aug 4–7: COMPLETE, `f949d8e`→`b09178e`, 14 commits — the largest unit shipped).** Legs: **1** exit-grammar product-wide ([?-U1]; matrix U2–U5; (aaaa) closed; delete-successor continuity on ALL five surfaces incl. the ratified bundle extension; a read-only recon fork overstepped and BUILT — disclosed, hunk-reviewed, fully re-gated, salvaged under G1, and the standing fork rule adopted) · **1b+G7** riders (G3 Auto-Fill focus · G4 tail ⌥⇧↑↓ field-preserve · G5a inspector record-step · G5b Escape ruled "stay" · G7 CTF post-close lands the active record's Model) · **1c** tail-Model ''-clear (closed G6 + the W-2 rider entirely) · **2** Lines found-set pin (the Aug-4 deferral discharged — the pin now lives on ALL FOUR find surfaces) · **2b** tail-Model free-text autocomplete ((gggg), same-session registered-and-closed) · **3** N-R2 menu digit chords (route-local capture claim; useNavShortcuts byte-untouched) · **0** DEV2-R2 sort-mode persistence (the print half CLOSED already-satisfied by `print_list_settings` per L0-R1; the redundant column dropped) · **4/4b** band-click activation, model + category ((eeee) closed both bands, K-2) · **5** V-1 COMPLETE — the forward mult-colour gate, FOUR-WAY per V1-R2-AMEND (Cancel=full revert / Break=permanent patch_color_break / Review / Update; V1-Q1 permanent; V1-Q2 revert-scenario of record; V1-R4 Change All defers-then-gates, ONE dialog, the D-2 full-unwind precedent; 688 chassis byte-untouched) · **6** E-CA — **Change All LIVE on ALL FIVE list surfaces** (693/694; the 25b2993 placeholders unguarded by sanctioned handoff; ECA-F1 quantity in · F2 checkboxes in v1-verbatim · F3 category plain · E2 Model never touches notes, Daniel v1 attestation, 551 unextracted · library becomes a pin consumer · tails incl. the Mult re-parent · **ECA-F4 through FOUR amends to the mechanism that stuck: CAPTURE-AND-THREAD** — the DOM draft is v1's ActiveFieldContents, resolved per field doctrine, the editor closes WITHOUT its single-edit commit, an override row threads to the dialog; supersedes the Amend-1 rAF race and the Amend-2 flushSync race, whose failure was PROVEN (React-Query notify = setTimeout(0) macrotask, cited in-code) · **F4-No v1-literal**: No/Escape commits the draft to the ACTIVE RECORD ONLY via the same silent per-record path; EDGE-1/2/3 + punches (hhhh)/(iiii) all DISSOLVED by the mechanism · the E3 declared baseline event, 8 frames gate-proven · the Amend-4 fixture-probe corruption: contained, Arch-restored under a declared exception, the probe rule registered). **P-1 RESOLVED en route (Aug 4): does-not-reproduce-as-regression — equipment Change All was NEVER BUILT (the 25b2993 guarded placeholder; the Roadmap's own Sprint-Plan line said so — Arch premise error, owned); #14 VERIFIED (cable's target set = the FS-R2 pin, test-cited); the latency unit NOT implicated. P-1 converted to E-CA by rulings 1–3 and shipped above.**

### S3-G — cable + tail print surfaces (COMPLETE Aug 7–9, `1055e76`; exits the docket at this migration)

All nine surfaces live end-to-end on the R-22 one-page chain (options + Which-X find + preview + Print/Save-PDF): Bundle Sheet · Cable Checklist · Tail Checklist · Cable Labels Small/Big · Tail Line/Block/Panel labels · Balun Labels · Cable Folder Labels — plus the P10 cable-row 🖨/green-→/Clear-Print-List system, P9 steps 1+2, the A5 mult-delete tail cascade (DDR-224), the A6 nav doors, and equipment print point-true fonts (C4). Architecture of record in CLAUDE.md (`cc96e82`); parity corpus at `docs/reference/evidence/print/parity/` (7 PDFs). Verification residue = docket rows GATE-VIS · GATE-PDF · GATE-OL25WR · GATE-C4.

### The Aug 9–10 housekeeping session — COMPLETE (exits the Next-Sessions block at v3.1)

Roadmap v3.0/v3.1 + Ledger v1.0 split installed (85-row docket; RM-6) · CLAUDE.md folded (`cc96e82`) and relocated (`4a1ee1b`) · **WF-1a commit pilot LIVE (`d5637e2`)** · Arch Prompt v28 installed · v2.46 archived byte-exact · the project-file prune verified rides PRUNE-CHK.

### S4-1 decks-clear — COMPLETE (Aug 10; four docket exits at this close)

**GATE-VIS** — the official Sprint-4 visual gate: PASS (ON RERUN) Daniel-accepted, 16/16 @ 0.0000% on a verified-quiet machine; flake surface #4 cited; two environment hard stops honored en route. **DWV** — the dead-write class proven CLOSED: zero surviving instances, both founding suspects capture-proven LIVE; no commit, the verification record (CC handoff 260810-0916) is the deliverable. **P9-DROP** — migration 40 applied (view 61→60 cols, invoker + grants re-specified, authenticated probe passed) + the types-regen rider `281958f`. **VERCEL-CLI** — `scripts/vercel-preopen.mjs` live at `1426e03` (REST design of record, project-scoped token, CLAUDE.md standing pre-open line); Daniel's screenshot step retired.

### Sprint 3 — CLOSED (Aug 7–9, the Aug-7 compression honored: Sprint 3 = GS-A + EP-P1 + S3-G)


**Sprints 0–2 + 2.5:** closed (v2.17 archive). Change All remains the unbuilt guarded unit ON THE EQUIPMENT SURFACE (the cable Change All is BUILT July 23–24; the equipment build inherits both cross-surface constraints when it opens). Nav set = ruled tab sections minus Print Library; (ff) covers off-set pages.
**Pre-BVSC:** Previous Revisions ✅ · revision-compare ✅ · **Cover Letter page — still owed** (extraction-first; `cover_letters` schema exists) · three-tier library architecture (registered).
**Sprint 3 — Cable** (spec-complete, import-gated): S3-A ✅ · schema ✅ · B1 ✅ · B2 line ✅ · CTF ✅ · B3 ✅ · B4 ✅ · B6 ✅ · B7 ✅ · S3-D ✅ · S3-E ✅ · (uuu) ✅ · **R7 ✅ (`3acbee8` — the line→line patch family; the propagation engine; the licensed smoke pattern)** · **S3-F ✅ (`327c100` — the Link Cable column + the ABSORBED cable↔line family; the cross-entity engine; the inline-editable types admin; two Arch-applied migrations; (www) known-open)** · **Find engine ✅ `b572fd4`** · (zzz) ✅ · D-2 ✅ · DEV-1 ✅ · **CL ✅ `5ddf30b`** · **EP-P1 ✅ `40aa945`** · **DEV-2p1 ✅ `7fad0f8`** · **Found-set pin + ⌥⇧J ✅ `0c0792c`** · **N-1 half ✅ `97b0377`** · **P-1 ✅ resolved→E-CA · GS-A ✅ `b09178e` (Legs 0–6 + amends; DEV-2p2 discharged inside it) · REMAINING (the Aug-7 compression): S3-G ONLY (gate closed; build = the next session)** — D-2b · baseline-event session · GS-C · S3-G-2 · Allocate · method-0 · the focus/polish mini-pass ALL → the Sprint-4 basket → Equipment Projection (EP proper, EP-R1..R6) at its own trigger · Allocate call · deprecated-layout recon · method-0 check · **method fields = autocomplete text from day one ✅ (held)** · **Cable views responsive from the start (held; see (kkk) + the F7 residual).**
**Sprint 4 — Boxes** (spec-complete): box list/contents/types/assignments · three PDFs · Allocate call · method-0 check · guarded bulk-delete deadline · deferred export unit (+S-12 rider) · **equipment-list method + box fields → autocomplete text inputs; box field editable; (iii) filter pulldowns → autocomplete; the (mmm) method/box/group band-editability half** · **Box views responsive from the start** · **box-logo print render (with box labels)** · **narrow-window responsive pass (kkk) queued immediately after** · **auto-mode rider EXPIRES at Sprint 4 close** (CC returns to accept-edits/standard before active projects live in v2).
**Post-BVSC:** GCM completion · Maintenance · Spreadsheet Export · RF/Walkies/Circuits · IAS Frequency Import · Bingo disposition · real-time collaboration · licensing build · Trash/Archive · PWA/offline (v3 candidate).

---

### Build Order history (v2.46 🗺 Build Order, carried verbatim — the forward remainder lives in the v3.0 Sprint Plan/docket)


1. **v1 Import Engine — COMPLETE** (July 14–15).
2. **v2→v1 Export / Round-trip — COMPLETE except Pass D v1-leg; entire unit DEFERRED to END OF SPRINT 4** (ruling of record, July 17). Consequence accepted: no proven path back to v1 after the ~Aug-25 cutover; **fallback of record = last pre-cutover `.mino` + hand-reconciliation.** HistoryID backfill SKIPPED. Pass D gates when revived: Import Show ingest leg + second dry-run (EXP-6 L1). **Revival rider (S-12, July 19): re-align `compareEquipmentDisplayOrder` with the ruled sort chains.** **Finding of record (July 17): v1 Import Show = straight ADDITIVE RESTORE** — Method:Add on every table into a clean file; HistoryID imports as plain data; photos don't survive; box-list Truck field needs post-import eyeballing.
3. **Zite-parity Workbook Import — COMPLETE** (July 15–16). The real BVSC port goes through Settings → Import (v1 engine) only.
4. **Sprint 3 Cable / Sprint 4 Boxes** — both spec-complete (`abeab54`); each opens with the method-0 windowing equivalent check; guarded bulk-delete deadline lands end of Sprint 4 (also the export unit's home). **Sprint 3 constraint of record (July 19): cable/tail method fields are built as AUTOCOMPLETE TEXT FIELDS from day one — never pulldowns retrofitted later** *(enforced July 24: the CTF dialog's Tail Method shipped as a select in first build — corrected to autocomplete before commit; correction on record).* **Sprint 4 additions (July 19): method + box fields on the equipment list convert to autocomplete text inputs; box field becomes editable (prerequisite for live-testing the box chain); (iii) filter pulldowns convert to autocomplete inputs in the same pass — one idiom, one baseline event.** **Sprint 4 addition (July 21, from (mmm)): the method/box/group band-editability half rides these registrations.** **Sprint 3 progress (July 22–31): S3-A RULED · schema REBUILT · B1 ✅ `5cf9c61` · B2 LINE ✅ `a63e03b`→`8329881` · CTF ✅ `c1d7ae6` · B3 ✅ `03b9753` · B4 ✅ `a066155` · B6 ✅ `4dd575d` · B7 ✅ `18d09ce` · S3-D ✅ `dfd95c9` · S3-E ✅ `670849d` · (uuu) ✅ `8505484`+`1786211` · **R7 ✅ `3acbee8`** · **S3-F ✅ `327c100`**. Pass sequence of record (AMENDED July 31): B1 ✅ → B2 ✅ → B2-A ✅ → B2-B ✅ (B5 dissolved into B2-B) → CTF ✅ → B3 ✅ → B4 ✅ → B6 ✅ → B7 ✅ → S3-D ✅ → S3-E ✅ → (uuu) ✅ → **R7 ✅ (`3acbee8`; both riders discharged)** → **S3-F ✅ (`327c100`; SR-1 absorbed the cable↔line family; both riders landed — CTF edit-wire + the bulk Update-Patches dialog; the Manage Cable Types admin grew to the inline-editable full-field grid per D3-AMEND-1/2)** → **Find engine ✅ (`b572fd4`; the bundle-list clause superseded by D-2)** → **(zzz) ✅ (`2720607`)** → **D-2 ✅ (`de1dda9`)** → *(off-roadmap deviations, Daniel-directed: DEV-1 ✅ `35e23c3`)* → **CL ✅ (`5ddf30b`)** → **EP-P1 ✅ (`40aa945` — Aug-25 criterion MET)** → **DEV-2 Phase 1 ✅ (`7fad0f8`)** → **Found-set pin + ⌥⇧J ✅ (`0c0792c`)** → **N-1 half ✅ (`97b0377`; N-R2 open)** → **P-1 → GS-A (exit-grammar · Lines pin · N-R2 · (eeee) · opt (bbbb)) → DEV-2p2 → GS-B (D-2b · S3-G print** (+ the length-less-bundle print guard + the phantom-writes retirement check + the `print_list` affordance owed)**) → Equipment Projection (EP proper)**; **two scoped baseline events executed by declaration (frames-01 `5ddf30b`; frame-07 `40aa945`)**; the full declared baseline event still lands ONCE at its sanctioned session (D-2+D-2b frames + the dark cluster + the KeyR unbind — placement = the open question of record).**

**Slot queue (July-16 step-back triage, updated through July 31):** slot 1 revisions-diff ✅ → slot 2 keyboard batch ✅ → slot 3 hoisting ✅ → slot 4 SORT UNIT ✅ → slot 5 FLT batch ✅ → slot 6 vicinity ✅ → #1/#2 focus-follow ✅ → (kk) bridge ✅ → **SPRINT 3: S3-A ✅ · schema ✅ · B1 ✅ · B2 LINE ✅ · (ooo)-2 ✅ · CTF ✅ · B3 ✅ · B4 ✅ · B6 ✅ · B7 ✅ · S3-D ✅ · S3-E ✅ · (uuu) ✅ · R7 ✅ · S3-F ✅ · Find ✅ · (zzz) ✅ · D-2 ✅ · DEV-1 ✅ · CL ✅ · EP-P1 ✅ · DEV-2p1 ✅ · Found-set pin ✅ · N-1 half ✅ → P-1 → GS-A → DEV-2p2 → GS-B (D-2b + S3-G) → Equipment Projection (EP proper)**; V-1 build + the baseline-event session placement = the open question of record; (lll)/(nnn) mini-pass queued behind.

**Aug-25 critical path (Daniel's side, ungated by any build):** **EP-P1 landed `40aa945` — the "cables project on the printed equipment list" criterion is MET.** final Export Show at cutover → port via Settings→Import (v1 engine, **dry-run first**) → theatre print verification. Shop prep runs in v1 from ~Aug 10. **Print-order note of record (July 19):** the first reprint of any live project from v2 will not match a v1-era print order byte-for-byte — the S-4 chains are the ruled, correct order arriving. Not a defect; do not debug it as one in the theatre. **Print-image note (July 21):** the print header renders the TOP RIGHT image only (signed-URL-resolved); top-left render rides the Print header line2 + autosize unit; box-logo render rides Sprint-4 box labels.

---

### Citation-only unit registers (both v2.46 Registered-Units sections' closed rows, verbatim)

- **Split unit** — BUILT July 20 (`98cbf67`). Retained for citation only. **Evidence addendum (July 29, R7):** scripts 652/658–661 extracted and held for its record; **661 carries a v1 wart** — `$newValue` captures Source Connection inside the Destination Connection loop. Adjudicate at that unit's extraction, not before. The split-family live line-name sync (S3E-R8) remains its registered design rider.
- **Library Add-to-List** — BUILT July 20. Retained for citation only.
- **Default-adoption prompts unit** — RESOLVED July 20 PM (dissolved on recon). Retained for citation only.
- **#1/#2 focus-follow pass** — BUILT July 21 AM (`c23f92d`). Retained for citation only.
- **(kk) cross-page preload/instant-nav** — BUILT July 21 (four legs). Retained for citation only.
- **CTF — cable-type creation flow** — BUILT July 24 (`c1d7ae6`). Retained for citation only; **the edit-mode wire + admin surface LANDED at S3-F (D3-AMEND-1/2 — the inline grid superseded row-click-edit; the dialog remains create-only).**
- **B3 Bundle List** — BUILT July 25 (`03b9753`). Retained for citation only.
- **(uuu) cross-view focus pass** — BUILT July 28 (`8505484` + `1786211`). Retained for citation only; **both R7 riders discharged July 29 (colour render; tracer retirement).**
- **R7 line-patch pass** — **BUILT July 29 (`3acbee8`).** Retained for citation only. Riders discharged.
- **S3-F Link Cable column + cable↔line patch family** — **BUILT July 29–31 (`327c100`).** Retained for citation only; SR-1 absorbed the OQ-R7-2 registration; both riders landed; **punch (www) ships known-open** (first bite next session or its own micro-pass).
- **Find engine unit — BUILT July 31 (`b572fd4` + fold `19104e7`). Retained for citation** (stale "NEXT" marker carried by v2.44, corrected here). Original registration (July 27 PM): ONE surface-agnostic find-mode engine extracted from the equipment list's proven behavior (mode state, blank-field toggles, multi-request semantics) parameterized by per-surface field maps; rollout = cable list / tail list / lines / bundle list in one gated pass; box surfaces register field maps at their own Sprint-4 build — no engine redesign.
- **V-1 — COMPLETE (`cd043f4` core + `618ffc8` four-way gate, GS-A Leg 5; V1-R1..R4 + V1-R2-AMEND + Q1/Q2). Retained for citation.** Original registration: the S3-F gap of record — no cable-list trigger exists for a mult's own colour edit (the plan + executor exist; `planReverseCableColor`'s mult branch is the candidate engine). The gate owes: (a) silent vs Update-Patches-gated (the 615 analog / R7-A5 divergence), (b) the Change All bulk / R7-AMEND OQ#2 single-dialog interplay, (c) `patch_color_break` exclusion confirmation. AM-5 diagnosis + existing-data recon (zero orphans, no backfill) in the 260731-1853 handoff file.
- **EP-P1 — Print cable-model projection — BUILT + LIVE `40aa945` (Aug 3–4; EP1-R1..R6+AMEND; the Aug-25 criterion MET). Retained for citation; the mixed-unit-lengths + non-generic-projection follow-ons registered (Aug 3–4 rulings block).** Original registration: the printed equipment list renders a cable band as "(Unknown)" — the raw row's blank model. v1's purge-and-refill derives **"[Model], [Length]'"** per cable at print time (e.g. "6-Pair, 25'"), which also changes print aggregation (model+length groups, not one lump; the Test Cable capture = the defect evidence; tails already correct — real model text on-row). The EP unit's print slice pulled forward; short design gate owed: where the derivation lives (view-derived column vs print-path join vs the S3-G Which-X substrate) + the aggregation key. Interacts with the phantom-`model`-writes retirement check (S3-G row) and ZA-2. Ordering cable off a mixed print is not right until this lands.
- **N-1 — COMPLETE (N-R1 `97b0377` + N-R2 `2279937`, GS-A Leg 3). Retained for citation.** Daniel ruling of record, Aug 1, verbatim: **N-R1** ⌥⇧1 from any non-menu page → project menu, preserving last-active-tab memory (existing behavior, now OF RECORD). **N-R2** at the project menu: ⌥⇧1 → Equipment, 2 → Cable, 3 → Boxes, 4 → RF, 5 → Walkies, 6 → Circuits, 7 → Bingo. N-1 implements the chords for today's surfaces (Equipment, Cable) with the full digit map reserved — Boxes (Sprint 4) and later surfaces inherit their numbers. Digit chords are clear of the B6/S3 letter-chord namespace. Small unit; rides a near docket.
- **W-2 — Return-key recon — DISSOLVED into the exit-grammar unit (GS-A Leg 1) per the Aug-3 grammar ruling; the REC finding of record: NEVER WIRED, not regressed; the TailListClient NULL-clear rider rides along. Retained for citation. Original registration:** lines/tails row-field Enter-advance dead. **Daniel field-confirmed Aug 1 on the tail list ("Return key doesn't move to next record").** Regression-vs-never-wired deliberately unresolved; **parity evidence first** — v1 Mult Lines/Tail List Return behavior (the FileMaker Return-vs-Enter grammar distinction pinned in extraction) — before any build. The K-6 cable-end Enter-advance is the sibling reference. Open scope rider: whether the cable list owes the same advance.
- **D-2 — Bundle-list surface unit** — **BUILT Aug 1–2 (`de1dda9`).** Retained for citation only. **D-2b → the SPRINT-4 BASKET (the Aug-7 compression; evidence banked, incl. d2b_01–03 `3ffd371`; the residual s504 variants still open)** (bundle Clone/Ext linkage; extraction-first on 390/441/455 — in-corpus, banked; the cable-page s390-interaction question; **the "Tails?/Lines? dialogs have no screenshots" premise CORRECTED — d2_04/05/06 cover them**; genuinely missing frames s441/s704/s504 = **Daniel's evidence errand, the only external gate left in Sprint 3**; LINK badges; R-11 auto-Invert; Trigger B linkage schema; + the B6 cable-Duplicate cascade gap — source-confirmed by REC: extend the two-phase pattern, never fork a third copy family).
- **CL unit — LANDED `5ddf30b` (Aug 4; + CL-R7-AMEND). Retained for citation.**
- **P-1 — RESOLVED (Aug 4: does-not-reproduce-as-regression; equipment Change All was UNBUILT — the guarded 25b2993 placeholder; Arch premise error owned; #14 VERIFIED against the FS-R2 pin; latency unit not implicated) → CONVERTED to E-CA and SHIPPED (`b09178e`). Retained for citation.** Original registration: dead from button AND ⌥⇧\ on the production equipment list (live show). Probes licensed: console/network readout, local-vs-prod repro. Required findings: root cause + fix; **#14 = confirm the target set is the FS-R2 pin** (design already ruled at `0c0792c` — verification, not a new gate); whether the freshly imported 100+-row show's data shape is implicated (feeds the latency unit). Production diagnostics never share a session with builds.
- **DEV-2 — COMPLETE.** Phase 1 `7fad0f8`; Phase 2 discharged at GS-A Leg 0 (`1c1d6db`): sort-mode persists to `projects.equipment_sort_mode` (migration applied, standing sequence); the print half CLOSED already-satisfied by `print_list_settings` (L0-R1; the redundant column dropped by corrective migration). Retained for citation.
- **Found-set semantics cluster — FULLY COMPLETE:** built `0c0792c`; the Lines pin landed `5b4afe3` (GS-A Leg 2) — **the pin lives on all four find surfaces + the library (`b09178e`)**; #14 verified at P-1. Retained for citation.
- **Field-exit / exit-grammar unit — COMPLETE (`f949d8e` + the 1b/1c riders `04752c6`/`fe49514`): the product-wide grammar live on every list surface; W-2 + (aaaa) + G6 closed. Retained for citation.**
- **S3-G — CABLE + TAIL PRINT SURFACES (design gate CLOSED Aug 7 per the In-flight register; BUILD = the next Arch session's unit; the drafted multi-leg handoff travels in that session's opener):** Leg 0 types-regen · Leg 1 evidence census (hard-stop on missing tail/folder artifacts) · Leg 2 scope plumbing (Which-X on R-23 · P6 checkboxes · the OQ5 🖨 toggle · Max-Label-Sets retirement · the P9 3-step · the P8-AMEND guard per extraction) · Legs 3/4 documents + the label family.

### The v2.46 Punch List register (frozen copy at migration — letters permanent; OPEN rows exited to the v3.0 Unified Docket, which is the sole live register from here; this copy is citation-only)


**Letter-collision resolution (ruled July 18):** the two July-17 items and five July-18 items are lettered sequentially from the true next-free letter.

**Open:**

| # | Item | Notes |
|---|---|---|
| (q) | Consolidate Zite source into ONE reference folder | Registered July 11; **verify status**. |
| (u) | Snapshot-fetch failure surfaces console-only — wire the existing `errorMsg` banner | |
| (dd) | `frozen-check.sh` fires on seeded-failure proofs — consider a sanctioned proof mode | No action owed. |
| (ee) | `bash-fence.sh`: exempt `/dev/null` redirects | **CLOSED July 23** — probe clean; no fix owed. **July-24 incident rider: the hook lost its execute bit on a governance-file save and ran silently dead — `chmod +x` after any hook placement is a CLAUDE.md standing instruction; verified +x, no hook errors, at the July-24 open.** |
| (ff) | Tab strip renders no selected button on revisions/settings | Confirm acceptable or rule. |
| (pp) | Duplicate latency recon | Field-entry half CLOSED by slot 3; remaining scope = item 13 below. |
| (yy) | Methods admin non-rename fields swallow DB errors | |
| (aaa) | ⌥⇧F find-seeding: seed on the focused field — misses Model specifically | Zite-extraction-gated. |
| (bbb) | Autocomplete inline-completion Enter commit: typed "foo" commits the completion | Zite-extraction-gated. |
| (ccc) | Quantity Escape-revert missing — the unmount net quietly commits dirty values | H-4 finding, July 18. |
| (eee) | Exact-match autocomplete suppression — suppress dropdown on exact single-entry match; lifts on typing | Zite precedent check first. |
| (fff) | REM column → editable quantity text field, no spinners, no checkbox | v1 parity; extraction-gated. |
| (ggg) | Find-strip dark-mode tokens — strip renders light-mode styling in dark theme | Frame 08 bakes the defective appearance into baseline (correct procedure); rides the S3A-31 event. The palette-publication rule of record guards new surfaces; (ggg) itself still rides its event. |
| (hhh) | Editing a category with its band header off-screen: typed text must live-update the sticky header | July 19 (sort-unit live gate). |
| (iii) | Filter pulldowns → autocomplete inputs (type-ahead; v1 parity) | Rides the Sprint-4 autocomplete conversion event. |
| (jjj) | Remaining-cell save-error-flash gap (SP-6 rollback flashes both, only Quantity wired) | Low-priority; contract-fenced surface — deferred pending ruling. |
| (kkk) | Narrow-window responsive — equipment-list header collision + library grid overflow | Queued after Sprint 4, its own pass. Sprint 3/4 constraint: Cable/Box views responsive from the start. **July-24 addition: cable-list intrinsic column width ~1508px — windows narrower than that scroll horizontally (F7 fixed the wide-window balloon: Description fixed-width, surplus collects FAR RIGHT); trimming other columns to close the narrow residual needs its own ruling and joins this pass.** |
| (lll) | ⌥⇧I dead on an UNCOMMITTED new-record model field (works post-commit) | Root traced by CC (banked, 260721-1201). **Rides the (lll)/(nnn) focus mini-pass.** |
| (mmm) | Band-editability asymmetry across sort modes — bands editable only under category sort | Scope split of record (July 21): category-band half = own future pass; method/box/group half rides Sprint 4. |
| (nnn) | Clicking a model band header doesn't set `currentRowId` | **Rides the (lll)/(nnn) focus mini-pass.** |
| (ppp) | Settings page: project menu bar joins the fixed header | Declared baseline event (frame 06); own small pass or rides the next settings-touching unit. |
| (qqq) | CTF New Cable Type dialog auto-closes ~100ms after an ENTER-committed unknown model (blur/Tab commit holds the dialog) | **NEW July 25 (surfaced by B3 A7 testing):** pre-existing at `745e447`, byte-proven by presence poll; likely the synthetic Enter reaching the dialog's window-keydown listener. **July-27 CONFIRMATION (S3-D AMEND-1 M4 isolation): a machine-fast Enter bled into the dialog's window Enter=Save and auto-SAVED with defaults — the same root; one hardening pass closes (qqq)+(sss).** Own small pass. |
| (rrr) | Dark-mode flagged highlight too bright — row wash reads as a lit bar | **NEW July 25 PM (B4 gate).** Ruled direction: hue at low alpha (~12–15%) for the row wash; full-strength color only on small chrome (chip text + a 2–3px left border). Extends to the not-flagged tint + R-10 threshold colors if hot in dark. Exact values judged in-browser; rides any future cable-chrome pass. |
| (sss) | CTF dialog: swallow its OPENING Enter — the window Enter=Save listener can fire on the same Enter that committed the model, auto-saving with defaults | **NEW July 27 (S3-D AMEND-1 M4):** (qqq)'s root mechanism confirmed by isolation proof; machine-speed only — blur-commit and Daniel's manual use never hit it. Rides a future CTF-dialog hardening pass WITH (qqq). |
| (ttt) | Product-wide muted-label contrast sweep — `text-surface-300` too light on light / too dark on dark surfaces | **NEW July 27 (S3-D gate):** root cause on record — the surface scale has NO mid level (400–700 unset). S3A-31 bars a baseline event, so S3-D fixed the tail surface only via the scoped `--tail-label-fg` fork. Rides the next SANCTIONED baseline window — bundle with (rrr). |
| (uuu) | Cable→tails focus continuity fails in Daniel's field use (3×) despite passing harness proofs; tails→cable works | **CLOSED July 28 (`8505484` + `1786211`)** — H2 confirmed, H1 not observed; fixed per UUU-R1..R5 + R1a + R3a (select-and-focus-on-landing across the triangle via `seamLanding`; band flash retired); field-verified on production. Retained for citation; the tracer was RETIRED at R7 (rider discharged). |
| (vvv) | Inverted-extension end DISPLAY may be un-swapped vs v1 | **NEW July 29 (S3-F Step-0.4 observation):** v2 stores device/connection un-swapped (`flagged_end` a pure flag), so an inverted extension may DISPLAY the parent's source-device on the ext's SOURCE end where v1's physical swap showed it on the DESTINATION end — UNLESS the cable-row render swaps displayed ends by `flagged_end` (unverified). Possible pre-existing B6 parity defect, out of S3-F scope. Verify against v1 evidence; fix in a later pass if real. Does NOT affect the 0.4 propagation conclusion (see the S3-F rulings block). |
| (www) | Mult-coupled colour gate: Update fails to repaint the mult in the browser | **NEW July 31 (S3-F third gate; ships known-open by Daniel's ruling "log as a bug for later and move forward"):** survived the FIX-3 rewire — the pure plan now targets the mult (tests green) but the browser behavior is still wrong, so the residual defect lives in WIRING or CACHE INVALIDATION and needs browser-level diagnosis. Line-coupled Update, Break persistence (`patch_color_break`), and re-patch reset all PASSED gate and stand. **CLOSED July 31 (Find close): a stale-chunk observation** — typed-parse AND 📎 paths browser-proven at HEAD (DOM pre-reload + DB + reload, twice); the three session observations resolved by fresh-server re-gates (now the standing gate preface); the FIX-3 retarget stands proven. Retained for citation. |
| (xxx) | Empty-state / "active filters" copy also covers find-only narrowing — wording pass owed | **NEW July 31 (Find D-5).** |
| (yyy) | Dark-mode FOUND strip renders light | **NEW July 31 (Find gate):** pre-existing, baked into the visual baselines (frame 08 blind to it). **Rides the Session-B dark-mode cluster w/ the DECLARED BASELINE EVENT** (with (rrr), (ttt), (ggg)). |
| (zzz) | Mixed-project equipment New: blank-band key mismatch → model editor never opens; typing mints phantom rows | **NEW July 31 (Find close, GUARDED) · CLOSED Aug 1 (`2720607`)** — see the (zzz) unit entry in Complete (Z/ZA rulings; pending-aware band key; list-only generic scope; three view migrations). Retained for citation. |
| (aaaa) | Tail-list Delete doesn't advance focus to the next record's same field | **NEW Aug 1 (Daniel field gate):** check against the B6 Delete focus rulings + whether S3-D inherited them. **Opens the four-letter series — triple letters exhausted at (zzz).** **Aug 3–4 disposition: resolves mechanically under the ruled field-exit grammar (delete lands successor-same-field) — rides GS-A Leg 1.** |
| (bbbb) | Print "Show Me" (no-method dialog) always lands on the equipment list — a no-method CABLE needs the cable list | **NEW Aug 1 (Daniel field gate):** surfaced BY the Z-1-AMEND full-type no-method count working correctly; the navigation target hardcodes the equipment surface. v1-evidence-gated per Daniel's "refer to v1 behavior" — extract v1's Show Me routing, then land type-routed navigation. **= GS-A Leg 5 (optional): DDR extraction + build-if-unambiguous, else OPEN QUESTIONS.** |
| (cccc) | Notes field renders one-line while focused (multi-line at rest) — must stay multi-line during editing | **NEW Aug 2 (Daniel production use); CLOSED Aug 3–4 (`7fad0f8`, DEV-2 Phase 1 — effect-dep root cause).** Retained for citation. |
| (dddd) | `# ` autoformat headings print literally (renderMarkdownForPrint has no heading branch) | **NEW Aug 3 (CL-R9, known limit accepted):** StarterKit stays at DEFAULT per Zite parity (lossless markdown loading); only the toolbar limits surfaced affordances. Hard-disable = a one-line configure if strict print fidelity is ever ruled. |
| (eeee) | Clicking into a model-band editor must make that band's record the ACTIVE record | **NEW Aug 3 (Daniel production):** K-2 pedigree — one navigation action sets both `currentRowId` and focus; a focus-taking click IS navigation. Pre-existing, not a Leg-C regression. Joins the focus family (natural sibling of (lll)/(nnn)); CC recon confirms the band→record mapping before fix. **= GS-A Leg 4.** |
| (ffff) | New Defaults Method/Group input boxes render DARK chrome in light mode | **NEW Aug 3 (Daniel, light-mode gate):** no light-mode pedigree — needs its own ruling/extraction. (The typable-filter half of the same observation = existing (iii), the Sprint-4 event — not double-registered.) |
| (gggg) | Tail-list Model field → free-text autocomplete from the project's existing tail models | **NEW Aug 5 (Daniel) · CLOSED same day (`322ddb9`, GS-A Leg 2b):** suggestions-only, never set-existing-only; Enter-advance + ''-clear preserved; no at-rest render change. Retained for citation. |
| — | `--update-baselines` rewrites ALL 16 frames — needs a per-frame scope flag (e.g. `--update-baselines 07`) so declared events don't require manual restores | **NEW Aug 4 (the frame-07 event).** Small harness pass. |
| (jjjj) | ⌥⇧I after a FRESH new-record model commit opens the Model band, not Category — works only once the record has committed before | **NEW Aug 6 (Daniel gate); root cause banked verbatim (Amend-4 handoff: editingModelGroupKey nulled on commit → the toggle's ELSE branch; + mid-settle band-key resolution; 4 shared call sites = blast radius beyond the chord).** **RULED: post-model-commit ⌥⇧I lands the CATEGORY band.** Focus family — rides the (lll)/(nnn) mini-pass (S4 basket). |
| (kkkk) | Model-band F4-No blinks the stored value one frame before the per-record write repaints | **NEW Aug 7 (Daniel gate).** Cosmetic (Escape-revert renders old → write lands); no data wrongness. Rides the (lll)/(nnn) mini-pass (S4 basket). |
| (hhhh)/(iiii) | — | **Registered Aug 6 (dialog-stacking · band-draft edges) and DISSOLVED same-window by the Amend-3 capture-and-thread mechanism (the single-edit commit never runs at invoke). Letters retired unused-in-build; never reused.** |
| — | Harness settle-wait (06-settings cold-load race; **July-24 sibling: one 15/16 cold-restart flake post-build; July-25 sibling: two cold frames at 0.0012% post-build, warm 0.0000%; July-26 sibling (B6-FIX): frames 01/02-light at 0.0012% post-build, deterministic 0.0000% warm; July-28 sibling ((uuu) Phase 1): one first-run 01/02-light 19px sub-threshold; July-28 NEW SIGNATURE ((uuu) AMEND-1): one single-run 05-gcm-light at 6.55% — the frame-05 sort-click setup racing, clean re-run 0.0000%; a SECOND 05 occurrence in any unit promotes it to a real harness punch; **July-29 NEW SIGNATURE (R7): 01-project-menu-light 92.1% cold-start PRE-PAINT FAIL ×2 across R7+AMEND — first route compiling on a fresh dev server, captured pre-paint; cleared by the cold-FAIL protocol both times; July-29 NEW SIGNATURE (R7 amend regate run 1): 04-library-dark 2.14% EMPTY-STATE capture — a data-load race under staleTime:0, surface untouched; single occurrence, a second promotes it; July-31 siblings (S3-F): post-build transient rasterization noise on warm-vs-cold runs incl. one first-run 01/02-light 19px — the known family, every warm re-run 16/16 @ 0.0000%, no new signature; **Aug-4 additions (the CL/EP ground gates): the 08-pair BYTE-DRIFT signature** (persistent across 3 attempts, sub-threshold, visually 0.0000% — B1-era baselines vs Next 16 Turbopack antialiasing; **restore-don't-adopt, the standing rule**) · the **04-library-dark theme-race** (2.1374% whole-frame, dark-only, cleared on re-run — the setTheme async re-sync race) · the **frame-03 sort-button capture flake** (one mid-capture abort, no baselines written) · attempt-1's 01/02/03-light transient byte-noise**) | Future harness punch item — a readiness settle beyond the clock pin; joins the 08-dark transient watch family. Cold-FAIL protocol of record: two consecutive clean 16/16. |
| (llll) | New-method commit blinked an existing method for a beat before the auto-created method rendered | **NEW Aug 10 (S4-AUTO gate 1) · CLOSED same unit** (AMEND-1 synchronous patch-then-reconcile; `1f06092`). |
| (mmmm) | Cable Name field — Enter commits but doesn't advance to the next record's Name (the S3F-FIX-2 exit-grammar family never wired the Name text cell) | **NEW Aug 10 (Daniel production).** Rides GS-C. |
| (nnnn) | Cable/tail Method fields: typing a NEW method should auto-create it in GCM (equipment leg-1 parity; amends the July no-op-revert for METHOD fields only; box/category keep no-op-revert; **"0" NEVER auto-creates** — see (oooo)) | **NEW Aug 10 (Daniel production).** Rides GS-C. |
| (oooo) | Method "0" special entry: typing "0" in any method field invokes Method-0 behavior — no GCM create, no silent revert; UI semantics need their v1 extraction (M0-WIN-adjacent; import ruling: method '0' is LOAD-BEARING) | **NEW Aug 10 (Daniel production).** Design gate at GS-C's open. |
| (pppp) | Equipment Library "Add with Method" renders in the method's color (method-pill idiom) | **NEW Aug 10 (Daniel).** Rides LIB-RENDER. |
| (qqqq) | Harness: the persisted `equipment_sort_mode` (DEV2-R2) races the capture's category reset — light frames can bake the wrong sort into baselines; force the sort mode per frame | **NEW Aug 10 (AMEND-1 baseline write; caught in-run by CC).** Joins the HARNESS pass (with the scroll-state frame-class question — sticky regressions are invisible to the at-rest gate, twice proven this unit). |
| (rrrr) | New Box ⊕ UX: create latency + apparent create without an explicit ⊕ activation (review the Enter/Tab-activate semantics + optimistic render; check for stray test boxes when Box List lands) | **NEW Aug 10 (re-gate, Daniel).** POSTPONED by ruling to end of Sprint 4, after box surfaces exist. |
| — | Method + box fields → autocomplete text inputs (not pulldowns); box field editable | **Sprint 4** (July 19 registration). |
| — | **Phantom `model` cache writes retirement** | **NEW July 24 (CTF F10):** `v_cable_list.model` is a real but EMPTY column for cables; the cable list now reads `cable_type_name` exclusively (`cableModelDisplay`), but the four optimistic commit patches still write `model:` alongside. Retire the writes once print/labels' read path is confirmed on `cable_type_name` — rides S3-G cable print. **July-25 PM second bite (B4 FIX3-1): the chain Model term silently read the phantom column — latent since B2. The column is an ACTIVE hazard, not dormant; S3-G's retirement check gains this as evidence.** |
| — | **Delete-path link dissolution — CLOSED July 27 PM (S3-E R9)** | Retained for citation. |
| — | **Stale library category text healing** | **NEW July 24 ((ooo)-2 finding):** deleted/renamed categories leave orphaned text in `equipment_library_items.category`; rows correctly sort in the trailing uncategorized block (O2-3). Whether to heal = a future data-hygiene ruling. |
| — | **Fresh-project Equipment Library seed toggle produces 0 rows** | **NEW July 24 ((ooo)-2 smoke, informational):** consistent with the seed TODOs in `dashboard/new`; not a regression. Rides the fresh-project seeding unit. |
| — | `resolveTargetId` null-guard asymmetry | Unreachable only by DB invariant. |
| — | Empty-string note decision | Product ruling owed. |
| — | Lint entrypoint (Next 16 removed `next lint`; no flat config) | Out of acceptance gates until an entrypoint exists. |
| — | `npm audit`: **7 findings (2 low, 5 high — Aug-3 adjudication of record: ALL pre-existing in next/supabase/postcss/sharp/js-yaml/brace-expansion; ZERO from the CL tiptap set;** `audit fix` deliberately NOT run) · `@supabase/ssr` 0.5.2→0.12 modernization | **Registered as the dependency-maintenance unit** (deliberate gated Next/postcss/sharp upgrade pass — likely a baseline-event class; pre-October beta; feeds the pre-paid-release security review). Absorbed-by-gate-(b) superseded. |
| — | Real `git config user.name/email` · Next 16 `middleware`→`proxy` migration | Housekeeping / eventually. |
| — | Minor `setTimeout` race in error-flag cleanup | Low. |
| — | Registered one-query check: any production snapshots lacking `items`? | July 17. |
| — | Item 13: optimistic duplicate (⌥⇧D speed) | Speed half; commit-first half landed slot 2. ⌥⇧D exists on the cable surface (verbatim copy, createCableFlow); the speed item remains equipment-side. |
| — | Contrast recon (filter-pulldown option text + producer/theater line) | |
| — | /dashboard capture frame (harness coverage gap) | Candidate for a future baseline event. |
| — | Cross-band Tab bridge design question | Parity ledger. |
| — | Hook-test infra (jsdom/happy-dom + @testing-library) | Someday-maybe, non-blocking. **RTL/component-test infra joined July 29 (R7 + S3-F: repeatedly disclosed as a dependency hard-stop, correctly held every time — FIX-2's keyboard fix shipped browser-verified in lieu of a unit test).** |
| — | Storage-cleanup orphan residue on delete-cleanup failure | Accepted risk (July 21, IMG-4); no action owed unless observed. |
| — | Dev-server blank-on-reload, single occurrence | Watch item (July 21). Sibling gotcha of record: `npm run build` and the dev server share `.next/` — restart dev after builds (in CLAUDE.md; honored at every build gate since). **Aug 2–3 incident rider (CL): repeated in-unit builds corrupted `.next`; a plain restart REUSES it — the protocol is stop · `rm -rf .next` · restart (hydration 10.5s→6.9s on the true clear). The residual airborne blocker was airplane-link Supabase latency (~5.3s/request server-side), correctly diagnosed environment-not-code; gates parked for ground. The rm-rf clause folded to CLAUDE.md (`81f6541`). **Aug-4 addition: CC's deny-list correctly blocked the delete at the ground gate; the harness's spawned rebuild covered it — if a stale-cache signature appears at a future gate, the `rm -rf .next` step is DANIEL-RUN.** |
| — | CTF edit-dialog seed helper (`cableTypeFormFromRow`) unused by the grid | **NEW July 31 (S3-F D3-AMEND-2 residue, accepted):** inline editing superseded the row-click edit dialog; the D2 seed helper stays exported + tested (harmless; the create-mode dialog unchanged). Retire opportunistically. |

**Letter provenance:** (a)–(g), (i)–(m) defined in Roadmap v2.13 (disk-only); (x) never assigned. **(vvv)/(www) assigned July 29/31 (S3-F). (xxx)/(yyy)/(zzz) assigned July 31 (Find close). The triple-letter series is EXHAUSTED; (aaaa)/(bbbb) assigned Aug 1 open the four-letter series; **(cccc)/(dddd) assigned Aug 2–3 (the DEV/CL sessions); (eeee)/(ffff) assigned Aug 3 (Sheet #2); (gggg) Aug 5 (registered AND closed same-day `322ddb9`); (hhhh)/(iiii) Aug 6 (dissolved unused); (jjjj)/(kkkk) Aug 6–7** (the Aug-1 in-conversation provisional letters (sss)/(ttt)/(uuu) were collisions with existing entries — corrected here, never of record; the Aug-1 Return-key finding folded into the existing W-2 as field evidence rather than a new letter).

---

---

## 📚 Reference registers

*(Stable non-item registers; pointer-style — artifacts live in the repo. The v3.0 Roadmap carries a forward pointer here.)*

### UX Reference Catalog (carried whole from v2.46; Daniel-confirmed destination)


Carried verbatim from v2.17: Sessions 1–5 done; Sprints 3+4 spec-complete; post-BVSC catalogs DDR-first; v1 residual open items unchanged. **Evidence additions July 23–31:** `docs/reference/evidence/cable/` carries b2a_01..05 (`2c85769`) + ctf_01..09 (`c1d7ae6`) + b3_01 (`03b9753`) + b4_01..02 (`a066155`) + b6_01 (`4dd575d`) + b7_01 (`18d09ce`) + frames 031–035 (`861b4f9`) + s3e_01 (`670849d`) + uuu_01 (`1786211`) + **s3f_01 (`327c100` — the v1 Mult Detail two-state Link Cable micro-column anatomy)** + d2_01–06 + d2_s293/s361/s390 (`de1dda9`) + the CL print evidence set + the Zite excerpt (`5ddf30b`) + the REC-1/REC-2 five recon files (`e5a7ecd`). `docs/reference/v1_Import_Show_script.pdf` at `7fd62f1`; the three v1 nav scripts extraction-verified in the (uuu) Phase-1 record. **Scripts extracted to the Arch record July 28–31 (verbatim text living in the session threads + the CLAUDE.md folds/handoff appendices):** R7 = 611/612/668/677 + 686/687 + 610/615 + 616/652/656–661 + 688 · S3-F = 609/665 + 421/422/423/424 + 681/683 (668/678 re-supplied). 652/658–661 held for the split unit.

---

### v1 export corpus + evidence-commit index (carried verbatim from v2.46 Current Status)

- **In flight:** **THE AUG-7 COMPRESSION RULING (Daniel): Sprint 3 = GS-A ✅ + EP-P1 ✅ + S3-G. Nothing else.** All remaining former-Sprint-3 scope moves to the **SPRINT-4 BASKET**: D-2b (Clone/Ext; its design gate keeps the s441/s704/s504 evidence; the B6 duplicate-cascade gap now lives into S4 on the production surface — flagged, accepted) · the declared baseline-event session (Bundle-List/D-2b frames + the (yyy)(rrr)(ttt)(ggg)(xxx) dark cluster + the KeyR unbind; the Bundle List stays outside the visual gate through S3-G — flagged, accepted) · **GS-C cable completeness** (SPL-C split · INS-1 inspector tail/line summary · (bbbb) Show-Me · candidate (qqq)/(sss) CTF hardening) · **S3-G-2** (the residual v1 outputs — Shop Orders Basic/Detailed + remaining reports; Arch-recommended home, **Daniel's one-word confirm owed**) · the Allocate call · the method-0 check · the (lll)/(nnn)+(jjjj)/(kkkk) focus/polish mini-pass. **S3-G — THE DESIGN GATE IS CLOSED (Daniel, Aug 7; the parallel-session decision block ruled by number):** P1-AMEND scope = Bundle Sheet + Cable Checklist + **TAIL Checklist + tail Line/Block/Panel labels + Balun labels** (the recon under-scoped the tail print family — Daniel testimony, superseded) + cable Small/Big labels + **Folder labels (OQ1: IN)** · P2 the R-22 render chain reused · P3+P12 anatomy + sort spines v1-VERBATIM · **P4-AMEND fonts v1-verbatim — the bundle NAME is the only giant Arial; everything else Century Gothic (the normalize proposal DEAD)** · P5 print scope = Which-X/applyFindWithFields + the print-list flags, the FS pin NEVER feeds print (v1 truth, deliberately confirmed) · P6 the Print List checkbox on all three finds · P7 structural label-quantity derivation (the S3D-R2 debt paid) + **OQ3 Max Label Sets RETIRED** · **P8-AMEND the length-less-bundle guard follows v1's ACTUAL behavior (extraction decides where it gates; labels NOT blocked; blank-not-zero)** · P9 the phantom-model retirement 3-step · P10 🖨/green-→/Clear-Print-List frame-verbatim + **OQ5 RULED: 🖨 is a toggle, green=on-list/grey=off** · P11 EP-P1 composition reuse + the steck-color rider · OQ2 NO current-found-set scope · OQ4 confirmed · OQ7 no Area/Volume. **The S3-G BUILD runs in a NEW Arch session (Daniel directive at this close) — the drafted multi-leg handoff (Leg 0 types-regen · Leg 1 evidence census, hard-stop on missing tail/folder artifacts · Leg 2 scope plumbing · Legs 3/4 documents + labels) travels in that session's opener.** Threaded design-gate units unchanged: the latency/optimistic unit · dependency maintenance (pre-October).

### Workstream/queue history at migration (v2.46 Parallel Workstreams, carried whole; open queue items exited to docket rows COMMS-Q · LEGAL-SEP · BRIEF-12; the CLAUDE.md fold queue is DISCHARGED at `cc96e82`)


**Communications:** all copy states export of every kind is PAID; October-announcement screenshots = the finished equipment section; then license-holder announcement email (by tier) · university outreach · landing page · help docs · support@ · demo video.
**Legal (September window):** as dated below.
**📋 Brief v1.2 queue (Strat-owned; Brief still v1.1):** items 1–7 carried verbatim from v2.17 plus item 8 (stack-truth, OBE in substance) + the July-22 stack-truth rider (the Brief's cable-table list is stale — the extension architecture is the live shape). The Brief bump rides the Strat queue.
**📋 CLAUDE.md queue:** all prior rewrites landed through `1c8bde2` + the (kk) rewrite + the Operating Contract v2 (`3b0b175`) + `72919e3`. **Folds landed:** July-24 `745e447` · B3 `e92deaf` · B4 `d049b41` · B6 `1d4a142` · B7 `2e90707` · S3-D `603c9c7` · S3-E `864c506` · (uuu) `c1fbdb5` · **R7 `5b2a689`** (the licensed smoke pattern · lineage/Next/baselines · the R7-6 vocabulary line · the R7 architecture of record · the tracer-retired fence). **Folds landed (continued):** the S3-F fold `7dd42d9` · the Find fold `19104e7` · the (zzz) fold `bfd75ca` · the D-2/DEV-1/CL fold `5230b4b` · **the AIR2 + fixture-event fold `81f6541` (executed at the Aug-4 open — lineage through `97b0377` + the `bfd75ca` correction · the fixture pristine state = 5 generic items + GATE-CABLE + the "6-Pair" type · the 08 restore-don't-adopt rule + the update-baselines caveat · the flake-registry additions · the Daniel-run rm-rf note · suite/baseline counts · Next → P-1/GS-A).** **QUEUE (ONE fold owed — rides the S3-G session open as a CC docs errand):** the GS-A/E-CA fold — lineage through `b09178e` · the STANDING FORK RULE (G1) · the NO-TRAILER rule (Co-Authored-By struck 8× this window — never in commit messages) · the PROBE RULE · fixture pristine state += `equipment_sort_mode='category'` (self-maintained) · the capture-and-thread architecture block (useChangeAllInvoker/changeAllInvoke + the setTimeout(0) proof) · Change All five-surface field registry + F4-No semantics · the E3 baseline set (02/03/04/08) · suite 1184/83 · types-regen owed (equipment_sort_mode; the `as never` cast drops at regen) · the unguarding of the two Change All placeholders (sanctioned) · Next → S3-G.
**📋 Strat queue addition (July 24):** standing CC credit-budget policy, if wanted.

---

### Open Questions register at migration (v2.46 ❓ section, frozen copy — live questions re-authored in the v3.0 Roadmap; RESOLVED lines stand here as the record)


- **RESOLVED since v2.45:** V-1 placement (rode GS-A, shipped) · the baseline-event session placement (→ S4 basket, Aug-7 compression) · d2b_01–03 delivered (`3ffd371`).
- **S3-G-2 as the Sprint-4 home for the residual v1 outputs** (Shop Orders + reports) — Arch-recommended; **Daniel's one-word confirm owed.**
- **The residual s504 delete-variant frames (3 of 4)** — when v1 cooperates; gates only D-2b's gate detail (now S4).
- **The R8 backup drill** — runbook delivered; **execution owed THIS WEEK (shop prep Aug 10)**; F4 feeds the attorney addendum retention number.
- **Script 551 extraction** — optional-future (83 steps; E2 stands on Daniel's attestation).
- **Cross-surface checkbox eligibility for CABLE Change All** — equipment/tails now include checkboxes (ECA-F2); cable excluded them at its build; consistency question, unscheduled.
- **House-wide Cancel semantics** — V-1's Cancel=revert vs the reverse-gate/688 edit-stands Cancel; asymmetry accepted, revisit if it chafes.
- **Used∩Ignored semantics** — Arch: keep-current; Daniel ruling owed.
- **(q) verification** · **(ff) treatment** · **empty-string note decision** — carried.
- **Import sheet order → sort_order** — the v1-importer half remains open, ruled change only (activating it = reading v1's latent Sort Override values; its own session with v1 evidence). The workbook half landed at DEV-1 (R9).
- **`service_role` GRANT restore** — housekeeping or gate (b).
- **Brief v1.2 pass scheduling** — Strat-owned; queue complete (+ the July-22 stack-truth rider).
- **Port-readiness gate criteria** — Arch refines; Daniel confirms pre-port (incl. the storage-backup addendum).
- **Comms Prompt structure** — next Strat session.
- **Stale-category-text healing policy** — data-hygiene ruling, unscheduled.
- **CC credit-budget standing policy** — Strat question (WF-2 owns the per-unit mechanics).
- **bundles UNIQUE — RESOLVED at D-2 (`d2_bundle_list_view_and_name_unique`); the link `field_name` CHECK backstop remains** the registered future Arch schema question (B6).
- **Cable-type RENAME affordance** — the future anatomy addition needing its own pedigree (S3-F D3 disclosure). Unscheduled.
- **Method-field-in-list autocomplete sequencing (#3, Aug 2)** — pull forward (early baseline event on frames 02/03) vs ride the Sprint-4 conversion event. Daniel sequences at the next map discussion.
- **CL a-1 evidence uploads (non-blocking, Daniel-when-convenient)** — the v1 Cover Letter CONFIG screenshot + a rendered v1 cover/title page (gates the Concept-A print-header unit) · a-2: Lovable cover-letter.tsx/functions source (optional reference only). **a-3 RESOLVED:** `projects.units` gained its first consumer at EP-P1 (EP1-R2; imperial default).
- **d2_01 red Members count** ("11" on Tech Distro) — trigger unknown; rendered neutral (carried).
- **CL multi-letter-per-project** (Lovable's `cover_letter_id` picker) — deliberately not built (CL-R3); future question.

---

---

*End of Ledger v1.0 initial state — August 9, 2026. Append below this line is prohibited; new entries go inside their sections above, dated.*

### Box evidence register (opened Aug 11)

- **`v1_elephantshoes_boxlist_bytruck.pdf`** (Elephant Shoes / Two River Theater 2026, printed 8/10/2026, 4pp, by-truck "Taco Truck") — `docs/reference/evidence/print/parity/`. Intake Aug 11 per the Evidence Intake Protocol (uploaded raw as `Taco Truck.pdf`, renamed and landed by Arch while no CC session was open). **The first box output PDF in the corpus**; the print reference of record for the S4-3 Box List print leg, and the artifact that corrected R-51.


## AUGUST 11, 2026 — S4-3 OVERNIGHT/DAY RUN + THE PRINT-PARITY CYCLE (append block)

*Append-only. Insert at the end of the live Ledger. Session: Cowork-Arch, continuous, Aug 11.*

---

### Migrations 45–48 (Arch-applied, Trigger B) — the migration ledger moves 44 → 48

Each ran the full runbook: live introspection → proof-run with forced rollback → residue check → apply → post-apply verification.

- **45 · `s4_3_p12_box_side_to_flag_remove_na_box_rows`** — the P12 box side becomes a FLAG. The 21 seeded `N/A - No Box Needed` spine rows deleted; `box_details.is_system` dropped; `guard_system_box` dropped; **`equipment_items.no_box_needed`** added. `equipment_items` 1945 → 1924, `type='box'` 24 → 3 (`Moose`/`Salmon`/`Tryout`, real user boxes untouched). Method side unchanged — 21 system rows, `guard_system_method`, and the `seed_na_rows` trigger all intact, the trigger now seeding the method only. Probed: a new project gets 1 N/A method and **0** box rows.
- **46 · `s4_3_repoint_zero_alias_methods_to_na_labels_only`** — the six `(combo tails)` items on methods literally named `0` (JOY ×4, Vape! ×2) repointed to their project's `N/A - Labels Only`. **Deliberately not a deletion:** the emptied `0` rows now surface in each project's GCM Delete Unused, where Daniel removes them through the product. Before-state recorded in the migration comment for reversibility.
- **47 · `s4_3_projects_box_volume_thresholds`** — `projects.box_warn_volume` (23) + `box_over_volume` (27). Migration 43 had built six of the v1 Box Defaults panel's **eight** fields and omitted both Volume thresholds.
- **48 · `s4_3_seed_visual_gate_fixture_boxes`** — Visual Gate Fixture only: 4 boxes (typed/untyped/nested/trucked) + 3 `box_types` marked `notes='fixture'`. Scope proven post-apply: generic still 5, cable 1, categories 2, methods 4 — so frames 02/03/05/08 could not move.

### Rulings of record (Daniel)

- **OQ-F — a box prints under whatever category it carries.** Verbatim: *"a box is a thing, so it displays under whatever category it has in the box list. I frequently call this category 11, but a user could call it any category."* **Never key logic to a category name.** Corroborated independently in code: `lib/queries/equipment.ts` — *"the printed list is the shop order — v1 functional truth."* The print path's unfiltered read is correct and was never the defect.
- **The P12 asymmetry is correct.** `methods` is a vocabulary table and can carry a "none of the above"; `equipment_items` is a things table and cannot carry a non-thing. Arch's earlier symmetry argument was the wrong abstraction, owned.
- **The `0`-alias items are the convention's first real members**, not junk — they generate labels but not gear, which is what `N/A - Labels Only` means.
- **Fixture box seeding approved** as a declared event; frame 07 refreshed accordingly, and now guards box-print behaviour.
- **REVERT of the equipment-list print layout to its Aug 4 state** (`40aa945`), applied at `9c3932f`. Scope verified file-by-file before applying: two files, one line, one baseline event; the revision-notes fix, legs 0–1, the types regen and the nine cable/tail print surfaces all confirmed outside the blast radius.

### Arch rulings (delegated, cited)

OQ-E baseline events (scoped, after treatment) · OQ-G `box_types.dimensions` = dead weight · OQ-H protected row stays fully read-only · OQ-I exclude the N/A row from the "managed methods" count (deferred, never built) · OQ-J find/import wiring rides the legs that touch them · OQ-K lint · OQ-Q `.eqp-r` → 10pt (deferred, never built) · OQ-R wrap divisor = 85 chars, measured · OQ-S `METHOD_HDR_H` stays derived, registered as fixture debt · **OQ-T the revision highlight covers the model row ONLY, unconditionally** · OQ-U the 1px left, deliberately.

### CORRECTIONS — Arch (all owned)

1. **A standing-rule violation instructed.** Arch's commit instruction said "push to `main` and report the deployment hash." **CLAUDE.md WF-1a: "push is always Daniel's paste."** Arch had no authority to direct a push; CC's return reports one. Owned in full — the instruction was wrong regardless of outcome, and the rule stands unamended.
2. **The migration-44 seed defect.** The P12 box row printed as a phantom equipment line on **all 21 projects**, including Matchbook Festival, a live show. Arch verified the on-screen list filtered `type='generic'` and never checked the print path. Root cause: a non-thing inserted into a things table, not a print bug.
3. **HOTFIX-1 §3 specified the wrong fix** — row-level, uneditable note display — driven by an unmeasured claim that per-item attribution was "mathematically unrecoverable." It was true of **11 of 1,366 items (0.8%)**. Corrected in HOTFIX-1a after measuring.
4. **The highlight "exception" was ruled from inference.** Arch told CC struck-through removed rows carry the fill through their notes. Pixel measurement across every highlighted block on three v1 pages: **zero note words inside any green fill**. Retracted.
5. **The note-column width was put to Daniel as a design question** when v1's own geometry already answered it — notes stop at x=445, exactly where `From` begins. Measurement should have preceded the question.
6. **Two column-geometry defects were diagnosed and then left as open questions rather than ruled**, so they went unbuilt for two passes while type was corrected around them. This is the sequencing error behind Daniel's *"it feels like we're pushing the problem around."*
7. **A stale handoff §4** listed the notes-to-print bug as open after CC had built it; files crossed.

### NOT corrections — CC work of record

CC's halt on the seed drift (refusing to build six legs on a red gate) · distinguishing the `04-library` failure from the registered dark-only flake by near-identical both-theme pixel counts · catching a **clipped document** by reading a recaptured frame where no threshold would have fired · the `item.notes === undefined` key-absence guard (a falsy guard would have silently overwritten deliberate blanks forever) · the React unitless-`lineHeight` catch (`LEAD_NOTE = 16` → a 213px line box), found by probe, not by reading CSS · **refusing to build the highlight rule without a v1 PDF in the repo**, which is what forced the measurement that corrected Arch · two sed disclosures, one self-reported as a defect and undefended · two self-inflicted JSX syntax errors disclosed rather than tidied.

### THE PRINT-PARITY CYCLE — closed, reverted, and fully documented

Five hotfix passes (HOTFIX-1/1a through HOTFIX-5) corrected type scale, leading, page box and column geometry in turn; the result read worse than the starting point and Daniel ruled the revert. **Everything measured survives** in `docs/reference/Minotaur_v1_Print_Spec_Measured.md` (builder-facing) and the project doc `Minotaur_Print_Parity_Findings_2026-08-11.md` (narrative + process).

**The three findings worth carrying:**

1. **The Aug-4 document was a uniform ~75% scale of v1** — internally consistent, which is why it read correctly. **v2's leading ratio has never matched v1's** (~1.40–1.62 against ~1.20–1.26). Correcting type without leading took a 75%-sized error and made it full-size. **Type and leading are one correction.**
2. **The visual gate was structurally blind to the surface it was meant to guard.** AMEND-3 C4 confined the point-true sizes to print media expressly *"so the frame-07 photograph is byte-unchanged"* — so no gate run could ever see the printed type scale, and a 25%-too-small body reached a live show's paperwork.
3. **Dev-server exports are inadmissible as parity evidence.** Stale chunks produced **three** misleading measurements in one day, each triggering a real diagnosis of a non-existent problem. **New standing rule:** print-parity judgements come from `rm -rf .next && npm run build && npm start` or a deployment only, and every print report states its build provenance. This joins the Print doctrines in CLAUDE.md.

### Registrations

- **PRINT-REDO** — the eventual re-attempt at equipment-list print parity. Opens only on Daniel's word, with a single agreed target ruled first, a measurement harness built before any layout change, and both cascades made to agree so frame 07 can guard the surface. Sprint-4 close at the earliest.
- **CLAUDE-FIX** — CLAUDE.md's fixture pristine-state is stale twice over (the N/A method row, then the four seeded boxes + three box_types). Text proposed by CC in `from-cc/260811-1250`; Daniel confirms and commits.
- **MULT-DUP** — duplicating a mult cable does not offer to duplicate its lines and tails; v1 asks. Needs the v1 dialog's verbatim wording recovered from the Cable catalog before build. → GS-C.
- **UNALLOC** — unallocate does not appear to return a cable (and perhaps tails) to a generic item. The cable path exists and flips `type→'generic'` (`CableListClient.tsx:2824`/`:2910`, `cableWrites.ts:14`); suspects are the tail surface having no equivalent, or a cache-invalidation gap. Needs a reproduction from Daniel.
- **LINT** — amended: there is **no ESLint config anywhere in the repo**. Next 16 removed `next lint`, which was the only thing supplying one. Lint has never run in v2; standing it up is config authoring, not a fix.
- **PARITY-PAIR** — every v1↔v2 comparison this cycle pitted a v1 *revision* list against a v2 *preliminary* list, which are different layouts. **A v1 preliminary list and a v2 revision-mode output from early August are both missing** and would make the next attempt tractable.

### Suite and gate state at close

Suite **1250/89 → 1325/92** · frozen **3/3 vs `be0769de`** throughout · visual **16/16 → 18/18 @ 0.0000%** (frame 09 box-list added) · dependency gate clean · migration ledger **48** · six commits `6b2868e` → `9c3932f`, production READY.


## AUGUST 11–12, 2026 — S4-3 LEG 2 ENTIRE + THE SHARE DESIGN ROUND (append block)

### Rulings of record (Daniel unless noted)

- **OQ-M RULED — box rollups are DERIVED AT READ; the schema question dissolved.** Primary evidence: the v1 DDR (`docs/reference/v1_ddr.xml`, UTF-16), Box List table 183 — `Weight` (field 32: `Actual Weight` else `Ceiling(EMA weightSubSummary + Additional Weight + Cable Weight Summary)`) · `Cost` (39: `Ceiling(EMA costSubSummary + Additional Cost)`, no cable term — N4's bug seen in source) · `Volume` (34: EMA + Cable volume summaries) · `Percent Full` (35: `Round(Volume / Box Types::Volume × 100, 1)`, blank on error); Equipment Manually Added table 134 — `costSub` (20: `If(Method ≠ 0; Quantity × Equipment Library::Cost; 0)`) · `weightSub` (38: per-foot × length else `Quantity × Library::Weight`); Cable — `Area` (π(OD/2)²), `Volume` (Area/144 × length), `Weight` (per-foot × length). **v1 never stored member weight/cost/volume — it computes live through the model→library text relation; an unmatched member contributes nothing.** v2 implements the formulas verbatim + the N4 cable term, in ONE shared module (`boxRollups.ts`/`boxMembers.ts`) consumed by browse, bands, footer and (leg 6) the PDFs. v1's `Method ≠ 0` cost exclusion maps to the `N/A - Labels Only` system method — **cost only**; weight and volume carry no method test. %-full's numerator is **cable material volume only** — pure equipment contributes zero in v1 (no item-volume field exists anywhere). Nested boxes contribute nothing to any parent rollup. Match-reality measured live: (project, model) unique across all projects, zero ambiguity; coverage 55% match / 40% cost / 1.2% weight (96 weighted library rows of 9,710).
- **OQ-L RESOLVED by OQ5's own reason of record** — the thresholds and the %-full column are the same quantity, percentage points. Bands `<23` normal · `23–27` warn · `≥27` over; only warn/over tint (R-10 family shades, `#C77700`/`#D91E0E`).
- **OQ5 RECONFIRMED — hard-coded 23/27 stands; migration 47's per-project columns ruled orphans and OUT** (→ migration 49 below). The Box Defaults panel is the OQ4 set with NO Warn/Over fields.
- **OQ-N TAKEN — the nav declared event, in two halves:** the `ListPageHeader` Boxes pill (palette `blue`, Daniel to CC — match the sibling list-surface pills) at checkpoint A, measured set **02/03/04/05/06/08 (+09 active-state)** — Arch's declared set was wrong (07 predicted; 06 actual; 07 renders no ListPageHeader) and CC halted on the mismatch per procedure; and the Project Menu tab (Rider A → **overturned at Daniel's gate**: the Boxes tab renders the **v1 menu card** — Add/Edit: Boxes · Box Types; Print Paperwork: Print Box List · Print Contents Lists (blue); Print Labels: Big · Small · Big Replacement Numbers — unbuilt entries disabled, Bingo precedent; evidence `docs/reference/evidence/boxes/v1_bvsc_boxes_menu_tab.png`).
- **Next-leg ruling:** leg 2 in order (Box List editing), not leg 4 — the canonical overnight numbering governs (2 editing · 3 cascades · 4 Box Types admin · 5 Contents · 6 PDFs).
- **effective_box_id CONFIRMED on primary evidence** — DDR Cable field `Box`: `If(Bundle ≠ ""; Bundle List::Box; Box Manually Added)`. A bundled cable's box IS its bundle's box; CC's divergence from the handoff's literal sentence was v1's own formula (measured: 8/38 cables bundled, 0 with own box_id — raw box_id would have counted no cables).
- **%-full on a TYPED box with no cable members renders BLANK** — a deliberate v1 divergence (v1 renders bare `0`); blank-beats-zero means *no terms*, and a stored real `0` is a term (renders `0 lbs`).
- **N7 RULED — TAILS become rollup members. Deliberate v1 divergence, N4's sibling.** Membership = the tail's OWN `box_id` (DDR: Mult Tail List `Box` field 15 is plain manual — no bundle/cable inheritance, kept). Weight `qty × lib.weight_lbs`, cost `qty × lib.cost` with the N/A exclusion (v2 extending its own rule — v1 has no tail cost term at all), **no volume term**. Member tables table-driven so RF slots in (P4 pattern).
- **CA-BOX — Change All on boxes excludes `name` and `In Box`**; an excluded field shows a plain passive sentence instead of the dialog (copy of record in the AMEND-2 return, Daniel-gated). B6 also fixed a worse latent defect: the armed-field ref skipped non-armable fields, so Change All could fire on a field the user had left.
- **OQ6-SELF — the self-nest gets its own copy:** "A box can't go inside itself." (id-branch, not name); true cycles keep the ruled sentence.
- **Row status checkboxes — ruled display-only-until-leg-3 in the morning, AMENDED at the gate the same day:** Rem/Star/Drawn live on the row now (shared write path with the footer, K-2 on click); **Packed stays inert until leg 3 lands the P4 cascade**, rendered clearly passive (per B7: a disabled native checkbox in the family idiom).
- **KEYR-RES — ⌥⇧R RESERVED until the RF unit exists** (the KeyA precedent; /rf was a live 404). The dead-chord sweep now expects an empty list — nothing grandfathered.
- **A8 — the Project Menu shortcut scheme, SUPERSEDING N-R2 (both Daniel rulings; A8 later and explicit):** ⌥⇧1 from anywhere → the menu on the last-selected tab (sessionStorage memory, `boxes` now legal); ON the menu ⌥⇧1/2/3 switch the Equipment/Cable/Boxes TABS and stay on the menu. ⌥⇧A project-wide → the Box List (the redeemed KeyA reservation; audited, no collision).
- **Uncoloured box Name cell is NEUTRAL** — R-47's tint is the box's own colour; the project input token comes off the no-colour branch.
- **B2 OVERTURNS A2's Enter/Tab-⊕ blessing (Arch-confirmed).** The activation created box types off PARTIAL matches (arrow-to-Trunk + Enter created "Tru"). Creation is now ⊕-gesture-only — mouse, or keyboard on the focused ⊕ button; exactly one `box_types` insert exists in the app, source-pinned by five gate tests.
- **Box colours resolve through `getMinotaurColor` (the 14-hex record canon) at full palette hex + paired text** — the 72%-toward-white tint derivation removed. `MINOTAUR_PALETTE` (project-paperwork pickers) accepted as a separate concern; PAL-UNIFY registered.
- **The SHARE design round (parallel session 260812-0915, folded in; all seven OQs ruled):** **SHARE-R1 — FOUR roles: owner / admin / editor / viewer** (admin = edit + roster + settings; Daniel's addition over the proposed three; name "admin" kept) · **SHARE-R2 — invites: owner + admin** · **SHARE-R3 — project settings: owner + admin; editors excluded** (the New-Defaults silent no-op resolves by not rendering the control for editors) · **SHARE-R4 — ownership transfer deferred past public beta** (manual support path) · **SHARE-R5 — phase-1 invites auto-accept** (existing accounts, in-app, no email; courtesy dashboard notice) · **SHARE-R6 — default invite role: editor** · **SHARE-R7 — phase split confirmed: phase 1 ships with SHARE; phase 2 (email invites, `project_invites`) strictly after AUTH-DOMAIN.** Libraries travel with the project (3TL inherits the personal-library leak-proof flag). **REV-B owed on the RLS delta + ADM-1 sketch for the fourth role** — folds into SHARE build-prep.
- **CL-HILITE registered** — cover-letter rich-text highlight control, post-Sprint-4 / pre-public-beta.

### Migration 49 (Arch-applied from the Cowork seat — SEAT-MIG scoped exception #2, Daniel-authorized in-session)

`s4_3_drop_orphaned_box_volume_threshold_columns` — drops `projects.box_warn_volume` / `box_over_volume` per OQ5-stands. Protocol held: dependency introspection (no view/function/trigger deps), proof-run with forced rollback, applied only after commit `070904f` (which removed all client reads) deployed READY; post-apply columns gone, 21 project rows intact, advisors unchanged at the 9 pre-existing warnings. Migration ledger **48 → 49**. Types regen rode commit `598f0bf` (exactly the six predicted lines).

### MMM-CAT — status established from repo evidence

**Never ran.** No `from-cc/` return exists; the sub-band is still the display-only div the handoff cited as "current state"; `toggleModelCategoryFocus` still carries its no-op branch and original deviation comment; `advanceBandEditor` still two-kind; zero `mmmcat_*` frames; no MMM-CAT commit. Consistent with the "defers to the next sitting" ruling — the next sitting became the overnight run. The 0216 handoff is STALE (HEAD, suite counts, frame count, line citations) — re-issue fresh on scheduling; do not point CC at it.

### CORRECTIONS — Arch (all owned)

- **The OQ-N declared frame set was wrong** (07 declared; 06 actual) — leg 1's prediction inherited without verification; CC measured and halted before re-baselining, which is the §4 guard working against its author.
- **Rider A's link design** — Arch accepted "the Box List is its own page" without a frame; Daniel's v1 screenshot disproves it (the Boxes tab is a menu card). Rebuilt in AMEND-1.
- **A2's Enter/Tab-⊕ activation** blessed the exact mechanism of the partial-match create defect B2 removed.
- **A4's suspected root cause** (boxes route missing the shortcut mount) was wrong — the mount lives in the project layout; the audit disproved it and found the real two gaps.

### CC record

- **Two single-file `perl` stream edits in two rounds — the same defect class, the second after an explicit instruction not to repeat it.** Both self-flagged with full verbatim ledgers, measured scope, and immediate frozen-gate runs; content verified correct both times. **A pattern, not an incident** — registered as a WF1A-SUNSET/contract datapoint (a mechanical backstop against in-place editors on single files is worth considering at the sunset review).
- **Work of record:** the A4 audit's false-pass on ⌥⇧A self-caught and corrected (an unbound chord "passes" when pressed from its own target); B2's exhaustive write-path enumeration before touching code; B6's discovery that Change All was armed on the wrong FIELD, not merely dead; the frame-13 sessionStorage leak root-caused across theme passes and resolved by the frame-13-last staging rule; the same-run restore trap caught by the gate reproducing Rider A's exact bounding box; distinguishing deterministic staging artifacts from flake by three identical measurements — twice.

### Harness register updates

- **Same-run restore rider** (CLAUDE.md): `git checkout --` restores against HEAD/index — the restore set must exclude frames an earlier uncommitted stage of the same run legitimately re-baselined; re-adopt from fresh captures if it fires.
- **Frame-13-last staging rule:** tab-memory writes leak across theme passes in one browser context; frame 13 captures last in each pass with the Equipment tab restored after.
- **Flake registry:** (5) added — 04-light blank-capture race; (6) WITHDRAWN as flake — deterministic staging artifact, kept as the lesson; **(3) amendment OWED** — a long-lived dev server (7 h) is a trigger for the fail-hard; remedy is restarting the server, not re-running against it.
- Frame count 16 → 18 → **24** (leg 2: 10 detail panel · 11 nesting error · 12 type-⊕) → **26** (13 boxes-menu-tab). `EXPECTED_CAPTURE_COUNT` 26; the adopted-set register bullet rewritten (EDIT 7).

### Suite and gate state at close

Suite **1325/92 → 1419/96** · frozen **3/3 vs `be0769de` throughout** · visual **18/18 → 26/26 @ exactly 0.0000%** · migration ledger **49** · commits `d35cc04` (checkpoint A) · `070904f` (checkpoint B) · `cbe004a` (checkpoint C + N7) · `598f0bf` (types regen + CLAUDE.md) landed and pushed, production READY; **the combined AMEND-1+2 commit + the docs(evidence) commit stand ready on Daniel's phrases at session close.** Leg 2 = browse + editing + rollups + nav + N7 tails, COMPLETE at that combined commit; **B1 (find/icons/trim) · B4 (row keyboard grammar) · B5 (Type autocomplete audit) exit to the docket** with CC's specs of record in `from-cc/260812-1630`.

---

## AUGUST 12–13, 2026 — S4-3 LEG 2 CLOSED: THE ⊕ CREATE DIALOG, THREE AMEND ROUNDS, FOUR BROWSER GATES (append block)

### Rulings of record

- **R-A (Daniel, Aug 12) — Enter HOLDS an unmatched box-type name; the ⊕ opens a create dialog.** Silently reverting the operator's typing is wrong. Pedigree: v1 ("v1 holds it as unmade until you hit the plus button") plus v2's own add-a-cable-to-a-nonexistent-bundle idiom (`UpdateBundleDialog`, create mode; `CableListClient.tsx:1677` gates before create; `:1890` Cancel leaves nothing). The dialog collects Name · Width · Depth · Height · Weight · Internal Volume.
- **BOX-VOL-R1 WITHDRAWN as a duplicate ID — the ruling is OQ3 (Aug 11).** Arch minted a new ID for a rule that already had one. Daniel restated OQ3's reasoning unprompted and identically eleven days later: internal volume is entered manually, never computed from outside dimensions, because walls, foam and casters make interior capacity a different measurement from exterior size. The build instruction was unchanged and correct; only the citation was wrong.
- **R-B (Daniel) — Change All REFUSALS become modal; CONFIRMATIONS stay passive.** P3's green rename toast is untouched. Refusing an action the operator asked for should stop them; confirming one they completed should not. `ConfirmDialog` gained an opt-in `singleAction`.
- **R-C (Daniel) — the footer inspector loses its four duplicate status checkboxes and the computed weight+cost readout.** `Add'l Weight` / `Actual Weight` / `Add'l Cost` are KEPT and remain editable: they are the only entry point for those values anywhere in the app. Arch flagged the distinction before building; Daniel confirmed the boundary.
- **OQ2 (Daniel) — a held box-type draft REVERTS on leaving the ROW.** It survives Tab onto the ⊕, the dialog opening, and Cancel. Reason of record: an unfocused cell showing a value the database does not have, on a row the operator has walked away from, reads as SAVED — a lie on screen in a dark theatre, and the yellow untyped fill is not enough signal alone. R-A protects typing from the gesture just made; it does not make the draft durable state.
- **The same-row idiom: REVERT via A2's existing path (CC's call, Arch-accepted).** CC built a same-row HOLD first and the browser smoke proved it unreachable — once focus is back in the field the cell is an ordinary autocomplete, and moving within the row is an outside-click commit, which for unmatched text is A2's ruled `revert`. Buying the hold meant forking A2's semantics for one cell to keep a value on screen the database lacks, which is what OQ2's own reason argues against.
- **OQ4 (Daniel) — the create dialog carries ONE line of body copy:** *Add "⟨name⟩" to this project's Box Types. Size and volume are optional.* No v1 copy exists to cite — v1's ⊕ has no dialog at all (Box Catalog §2.4.1) — so this is a v2 invention off the bundle idiom, pedigree (a). **Deliberately omits "you can fill them in later"**: the Box Types admin is leg 4 and does not exist yet; a test now fails if anyone adds the clause before it ships. Blank name degrades to *Add a type to this project's Box Types.*
- **R-D (Daniel, Aug 13) — Tab out of the Type cell OPENS the create dialog** rather than landing focus on the ⊕. One gesture where the build had two, and safe for the same reason capture-Enter is: the dialog gates the create, so Tab raises something confirmable and never writes. Accepted cost, stated at ruling time: Tab is the app's "move on" key, so Tabbing out of a cell holding unmatched text now raises a modal. Shift+Tab stays ordinary.
- **The ⊕ condition is PRESENCE, not provenance (Arch, Aug 13).** The ⊕ hides whenever an option is currently highlighted in the open dropdown, **however it got highlighted**. A highlighted row is a promise that Enter takes it; the ⊕ must not contradict a promise the screen is making. Corollary ruled with it: a hover-set highlight clears when the pointer leaves the list, a keyboard-set one survives — otherwise a stale hover strands the ⊕ invisible with no keyboard route to creation.
- **Declared baseline event GRANTED for frames 09/10/11/12 × both themes (8 frames).** Arch's §6 predicted "frame 10 only"; the footer panel is `always visible` by construction (CP-B1) and is therefore in-frame in all four box captures. CC measured per-frame, proved every changed pixel fell inside one 213×62 box in the panel's column 4, and re-baselined NOTHING on its own authority.
- **KNOW-HYG (Arch proposal, Daniel BANKED for Arch Prompt v30).** Close hygiene for claude.ai project knowledge: retire a superseded governing doc in the same step that installs its replacement; never manually upload a document the GitHub connector already syncs. Not cut as its own version — a natural v30 is already forming (COWORK-REV's ruling, COWORK-PORT, WF1A-SUNSET, seat consolidation). Operating from the docket meanwhile.
- **The Sed Disclosure Rule is widened to the RISK CLASS, not the tool name** (CLAUDE.md, Aug 13): it governs any repo-file modification made through the shell, including a `python3`/`node` heredoc, because all of them bypass the PostToolUse frozen-check hook.

### Corrections of record

- **FOUR Arch corrections this session, and they are one pattern: specifying or asserting against a REMEMBERED or INFERRED surface instead of a MEASURED one.**
  1. The AMEND-2 re-gate script opened the B6 test with "click into Header" — Header is display-only on `BoxRow`, so the stale-arm half of the test could not run, and B6's real root cause sat unverified until a re-test.
  2. AMEND-3 §6 predicted "frame 10 only" without checking that the footer panel is always visible; eight frames moved.
  3. AMEND-3 §6 required the five source-pinned gate tests to survive *while also* ordering the keyboard route restored — four of them pinned its ABSENCE. A self-contradictory instruction; CC re-pointed them and flagged it rather than fixing it silently, and the replacements are a stronger gate than the block they replaced.
  4. Arch told Daniel the dialog's blank-name path was dangerous ("OK will clear the box's type") by reasoning from `planBoxTypeCommit` without checking that `planNewBoxType` guards first and refuses. Daniel's screenshot disproved it.
  **This is the same failure the Aug-11 entry already recorded** ("never design against a remembered schema — only the generated types describe v2") reappearing in the UI, the harness and the test layers. **Standing correction: read the surface before writing the instruction that constrains it.** All four were caught downstream — twice by CC, twice by Daniel — which is the system working, but each cost a round.
- **CAT-VOL-FIX — corrected in the corpus, not just in this Ledger.** `docs/reference/Minotaur_v1_Box_Behavioral_Catalog.md` §3 and `v1-cable-box-catalog/Minotaur_v1_Cable_Box_Catalog.md` both still claimed Box Types' Volume "computes from W×D×H" — the exact claim this Ledger's Aug-11 block had already recorded as wrong. Both now carry the correction inline with its date and ruling.
- **CLAUDE.md's run fence was stale** — it still read "S4-AUTO (S4-2) OPEN at `1426e03`", a unit closed on Aug 11. CC flagged it and correctly left it alone as Arch's section. Rewritten at this close.
- **CLAUDE.md's Return Handoff File Protocol still named the `~/Desktop` destination** that Arch Prompt v29 retired on Aug 10. CC corrected it unprompted during AMEND-3 and flagged it rather than burying it; Arch ruled it into the same commit.
- **The TYPE-JUNK docket row was stale in both directions.** It named two junk types where four existed, and said `Mystery Type` was used by one box — measured, zero, so the "clearing blanks that box's type" hazard had already evaporated.

### THE PROPAGATION THEME — a correction that lands only in the Ledger is INERT

Four instances surfaced in one session: the volume rule (Ledger corrected, catalogs did not), CLAUDE.md's handoff protocol (v29 retired the Desktop path, CLAUDE.md did not), CLAUDE.md's run fence (stale by two days), and the AMEND-2 gate tests (pinning a shape a later ruling superseded). **The Ledger and Roadmap capture rulings; CLAUDE.md and the reference catalogs are what CC actually READS.** Registered as the second Arch Prompt v30 candidate alongside KNOW-HYG: when a ruling corrects a prior statement, the close names every document carrying the old text and updates them in the same package — a propagation check, not a memory test.

### Work of record — CC

- **The D1 root cause underneath Arch's root cause.** Arch identified that `showTypePlus` ignored a navigated highlight. CC then found the deeper defect at Daniel's "works sometimes": the flag recorded **how** the highlight was set, not **whether** one existed — `onMouseEnter` set the highlight and nothing else, so the create gate stayed open through the mouse door while every probe drove it by keyboard. The fix derives presence from render state and announces it from ONE effect, "so no code path can set a highlight and forget to announce it, which is the exact failure mode D4 was."
- **Three defects found by CC's own browser probe before reaching Daniel:** the dialog's Cancel reading as an outside-click and wiping the held draft (R-A defeated); arrow-selection leaving the cell showing the partial; and the same-row hold being unreachable.
- **Refusing to re-baseline on its own authority**, and refusing to adopt frame 10 alone because a knowingly inconsistent baseline set is worse than none.
- **Proving the 02/03 dark byte-drift with controls** (frame 13 negative, 09-light positive) and handing the restore line to Daniel per the standing rider rather than running it.
- **Disclosing the `python3` heredoc unprompted**, on the reasoning that a bash write bypasses the frozen hook even though the rule as written named only stream editors — with a single-match assertion per pattern and the frozen gate run immediately after. Logged for WF1A-SUNSET as evidence the RULE was underspecified, **explicitly not as a discipline strike**; the prior two `perl -pi` datapoints were the opposite behaviour.
- **Reading the gate against the standing rule rather than the tool's verdict** when eight frames sat nonzero under the threshold.

### Harness register updates

- **Flake (3) amendment DISCHARGED into CLAUDE.md** — a long-lived dev server is a trigger for the fail-hard; the remedy is restarting it, not re-running against it.
- **NEW — threshold vs declared event.** The AMEND-3 gate printed `PASSED` with eight nonzero frames because all sat under the 0.1000% threshold (top 0.0906%). Only the any-nonzero-is-a-finding rule caught it; **a real regression up to 0.0999% would print PASSED.** The rule is doing work the tool should do. On HARNESS.
- **02/03 dark byte-drift recurred** under `--update-baselines` and was again restore-don't-adopt, proven at 0 changed pixels.
- Frame count unchanged at **26**; the eight box-family baselines re-adopted once, then untouched.

### Docket movements

**CLOSED:** TYPE-JUNK (cleared twice — 7 boxes + 5 junk types, then the gate residue; `PRG Half` kept then swept) · PRUNE-CHK (13 claude.ai project docs retired: Roadmap v3.6 plus 12 evidence catalogs duplicated by the GitHub sync, proven by RAG returning both copies of the same passage) · CAT-VOL-FIX (opened and closed same session).

**OPENED:** AC-HANDLED · DIALOG-BLANK · BOX-CELLS · TAB-HATCH · KNOW-HYG.

**AMENDED:** HARNESS (threshold-vs-declared-event; 02/03 recurrence) · WF1A-SUNSET (the heredoc datapoint) · GIT-CFG (`next-env.d.ts` diagnosed benign) · BOX-KEYS/B4 (now also inherits the New Box focus landing and Tab-between-fields) · BOX-AC-AUDIT/B5 (now also inherits whether the hover-clears rule intersects (bbb)/(eee)) · PORT-COV (measured: the importer covers 4 of 17 export sheets) · R8-DRILL (its unmet restore-proven leg is what made the Aug-12 JOY reimport unsafe).

### Suite and gate state at close

Suite **1419/96 → 1461/97** · frozen **3/3 vs `be0769de` throughout** · visual **26/26 @ exactly 0.0000%** · migration ledger **49**, unchanged (no schema this round) · dependency clean · fixture pristine across every round, with all probe projects created and destroyed in-run. **Leg 2 CLOSED at `6d52921`** — AMEND-3 + §B + AMEND-4 in ONE commit, 18 files, +1323/−196, pushed and production READY.

---

## August 13, 2026 (evening) — Cowork-Arch session: S4-3 leg 3, B4

### Rulings of record

**BOX-CELLS — SETTLED FROM THE DDR (Arch, confirmed Daniel Aug 13).** v1 edits Header, Number, Truck, Stack and Location **inline on the Box List row**. Citation (b): `docs/reference/v1_ddr.xml`, `<Layout id="179" name="Box List">`. The body band at `top=206` draws two object classes — editable `FieldObj` (Box Name 14 · Box Type 217 · **Box Header 398 · Box Number 498 · Truck Manual 577 · Box Stack 676 · Box Location 786**, plus the four checkbox fields Remaining 922 · HistoryIDOpen 957 "Star" · Drawn 1005 · Packed 1059) and display-only merge `Text` (`<<Percent Full>>%` 850 · `<<Cost>>` 963 · `<<Weight>>` 1077 · `(in <<Box in Box>>)` 582). v1's footer band (`top=240–283`) contains **none** of the five. **The row is their parity home, not the panel.** Daniel confirmed all five in one unit rather than splitting Stack out.

**TAB-HATCH — RULED: no dedicated hatch (Daniel, Aug-13 browser gate).** R-D stands unchanged (Tab with a held box-type draft raises the create dialog). Escape-then-Tab is the hatch, and with the five cells live, Tab from the Type cell now reaches Header rather than stranding. Gated in practice, not reasoned.

**Nested-row treatment — RULED AS BUILT (Daniel, Aug-13 browser gate):** *"this behavior is good — leave as is."* On a nested row the Truck cell is not editable (the `(in ⟨parent⟩)` text stays, R-50) and is skipped in the tab order; Stack stays editable and in the order.

**The nested-row hop — RULED (Arch, Aug 13).** A ⌥⇧↑/↓ hop whose desired column does not exist on the landing row **moves the record, focuses nothing, and remembers the column**, so the next hop resumes it. Falling forward to Stack or back to Header would silently move an operator walking the Truck column into a different field, where the next thing typed lands wrong; refusing the hop strands them. Written against a `fieldExists` predicate rather than against Truck, so the next conditional cell inherits it. **Superseded in part the same evening by PANEL-NAV** for the panel-field case (see below).

**Footer Enter — RULED (b), commits and blurs (Arch, Aug 13), then DEFERRED-SUPERSEDED.** Arch's §7 instruction pointed the wrong way against its own citation; CC measured and stopped rather than building it. `CableInspectorPanel.tsx:141-142` and the box panel's own `DetailText:141` are byte-for-byte the same rule — commit and blur — so aligning to "commit and stay" would have changed the text cells against their cited artifact. Built as (b): all four panel cell kinds now follow one Enter rule, textareas exempt. **Daniel then ruled at the re-gate that the eventual rule should be advance-to-next-record (PANEL-NAV, post-private-beta).** Noted of record: the single rule was the precondition for changing the rule.

**TYPE-HOLD — MILESTONE TRIGGER B halted, three rulings banked, build deferred (Daniel, Aug 13).** Daniel **reversed OQ2**: an unmade box-type name should stay in the cell, yellow, with the ⊕ up, rather than discarding on row exit — and stored, the way v1 stores it. v1 can because Box Type is a text field with a value list (the yellow is the conditional format `Box List::Box Type ≠ Box Types::Box Type`); v2's is a foreign key, so this needs a column. Arch issued a **[MILESTONE HALT]**; Daniel **authorized a SEAT-MIG exception** for the build and mapped it to Sprint-4 close / private-beta prep, leaving current behaviour in place. Rulings: **TH-R1** healing is automatic — a box adopts the real type the moment one with that name exists, no touch, no prompt. **TH-R2** the name prints regardless of whether it is a defined type. **TH-R3** Change All writes the name and the flag travels with it; creating the type heals every box at once, TH-R1 doing the work — making Change All plus one ⊕ a legitimate bulk workflow.

**Change All gains `stack`** per **CA-BOX** (Name and In Box remain the only exclusions; everything else bulk-applies per R-8). Verified from all five row cells at Daniel's gate, and the Name cell still returns the R-B refusal modal.

### Corrections

**Arch, four this session, three of them one pattern.** (1) The B4 handoff spelled the row field member `number` while `CHANGE_ALL_FIELDS`'s key is `box_number` — two names for one thing in one document, which would have armed Change All on a field it could not find: the B2 fingerprint, in the one place the handoff sent CC to look for it. Caught by CC's §3 recon. (2) The declared baseline event named frames 09/10/12; measurement said 09/10/11/12 — frame 11 renders the same box rows. Caught by CC's bounding-box pass. (3) §7's footer-Enter instruction contradicted its own citation. Caught by CC, which measured and stopped. (4) §9 and §12 cited two CLAUDE.md rules that did not yet exist at HEAD — forward-dated citations leaning entirely on a §0 precondition to catch them. All four are the standing Aug-13 correction firing again: **read the surface before writing the instruction that constrains it**; (2) adds its sibling, **a declared event's scope is measured, not reasoned**. Both are banked for Arch Prompt v30.

**A governing-doc delivery defect, and its fix.** The amended CLAUDE.md from the Aug-12–13 close went out as a download, was never saved, and blocked leg 3 at its §0 precondition the next morning — CC halted with the run fence still reading `S4-AUTO (S4-2) OPEN at 1426e03`, two units stale. Root cause: v29 §Seats makes the repo root read-only to the Cowork seat, so a repo-root file can only leave as a download, the one delivery form with no trace on disk. Fix of record, banked for v30: **every repo-root deliverable also gets a `Minotaur-Cowork/drafts/` copy.** The regenerated file was produced by editing the staged repo copy rather than retyping it — 82,217 → 86,112 bytes, five diff hunks at exactly five intended locations, 36 removed lines accounted for line by line.

### CC work of record

- **Halted at §0 twice-correctly:** first on the stale contract, refusing to proceed on a two-unit-old run fence; then, at the very end, on a stale `.git/index.lock` — with the approval phrase already given — rather than deleting anything inside `.git/` on its own authority. It then caught that a subsequent `git push` reporting "Everything up-to-date" meant the commit had never happened, rather than reading it as success.
- **Caught the `number`/`box_number` collision** in recon, before it could mis-arm Change All.
- **Measured the declared event** and found it a frame short of Arch's declaration, with every changed pixel attributed to a named cause — including frame 10's wider bounding box, which turned out to be Change All arming, i.e. required behaviour appearing in a photograph.
- **Refused to build §7** because its citation evidenced the opposite of its instruction, and reported both readings rather than picking.
- **Caught its own confound in the LAT measurement.** A first pass showed +743ms on a production build and would have supported "the create path is slow." CC noticed its dev server was warm while the production server had just booted, re-ran the identical probe against the same still-running process, got +89ms, and inverted its own conclusion — *"the first measurement was a trap I nearly reported."*
- **Disclosed a stray `perl -0pi` fragment aimed at `/dev/null`** that touched no file, hours after the widened stream-edit rule shipped with an origin note saying over-disclosure is always the right call.
- **Disclosed a probe bug in its own cleanup:** it scraped a project name from a dashboard tile whose `textContent` runs the name into its box count, so `ZZ-LAT-throwaway-prod1` came back as `…prod18`, the confirm field never matched, and **the type-the-name delete guard correctly refused a bad automated actor** — recorded here as evidence that guard does its job. Fixed by taking the exact name from a `SELECT`; census proved zero throwaways remained. (Arch subsequently read `ProjectTile.tsx:52-53` and `:61` and confirmed the name and count are separate flex children — the run-together is a scraping artifact, **not** a rendering defect.)
- **Extracted decisions to pure modules so they could be tested at all** — `boxRowCommit.ts` and `boxRowNav.ts`, continuing the leg-2 pattern — and unified `boxRowFieldExists` as the single definition of which cells a row has, shared by the tab order, the render branch and the hop, so they cannot drift.

### Catalog corrections (three, all in `Minotaur_v1_Box_Behavioral_Catalog.md`, dated 2026-08-13)

§2.3's footer-panel field list wrongly included Header · Number / Truck / Location; §2.1's sort-button list omitted Stack though `Sort Boxes by Stack` exists on layout 179; and §2.3 further claimed a footer "status row (Rem/Star/Drawn/Packed + computed weight + cost)" when all of those are body-band objects — the checkboxes at `top=207–208` and the weight/cost as merge text at `top=209` that v1 cannot edit at all. **Three groups of body-band objects filed as footer fields in one section, from one cause: the list was assembled by reading a screenshot rather than the layout.** This is the Aug-13 finding recurring — *a correction that lands only in the Ledger is inert*, because the Ledger records rulings while the catalogs are what CC reads.

### Counts and state

Suite 1461/97 → **1495/99** · frozen **3/3 vs `be0769de`** throughout · visual **26/26 @ exactly 0.0000%** across a four-frame declared event (09/10/11/12, both themes), with `--update-baselines` never invoked in the closing round and the 02/03-dark drift restored-not-adopted for the third consecutive round · migration ledger **49**, no schema · dependency clean · fixture pristine, census-proven character-identical before and after every probe. Daniel's browser gate **10/10**; the closing re-gate produced three deferrals, not defects. **B4 is BUILT AND UNCOMMITTED at the close — HEAD `6a71ece`, the unit in the working tree, blocked by a stale lock now cleared.**

### Post-close addendum (same session, appended after the block above was drafted)

**B4 CLOSED at `8461c2a`.** The stale `.git/index.lock` was cleared and the standing approval phrase — subject-bound and per-commit — was consumed as designed. CC staged by explicit filename, verified the staged set matched FILES CHANGED exactly (12 `M`, 4 `A`, `next-env.d.ts` correctly unstaged), and committed: **16 files, +1131/−117, four new files**, the no-trailer hook passing. Daniel pasted the push; `6a71ece..8461c2a main -> main`. The paragraph above, written while the unit was still uncommitted, is left standing as the honest record of the moment it was drafted — and the close package's Roadmap and Session Log were corrected before install rather than allowed to propagate a false blocking row, which is the **propagation check** (banked for Arch Prompt v30 the same evening) doing its job on itself.


### Aug 14 rulings + closes (the v30-install / HARNESS-H1 / leg-3-cascades session — Cowork principal seat)

- **ARCH PROMPT v30 INSTALLED (Daniel, Aug 14).** Byte-identical copy from `handoffs/parallel/260813-1816_arch_prompt_v30.md` into the Minotaur-Cowork root; claude.ai project knowledge updated in the same step — v29 retired, v30 + Roadmap + Session Log copies refreshed (KNOW-HYG applied to itself; its docket row EXITS). The cover note's instruction to correct Session Log entry 7 in place was **declined of record as conflicting with the log's append-only law** — the index.lock correction (the "stale lock" that blocked B4's commit belonged to the parallel Cowork session, whose `git status` on the deny-unlink mount orphaned it) enters the record here and in log entry 8; prior entries untouched. The propagation set for the correction: Roadmap v3.9's header note and log entry 7 carried "stale lock"; v3.10 and entry 8 carry the truth; entry 7 stands as written under the append-only law.
- **COWORK-REV: CLOSED — and the double record resolved.** Daniel re-confirmed Aug 14. The Ledger's own Aug-11 block already recorded "COWORK-REV CONFIRMED — EXPAND, the docket row exits," while Roadmap v3.5→v3.9 and the log header carried AWAITING-RULING for three more days — four deliveries propagating a stale status. Registered as a propagation-check exhibit (v30 amendment #2's class), Arch's error to own: the ruling landed in the Ledger and never reached the Roadmap. The row EXITS with the Aug-11 riders intact.
- **HARNESS-H1 CLOSED at `e55269c`** (Aug 14, 4 files, +394/−16, one CC session, WF-1a held): every gate line prints `measured @ch0` truth (zero-tolerance pixel count · max channel delta · bbox) beside the judged numbers, with the header naming both thresholds and the run's load average; combined mode flags are a startup hard-error (the CLAUDE.md memory rule retires); `os.loadavg()` refusal above **5.0** (`GATE_MAX_LOAD` / `--force-load`; ceiling standing, Daniel's re-rule invited — measured context: his machine rests 3.9–5.8 under Parallels+Logic, and the gate correctly refused during a build spike at 9.87); `fail()` throws a reaped sentinel and `ensureServer()` reaps its own timeout spawn. **CC refuted the handoff's clean-path premise by measurement** (npm 11.12.1 forwards SIGTERM; its own premature `detached:true` fix reverted, the wrong-order check owned) and caught its own header over-claim before shipping. CLAUDE.md gains the two v30 clauses: the index.lock halt rule and **the Surface Rule, CC's side — "unread is unwritten."** 7 new pngDiff unit tests; suite 1495/99 → 1502/100; visual 26/26, zero frame movement, no baseline written.
- **H1's first working day produced H4's exhibit and H3's best evidence.** The leg-3 checkpoint-A gate printed **26/26 PASSED over eight really-changed frames** (all sub-threshold; visible only in the measured lines) — the AMEND-3 blind spot photographed live. And the 02/03 drift gained three datapoints in one day: two same-baseline runs disagreeing with each other; a second site once (`12-box-type-plus-light`, 6 px, Δ1, bbox 262,107–299,108); frames byte-identical at a baseline refresh yet printing 16 px in a gate run minutes later, same session. **Per-run capture nondeterminism, not baseline staleness** — H3 re-tests the warm-up-boundary model against this.
- **R-E (Daniel, Aug 14 — the Stack/panel question, ruled on the fourth ask after a plain-language rewrite):** the footer panel drops ALL FIVE identity fields (Header · Number · Truck · Stack · Location — Stack was never there, itself the tell); the row is their sole editor; the panel keeps the three label-count overrides + notes, which have no other entry point. R-C's reasoning extended now that the row owns all five (B4). Built into `254ec1c`.
- **R-F (Daniel, Aug 14): P5 re-scopes to leg 6.** Measured basis: script `804_Check things are boxed` — header comments "#Goes to each layout to find things unboxed" / "#Called by 'Print Box Contents List'" — a **five-class pre-print sweep** (equipment · bundles · cables · tails · RF gear), each class its own "Things not in boxes" dialog, Show Me halting the print into that sorted list, Ignore default. The catalog §"entering the Bundle List" line was inference (its own "presumably") and is corrected in-repo at `254ec1c`. v2 builds it v1-shaped when leg 6 builds the print it rides.
- **R-G (Daniel, Aug 14): the Packed cascade's Remaining prompt is ONE consolidated dialog per gesture,** v1's message verbatim ("Some of the things in this box show remaining quantities. Since the box is packed, do you want to mark those as no longer remaining?"), Yes default / No, fired after the packing write and never gating it, covering **the whole recursive member set** (confirmed over direct-members-only — v1 re-asks per class and per nested box; the repetition was FileMaker layout-walking, not design). Script 690's other truths of record: the cascade fires **only on CHECK** (the outermost `If [Packed = 1]` wraps everything; uncheck writes one flag); R-43's "no prompt in either direction" described the pack cascade, not the script entire — the catalog line is amended in-repo.
- **R-H (Daniel, Aug 14 — over Arch's recommendation): R-46 RETIRES.** The Box List's per-column header sort buttons will give way to the cable list's toolbar sort idiom (B4-A2/A4) in a pass of its own — registered as a **deliberate v1 deviation** (v1's Box List layout carries the 8 sort buttons; DDR layout 179's button list is of record). New docket row BOX-SORT; until it runs, the built header stands. CC's refusal to align the surfaces on its own authority — both sides carrying Daniel rulings — is the Parity Rule working as written.
- **R-I (Daniel, Aug 14 — gate item 12): ⌥⇧I on the Box List.** Not in the current record's Name → land in Name, contents selected; in Name → land in the footer panel's Big Labels; from the panel the same rule returns to Name (the cycle is the ruling's letter applied, not an extension — CC's argument, accepted of record over Arch's "flag for veto" framing). Route-local claim, the cable `cycleInspectorFocus` family; six-dialog guard. **This gave `setDetailFocus` its first live caller** — the panel's keyboard door now exists; PANEL-NAV builds on it post-beta.
- **S4-3 LEG 3 CLOSED ENTIRE at `254ec1c`** (Aug 14, 16 files, +853/−49, pushed `e55269c..254ec1c`, terminal-verified): P4's cascade as a pure plan module (`boxPackCascade.ts`, 13 tests) off `boxMembers`' single membership definition — bundles pack though they don't roll up, no N/A exclusion, both script-cited; write targets verified from generated types AND the live view definition before any query (cable packed lives on the spine row; `packed_manual`/`effective_packed` are projections); batched per table; optimistic paint family-wide with the partial-outcome rule on bundle-write failure. B1's find engine ported site-by-site (`useFindController` + `FindPanel`; the 1:1 field map — Type finds the name, Number numeric, Truck never matches the R-50 annotation; D-5 exemption; Delete Found (n)). Two CC rounds, an 11-item gate (items 1–2, 6–11 pass; 3–5 blocked by (fff) and machine-proven on a throwaway — the R-G prompt's both answers and the uncheck-only phase read from the DB between gestures; fixture census identical) and a 5-item re-gate (all pass). The 8-frame declared event was **measured, per-band attributed, and confirmed before re-baselining**; the refresh changed exactly the eight on disk, no restore paste needed.
- **B5 delivered as a REPORT, zero code changes (the audit-only constraint held):** (1) narrowing is substring where v1's value lists are prefix — unruled app-wide divergence, now an Open Question riding AC-ENTER; (2) completion happens invisibly at commit (`commitTarget` → `items[0]`), where v1 completes visibly in the field — the sharpest divergence; (3) the box Type cell **cannot** reach that branch by keyboard: R-D's dialog gate and `showBoxTypePlus`'s visibility condition are complements — the dialog gate, not the shared component, is the cell's protection; (4) **(bbb)/(eee) ANSWERED:** AMEND-4's hover-clears rule narrowed but did not close the window — a hover-set highlight outranks both the completion branch and a typed exact match, so Enter's outcome depends on where the pointer rests; the first AC-ENTER design question is whether a hover-set highlight is a commit target at all (`highlightFromHoverRef` already tracks the distinction); (5) **AC-HANDLED's fix direction:** clear the latch when a new edit begins — never removal; the seams need it true between commit and blur.
- **Two Arch premise errors, both CC-caught — standing-correction instances SIX and SEVEN, both Arch's own:** (6) the AMEND-1 handoff cited `BoxListClient:1588` as ⌥⇧J's registration; it was the FindPanel's prop and **the chord was silently dead on the surface** — CC fixed it at the shared slot, gated on the find; new docket row **SLOT-AUDIT** (an unregistered shared-slot chord no-ops by design). (7) "No UI path exists for remaining_quantity" was half-wrong — creation has one (New Defaults "Mark All As Remaining", `newItemSeed.ts:114`) — so the probe staged through the UI, `execute_sql` stayed SELECT-only, and **(fff) re-scopes** to editing remaining on an EXISTING row, re-pointed post-private-beta (Daniel).
- **Housekeeping closes (Daniel, Aug 14):** **ZERO-CLEAR** — both `0` methods (JOY, Vape!) deleted through GCM Delete Unused; EXITS. **PROJ-CLEAN** — the `Amend2`/`FIX1` batch already cleared; "B4 GATE delete me" deleted on the flag; keepers of record: JOY · Matchbook Festival · Our Town in Space · Test · Test Cable · Vape!; EXITS. **EVID-LAND** — the four S4-AUTO frames identified from the Aug-10 files (v1 pair 12:02 PM, gate pair ~4:25 PM), renamed per the Aug-11 register and landed in `docs/reference/evidence/equipment/` (untracked, evidence exemption, next docs(evidence) commit); EXITS. **TOK-VC re-pointed** to private-beta start, with the ~Nov 8 expiry named as the immovable backstop.
- **Process record:** the session's last catch was an **attested-but-unpushed push** — Daniel reported the cascades push done; the repo's refs (ref file + reflog, three witnesses) said the origin ref hadn't moved since H1's push; Daniel's own terminal confirmed; the push then ran and printed `e55269c..254ec1c`. Logged beside CC's Aug-13 "Everything up-to-date" read as the same lesson from both sides: **a push is verified from the refs, never assumed.** WF1A datapoints: CC's voluntary scratchpad-`perl` disclosure (outside the rule's scope, disclosed anyway — the widened rule's origin note working); phrase-bound commits held twice. FOCUS-MP amended: ⌥⇧↑/↓ lacks the dialog guard R-I got (predates leg 3). The evid-tmp copies the seat could not unlink (deny-unlink mount) are parked at `handoffs/_to_delete_evid-tmp/` for Daniel.


### Aug 14 rulings + closes, part 2 (the HARNESS-H2 / PORT-COV session — Cowork principal seat, opened after the leg-3 close installed)

- **HARNESS H2/H3 SCHEDULING RULED (Daniel, Aug 14): H2 as its own unit, ahead of leg 4.** Arch's recommendation taken over both stated alternatives (H2+H3 bundled; leg 4 first under the hand discipline), on the grounds that legs 4–6 each write a declaration and shipping without scoped writes would leave the pathology H2 exists to remove live for three legs. **H3's build is NOT scheduled**; its step-1 positional control (two gate runs, zero code) rides leg 4's gate runs, which produce the datapoint anyway.
- **HARNESS-H2 CLOSED at `ddcf929`** (Aug 14, 4 files, +1075/−103, one CC session + one amend round, WF-1a held, push refs-verified `254ec1c..ddcf929`, production READY at `ddcf929` exit 0). `--update-baselines` now refuses without `--declare <path>`, measures the moved set itself, hard-fails on any disagreement writing **nothing**, and on agreement writes **only** the declared frames, printing both lists so the run's output is the record. The comparator is a pure module (`scripts/lib/declaredEvent.mjs`, `classifyFrames`) with nine numbered rules, `toWrite` empty whenever `ok` is false; 34 tests. `--gate --emit-declaration <path>` emits a candidate from what the gate measured (bbox padded 2px/side, `maxPixels` = measured × 1.1 + 8, every `reason` blank on purpose and refused if left blank). **`git checkout --` has left the baseline workflow, and the same-run restore trap has nothing left to revert** — the Aug-12 CLAUDE.md rider retires with the mechanism it guarded, not with better documentation. Suite 1514/101 → **1548/102**; frozen 3/3 vs `be0769de`; visual 26/26 @ 0.0000%; `screenshots/baseline/` byte-identical after four capture runs across the unit.
- **H2 judges the moved set at the gate's EXISTING threshold, deliberately** — zero tolerance would make the 02/03 drift hard-fail every declared event and put H2 behind H3/H4. Rules 3, 4 and 9 consume the zero-tolerance measurements. Tightening the moved-set definition stays H4's job, and H2 leaves it a clean seam: one constant plus the registry.
- **Rule 9 registered (Arch ruling, Aug 14, on CC's OQ1):** `declared region far larger than measured`. Rule 3's containment check had no upper bound, so a whole-frame bbox satisfied it for any movement anywhere — the check reading as satisfied while doing nothing, which CC found by using it in its own scratch declaration. Ruled as a NINTH numbered rule rather than an extension of rule 3 (different failure, different message), and as an **area ratio relative to the measurement** rather than an absolute cap — the abuse is orders of magnitude, the false-positive risk is a few percent of shrink, and an absolute cap would reject a legitimately whole-frame event. `area(declared) <= area(measured) × FACTOR + ADD`. **Rule 4 guards the flank**: rule 9 bounds the region, rule 4 bounds the pixel volume, and the pair is tight in a way neither is alone — which is why a generous ADD costs nothing.
- **DECLARE_BBOX_MAX_AREA_ADD shipped at 4000, not the ruled 400 — CC's argument accepted, and Arch's constant was wrong.** The handoff's own required sanity check ("the emit path stays legal") fails at 400: padding a THIN measurement 2px per side multiplies its area rather than nudging it — a 1596×1 full-width line emits at 1600×5 = 8000 px² against a ceiling of 5188, so the harness's own emitted declaration would have failed its own rule. CC swept every w×h to 1600×1000; worst overshoot 3212 px² at exactly that shape. FACTOR untouched at 3.0. Both worst-case shapes pinned by tests so the constant going stale is caught rather than discovered.
- **Two Arch corrections, both CC-caught — standing-correction instances EIGHT and NINE.** (8) The H2 handoff's §0 asserted `visual-capture.mjs` at **633 lines** where it is **632**; CC proved byte-identity to `254ec1c` with `git diff HEAD` and proceeded rather than halting on a proxy. The lesson is sharper than the miss: **a precondition must assert the property it cares about, not a proxy for it** — the intent was "this file has not moved," and a line count was a stand-in whose counting convention had not been checked. (9) §10 instructed "stage by explicit filename and stop," contradicting CLAUDE.md line 253 — **"No phrase, no stage, no commit."** CC declined to stage and flagged it as a handoff defect rather than following. Both are the Surface Rule's own species: text that was already in front of Arch.
- **§6's propagation list was short, and CC left the neighbours correctly.** The amendment named the run fence but not the two-units-stale paragraphs around it (the leg-2 opener with counts "1461/97", the "CURRENT UNIT IS S4-3 LEG 3 — B4 FIRST" block, the BOX-CELLS/layout-179 paragraph, B4's browser-gate checklist). CC left all of them under §6's own "not on the list is an OPEN QUESTION, not a judgment call" instruction, and AMEND-1 brought the whole section current. Arch's omission; the propagation check catching Arch rather than the reverse, third instance this week.
- **The sub-threshold drift is renamed of record: "02/03-dark byte-drift" is too narrow.** Four capture runs in one session landed on four different sets — 12-pair only (light 6 px Δ1 bbox 262,107–299,108; dark 6 px Δ2 bbox 597,108–723,108) · 02/03-light only (16 px) · none · 02/03 both themes (16 px, bbox 1582,18–1583,27). The 02/03 instances all sit at the topbar glyph and fit the parallel block's warm-up-boundary model; **the 12-pair is a second site the model never addressed**, at y≈107–108 and — the part worth carrying — **at different x ranges in light and dark for the same frame**, which is not a shared-element signature. H3's remit grows; the single-pill diagnosis is incomplete rather than wrong. Every one of the four went undeclared and was therefore never written: **"inert by construction" observed, not asserted.**
- **IMP-SPINE registered — a live data-loss path found by audit (Arch, Aug 14; NOT designed, rides leg 4).** The exporter filters `equipment_items` to `type = 'generic'` (`runExport.ts:238`); **the importer's current-state fetch does not** (`ImportV1Client.tsx:63–75`, `project_id` only). Mirror mode plans a delete for every current row the incoming sheet does not match (`diff.ts:133–142`), and cable/tail/box rows can never match — their v1 sheets are register-only. `import_apply_v1` deletes exactly those ids, scoped by `project_id` alone. Live exposure at audit: **41 cable · 14 tail · 13 box rows**, against 1,930 identity-map rows all carrying `entity_table='equipment_items'`. Cascade measured: `cable_details`, `tail_details`, `box_details`, `cable_mult_lines` are ON DELETE CASCADE off the spine row; `bundles.box_id`, `equipment_items.box_id`, `cable_details.source/destination/split_of/allocated_from`, `tail_details.parent_cable_id`, `box_details.within_box_id` are SET NULL, so surviving cables lose links. **The pre-apply "snapshot" does not cover it** — it is `buildRevisionSnapshot` over `v_equipment_list`, a printed-list artifact carrying no cable, tail, bundle or box detail. The dry-run does show the deletes, labelled "Equipment" and rendered by `model`, so a cable row appears unlabeled: the gate is visible but does not communicate the risk. Split OUT of PORT-COV of record — PORT-COV is coverage, this is a scoping defect on a destructive path.
- **PORT-COV parallel session returned and folded in (Cowork, read-only, alongside a live CC session).** Boundaries held: zero writes to `Minotaur`, zero git, SELECT-only, wrote only to `handoffs/parallel/`. Returns of record: `260814-1909_port-cov-sheet-digest.md` · `_export-scripts.md` · `_identity-design.md` · `_cover-note.md`, plus the main line's `260814-1930_port-cov-corpus-and-import-show.md`.
- **PORT-COV rulings (Daniel, Aug 14 — all as recommended):** **(1)** identity entries partition by the detail table's physical name — `'cable_details'` · `'tail_details'` · `'box_details'` · `'bundles'` · `'cable_mult_lines'` — no schema change; a CHECK constraint later would be its own Trigger B, and the current-state fetch's hardcoded single label becomes per-partition queries. **(2)** non-UUID historyIDs (script 506's timestamp+random backfill) are treated as null and carried by the row hash; widening `v1_history_id` to text is declined — measured basis: zero non-UUID IDs in the JOY corpus, 12 null IDs in 1,957 rows. **(3)** **recompute** is blessed as a third import class of record alongside mirror and register-only, stated generally rather than for the two clone-links sheets alone. **(4)** **the round-trippable column set is script 567's own field map, per sheet** — see below. **(5)** Groups' `Notes` and `Start Date` are **deprecated** (0/18 populated in JOY; v1 does round-trip them by design, so this is a deliberate drop, not an oversight). **(6)** the equip export's `Box` column **emits the box's name** — RC-8's blank-emit deferral is amended, its stated reason having expired when `equipment_items.box_id` landed; evidence three ways (DDR: `Equipment Manually Added::Box` is Text/Normal, no calc · rel 80 joins the bare `Box Name`, not the `Box Number Name` composite · value list 24 writes names). Write-only until the boxes importer lands, and now carrying the EXPORT-ORDER constraint below. **(7)** the `RF Cities.xlsx` → `rf city list.xlsx` filename correction is taken with its propagation list (Mapping Spec §1 · `spec.ts:266` · the opener family).
- **Ruling 4 in its final form, and it came from v1 rather than from us.** Script `567_Import Show` maps every re-ingest **positionally** (`Source field N import to Table::Field`) and its map has deliberate gaps — the gaps are the derived columns. Cable imports `Box Manually Added`, `Group Manually Added`, `Length Manual`, `Packed Manual` and never their derived twins; Mult Lines imports only the six ` Manual` twins. **So the hash-input set is a list to read, not a principle to apply.** One precision of record: 567 imports **both** members of the three ` Original` pairs (`Flagged End`, `Split End`, `This is a Mult`), so ` Original` is NOT a calc-twin marker and is excluded from the rule — only ` Manual` and ` Manually Added` mark the stored twin.
- **v1 has no mirror semantics at all — stated of record because the identity design's justification depended on believing it did.** Every step of 567 is `Method: Add`. v1's round-trip is export-everything → import-into-a-blank-file: no update, no delete, no reconciliation, and **HistoryID is imported as data, never used as a match key**. Mirror mode and `import_identity_map` are v2 inventions that work *because* v1's HistoryIDs are stable, not because v1 uses them that way. The design stands; its borrowed precedent does not.
- **EXPORT-ORDER registered.** Because v1's re-ingest is positional, column ORDER is part of the contract. 567's boxes map runs to source field **38** where JOY's `boxes.xlsx` carries **36** columns — v1's own import expects a wider file than v1's own export writes (this also reconciles the digest's "38 of 45" against the measured 36). Any v2-emitted v1-compatible export must match the order 567 expects, not merely the names — a live constraint on ruling 6.
- **WIRE-TYPE registered.** `cable.xlsx` exports `Wire Type` and `Wire Type Quantity`; 567 maps field 60 → `Cable::Wire Type`; v2 has no column for either in `cable_details` or `cable_types`. Confirmed from three sides rather than inferred from absence.
- **The corpus was never missing, and the recon ask is WITHDRAWN.** `~/Minotaur_v1_exports/JOY/2026-07-12_1610` holds all thirteen sheets **including `boxes.xlsx`**, plus `JOY.mino`; BVSC holds everything except `boxes.xlsx`, which is exactly why the July scan concluded no box corpus existed. **The evidence corpus lives in two places and the repo is not the whole of it** — three sessions in a row reasoned as though it were. Containers are settled by v1's own closing dialog: *"Photos weren't imported — sorry."*
- **§Seats amendment PROPOSED (not installed):** the rule that earns its keep is not "no Cowork session while a CC session is open" but **exactly one writer at a time across all seats and CC**. That formulation permits tonight's read-only parallel arrangement, still forbids the Aug-13 `.git/index.lock` incident, and names its own failure mode. Recorded as a datapoint for the **Seat consolidation** open question rather than ruled here; the related fact that the Cowork seat holds Supabase MCP (two scoped exceptions already granted against the "migrations from claude.ai" clause) is registered alongside it.
- **Housekeeping found, not yet cleared:** `Minotaur/{src/` — a stray tree from an unquoted brace expansion (`{app`, `{auth,dashboard,project,api},components`), empty but for `.DS_Store` files, which PROJ-CLEAN did not reach · `Minotaur/_to_delete/` is empty · `Minotaur-Cowork/handoffs/_to_delete_evid-tmp/` holds one file, `ledger_append_aug14.tmp.md`, **not** the four duplicate PNGs the Roadmap row describes — the row was stale in both directions and is corrected at v3.11.
