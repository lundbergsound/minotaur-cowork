# ARCH PROMPT v33 — ASSESSMENT REQUEST (paste block copy, per the drafts/ deliverable-trace rule)

Follow the Architecture Prompt. **This is a SHORT session: assess and propose. Do not run a build unit.**

## The problem

Daniel called the 2026-08-27 session **"verbose and incomprehensible"** and could not read the Roadmap's own Next Sessions block — the one section written for him. Arch's diagnosis was roughly **60% structural, 40% session length**. Assess that split before accepting it.

## Evidence, all from that one session

- **The Next Sessions block reached 24 dense bullets.** RM-6 says *"plain English, kept current"* and sets **no bound**, so it grows by accretion and nothing prunes it. The Session Log does not rot the same way because it has a rollover trigger at 10 entries.
- **Arch v32 already carries both rules that would have prevented this** — *"every identifier gets a gloss on first use, or it does not appear"* and *"plain language first, always."* Both were broken repeatedly. **Nothing checks either.**
- **Daniel asked twice, in the same session, for a simpler question.** Both times the following message regressed.
- **The session ran ~18 hours**: opener → build unit → full CC cycle → two parallel fold-ins → browser gate → defect hunt → close package → the start of an operational drill. **CLOSE-THEN-CONTINUE (registered 2026-08-26) actively permits this.**

## Three amendments proposed — to be ASSESSED, not adopted

1. **RM-6 hard cap.** Six bullets, plain English, no un-glossed identifiers; overflow goes to the Docket. *(Already applied to Roadmap v4.9 as a self-describing heading; the prompt should carry the rule so it survives the next Roadmap.)*
2. **One session, one arc.** After a close package is installed, the next unit opens a **NEW** session. Amends CLOSE-THEN-CONTINUE, which today only tells a continuing session to take its own log entry.
3. **An identifier gate on messages to Daniel** — sibling to the existing *"if you cannot write one plain next step, stop and re-scope"*: if a message needs more than a few identifiers to be read, it is an Arch document, not a message to a person.

## Assess these, do not assume them

- **Does (2) collide with *"a seat that has issued a CC handoff has NOT ended its session — it is waiting"*?** CC's return belongs to the same session and the same log entry. The new rule must not re-open O-6's premature-close wound. Draw the line precisely or do not draw it.
- **Is (1) better as a hard count, or as a fixed SHAPE** — shipped / next / broken / owed — **with no count at all?** A count is checkable; a shape is readable. Say which failure you are optimising against.
- **Is (3) enforceable, or is it a fourth rule with no gate?** Three rules already existed and none of them fired. **If it cannot be checked, say so and do not add it** — the finding would then be that Arch needs a gate mechanism, not another rule.

## Deliverable

Either **Arch Prompt v33 as a complete file retiring v32**, with a version note whose "nothing else changed" claim is a **measured** diff count per O-9 — or **a written recommendation not to amend**, naming what would actually fix it instead. One short session, then close.
