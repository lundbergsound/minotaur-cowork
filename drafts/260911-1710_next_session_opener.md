# Next session opener — Cowork-Arch, written 2026-09-11 (afternoon close)

*⚠ **Measured, not asserted: the paste block is 1,020 characters and the whole file 2,992** — against the previous opener's 1,056 and 3,147. ⚠ **That opener claimed "1,400 characters" and neither of its halves is 1,400; the figure was never counted.** The Roadmap's **▶ NOW** and **⏭ NEXT** carry what an opener used to have to repeat. **If a future opener grows past two screens, the Roadmap has re-bloated — fix the Roadmap, not the opener.***

---

## ⚠ WHAT DANIEL PASTES — nothing else is needed

```
Open the Minotaur Arch seat.

Read, in this order, from ~/Developer/Minotaur-Cowork/:
  1. Architecture_Prompt_v35.md
  2. Minotaur_v2_Roadmap_v4_29.md   <- NOW and NEXT say where we are
  3. Minotaur_v2_Session_Log.md     <- the header, then the last entry

NO ROLLOVER IS DUE — the log stands at 6 and it fires at 10.
NOTHING IS OWED FROM ME. Version history's database half is complete:
the hourly backup and the retention rule went live at migration 59 and
the timer has been running on its own since 17:00 UTC on the 11th.

THE UNIT IS VERHIST-FANOUT — the other fifteen destructive commit points.
It is a CLAUDE CODE unit, not a migration: no schema, no Trigger B.
The census already pins all seventeen; two are wired.

Verify state from files first (never from memory, never from packed-refs),
run the production READY check with me — it is TWO deploys stale — then
scope the unit from the census at source, not from the Roadmap's summary
of it. Tell me in plain English where we are before you propose anything.
```

---

## For the seat, not for Daniel

**The unit.** Fifteen remaining commit points: cable, box, bundle, tail, the two admin grids under v1's **"Delete Unused"** label, and the three import-apply sites. The guard pattern is shipped and proven — `snapshotBefore` calls `snapshot_project_v1`, **refuses the gesture on failure, and treats a NULL return as success-with-nothing-written**. Daniel's 20-row threshold and Delete All's exemption carry across unchanged.

⚠ **The three import sites carry the one constraint that is not copy-paste: the snapshot must be its own COMPLETED request before the apply begins**, because the apply already exhausts its 8-second budget and must keep single-transaction atomicity.

**Do not:** re-open any of VERHIST's seven rulings, the 20-row threshold, Delete All's exemption, or the manual-save exemption ruled 2026-09-11 · add a snapshot to a single-record gesture · re-implement skip-if-unchanged, which is in the function and proven.

**Two things this close registered that the next unit should not rediscover:** **VERHIST-SWEEPWATCH** — `verhist_sweep_log` records every sweep's failures and nothing reads it yet · **REPO-MIGRATIONS-ABSENT** — two migration files in the code repo against fifty-nine applied.

**Owed at Daniel's desk, unchanged:** AUTH-RESETPAGE (gates public beta on its own) · the auto-mode rider's Sprint 4 sunset, carrying **CCSED-260911** as evidence · three ignore lines in the code repo · the two postponed browser gates · the 5167 sheet · the Tail Panel reprint · **CAND-4, overdue**.
