Open as Cowork-Arch on Minotaur v2.
READ FIRST, in order:
  1. Architecture Prompt v30 — INSTALLED, governing; Minotaur-Cowork root.
  2. Roadmap v3.11 (WHOLE)
  3. Minotaur_v2_Session_Log.md — entry 9 is the last session
  4. On citation only: the H2 returns 260814-1849 and 260814-1912; the
     PORT-COV parallel set 260814-1909_* plus the main line's
     260814-1930_port-cov-corpus-and-import-show.md.
STATE: HEAD/origin/production `ddcf929`, refs-verified, READY exit 0.
HARNESS H1 (`e55269c`) and H2 (`ddcf929`) both CLOSED; S4-3 leg 3 closed
entire at `254ec1c`. A declared baseline event is now a checked-in
`screenshots/declared-event.json` the harness computes and enforces —
nine rules, hard-fail writes nothing, agreement writes only the declared
frames. `git checkout --` has left the baseline workflow. Suite 1548/102
· frozen 3/3 vs `be0769de` · visual 26/26 @ 0.0000% · baselines
byte-identical across four capture runs · migration ledger 49. Tree
clean apart from the generated next-env.d.ts (GIT-CFG, never committed)
and the four untracked evidence PNGs in
docs/reference/evidence/equipment/. Minotaur-Cowork committed + pushed,
Sync-now clicked. No CC session open. No parallel session open.
FIRST ACTIONS:
  1. Verify HEAD/origin/production at `ddcf929` from the repo's own ref
     files — never by running git; the mount denies unlink and a
     `git status` orphans `.git/index.lock`. Live-read the migration
     ledger (expect 49).
  2. Scope leg 4 — Box Types admin. Read the v1 evidence AT SOURCE
     before writing anything: DDR layout 239, scripts 588 (Add Box Type)
     and 777 (Export Box Types), and the box catalog §Box Types — which
     leg 3 corrected twice, so trust it only where it cites the layout.
     BOX-VOL-R1/OQ3 already settles the volume question: stored volume
     of record, thresholds are percent. No schema expected; halt at
     Trigger B if one appears.
LEG 4 CARRIES:
  - **This is H2's first real exercise** — the first leg to write a
    declared-event.json. Emit the candidate with
    `--gate --emit-declaration`, fill every `reason` (the tool refuses a
    blank one), and check the file in with the commit.
  - **IMP-SPINE rides it.** The v1 importer's current-state fetch has no
    `type` filter, so cable/tail/box spine rows are planned for deletion
    on any mirror run — 68 rows live, CASCADE onto their detail tables,
    and the pre-apply snapshot is a printed-list artifact that does not
    cover them. The fix mirrors `runExport.ts:238`, which already
    filters `type = 'generic'`. Small shape, destructive path: it wants
    tests, not haste.
  - **H3's step-1 control rides free** — two gate runs, no code, printing
    the pill signature per frame in both passes. The drift's diagnosis is
    incomplete, not wrong: the 02/03 instances fit the warm-up-boundary
    model, the 12-pair is a second site it never addressed (different x
    ranges in light and dark for the same frame).
  - DIALOG-BLANK rides leg 4's dialog touch (consider disabling OK while
    the name is blank).
  - SLOT-AUDIT rides if the surface has a list.
  - TYPE-HOLD is NOT leg 4 (Sprint-4 close / private-beta prep) — but the
    admin surface must not foreclose it: the type list will later show
    names in use that are not yet defined types, healing per TH-R1.
  - SEED is unblocked — `box types.xlsx` is in the JOY corpus (61 rows).
ALSO CARRY:
  - PORT-COV's v1 side is DONE and does not need re-reading. All seven
    rulings are taken. Key facts of record: v1's re-ingest is POSITIONAL
    (script 567), its field map IS the round-trippable column set, and
    v1 has NO mirror semantics at all — mirror mode and the identity map
    are v2 inventions with no v1 precedent. EXPORT-ORDER and WIRE-TYPE
    are new rows.
  - The evidence corpus lives in TWO places: the repo, and
    `~/Minotaur_v1_exports` (grant it if a session needs v1 exports).
    Three sessions in a row reasoned from the repo as if it were the
    whole of it.
  - Standing corrections, now NINE instances. Newest two, both Arch's,
    both CC-caught: a precondition must assert the property it cares
    about, not a proxy for it (a line count is not byte-identity); and
    an instruction must not contradict CLAUDE.md text already in front
    of the author. Siblings: read the surface before writing the
    instruction that constrains it · a declared event's scope is
    measured, not reasoned · a push is verified from the refs.
  - The §Seats one-writer amendment is PROPOSED, not installed — it joins
    the Seat-consolidation open question at COWORK-PORT.
  - ROLLOVER: the log is at 9 entries and fires at 10. The session AFTER
    this one opens with a rollover before any work.
OWED BY DANIEL: the R8 drill before September (the one hard date; also
unblocks PORT-READY's restore-proven leg and feeds the attorney
addendum) · TOK-VC at private-beta start, rotating by ~Nov 8 regardless ·
PARITY-PAIR whenever PRINT-REDO opens · COWORK-PORT + WF1A-SUNSET at
Sprint 4 close · three housekeeping strays to delete (`Minotaur/{src/`,
`Minotaur/_to_delete/`, `Minotaur-Cowork/handoffs/_to_delete_evid-tmp/`).
