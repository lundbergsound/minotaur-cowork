# Next-session opener — Minotaur Arch, after the 2026-09-14/15 close

Paste into a new Cowork Arch chat:

```
Open the Minotaur Arch seat. Follow Architecture_Prompt_v37.md.

Read, from ~/Developer/Minotaur-Cowork/, in this order:
  1. Architecture_Prompt_v37.md
  2. Minotaur_v2_Roadmap_v4_38.md — NOW and NEXT are my words; do not rewrite them
  3. Minotaur_v2_Session_Log.md — the header, then the last entry only

⚠ ROLLOVER IS DUE. The log is at 10 entries. Run it before any work: archive the
oldest 8 to session-log-archives/, keep the newest 2, show me the result and wait
for my confirm before installing.

If the seat check, the state check and the rollover all pass, say
"Session open: passes" and nothing else.

State at the last close: code 3b98ce9 = origin/main, live. Migration ledger 64.
Nothing is building; CC is idle. The twenty-first Ledger append has this session.

⚠ HARD RULE, above everything else this session: nothing but 'owner' goes into
project_members until migration 66 is applied. Not even a test row. Until then a
shared show's backup silently comes out with no cable system in it.

This session, in order:
  1. FIRST, before anything else: write the BATCH-COHORT-1 handoff to CC and give
     me the exact thing to paste. Every member is already ruled — do not ask me to
     decide anything to write it. It is independent of the sharing schema, which is
     why it goes first: it keeps CC busy while the rest of this session runs.
     ⚠ Put my answers IN the pointer. Never hand me a blank to fill in.
  2. When I tell you CC is working, spin off the APPLY-TIMEOUT parallel opener —
     measure what a real full-size import actually costs before proposing any
     ceiling change. Do not let it run a heavy measurement while CC's visual gate
     is running.
  3. When CC returns: apply migrations 65 through 69, in order, one Trigger B and
     one confirm at a time. Add a storage probe against a real bucket object at
     apply time — the four storage policies were rehearsed as DDL only.
  4. Then SHARE-ROSTER. ⚠ Study what Zite did with sharing BEFORE you write that
     handoff, not during the build. Parity is not wanted; the interaction ideas are.

Nothing sends mail — MAILGATE.

Plain English, every message. No jargon I cannot act on. Do not open with
questions. Deliver one step at a time, and when you give me an instruction, give
me the exact thing to paste or click. If a sentence of yours needs a second read,
it has failed.
```

**Owed by me, not the session:** the fourteen remaining cohort accounts · the invite note and the one-page cohort brief · a cable label print on real stock with the 5167 sheet and the production READY check · which methods palette is v1's (METHODS-PALETTE) · CABLEDEL-FAILMSG's wording · YES or KEEP on the sign-in placeholders · whether first and last name become separate columns · `Versioning Test`, keep or delete.

**Two things the last session changed that the next one should not re-derive:**

- The Roadmap is **v4.38**, 32,355 characters, cap ratcheted to 40,000. Seventy-one rows became twenty-eight bundles. Follow it; update it at close.
- **FONT-IS-NOT-SHIPPED** is now a STANDING rule: any measurement of a printed or laid-out surface states which typeface rendered it, because the app ships no font file.
