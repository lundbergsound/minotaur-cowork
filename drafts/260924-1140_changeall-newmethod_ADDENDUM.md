# CHANGEALL-NEWMETHOD — ADDENDUM: the refusal is invisible at the gate, and one screenshot Daniel needs

Tier: claude-opus-5
Model: claude-opus-5
Session: CONTINUE

*Arch, main line, 2026-09-24 11:40 ET. Against your CHANGEALL-NEWMETHOD round. Nothing is committed; the twelve
files stand. Code base `d25d366`. All §0 hard stops from `260922-1255_changeall-mmmcat.md` still apply. This is the
gate's own repair, not new scope.*

**Daniel's gate, 2026-09-24, his words:** 1 passes · 2 passes · **3 fail — "nothing happens, it reverts"** · 4 pass
· 5 he could not find the surface (see C2) · 6 pass · 7 pass.

## C1 · The refusal must be visible where he was working

Your build refuses correctly — nothing is written and the field reverts. **He saw no message.** A silent revert is
the same shape this unit exists to close: the operator typed something, the app disagreed, and said nothing.

- **Measure where the message actually rendered** in his gesture (inspector field, Enter, then Change All from the
  toolbar) versus where you measured it. `actionError` may be off-screen, cleared by the remount nonce, or never
  reached on that path. Read it, don't assume it.
- **Then make it land in the operator's eye**, in the app's existing idiom — no new component. The text stays as
  built: the field, and the word he typed.
- Do the same check for all four fields (method, category, colour, model) and for both invoke paths.
- The regression test pins the message's presence, not only the absence of the write. Deletion control RED.

## C2 · One screenshot, so the gate step can be run

He could not find the surface in step 5, twice. Words are failing; a picture will not.

On a seeded throwaway in **Method sort**, with the equipment list showing at least two methods and a category
inside each, capture **one screenshot** and write it to `docs/reference/evidence/equipment/`, named per convention,
with the category sub-band **circled or arrowed** (any plain annotation). Name in the return the exact on-screen
text of the band in that shot, and the row above and below it, so Arch can write the step in Daniel's own terms.
No behaviour change.

## C3 · Register, do not fix

Daniel, at the gate, on the equipment list: *"I tried clicking into a method and changing it for an item, it asked
me just that item or all, I said just that item, and then when I hit tab, I was in the top menus."* So after the
scope dialog closes, **Tab leaves the grid for the browser chrome** instead of moving to the next cell. Measure it
far enough to say whether focus is dropped or moved, name the file and line, and **stop**. It goes on the Docket.

## Close

The gate set as before, then the COMMIT BLOCK for the whole unit — twelve files plus whatever C1 touches.

**Daniel's gate, the two that failed, in his terms.** Restart `:3100`.

1. On the cable list, click a cable, type `Nutes` into the Method box at the bottom, press Enter. **A message says
   there's no method called "Nutes"**, the box goes back to what it was, and no Change All dialog appears.
2. Step 5 as Arch will rewrite it from your screenshot.

Proposed subject unchanged.
