# Parallel opener — PROCESS-REFACTOR

**RULED (Daniel, 2026-09-12): the governing documents get cut back to something he can read.**

⚠ **DO NOT OPEN THIS BEFORE THE RELEASE TRIGGER: the private-beta invite has gone out AND the first week's cohort fixes have shipped.** Nothing here blocks ≈Sept 18 and nothing here is worth one hour before it.

⚠ **This opener is deliberately short. An enormous opener for a shrink-the-documents job has already failed.**

---

## The four targets

1. **A bounded `Minotaur_v2_Standing_Facts.md`**, created and read into the open order. ⚠ **It must exist BEFORE target 2** — a header rewrite done first deletes standing rules that live nowhere else.
2. **The Session Log header** — measured at **70,554 characters and 102 bullets** — back to the six mandated fields, **under 4,000 characters**. Everything durable in it moves to target 1 first.
3. **Docket rows: ONE LINE each**, saying what is owed and who owns it. The reasoning goes to the Ledger. ⚠ **Ledger append FIRST, always** — five worked pairs are in `260912_meta_DOCKET-ROWLENGTH_PROPOSAL.md` as the pattern.
4. **The Architecture Prompt back to roughly v15's length (~11,000 characters), with NO NEW RULES.** Subtraction only. ⚠ **It is at v36 as of 2026-09-12; this produces v37.**

## Acceptance — Daniel's, and it is a reading test

**He reads the Roadmap's top and the Architecture Prompt end to end and can follow both.** Not a character count, not a checklist. If he cannot, the pass has not landed.

## Boundaries

- **No CC handoffs. No repo access. No migrations.**
- **Writes nothing the main line writes** — proposals as separate files in `handoffs/parallel/`, with `drafts/` copies, because `handoffs/` is gitignored entirely.
- **The main line installs. Everything here is a proposal.**

## Not to be re-litigated

The Roadmap's four sections (▶ NOW · ⏭ NEXT · 🏗 THE SEQUENCE · 📌 THE DOCKET) are settled and this pass prunes *within* them. WF-1a's shape is settled. `--bare` and `--restricted` are struck. SEDRULE-REMAINDER is Daniel's at Sprint 4 close.

## Inputs

`260912_meta_RETURN.md` · `260912_meta_DOCKET-ROWLENGTH_PROPOSAL.md` · `260912_meta_SESSIONLOG_PROPOSAL.md` · `260912_meta_INSTALL-ORDER.md` Tier 4 (items 10–15, dependencies marked). ⚠ **Do not re-measure any of it** — every figure came from a run or a file read with its method stated beside it.

## Return

`handoffs/parallel/<date>_process-refactor_RETURN.md` plus one proposal file per target, each installable whole. **Plain English first.**
