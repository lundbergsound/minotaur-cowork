# PAPERWORK-1: the ruled print fixes, the settings preview, Bundle Summary, Line List, and the box documents' first v1 round

Tier: 3
Model: claude-opus-5
Session: FRESH

*Arch, Cowork main line, 2026-09-18 16:59 ET, under Architecture Prompt v39. HEAD is `7c96904`, which
is also `origin/main` (read from the ref files). Migration ledger **78** (live read). This is the first
build under Daniel's PAPERWORK order, ruled 2026-09-18 (`drafts/260918-1218_paperwork-order_RULED.md`):
step 1 (the ruled print fixes, the box documents' first v1 parity round, the paperwork preview), then
step 2 (Line List and Bundle Summary). They go in one handoff because no member re-baselines the whole gate.*

**⚠ Every answer you need is in this file. There is no blank for anyone to fill in.**

## §0 · HARD STOPS

1. **No migrations and no SQL writes.** Reads only.
2. **MAILGATE.** Nothing sends mail.
3. **Declared baseline events: frames `06-settings` and `07-print-revision-stamp`, both themes, and no
   others.** If any other frame moves, stop and report.
4. **Frozen layer untouched:** frozen 3/3 vs `be0769de`.
5. **Scope is measured.** Every citation below is from `7c96904` unless marked "was". Re-read each one at
   CP0. Where a line has moved, find it by its symbol and quote it.
6. Raw Output Rule, for every command whose output matters: *"Reproduce the complete raw output verbatim —
   into the handoff file's RAW OUTPUT section if the project uses one, otherwise inline. Do not
   summarize, paraphrase, or describe it. Show every line."*
7. **GATE-NOGREP.** Write gate runs to a file, then read the file.
8. **Parity evidence is Chrome Save-as-PDF from Daniel's desk only (PDF-CHROME-PATH).** A Playwright
   `page.pdf` is a reproduction, never parity evidence. Cable Parity Fixture belongs to
   `daniel+test01@` and you cannot see it (HARNESS-RLS-CORPUS). Use the Visual Gate Fixture or seeded
   throwaways as the test user, and clear them afterwards with a zero census.
9. **Commit only on Daniel's `COMMIT APPROVED: <first words of the subject>`**, after his browser gate.

## §1 · Census

| Member | Pedigree | Rides |
|---|---|---|
| **EQUIPLIST-DOTRULE**: solid row rules | (a) Daniel, 2026-09-13: "solid row rules, as v1 draws them". Target (b): `drafts/260906-1457_equip-list_leading-and-rules_measurement.md` §6. v1 draws **one solid 1.00 pt stroke, grey `#DEDEDE`, per row, x 25.27 → 586.27**. v2 draws 372 rects per row (1.50 × 0.75 pt on a 2.25 pt pitch, x 27.00 → 584.25). | CP1 |
| **PREVIEW-CLIP** | (a) Daniel, 2026-09-05: content is cut off in the on-screen print preview but not in the PDF. It was never measured. | CP1, after DOTRULE |
| **METHOD-SORT-ORDER** | (a) Daniel, 2026-09-05: methods **without** a sort override print **after** methods that have one. | CP1 |
| **PRINT-HINT-EQUIP** | (a) Daniel's verbatim text: `Choose "Save as PDF" in the print dialog — not "Open PDF in Preview," which prints landscape pages sideways`. The site is `PrintEquipmentListClient.tsx:1071` (was `:951`), which still carries only the first half. `docChrome.tsx:183` already carries the full ruled text. | CP1 |
| **EQUIPSORT-NODESC-PRINT** | (b) v1 `~/Minotaur_v1_exports/equipment/260829-1804_v1_equipment-labels_BVSCNY_blank-find_sort-description.pdf`, page 1. With a blank find, sort By Description and no labels skipped, the column-1 label slots 1–2 print **empty** and slot 3 is `5045`: **v1 sorts a blank description FIRST.** The screen already does this (`compareDescriptions`, `src/lib/equipment/sortChains.ts`). Daniel was told and said "go", 2026-09-18. | CP1 |
| **EQUIPLIST-NAMEWRAP** | (b) BATCH-FRONTDOOR return, `from-cc/260918-1216_batch-frontdoor.md` OQ3 and RAW OUTPUT §3: a show name that wraps at band size measures **107.0000 against the estimator's 83.0000**, so **24 px per page are cut off**. | CP1 |
| **SETTINGS-PAPERWORK-PREVIEW** | (a) Daniel, 2026-09-17 (`drafts/260917-1145_letterhead-topleft_SPEC.md` §2): "bring back the paperwork head example to the project settings page". UX benchmark (b): Zite `docs/reference/visual-extraction/zite/src/pages/ProjectSettingsPage.tsx:180-222` (`HeaderPreview`). | CP2 |
| **LAYOUT-BUNDLESUMMARY** | (b) `handoffs/parallel/260828-1430_remaining-layouts-design.md` §3.1; v1 script 360; arbiter `~/Minotaur_v1_exports/cable-box/Cable PDFs/bundle summary.pdf` | CP3 |
| **LAYOUT-LINELIST** | (b) the same design, §3.2; v1 script 67, layouts 260 and 75; arbiter `…/cable-box/Cable PDFs/Mult Detail.pdf`. The find fields are corrected by `260918-1224_paperwork-findscreen_DESIGN.md` §3.2 item 1 and table row 7. | CP4 |
| **BOX-DOC-PARITY**: the Box List and Contents List's first v1 round | (b) v1 `~/Minotaur_v1_exports/BVSC_NY/2026-08-20_0120/box by truck bvsc ny.pdf` (12 pp, landscape) and `contents bvsc ny.pdf` (104 pp). The v2 side is **Daniel's two Chrome prints** (§6). | CP5 |

**Out, and why:**

- PRINT-RULES (a) and (c): the Urbanist-only quantity spacing and label ink height. Both wait on Daniel's
  5167 print.
- PRINT-RULES (b), `Male Tails`: needs a v1 ruling.
- The FONT-FALLBACK print half: that is a decision, and nobody has made it.
- REVGROUP-SPLIT: not part of this unit.
- BOX-PRINT-CLIP and the rest of BATCH-BOX: they follow this round. **If you see a box clip, report it
  and do not fix it.**
- The duplicate font literals (`printEngine.ts` ≡ `nordicSkin.ts`): leave them. Report them only if
  CP1 touches them.

## §2 · CP0: citations

Quote every cited line at HEAD, as it stands:

- `printEngine.ts`, both sort branches (by-method ~`:920-931`, by-category ~`:959-979`), `groupByModel`
  `:269`, `groupByModelAndNote`, and `pageHeaderHeight` with `HDR_LEFT_IMAGE_H`.
- `PrintEquipmentListClient.tsx:1071`.
- `docChrome.tsx:183`.
- `sortChains.ts`: `compareDescriptions` and `compareLabels`.
- `MenuPageLoader.tsx:194` and `:199`, the Bundle Summary and Line List entries, both `disabled: true`.
- `visual-capture.mjs`, the frames `06-settings` (~`:924`) and `07-print-revision-stamp` (~`:962`).

## §3 · CP1: the equipment-list print

1. **DOTRULE.** Replace the dotted separator with one solid stroke per row: **1.00 pt, `#DEDEDE`,
   x 25.27 → 586.27 in PDF points**. Prove it on a production build under print media:
   - count the drawing objects per page before and after;
   - show that the row pitch and every existing page-budget numeric are byte-identical. A rule's
     weight must not change pagination. If it does, stop.
2. **PREVIEW-CLIP.** Measure it after DOTRULE, because the rule change may be its cause.
   - Render the on-screen preview for a many-page list at the standard viewport.
   - Compare each page's visible rows against the same page in a `page.pdf` reproduction.
   - **Root cause before fix.** Fix it only if the cause is inside the preview's own container.
   - If it is the paginator, stop and report the measurement.
3. **METHOD-SORT-ORDER.** First measure whether it already holds at HEAD in both branches: by-method
   section order, and by-category `methodTerm`. Use a fixture with at least two ranked methods and two
   unranked ones.
   - If it holds, pin it with a test and report it as closed by an earlier unit.
   - If it does not, fix it so that every method without an override follows every method with one, and
     the unranked methods keep their existing name order among themselves.
   - **Do not change section numbering.** If a non-override section's number can collide with an
     override number, report it.
4. **PRINT-HINT-EQUIP.** Replace the text at `:1071` with Daniel's full text, verbatim, from the §1 row.
   Keep its existing style.
5. **EQUIPSORT-NODESC-PRINT.** In **both** branches, the description term uses `compareDescriptions`
   instead of `compareLabels`. **`compareLabels` stays byte-unchanged**, because its empty-LAST contract
   is relied on by about 20 modules.
   - Test: a model with two notes, where the blank-description item's note sorts second by label. After
     the change, its note row prints first.
   - Deletion control.
6. **NAMEWRAP.** The page budget must learn a wrapped show name the same way it learned the top-left
   image: from measured heights, with no new geometry literal.
   - Reproduce FRONTDOOR's WRAP probe on a production build under print media at 816×1056. Expected
     before: 107 against 83. After: the estimate matches.
   - **It must not under-count in either face** (Century Gothic present, or Urbanist only). **(c)
     Proposal:** where the two faces differ, over-counting by one line is acceptable and clipping is not.
   - Keep the image path intact: a top-left image removes the name, so it stays 81.6719.
   - Pin the shapes in `printEngine.test.ts`, with a deletion control.
7. Gate. **Frame 07 moves** because of the hint line and the rule, and it moves in that frame only.
   Show the diff region.

## §4 · CP2: the paperwork preview on Project Settings

At the foot of `app/project/[projectId]/settings/page.tsx`, add a live preview of the assembled print
header, captioned `Paperwork Example`, which is Zite's text. It must stay in step with what prints:

- **(c) Proposal:** render it with the real print header component and data resolution (the same
  `PageHeader` the equipment list uses, with both images through `signedUrlFor`), scaled to fit. Do not
  write a second copy.
- It updates when the fields and images are saved, not on every keystroke.
- **It does not include Zite's "Production licenses must include the year…" line.** That line is not in
  any v1 evidence on file, so it is uncited and therefore unbuilt.

Frame `06-settings` moves, in both themes. Show the diff region. A top-left image on a test throwaway
must visibly replace the name block in the preview.

## §5 · CP3: Bundle Summary · CP4: Line List

Both use the cable document family's existing chrome (`docChrome.tsx`) and enable their menu entries.

**Bundle Summary**, at route `cable/print-bundle-summary`:

- No find screen. Show All, then sort **Group asc, Bundle Name asc** (script 360).
- One line per bundle, page-broken by bundle group. Each line carries: name · length · the flagged end's
  label · the not-flagged end's label · box (number + name) · group · the count of cables in it · a
  Bundled mark · a Tested mark (design §3.1).
- Read the anatomy from the arbiter itself: 8 pp, **71 rows under 7 group headings**, including the
  "…Continued" overflow page.
- ⚠ **Print the marks from the data. Do not copy them from the arbiter.** The July print shows Bundled
  and Tested on all 71 rows, and the August data does not (BUNDLESUM-MARKS). Daniel's parity check
  excludes those two columns.
- Arch's live read of Cable Parity Fixture, for his check: 71 bundles, 7 groups, Bundled 1, Tested 0.

**Line List**, at route `cable/print-line-list`:

- The **plain shipped find**, the same shape as the existing cable finds.
  - Legend, verbatim: *"Which Mults? (Leaving this blank will show all mults.)"*
  - Fields: **Mult · Model · Method · Group**, with the `+` request.
  - No options and no skip.
- One page per mult cable.
  - The header carries: name · model with length · both device ends · both default ends · both tail
    models and their connectors · Pulled and Labeled marks.
  - Below it, the cable's lines, numbered, each reading *line description · from `<device>
    <connection>` · to `<device> <connection>`*.
  - The sort is v1's Device-name natural-sort chain (design §3.2). Match it with the shared comparators.
- Read the anatomy and geometry from `Mult Detail.pdf` (96 pp).
- Arch's live read of Cable Parity Fixture: **97 mult cables, 561 lines.** It does not match the
  arbiter's 96, because the arbiter is from a different, earlier data set. Report only; Daniel's print
  settles it.

For each layout, render a many-page throwaway, show that no page clips (the PRINT-CLIP class is a hard
stop), and add unit tests for the sort and the grouping.

## §6 · CP5: Box List and Contents List, the first v1 round

**The v2 side needs Daniel's two prints:** Cable Parity Fixture, signed in as test01, blank find, Chrome
Save-as-PDF, saved to `~/Minotaur_v1_exports/parity-pairs/260918_box-docs-pair/`.

**If that folder is empty when you reach this CP, skip it and say so in the return.** The round then
becomes its own follow-up.

If the prints are there:

1. Measure v1 against v2 on each document:
   - page count, orientation and margins;
   - the header;
   - the column set and order (v1's Box List columns: `# · Name · Type · Location · Size (WxDxH) ·
     Weight · Notes`);
   - the row count, sort and group breaks;
   - the rules and fonts.
2. Tabulate every divergence with both values.
3. **Fix only the divergences where v1 is unambiguous and the fix is confined to those two documents'
   rendering.** Anything that is data, a sort ruling, or more than six items goes into the return as a
   list, and Arch scopes it.

## §7 · Close

- Run the full suite, typecheck, build, frozen 3/3, `npm run lint` (exit 0), and the visual gate:
  **28/28 with only 06 and 07 moved**, then re-baselined as declared. Report the migration ledger at 78.
- Replace the run fence in `CLAUDE.md` with this unit (FENCE-ONEUNIT).
- Return to `handoffs/from-cc/` as DONE / FILES CHANGED / OPEN QUESTIONS / RAW OUTPUT, with a COMMIT
  BLOCK (`git add` by filename).
- **Daniel's browser gate**, listed plainly:
  - the equipment print shows solid lines and the new hint line;
  - Project Settings shows the Paperwork Example;
  - Bundle Summary and Line List open from the menu and print;
  - then his Chrome prints of both new layouts on Cable Parity Fixture, for parity.
- Proposed subject: `PAPERWORK-1: solid row rules, print sort fixes, name wrap, settings preview, Bundle
  Summary, Line List`.
