**⚠ UPDATED 2026-08-18 — DOOR + LANDING HAS CLOSED. Read this block first; it supersedes the statements it names. Everything else below stands and is still the operative opener.**

- **THE PEN IS BACK HERE.** The DOOR+LANDING build seat closed its unit at **`e92f2c9`** and wrote Roadmap **v3.14**. There is no parallel writer. The "CHECK THE PEN FIRST" section below is discharged.
- **STATE, corrected:** HEAD = origin = **`e92f2c9`** (`13a8233..e92f2c9`), **Vercel READY in production, confirmed 2026-08-18**. Suite **1639/106** · frozen **3/3 vs `be0769de`** · visual **28/28 at zero tolerance on two consecutive rounds** · migration ledger **50**, unchanged by DOOR. `screenshots/declared-event.json` still describes leg 4's event and was untouched. Session Log is at **6 entries**; **read Roadmap v3.14, not v3.13.**
- **STANDING CORRECTIONS NOW STAND AT EIGHTEEN**, not fourteen. Fifteen: a precondition demanded a clean tree the Roadmap's own `GIT-CFG` row says cannot exist. Sixteen: two commands cited without being run — *the Surface Rule as practised checks that a named thing exists, not that a named command works.* Seventeen: an observation matched to flake (6)'s deterministic model when SUBPIXEL-WANDER's intermittent one already fit it better. Eighteen: a directional correlation asserted from n=1, disconfirmed by the next run. **Two of the four were CC-caught.**
- **DOOR-SEO's Comms Prompt v10 banking is SUPERSEDED by R-W:** Comms Prompt v9 is **deleted and rewritten from scratch** before the next Comms deliverable. The v10 amendments become inputs to that rewrite. See docket `COMMS-REWRITE`.
- **THE GATE HAS A KNOWN INTERMITTENT FAILURE — `GATE-THEMERACE`.** `01/02-light` at **Δ42** over the topbar theme-toggle, position-identical in four runs and absent in two. It is **above channel-24, so it reaches the judged verdict**. **Re-run once; if it clears, proceed. Never register it as a diff-exception** — that is `DIFF-EXC-BOUND`'s trigger exactly.
- **`npm run test:visual` DOES NOT RUN** (no `--env-file`). Use `node --env-file=.env.local scripts/visual-capture.mjs --gate`. Docket `VIS-ENVFILE`.
- **CAPTURE-FLAKE (3) IS ROOT-CAUSED** — `visual-capture.mjs:426` uses `locator.count()`, which does **not** auto-wait, so a slow equipment list fail-hards with no retry. The registry's "dev-server age" trigger is wrong. Docket `GATE-COUNTWAIT`. **The harness line is scheduled behind the box unit, the v1 import/export work and SHARE (R-V) — do not fix it inside a leg.**
- **OWED BY DANIEL, corrected:** Vercel at `e92f2c9` is **confirmed**; **the R8 drill is now OVERDUE**; the four CAND-4 texts stand; **the hero's bid-to-opening line goes plural when Vape! Chicago opens** — `HERO_BID_TO_OPENING` in `src/app/page.tsx`, a one-line edit.
- **NOT THIS SESSION, corrected:** DOOR+LANDING is **closed**, not held elsewhere. `DOOR-V1LINK` is open and is Daniel's call, not this session's.

---

Open as Cowork-Arch on Minotaur v2 — the MAIN LINE. READ FIRST, in order:

Architecture Prompt v30 — INSTALLED, governing; Minotaur-Cowork root.
**The current Roadmap, WHOLE** — v3.13 if the DOOR+LANDING build has not closed yet, **v3.14 if it has.** Check which exists before reading; do not read v3.12, it is retired.
Minotaur_v2_Session_Log.md — 5 entries after the Aug-15 rollover (6 if DOOR+LANDING has closed). Entry 4 is Comms (landing copy), entry 5 is this Arch line's H4 close.
On citation only: the Ledger's Aug-16 H4 block; the H4 returns `from-cc/260815-1810_harness-h4.md`, `260815-1829_…-amend1.md`, `260816-0859_…-amend2.md`; the DOOR design block `handoffs/parallel/260816-1210_door-landing-design-block.md`.

STATE: HEAD/origin **`13a8233`** — **HARNESS-H4 CLOSED and pushed** (`23a34d6..13a8233`). Suite **1611/105** · frozen **3/3 vs `be0769de`** · visual **28/28 at the NEW zero-tolerance default** · migration ledger **50** (`door_waitlist_signups_and_join_rpc`, applied by the main line 2026-08-16, smoked at privilege level, census to zero). `screenshots/declared-event.json` still describes leg 4's event and was untouched by H4. Tree clean apart from the generated `next-env.d.ts` (GIT-CFG, never committed) and the four untracked evidence PNGs in `docs/reference/evidence/equipment/`. **Public signup CLOSED, confirmed and tested by Daniel.**

**⚠ CHECK THE PEN FIRST.** A DOOR+LANDING build seat was granted repo write, CC-handoff authority and the pen on 2026-08-16 (`handoffs/260816-1249_arch_opener_door-landing-BUILD-AUTHORITATIVE.md`). **If that unit has NOT closed, it still holds the pen: this session writes no Roadmap, Ledger or Session Log** — design and delegate only, and hand any document delta to it as a HOLD sheet in `drafts/`. If it HAS closed (v3.14 installed), the pen is back here.

FIRST ACTIONS:

1. **Verify HEAD/origin from the repo's own ref files — never by running git.** The mount denies `unlink`, so `git status` takes `.git/index.lock` and cannot release it, orphaning a lock that halts any live CC session. Live-read the migration ledger (expect **50**, or 50+ if DOOR added nothing — it added exactly one).

2. **TAKE THE `ADMIN-NAV` RULING BEFORE SCOPING LEG 5.** It is registered, measured, and explicitly wanted before leg 5 builds another list surface. Measured: ⌥⇧↑↓ moves selection *and* focus on the Box List (K-2), but only the selection on the **Box Types admin** and on the **Methods admin** it was built from (`MethodsAdminClient.tsx:251-266` calls `setCurrentRowId` alone) — three surfaces, one divergence, none of it leg 4's doing. **The question is whether an admin grid is deliberately a different kind of surface or whether this is a defect.** The answer decides whether leg 5 inherits the idiom or diverges from it. Put it to Daniel in **plain text, not the question widget** — the widget dropped two of four answers on Aug 15 and has now been pre-empted twice by convention.

3. **LEG 5 — IT IS NOT A PORT, AND THE ROADMAP'S ONE-LINE NAME IS MISLEADING.** Measured from `docs/reference/v1_ddr.xml` and carried forward so it is not rediscovered:
   - **v1 has NO on-screen box-contents surface.** Layout **179 `Box List`** is the sole box browse surface. Every other box-named layout is print, label or find: 185 · 186 · 189 · 194 · 209 · 210 · 212 · 216 · 310 · 316 · 324 · 325 · 326 · 357.
   - **Layout 180 `Boxes` is EMPTY** — 120 characters, zero `<Object>` elements. A table stub, not a surface.
   - Therefore **"Contents" has no v1 pedigree to port.** Designing an on-screen contents view is a fundamentally new user flow = **Trigger A**. Halt and take Daniel's scoping ruling before any design.
   - **"Assignments" is largely already built** — box autocomplete and New Box ⊕ at S4-AUTO, row grammar at legs 2–4, `effective_box_id` live, In Box nesting ruled (R-42) and built. **Establish what actually remains** before assuming a unit's worth of work exists.
   - **The contents PRINT chain is leg 6's:** scripts 280 / 714, layout 212, find layout 316; **P5 (script 804) rides leg 6 by ruling R-F.**

4. **H4 CHANGED HOW LEG 5 DECLARES ITS BASELINE EVENT. Read this before writing the handoff.**
   - **The `GATE_DIFF_THRESHOLD_PCT=0` emit/write workaround is RETIRED.** Leg 5 emits and writes with plain flags; the tool judges the moved set at zero pixel tolerance itself. Any handoff still carrying the env override is stale.
   - **`CHANNEL_THRESHOLD` stays 24** — the diff definition of record. Do not touch it.
   - **Rule 9 is per-dimension** at `DECLARE_BBOX_MAX_DIM_FACTOR` **1.75** / `DECLARE_BBOX_MAX_DIM_ADD` **8**; the area constants are retired.
   - **`screenshots/diff-exceptions.json` is checked in with four sites**, two provisional, each with a `refineBy`. **Frame 11's site is n=1** (`184,358`, dark only, `maxPixels` 1): **if leg 5 hits a rule-3 refusal on frame 11, check whether that 1×1 registration was adequate** — that is the first thing to look at, and a second observation upgrades it on the spot.
   - **`DIFF-EXC-BOUND` fires on its trigger, not on a schedule:** the first time a site is registered **above Δ1**. The harness prints a warning naming it.
   - The banked **amend-round re-declaration procedure** is unchanged and is in CLAUDE.md.

ALSO CARRY:
**Standing corrections stand at FOURTEEN, and H4 produced two new shapes.** (13) **Proved a claim on the cases I had in mind and stated it as universal** — "per-dimension rule 9 is strictly tighter" was true for both examples Arch held and false across 41,752 swept cases; CC found it because the instruction said "prove it with tests," and the proof came back negative. Generalising from confirming examples without searching for the disconfirming region. (14) **Conflated two surfaces read in the same sitting** — Arch attributed `_measuredAt` and the workaround text to `emitDeclaration`'s boilerplate, when the emitter writes `_generatedBy`/`_headroom`/`_todo` and `_measuredAt` was hand-written into leg 4's `declared-event.json`. Both files were read; their contents were merged in the writing.
**DOOR-SEO is a COMMS-tier error, not an Arch instance** — but it is the first live proof of the gap v30 §Document Hygiene names, and it is banked for Comms Prompt v10.
**The evidence corpus lives in TWO places** — the repo, and `~/Minotaur_v1_exports` (grant it if v1 exports are needed).
**CC's record across H4:** it refuted Arch's "strictly tighter" claim by sweep rather than building on it; it reasoned ADD from the binding bound rather than the one Arch named; it volunteered, unprompted and repeatedly, that the visual gate did NOT prove rule 9; and it declined to register a wander site on n=1 and asked instead. Weight its judgement accordingly.

NOT THIS SESSION: **DOOR+LANDING** (its own seat holds it) · **AC-HANDLED** (open, app-wide, own pass) · **Print** (closed; PRINT-REDO on Daniel's word only) · the login/landing page's copy and policy · **project and account cleanup** (sequenced after R8-DRILL, deliberately) · units/metric (R-M) · BOXTYPE-CA · BOXTYPE-BTN · IMP-SPINE-2 · R-H's header sorts · **H3** (unscheduled; the wander is tolerated, not explained).

OWED BY DANIEL: **the R8 drill — early week of Aug 17, i.e. NOW.** The one hard date; it unblocks PORT-READY's restore-proven leg, feeds the attorney addendum's retention number, and gates the account/project cleanup · **the four CAND-4 texts this week** · **confirm Vercel reached READY at `13a8233`** (`node scripts/vercel-preopen.mjs`; not verifiable from the Cowork seat — the token sits outside the mounts and the device shell has no network) · TOK-VC at private-beta start, rotating by ~Nov 8 regardless · PARITY-PAIR whenever PRINT-REDO opens · COWORK-PORT + WF1A-SUNSET at Sprint 4 close · the three housekeeping strays under GIT-CFG.

ROLLOVER: clear. The log is at 5 entries and fires again at 10.
