| **DOCKET-ROWLENGTH** | ⚠ **NEW, and it is the half of the Roadmap problem this version did NOT fix.** The restructure deleted 35,541 characters of history and put NOW / NEXT / THE SEQUENCE at the top. **It did not touch row length.** The Docket is **61% of the file**, ▶ ACTIVE alone is 35%, and several rows are multi-paragraph essays that belong in the Ledger with a one-line pointer here. **The rule to apply next: a Docket row is ONE line that says what is owed and who owns it; the reasoning goes to the Ledger.** Pruning it is what lets the 135,000-character cap ratchet down | **Its own small Arch pass, before the next Roadmap version** |
| **CHANGEALL-MODEL-SERIAL** | ⚠ **NEW, found by Daniel on the running app 2026-09-11 and measured at source — NOT introduced by VERHIST-GUARD.** `applyModelChangeAll` (`EquipmentListClient.tsx:2295`) is a `for` loop with an **`await updateItem(id, patch)` inside it**: a model Change All across 341 records is **341 sequential round trips**. Every other field is ONE statement for the whole found set. It is deliberate and has pedigree — it replicates v1's per-record cascade (694, E2, Daniel's v1 attestation 2026-08-05) and each row makes its own CAT-1..4 blank-fill decision, which is why it was never batched. ⚠ **What is new is only that VERHIST-GUARD's ~0.5 s pause in front of it made the loop visible for the first time.** Whether it can be batched is a real question with the per-row category decision in the way | **Its own unit. Not a VERHIST item** |
| **IDMAP-ORPHAN-REIMPORT** | ⚠ **NEW, measured 2026-09-11, and it is a PRODUCT behaviour nobody has specified.** BVSC Mexico City (`cf780353`) holds **1,059 import identity rows of which 823 point at equipment that no longer exists — 77.7%** — against the US Tour's **0 orphans of 2,591**. The cause is benign and confirmed by Daniel: he seeded CDMX with the Tour's entire 1,059-row equipment list and cut it down to the Mexico City rig. ⚠ **That is an ordinary workflow every beta user will perform, and what a re-import does when it meets that residue is UNMEASURED.** `HASH-FREEZE-LABELS` states orphaned identity entries re-import as duplicates, and `PORT-COV-REIMPORT` makes re-import a product requirement, so this is not hypothetical | **Before the next import unit, and before private beta** |
| **VERHIST-RESTORE-IDENTITY** | ⚠ **NEW — the one design question VERHIST-UI cannot avoid.** Restore is fork-only, so a restored production gets NEW row ids. **Carry the import identity map into the fork and you have copied dead pointers at scale** — manufacturing `IDMAP-ORPHAN-REIMPORT` on every restore. **Drop it and the fork cannot be re-imported against at all.** The third option is to REMAP `v2_id` to the fork's new ids at restore time, which is possible because the old→new mapping exists in that moment. **The bytes are captured either way** — `project_blobs` holds the identity map deduped at **384 KB**, larger than the library's 283 KB, so the option is preserved and costs one blob per production | **VERHIST-UI's, and it is a ruling not an implementation detail** |
| **CCSED-260911** | **NEW, small, and it is EVIDENCE rather than a task.** Claude Code made **four single-file repo writes through bash heredocs instead of the Edit tool** in VERHIST-GUARD round 1, two of them without the immediate frozen-gate run the rule requires — disclosed by CC itself, full ledger in `handoffs/from-cc/260910-2135_verhist-guard.md`. Round 2 made none. **The tree is not in question**; a bash write bypasses the PostToolUse frozen-check hook, which is the whole reason for the rule. ⚠ **Feeds `SEDRULE-REMAINDER` and `WF1A-SUNSET` directly: the rule was broken by the seat that had just been handed it, in the pass that quoted it** | **Sprint 4 close, with the auto-mode rider decision** |
===VERHIST-REPLACEMENT===
| **VERHIST — durable per-production version history** | ⚠ **THE SCHEMA AND THE GUARD ARE SHIPPED.** Migration **58** (`20260911002959`) created `project_snapshots`, `project_blobs` and `snapshot_project_v1` — SECURITY INVOKER, `authenticated` and `postgres` only, `anon` refused, and ⚠ **`TRUNCATE`/`REFERENCES`/`TRIGGER` explicitly REVOKED, because Supabase grants them by default and `TRUNCATE` ignores RLS** (the proof run caught that in Arch's own migration). **VERHIST-GUARD shipped at `951557c`.** **All seven rulings stand** — fork-only restore · hourly with skip-if-unchanged, retention 48h/30d/weekly · snapshot before the four destructive gestures plus a manual Save Version · **refuse the gesture if the snapshot fails** · ⚙ PROJECT → History · the library included, content-hash deduped, blobs never expired — **plus the 20-row threshold and Delete All's exemption (Daniel, 2026-09-11)**. ⚠ **MEASURED IN PRODUCTION, not estimated: three snapshots shared ONE library blob and ONE identity-map blob — 1.88 MB where inline would have been 3.15 MB.** Stored cost is **16.4%** of JSON text for the spine and **18.1%** for the library, better than the 26–28% expected from print-shaped revisions, so **a snapshot is 0.58 MB and the whole function runs in ~570 ms against an 8,000 ms budget.** **What is NOT built: the hourly timer, the retention delete, the Save Version button, and fifteen of the seventeen commit points** | **Continues as VERHIST-TIMER → VERHIST-FANOUT → VERHIST-UI, in THE SEQUENCE above** |
