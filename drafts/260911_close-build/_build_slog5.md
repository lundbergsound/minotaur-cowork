===NEWBULLETS===
- ⚠ **VERHIST'S SCHEMA IS APPLIED AND ITS SAFETY NET IS SHIPPED.** Migration ledger **57 → 58** (`20260911002959_verhist_mig_project_snapshots_and_blobs`) — `project_snapshots`, `project_blobs`, `snapshot_project_v1`, SECURITY INVOKER, `authenticated` and `postgres` only — **the first ledger move in ELEVEN units.** **VERHIST-GUARD at `951557c`**, one production deploy, 8 files, +1893/−11. On the equipment list a Change All or Delete Found over **20 records**, and **any Delete All**, now writes a backup first and **refuses the gesture if the backup fails**. ⚠ **NOT BUILT: the hourly timer, the retention delete, the Save Version button, and FIFTEEN of the SEVENTEEN commit points.**
- ⚠ **THE PROOF RUN CAUGHT A `TRUNCATE` GRANT IN ARCH'S OWN MIGRATION, AND `TRUNCATE` IS NOT FILTERED BY RLS.** Supabase's default privileges grant `TRUNCATE`/`REFERENCES`/`TRIGGER` on every new table in `public` to `authenticated` AND `service_role`, so **a `grant` does not narrow that — it adds to it.** As first written, any signed-in user could have emptied version history for every production. **Fixed by revoking from `public, anon, authenticated, service_role` FIRST, then granting — and re-proved by RUNNING the attack, not by reading the grant table.** *TEST THE GUARD, DO NOT READ IT, applied to a grant for the first time.*
- ⚠ **THE `1,024` SPINE ROWS OF RECORD FOR BVSC MEXICO CITY (`cf780353`) ARE WRONG. IT HOLDS 292**, all `generic`. **No data was lost and the cause is benign, proven rather than assumed:** Daniel seeded CDMX with the US Tour's entire equipment list and cut it down. **The Tour's `generic` count is exactly 1,059 — identical to CDMX's pre-prune population — and all 1,059 CDMX identity rows match a Tour row by content hash, 100%.** 1,059 seeded − 823 cut = 236 survivors + 56 typed by hand = **292, to the row**; the 2026-09-07 PDF matches the database on three independent numbers. ⚠ **What it leaves is real: 823 identity rows (77.7%) point at equipment that no longer exists, against the Tour's ZERO of 2,591** — the normal residue of an ordinary workflow every beta user will perform, and re-import behaviour against it is UNMEASURED (**IDMAP-ORPHAN-REIMPORT**).
- ⚠ **THE DEDUP IS PROVEN IN PRODUCTION, NOT ESTIMATED.** Three snapshots share **ONE** library blob and **ONE** identity-map blob — **1.88 MB where inline would have been 3.15 MB**, and each further snapshot now adds zero for either. **Stored cost on a real data-shaped payload is 16.4% of JSON text for the spine and 18.1% for the library**, better than the 26–28% expected from print-shaped revisions, so **a snapshot is 0.58 MB, not 1.40.** ⚠ **The identity-map blob is 384 KB — LARGER than the library's 283 KB**, and Arch's first sketch carried it inline in every snapshot; measuring it before writing it is the only reason it is not costing 384 KB an hour, per production, forever.
- ⚠ **`snapshot_project_v1` RUNS IN ~570 ms AGAINST AN 8,000 ms BUDGET** — measured as `authenticated`, RLS on, the timeout ARMED, on the largest production; four repeat runs of the build-and-hash half at 383/323/315/318 ms. **APPLY-TIMEOUT's exposure on the snapshot half is discharged with ~18× headroom**, so refuse-on-failure is safe. ⚠ **The payload's definitive table set is SETTLED at 20 tables, and `box_details` and `rf_device_details` carry NO `project_id`** — they key on the equipment spine id, so a snapshot filtering on `project_id` alone would have captured **no boxes at all**, silently, surfacing only at a restore.
- ⚠ **THE ROADMAP IS RESTRUCTURED AND THE PROMPT REWRITTEN, BECAUSE THE PROMPT WAS THE CAUSE.** v4.27 was **161,971 characters** with the block Daniel reads at **1,546 — one percent** — above **28,000 characters of history** he scrolled past to reach the sequence. **Architecture Prompt v34 bounded that one block and nothing else, while every close instruction ADDED and none removed.** **Roadmap v4.28: four sections — ▶ NOW · ⏭ NEXT · 🏗 THE SEQUENCE · 📌 THE DOCKET — history deleted outright, 161,970 → 119,008 characters.** Every carried Docket row is **byte-identical by construction**, the file assembled by extraction rather than retyped. **Architecture Prompt v35** replaces the Roadmap rule and **DELETES two: the Docket Sweep line and "read the Roadmap WHOLE."** ⚠ **DOCKET-ROWLENGTH is what this did NOT fix** — the Docket is still 61% of the file.
- ⚠ **GREP-NULBYTE IS STALE.** `BundleListClient.tsx` is **75,051 bytes with ZERO NUL bytes** and `file` reports plain UTF-8 — measured by CC and re-measured independently by Arch. True of the record, no longer true of the repo. **A sweep that skips that file citing this rule is skipping it for a reason that no longer exists.**
- ⚠ **CHANGEALL-MODEL-SERIAL — found by Daniel on the running app and NOT introduced by VERHIST-GUARD.** `applyModelChangeAll` (`EquipmentListClient.tsx:2295`) is a `for` loop with an **`await` inside it**: a model Change All across 341 records is **341 sequential round trips**, where every other field is ONE statement for the whole found set. Deliberate, with pedigree — it replicates v1's per-record cascade and each row makes its own CAT-1..4 blank-fill decision. **What is new is only that the guard's ~0.5 s pause made a long-standing loop visible.**
- ⚠ **RULED (Daniel, 2026-09-11): a bulk gesture only backs up above 20 RECORDS** — strictly greater; 20 takes none, 21 does — **Delete All EXEMPT and always backing up**, and **the refusal wording is his, verbatim**, with a test that fails if the threshold drifts from the sentence naming it. ⚠ **A refusal must say WHY the backup was triggered, not merely that it failed.** **Closed by arithmetic rather than by ruling:** Change All's "No — just this record" writes one row, one is below twenty, so it never backs up. ⚠ **And a standing instruction: NO ARCH REWRITES OR ADDITIONS WITHOUT REMOVALS.**
===ENTRY===

### September 11, 2026 — Cowork-Arch session, the MAIN LINE (**VERHIST'S SCHEMA APPLIED — migration ledger 57 → 58, the first move in ELEVEN units — and VERHIST-GUARD SHIPPED at `951557c`, one production deploy** · ⚠ **the proof run caught a `TRUNCATE` grant in Arch's own migration that RLS does not filter** · ⚠ **a corpus fact of record wrong by 3.5× on the driver production, cause proven benign by arithmetic that closed exactly** · **the dedup proven in production rather than estimated** · ⚠ **the Roadmap restructured and the Architecture Prompt rewritten, because the prompt was the cause** · **six Arch errors, four caught before they reached anything** · model `claude-opus-5`)

**Seat check passed** — all three mounts reachable. **State verified from the ref files, never by running git:** code repo `31b4c26` at the open, `Minotaur-Cowork` `31f12e0`, no orphaned lock files, `CLAUDE.md` **115,579 characters** measured with `wc -m`, migration ledger live-read at **57** and byte-matched, Session Log at 4 — rollover clear. ⚠ **Production READY was five deploys stale and the check could not be run from this seat** — the Vercel token sits outside the three mounts, proven by running the script and getting exit 2 rather than by inferring it. **Daniel ran it: `LATEST READY 31b4c26`.**

**VERHIST-MIG — APPLIED.** Designed from live introspection at source, not from the previous session's notes. **Rehearsed before written:** `snapshot_project_v1` measured at **~570 ms against the armed 8-second timeout** on the largest production, as `authenticated` with RLS on. **Proof-run in a transaction, exercised against the live Tour, then rolled back with a clean residue check** — no tables, no function, no policies, ledger still 57, every corpus count unchanged. ⚠ **That proof run is what caught the `TRUNCATE` grant** (see the header). Applied on Daniel's explicit confirm, to `main` on his call. **Post-apply verification including privilege level: `authenticated` holds SELECT/INSERT on the blobs and SELECT/INSERT/UPDATE/DELETE on the snapshots, `anon` and `service_role` hold NOTHING, and there is no `TRUNCATE` anywhere.**

**VERHIST-GUARD — SHIPPED, two rounds.** ⚠ **The obvious seam was the wrong one:** `lib/bulkWrite.ts` carries every batched write in the app and never sees a `project_id`, so the guard belongs at the gesture, not the helper. **Arch estimated "more than a dozen" commit points; CC measured SEVENTEEN** — 7 Change All, 7 bulk delete, 3 import apply, with two bulk-delete surfaces Arch never named because they carry v1's **"Delete Unused"** label. **AMENDMENT 1 carried Daniel's 20-row threshold, Delete All's exemption and his refusal wording.** ⚠ **CC's own test found a defect in the guard: the database call sat outside the error handling, so a malformed client would have made it THROW rather than refuse — and because every call site in this app decides on a returned value, the write would have run anyway.** The guard would have failed open, silently, in exactly the case it exists for. **CC also deleted two of its own round-1 tests for asserting nothing, one a literal `expect(true).toBe(true)`.**

**Daniel's browser gate passed on all, before the confirm phrase**, and ⚠ **Arch verified end to end from its own seat what no harness in that repo can see:** three snapshots landed with the right reasons, the under-20 delete wrote none — **proven by the row-count gap between two snapshots rather than assumed** — and the single-record delete wrote none.

⚠ **CC disclosed two discipline defects of its own:** four single-file repo writes through bash heredocs instead of the Edit tool in round 1, two without the immediate frozen-gate run. Round 2 made none. **Registered as CCSED-260911 and it feeds the auto-mode rider's sunset decision directly.**

**The governing-document work, on Daniel's ruling.** He could not read the Roadmap and had said so for a fortnight. **Measured rather than assumed, and the cause was this project's own prompt** — see the header. **Roadmap v4.28** (four sections, history deleted, −42,962 characters, carried rows byte-identical by construction) and **Architecture Prompt v35** (the Roadmap rule replaced, two rules deleted to pay for it, net +856 disclosed rather than shaved). **A note for the Elephant Scribe Arch seat is in `drafts/260911-0215_ES-arch_roadmap-rule-change.md`,** because ES has **no Ledger** and would otherwise inherit a rule pointing at a document it does not have — its Roadmap history goes to the Session Log and its archives.

⚠ **Six Arch errors, four caught before they reached anything:** the `TRUNCATE` grant · the commit-point undercount · the identity map nearly carried inline at 384 KB per snapshot forever · **the CC handoff and a gate question delivered in the SAME message with the action first**, so Daniel started CC before reaching the question and the round-1 return correctly recorded the refusal string as unruled · a symbol named in the amendment without checking it at source · and ⚠ **the register failed a FOURTH time** — a paragraph of seat shorthand drew *"what does this mean? is this a good thing or a bad thing?"*, in a message about making documents readable. Rewritten from first principles rather than annotated.

**Next action:** Daniel commits `Minotaur-Cowork` with the block in this entry, then uploads the four governing documents to project knowledge. **Then VERHIST-TIMER opens in a NEW session on a fresh opener** — Trigger B, Arch's, rehearsal-first — **gated on one ruling from Daniel: whether a manually named Save Version is exempt from the retention cleanup.** Arch's view is no.
