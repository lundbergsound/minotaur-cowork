# PARALLEL OPENER — BUNDLE-CLONE-EXT: cloning a bundle and extending a bundle (design only, D-2b)

*Arch parallel, opened from the main line 2026-09-22 17:35 ET, under Architecture Prompt v39. Main line: Cowork-Arch,
CC is running CHANGEALL-MMMCAT. Code `d25d366` = `origin/main`. Migration ledger **83**. Read the governing set in
the prompt's order first: Brief v1.2 · Roadmap v4.49 · the Ledger on citation · the Session Log.*

## Your scope, and nothing else

One of Will Pickens's eight rows, ruled 2026-09-22: **BUNDLE-CLONE-EXT**, Daniel's *"bundle extensions and bundle
clones"*. Single cables can be cloned and extended; bundles cannot. This is the old **D-2b**, which fell off the
Roadmap and is being restored under this name. Its intake is
`handoffs/parallel/260922-1237_bundle-clone-ext_TO-MAINLINE.md` — read it first; it names the state at HEAD, the
Ledger's REC-1 / REC-2 shape (extend the existing two-phase bundle ride, never fork a third copy family), and the
recon at `docs/reference/evidence/cable/d2b_clone_bundle_recon.md`.

Take the design as far as the evidence allows, and no further:

1. **What is measurably true today** — the bundle list's controls, the two-phase ride, the link-group membership
   model, and `projects.bundle_invert_extensions` (R-11), which exists and which nothing reads for bundles.
2. **The design close** needs four v1 behaviours the corpus does not hold: *"Enter Clone Bundle Name"* (script 441),
   *"Add Cable to Cloned Bundles?"* (script 704), and the four *Delete Cable with Clones* confirm variants (script
   504). Read what the DDR does carry for those scripts, say exactly what is still missing and why a screenshot
   would settle it, and **write the evidence request Daniel works from** — one short list, each item naming the
   gesture that produces the frame.
3. **Where the design can proceed without them**, mark each such choice **(c)** so Daniel can strike it by number.
4. **Linkage schema.** Whether bundle-level linking needs new structure, measured against `link_groups` and the B7
   membership model at source. If it does, write the migration as a **proposal** with its policies, grants and
   the measurements you would take at rehearsal — the rehearsal and Trigger B belong to the main line.

## The rules of a parallel (Architecture Prompt v39)

- **No CC handoff. No schema change and no SQL writes** — SELECT and counts only, never a cohort member's project
  content ("option C", 2026-09-21). **No governing-document edit**, nothing written to the code repo, and **never
  run git in either repo folder** — read HEAD from the ref files.
- Land the return in **`handoffs/parallel/`** *and* a **`drafts/`** copy. **PARALLEL-CLOCK:** the filename's
  timestamp is written at the time of writing.
- Write for Daniel first, in plain words, every identifier glossed; the measurements below it. **Surface Rule:**
  open the code, the DDR or the recon before writing the sentence; where you have not, write "measure".
- ⚠ **REHEARSE-BEFORE-RULING.** Do not ask Daniel to rule a mechanism you have not rehearsed on paper.

## What to return

`handoffs/parallel/<YYMMDD-hhmm>_bundle-clone-ext_RETURN.md`, plus the `drafts/` copy: the design as far as evidence
allows; the evidence request for Daniel; the batched rulings you need, one word each; the linkage finding with its
migration proposal if there is one; and the main-line material (the restored row, its D-2b alias, Ledger material)
stated, not acted on.

⚠ **This row carries "Keep Will Pickens updated."** MAILGATE holds: Daniel sends it.
