# BATCH-FRONTDOOR — ADDENDUM (Arch rulings on the return's OPEN QUESTIONS)

Tier: 3
Model: claude-opus-5
Session: CONTINUE

*Arch, Cowork main line, 2026-09-18 12:43 ET, against `from-cc/260918-1216_batch-frontdoor.md`.
Nothing here changes source code. **No gate re-run is needed** — only two file groups join the commit.*

1. **OQ1 — the proof project:** leave it. Arch removes `FRONTDOOR import-failure record (test user)` (`08e76e42…`) at session close, after the deploy.
2. **OQ2 — `printEngine.ts`: ENDORSED.** The page budget's model of the left column is part of (b); `HDR_LEFT_IMAGE_H = HDR_RIGHT_IMAGE_H` is the ruled box, not a new decision. A clipped page was the hard-stop class.
3. **OQ3 — EQUIPLIST-NAMEWRAP:** registered by Arch into PAPERWORK leg 1 (print-rule fixes). Not this unit.
4. **OQ8 — the run fence: YES, in this commit.** Replace `CLAUDE.md`'s "Current run fence" with this unit, your measured values (suite 3449 / 182, migrations 77, visual 28/28, frozen 3/3). One unit, not a history (FENCE-ONEUNIT). Touch no other section.
5. **OQ9 — the five migration files: YES, in this commit, unedited.** Each was checksummed against `schema_migrations` when landed (Ledger, 26th append). Arch re-read them at 12:43 ET: md5 prefixes `62edc2eca78a` · `2305c119c8f6` · `5d67a3cb3018` · `4f4cd08b2657` · `38b8560fba48` (in timestamp order). Quote `md5sum` of all five into RAW OUTPUT; if any differs, stop.
6. **OQ7 — the message reflow:** accepted as is. It shows only on a failed delete; the wording is ruled. Carried to CONTRAST-PASS as a layout note.
7. **OQ10:** noted for BATCH-HARNESS. OQ4/5/6 are Daniel's browser gate.

**Revised COMMIT BLOCK.** Add `CLAUDE.md` and the five files to step 1's `git add`. Step 2 now expects **26 staged lines: 16 `M ` and 10 `A `**, and no `??` migration files left. Subject and confirm phrase unchanged: `COMMIT APPROVED: BATCH-FRONTDOOR: top-left print logo`. Append to the commit body: *"Also records migrations 72-76's files of record and the run fence for this unit."*

Raw Output Rule: *"Reproduce the complete raw output verbatim — into the handoff file's RAW OUTPUT section if the project uses one, otherwise inline. Do not summarize, paraphrase, or describe it. Show every line."*
