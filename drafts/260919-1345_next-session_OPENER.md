# NEXT SESSION — opener

*Written at the close of the 2026-09-18/19 Cowork-Arch main line, under **Architecture Prompt v39**.
Read the governing set first, in the order the prompt gives.*

## State — read from the files at the close, 2026-09-19. Verify it anyway.

- Code **`6a8f3d4`** = `origin/main` in both ref files. **Never run `git` in either repo folder.** The two
  `ORPHANED-index.lock.*` files are still Daniel's to remove at the terminal.
- **Migration ledger 80.** ⚠ Live-read it; this number is not a citation. `supabase/migrations/` holds **19**
  files, and **72–80 all have a file of record**.
- Session Log at **2** entries after this close's rollover.
- Governing set: **Architecture Prompt v39** · **Roadmap v4.47** · Brief v1.2 · the Session Log.
- **Nothing is in flight. No CC session is live. No parallel is open. This seat holds the writer.**

## First action — PRINT-REPARITY-LABELS, as one CC handoff

**Census the members before writing it (HANDOFF-BUNDLE):**

- **Small Cable Labels to v1.** The handoff is drafted in
  `handoffs/parallel/260919-1124_print-reparity_RETURN.md` §8 A, with all four rulings in §0 (R1–R4, Daniel's
  words quoted). Read §4.4 for the numbers.
- **Tail Panel "by model":** the latent re-sort (REVPAIR-TAILSORT return OQ6). Tail Panel is on the same
  label client, and today it is invisible on JOY.
- **Candidate to bundle: EQUIP-PARITY-2's nine CC-ready items**
  (`handoffs/parallel/260919-1105_revlist-regression_RETURN.md` §6 A, approved 2026-09-19). The equipment
  list is a different surface, so the census decides. It is **out if** its sub-note-wrap measurement or the
  page-count item (20 against v1's 19) needs Arch first.
- ⚠ **BOX-ROW-TYPE stays out.** It is a design call, and it is Arch's.

**While CC runs:** rehearse the `v_equipment_list` change that adds the equipment `address`. Without it,
every Equipment Label prints a blank address. Carry the print-settings column default to v2's shape in the
same migration. **It is a view recreate:** capture the grants, `security_invoker` and dependents first
(POLICY-CAPTURE-FIRST, MIGRATION-FILE-FROM-SOURCE). Trigger B; Daniel's go-ahead. **Then STECK-EQUIP-LABELS**
(design complete; the v1 JOY Steck and Equipment-by-group prints are filed), then Daniel's one 5167 check.

## Owed to Daniel

- Nothing is owed as a walkthrough.

## Traps

- ⚠ **WRITE TO DANIEL, NOT TO YOURSELF.** Plain words first; jargon belongs only in CC handoffs.
- ⚠ **TEST SHOWS ONLY.** Every gate and probe runs on JOY or the `daniel+test01@` projects, never Buena
  Vista Social Club. (Arch told Daniel to gate on BVSC this session.)
- ⚠ **A Roadmap rewrite censuses EVERY UNIT ID in the version it retires.** Each ID must be present, closed
  in the Ledger, or retired by a quoted ruling. **This replaces the Brief-section census.** PRINT-PARITY,
  MMM-CAT and six v1 sections were all lost at v4.28 because only the Docket was checked.
- ⚠ **A migration's file of record is the executed statement**, read back and md5-matched. It is never
  Arch's own copy with added comments.
- ⚠ **Quote a parallel's citations only after re-reading them at source.** Two were wrong this session.
- **The parallels' returns are the plan.** Don't re-measure what they measured; issue from them.
- **MAILGATE holds.**

---

## For Daniel

**This session shipped three releases, and every one passed your check on JOY.** The paperwork's first
part is live: solid row lines, Bundle Summary and Line List matched to v1, and the Paperwork Example.
Methods now sort the way you ruled: numbers first, then no method, then blanks A–Z. The revised list
compares correctly in any sort; that fault dated from July. "By model" tail labels sort by model, and
JOY's Small Tail Block labels went from 14 sheets to 3.

**The full v1 re-check is back on the Roadmap, first in line.** It fell off on 11 Sept in a rewrite, along
with category-band editing, and both are restored. A check now stops that happening again.

**Next time starts on Small Cable Labels.** Today they print a third of a sticker low. Then Steck and
Equipment labels, then your one 5167 check on all four label types.

**Still yours:**

- thirteen accounts and their real names;
- the invite note and brief;
- a matched JOY box pair (v1 and v2);
- a v1 JOY Equipment Labels print By Description;
- a fresh v1 export of BVSC US Tour;
- a fresh licensing export;
- wording for the bug-report form, and for an import too big even for ninety seconds.
