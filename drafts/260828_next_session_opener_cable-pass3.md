# NEXT SESSION OPENER — Cowork-Arch, MAIN LINE

**Written 2026-08-28 at the close of the session that shipped PRINT-CLIP and BOX-PRINT-CLIP and closed ORIENT-FIXFAILED.**
**This session opens on CABLE PRINT PARITY, PASS 3. Read §3 before anything else.**

---

## 0 · SEAT CHECK

Cowork, three mounts reachable: `Minotaur` · `Minotaur-Cowork` · `Minotaur_v1_exports`. **If not, STOP and say so.**
**Mounts do NOT persist between sessions — expect to grant all three at open.**

**A PARALLEL Arch session may be LIVE** on the seven queued layouts (`handoffs/parallel/260828-1045_remaining-layouts-design-opener.md`). It issues no CC handoffs and writes nothing you write. Its returns land beside its opener and **fold into this session's close as evidence**.

## 1 · ROLLOVER — CLEAR

Session Log stands at **3 entries**; rollover fires at 10. Last archive `Session_Log_Archive_2026-08-27.md`. Nothing to run.

## 2 · READ ORDER

`Architecture_Prompt_v33.md` → `Minotaur_v2_Roadmap_v4_12.md` (WHOLE) → `Minotaur_v2_Session_Log.md` (STATE OF PLAY + the Aug-27–28 entry) → this opener → then, on citation only, `handoffs/_staging/260828_cable-pass3_DRAFT.md` and the cable-parity evidence trio in `handoffs/parallel/`.

**Verify state from files, never by running git.** **⚠ Read `.git/refs/heads/main` — NOT `.git/packed-refs`, which carries `be0769de`, six commits stale, and is also the frozen-gate baseline so it looks plausible.** HEAD should read **`ff82281`**. Migration ledger **55**. Suite **2468 across 135**. **CC is IDLE. Nothing is in flight.**

**Pre-open drift check, and it has failed four opens running:** confirm `Minotaur-Cowork` carries this close. Its last commit before it named Roadmap v4.10 and log entry 9.

## 3 · FIRST ACTION — CABLE PRINT PARITY, PASS 3

**Daniel's priority. The evidence is gathered and filed; nothing is owed by him to start.**

`~/Minotaur_v1_exports/parity-pairs/260828_cable-family/` — eight v2 outputs printed 2026-08-28 from **`Import Test 5`** through Chrome's own Save-as-PDF, plus sixteen options screenshots in `screens/` recording exactly which sorts and finds produced them. v1 arbiters: `~/Minotaur_v1_exports/cable-box/Cable PDFs/`, all seventeen traced and **data-derived — no fresh v1 print is owed.**

**A DRAFT handoff exists at `handoffs/_staging/260828_cable-pass3_DRAFT.md`. It is UNISSUED. Re-anchor every line citation in it at `ff82281` before pointing CC at anything** — on the last handoff three of Arch's cited line numbers had already drifted and CC caught all three. Re-anchoring is cheap; a stale anchor costs a round trip.

### §3.1 · CHECKPOINT 1 IS **CHECKLIST-GROUPCOUNT**, NOT A PIXEL

The Cable Checklist printed on Aug 28 shows **16 group sections**. v1 shows **27**. The M0-WIRE close records **"27 group sections exact"** on this same fixture the day before. The options screen was clean — sort By Group, then by Bundle, then by Cable · Model-before-name off · Which Cables blank · Print List off — so **it is not a find narrowing the set.**

Either the earlier 27 was counted by a different rule, or something moved. **Settle it with a STATED counting rule, on the artifacts, before a single measurement of geometry.** Geometry on an unstable section structure is work that gets redone — the parallel said so in its own §17, and the box family was already done twice.

### §3.2 · WHAT IS ALREADY TRUE, so it is not re-derived

- **TIER 2 IS CLOSED.** All eight outputs are `Skia/PDF`, **zero non-upright characters**, and the three landscape ones are genuinely 792×612. The parallel's §15 orientation tier is retired.
- **TIER 1, after the selector work:** Cable Folder Labels **24 ✓** · Tail Checklist **116 ✓** · Tail Panel Labels **137 ✓** · Bundle Sheets **71 pages ✓** · Cable Checklist carrying **190 of 190** tail lines.
- **The four PORTRAIT label grids' v1 geometry stands** — Small Cable Labels, Small Tail Block Labels, Tail Line Labels, Cable Folder Labels, measured in points in `260827-0020_cable-parity-v2-pairs-MEASURED.md` §16. **Any geometry that document measured on a LANDSCAPE output is void** and must be re-measured (PDF-CHROME-PATH).
- **Arch's own counts on three label sheets were PROXIES**, not the parallel's counting rules — bullet glyphs and filled rectangles. Tail Panel Labels landing exactly on 137 suggests the proxy works there; **Tail Block Labels reading 113 against v1's 116 is as likely the proxy as a defect. Re-derive the rules; do not inherit the shortcuts.**

## 4 · THEN

**The small clearing unit** — **GATE-METHODRACE** (diagnose the harness race; `visual-capture.mjs:425` queries with no settle wait, and **load is NOT the variable**: it fired 4× at loads 3.70–4.27 on Aug 27 and not at 4.91 on Aug 28) · **`CLAUDE.md`'s run fence**, now three units stale with the counts **re-measured, not copied** · **PRINT-HINT-EQUIP**, one ruled line at `PrintEquipmentListClient.tsx:951` whose only blocker was a working gate. Then **CABLE-SPLIT's Trigger A decision block**, once Daniel exports a v1 show that uses splits. Then **2-MIG-c** — **now WITHOUT the split arm** — carrying APPLY-TIMEOUT · GRANT-TRUNCATE · HARNESS-READ, Trigger B, rehearsal-first. Then **BOX-DOC-PARITY**, then the seven queued layouts as the parallel scopes them, then EXPORT-HISTID-DROP re-measured with EXPORT-CARRIES-STATE, then the staged box residuals.

## 5 · OWED BY DANIEL

**Commit + push `Minotaur-Cowork`** — four opens now carrying the same exposure · **export a v1 show that actually uses cable SPLITS** (the corpus has 3 rows in one show; this blocks CABLE-SPLIT) · **fresh v1 prints of Group Parts, Device Patch and Equipment Labels** — there are no equipment-side v1 PDFs in the corpus at all · delete BVSC NY's two junk boxes · the folder-label **ORDER** ruling · **BUNDLE-LINK-IMPORT** · **WAITLIST-NOTIFY** (Trigger A) · three **box** print reprints · **FENCE-PUSH** (Strat-tier) · **METRIC-UNRUN's browser gate** · CAND-4 · GIT-CFG · **PROJ-CLEAN-2 — `Visual Gate Fixture` AND `Import Test 5` must NOT be swept** · **a STRAT-record entry for the Pro upgrade.**

## 6 · DO NOT RE-DERIVE, AND DO NOT REPEAT

- **⚠ PDF-CHROME-PATH.** A parity print is valid evidence **only** from Chrome's own Save-as-PDF destination. *"Open PDF in Preview"* and *"Print using system dialog…"* route through **Quartz**, which discards the CSS `@page size` rule. **Every landscape v2 artifact taken before 2026-08-27 is invalid.** Portrait ones are unaffected — the box label geometry stands. **v1's own Quartz prints are FINE**, because FileMaker declares its page setup natively.
- **⚠ ORIENT-FIXFAILED is CLOSED and `docChrome.tsx:209-222` is CORRECT.** Do not re-open it, do not "fix" the `@page` rule. The fix never failed; it was never exercised.
- **PRINT-CLIP and BOX-PRINT-CLIP are closed and PROVEN ON PAPER** — 190/190 tail lines, 136/136 boxes, against live-read denominators. `paginateByHeight` is **deleted**; do not reintroduce an estimate paginator.
- **⚠ BVSC NY is NOT a valid arbiter** for anything box-shaped and its completeness is suspect generally (BVSCNY-SEED-JUNK: 136 boxes against v1's 134, two of them named `In Box` and `Box Name`, missing types and weights). **`Import Test 5` is the engine-built fixture** — 134 boxes, 237 cables carrying steck, 2,030 real v1 identity rows. **Never re-import into BVSC NY**: it plans 1119 INSERTS and would double the show.
- **The harness cannot reach BVSC NY or Import Test 5** — owned by `daniel@daniellundberg.com`, harness signs in as `daniel+pt@`, RLS blocks it. **Never write a gate against them.** Throwaway projects, created and destroyed in-run.
- **A full 21-file import still times out at 8s; a 13-file cable-side subset applies cleanly.** `service_role` cannot read — SELECT on 2 objects, TRUNCATE on 35.
- **⚠ EXPORT-HISTID-DROP's carried figure is WRONG** — measured as 0 v1 / 2,811 assigned. Its "join that misses" attribution is **unproven, not wrong. Re-measure before designing that unit.**
- **GATE-THEMEGLYPH must NEVER enter the diff-exception registry.** Fourth observation on a third tree; Δ42 is a real visual difference and DIFF-EXC-BOUND is owed first.
- **Do not re-litigate:** FOLDER-NABOX's 24 · the spend cap ON · CONTRAST-PAIR · PRINT-HINT's wording · **Q-4 — all seven disabled outputs get built, each with a v1 parity round** · **MENU-DEPRECATE** · that PRINT-CLIP shipped with the gate owed, ruled by Daniel over CC's recommendation and recorded.

## 7 · NOT THIS SESSION

CONTRAST-PAIR · WAITLIST-NOTIFY · DEEP-ARCHIVE · GATE-THEMEGLYPH's diagnosis · the `0` vocabulary-row fold · S4-late · EXPORT-HISTID-DROP · the seven layouts' BUILD (the parallel is still designing them).

## 8 · CONDUCT — what this session's record says

1. **Test the guard, do not read it — four instances, and the fourth is the sharpest.** Three sessions read `docChrome.tsx:209-222`, saw the symptom its own comment described, and concluded the fix had failed. **Nobody asked what wrote the file.** One metadata field settled it.
2. **When something looks broken, ask what changed AROUND it.** This session found two environmental variables masquerading as code defects — the print pipeline and the gate harness — and Arch called the second one blocking when it was not.
3. **A measurement's denominator is evidence too.** The printed proofs mean something because 190 and 136 came from `select count(*)`, not from the record.
4. **An estimate that is right by cancellation is not right.** The Box List's two errors nearly offset each other and three of four sorts survived on a 14 px margin.
5. **Mark a proxy as a proxy.** Arch's bullet-glyph counts are in the record labelled as shortcuts, so nobody inherits them as measurements.
6. **A mechanism accepted approximately is a mechanism weakened.** If the commit phrase is a guard, a matcher should check it.

---

*Governing set at this close: **Arch v33** + `Architecture_Provenance.md` · **Roadmap v4.12** (retires v4.11) · Ledger v1.0 (Aug-28 append) · Session Log at **3, rollover clear**. HEAD **`ff82281`**, pushed. Migration ledger **55**. Visual gate **28/28 at load 2.85, recorded as intermittent**. Evidence: `parity-pairs/260828_cable-family/`. Parallel: `parallel/260828-1045_remaining-layouts-design-opener.md`. Unissued draft: `handoffs/_staging/260828_cable-pass3_DRAFT.md`.*
