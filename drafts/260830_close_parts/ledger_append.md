
### 2026-08-30 (evening) — cable pass 4 and the push lift

**PRINT-PARITY cable pass 4 — the anatomy diff, then CELL-AND-ROW at `2cc6b14`.** Passes 1–3 each fixed only what somebody had already noticed, which is why three rounds of "fixed" produced sheets Daniel described as not close to v1. Pass 4 measured first: **nine element classes × four outputs, both sides, in points, per-cell variation recorded rather than averaged, before a line of code.** The instrument did not exist — `pdfgeom2.py` reports type and repeating boxes and no text positions at all — so `pdfruns.py` was written beside it, outside the repo, and self-checked against constants already banked in `cableGeometry.ts` before anything was trusted. **Sixteen ranked gaps, ranked by what a person holding the sheet sees first.**

**Three gaps nobody had named, all ranking above the two that had.** v1 sets ONE right-aligned field `"<End> Tails"` wrapped onto two lines sharing one right edge, printing `" Tails"` with a leading space when the end name is empty; **v2 had it upside down as two separate absolute elements, which is most of why its cell needed 39.18 pt of ink in a 36.00 pt row.** The right-hung slot **hung 19.11 pt past the label's cut edge** in every cell of every column, against v1's 125.19 on a 126.00 pt label, 1,160 of 1,180 instances. And **Tail Panel is a ruled table in v1 — 76 hairlines per page at 0.5 pt `#DEDEDE` — and had none in v2**, removed under a comment true of the other three outputs and false of that one.

**The structural change: v1's tail cell is a set of FIXED ABSOLUTE SLOTS that overlaps rather than reflows.** A block cell with no description still sets its `@` at em-top +25.66, exactly where a cell with one does; and a wrapped `@` sets its second line ~8 pt into the next die row **while that row's chip is still stamped at its own origin.** Ladder before/after: Tail Line line chips **5 values → 1**, block chips **4 → 1**, Small Tail Block **8 → 1**. The landed value is `+0.75` against v1's `+1.00` and `+0.43` — **reported honestly rather than as the acceptance's number**, because Chrome snaps a background rect to its 0.75 pt paint grid and the 0.57 pt by which v1 distinguishes its two chip classes is not expressible there.

**GO TO THE SOURCE THAT MADE THE INK — the method entry of this session.** Two v1 behaviours had defeated every artifact correlation. CC ran the correlation first (a clean character-count separator for two step sites, **no clean separator at all** for the tint) and then, rather than ship a threshold fitted to one corpus, read v1's own layout definitions in `docs/reference/v1_ddr.xml`. **The type step-down is five `Length()` tests at THREE thresholds** — >10, >9, >11, >9, >14 — each tuned to its own field width, so one constant would be wrong on two of the three sheets. **The description tint is the description field's own background, a 12-arm conditional format on `Mult Lines::Line Color`** — a per-line colour, a different column from the cable colour that fills the number chip, **which is why no artifact-only rule could exist: the discriminating datum prints nothing of its own.** Measured separator and source condition agree exactly wherever both exist. ⚠ The DDR is **UTF-16LE**; a byte-oriented grep finds nothing in it.

**Daniel's rulings.** **PAGEBREAK-MODEL: a model section starts a new PAGE on all three tail outputs** — and CC had measured that **v1 breaks to a COLUMN on the two Avery sheets** (8/8 and 7/7 markers at row 0 of a column, only 3/8 and 2/7 at a page top) and to a page only on the panel, which has one full-width column. **Daniel ruled the page break anyway: a deliberate divergence, pedigree (a), and the source comment says so outright** — *do not "fix" this back to a column break by citing the artifact.* **CHIP-CABLECOLOUR: the line-number chip takes the cable colour alone** — v1's ladder is fifteen `Cable::Color` arms with no line-colour arm, and v2's `?? lineColor` fallback made chip and tint disagree on **63 of the arbiter's 79 tinted cells**; the anatomy diff had recorded that as MATCHING because it sampled cells where the two agree, and **CC found it, not Arch.** Also ruled: the print date added where v1 has it in v2's own format; the step-down's trigger measured before it is built; **the `<End> Tails` field follows the database-wide text rules.**

**THE FIRST MATCHED v1/v2 PAIR IN THE PROJECT'S HISTORY.** Daniel printed both renderers on the same mult, the same nineteen lines, one afternoon. **Every prior comparison was two different found sets; FOUNDSET-NOT-COUNT does not apply to these two files.** Line chip x **54.00 both** · row pitch **36.00 both** · `bDRUMS•1` **67.86 vs 67.85** · right-hung right edge **144.98 vs 144.95** · **the ladder one value on both sides at 38 chips against 38.** Filed at `parity-pairs/260830_cable-pass4-matched-pair/`.

**CHIPTEXT-PAIRED, and the defect was invisible by construction.** `bDRUMS`'s cable colour is black and v2 printed the block cell's `Female / Tails` **black on that black chip** — the field did not appear on the page at all. **v1's six-colour white-text condition is exactly `MINOTAUR_COLORS`' own white-text set**, so the fix was a missing override, not a palette change. **Arch proposed a geometric general form and CC refuted it with evidence:** v1's mechanism is per-object, the line cell's end-word object carries no conditional format, and a geometric rule would have turned it white on a long end name. **The artifact settles it on one page — the same mult's block-cell `Female` is white and its line-cell `Female` is black.** A 22-run colour audit followed; one run was wrong.

**FENCE-PUSH at `0bf6df3`, and CC hard-stopped in the middle of it.** Daniel ruled the push fence lifted and `git push` a natural follow-on to the confirm phrase. **Arch read "break the fence" as a NARROWING rather than a removal** — deleting `"Bash(git push *)"` would have handed over force, `--delete`, `--mirror` and refspec pushes in one step, none of it ruled. The harness then refused CC's writes to `.claude/settings.json` and `.claude/hooks/bash-fence.sh`, and **CC stopped rather than routing around the one guard it must not route around** — the unit's core is CC loosening the fence that constrains CC. It battery-proved both candidates in a scratchpad so the stop cost one paste, and **argued against the alternative**: granting `Edit(.claude/**)` is a permanent concession strictly larger than the push lift Daniel ruled. Arch adopted that whole. **The control run is the evidence: the same 34-case battery against the hook at HEAD differs in exactly one case — `git push` — and agrees on all 30 others.** CC added `-d` beyond the ruled list and was upheld (git's real short form for `--delete`; omitting it because a prose list did not enumerate it is the FENCE-COMMITA hole class). Arch's one hardening — `"Bash(git push -f *)"` → `"-f*"` — was **proven live, not argued**: `git push` succeeded, `git push --force` and `git push -f` were both REFUSED at the settings layer, all three run **before any commit on a deliberate no-op** so the test could not deploy anything. **And the rest of the fence proved itself unasked**: CC's own gate-log redirect was blocked because the compound ended in `echo`, tripping the redirect whitelist the unit had promised not to disturb. **`0bf6df3` is the first commit CC pushed, and a WF-1a commit is now a production DEPLOY.**

**CC declined §2 of that handoff and Arch's handoff was wrong to ask for it.** Flipping `CLAUDE.md`'s forward-dated push blocks to live while both layers still denied push would have produced a contract asserting an authority CC did not have — **the FENCE-STALE shape, inside the unit whose own text warns about it.** §2 landed with §1 in the second pass, ten sites, each carrying `FENCE-PUSH, 2026-08-30, Daniel's ruling`.

**The `06-settings-light` gate failure, and GRAPH-BEATS-RERUN.** Run 1 failed 27/28 at **1451200 px, bbox 0,93–1599,999** — the exact pixel count and bounding box `CLAUDE.md` records for the leg-6 theme-race flake, but on the **light** half where the entry says dark-only. **Arch ruled it settled by import graph rather than by re-running:** a re-run says whether it recurs; the graph says whether the frame could ever have seen the change. **Zero of the eight changed modules reachable from `06-settings`, against a control of 8 of 8** from the print route, and `minotaurColors.ts` — the one module reachable from settings — untouched with an empty diff. The re-run was green. **Capture-flake entry (2) is widened a second time: not dark-only, not frame-specific; the invariant is the pixel count and the bbox.** It stays PROSE and never becomes a `diff-exceptions.json` site — masking 1.79% of a frame would blind that page permanently.

**RETURN-ON-CONTROL — the Return Handoff File Protocol, re-triggered.** Daniel diagnosed that CC returns control with results living only in terminal scrollback; twice in one day a measurement reached this seat as a photograph of a terminal. **He proposed a 30-second / 10-line trigger and Arch pushed back**: CC cannot check elapsed time against an artifact, ten lines fires on a `git status`, and O-12 says rules checked at discrete events hold while continuous properties do not. **The trigger is RETURNING CONTROL**, the last terminal line is the return file's path alone (Daniel's words, kept), and **one file per unit, appended** — which kills the proliferation objection by changing what gets batched. **His three exclusions are written into the rule, numbered:** waiting on a step the COMMIT BLOCK already documents · being interrupted rather than finished · a clarifying question before any work.

**"Strat invariant 1", asked and answered.** It is **WF-1, Daniel's ruling of 24 July 2026: CC never stages, commits, or pushes.** **There is no register of Strat invariants** — no invariant 2, nothing in `Strategy_Prompt_v14.md`; the phrase appears with content behind it exactly once, in this Ledger's WF-1 entry, and every other occurrence quotes the fence's own error string. **And the ruling had already been narrowed twice without the fence noticing** — WF-1a took the stage and commit clauses on Aug 9, Arch v32 took the push clause on Aug 26 pending this unit. **So this lift is not a Strat retraction; it is the last third of WF-1 catching up with WF-1a.** **WF-1's push clause is retired by FENCE-PUSH (Daniel, 2026-08-30).** The fence now cites no authority for the destructive family, deliberately; **Arch's ruling is that its real authority is `CLAUDE.md`'s hard-stop list — a force-push is a destructive or irreversible operation — and it goes in at the fence's next touch, not into a tested commit at the last moment.**

**Four corrections to Arch's own record, all from measurement, all propagated.** **JUSTIFY-PERCELL does not alternate** — two axes selected by the tail END (Female 67.48, Male 65.98, 1.50 pt apart, 238/238, zero exceptions), with the DDR's hide conditions as the mechanism. **ROW-LADDER's quoted v1 baselines were EM-TOPS** (74.68 / 76.34 / 81.68 / 89.66 / 97.66; baselines 79.43 / 86.43 / 86.43 / 96.00 / 104.00). **SKIP-LABELS exists in v2** on four surfaces, inserting exactly-36.00 pt blanks, where the Roadmap said it had nothing like it. **`FOLDER_CELL`'s em-tops are 8.67 / 30.67**, and the same instrument reproduced four other constants from that file exactly.

**Arch errors this session, all caught downstream, all recorded.** The **geometric colour rule**, proposed as a general form and refuted at source and on the artifact. The **yellow bands read as a missing-description highlight**, inferred from a correlation on one page — they are the ordinary line-colour tint over a data convention. **Asking CC to flip the forward-dated `CLAUDE.md` blocks in the same pass as the fence lift.** And the anatomy diff's **"chip colours match to 1/255"**, true of the cells it sampled and false of the 63 it did not.

**Registered 2026-08-30 (evening).** **STOCK-IS-DIE promoted to the main line** — columns 2/3/4 measured at −3.75 / −6.75 / ~−10.15 against the 5167 cut on Daniel's own print, ruled but unbuilt. **CHIP-CABLECOLOUR · CHIPTEXT-PAIRED · PAGEBREAK-MODEL · DDR-IS-THE-SOURCE · GRAPH-BEATS-RERUN · RETURN-ON-CONTROL · HARNESS-RLS-CORPUS.** **FENCE-HEREDOC consciously left twice with the file open** — any interpreter can write a file a dozen ways, and a matcher written in passing is either bypassable or a false-positive machine.
