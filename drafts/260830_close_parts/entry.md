
---

### August 30, 2026 (afternoon–evening) — Cowork-Arch session, the MAIN LINE (**cable pass 4 SHIPPED — the anatomy diff, then CELL-AND-ROW at `2cc6b14`: the row ladder collapsed from nine values to one and v1's two unknown triggers quoted from its own layout definitions** · **FENCE-PUSH at `0bf6df3` — CC pushes now, and a commit is a production deploy** · **the first matched v1/v2 pair in the project's history** · **four of this seat's own recorded facts corrected by measurement** · model `claude-opus-5`)

**Outcome.** Opened off `260830-0800_next_session_opener.md`; seat check passed, three mounts granted. State verified from ref files: `Minotaur` HEAD = origin = `5488915`, migration ledger live-read at **55**, byte-match, no orphaned lock. **The opener's pre-open drift item was DISCHARGED at open** — `Minotaur-Cowork` read `c8ae063` = origin, carrying the Aug-30 morning close, so Daniel had committed and pushed it. The opener said the Session Log stood at 4; it stood at **5**, the continuation entry having been written after the opener. Two units shipped, both pushed, and this close is the third thing owed.

**CHECKPOINT 1 — THE ANATOMY DIFF, and it is why this pass worked where three did not.** Passes 1–3 each fixed what somebody had already noticed, which is why three rounds of "fixed" produced sheets Daniel described as not close to v1. Pass 4 measured the whole anatomy first: **nine element classes × four outputs, both sides, in points, per-cell variation recorded rather than averaged, before a line of code was written.** It needed an instrument that did not exist — `pdfgeom2.py` reports type and repeating boxes and **no text positions at all** — so `pdfruns.py` was written beside it, outside the repo, and self-checked against the folder-cell constants already banked in `cableGeometry.ts` before anything was trusted. **Sixteen ranked gaps, ranked by what a person holding the sheet sees first rather than by size in points.**

**Three that nobody had ever named, all of which come before the two that had.** **The right-hand column of every tail label was upside down** — v1 sets ONE right-aligned field `"<End> Tails"` wrapped onto two lines sharing one right edge, and prints `" Tails"` with a leading space when the end name is empty; v2 had `Tails` at the top and the end name at the bottom as two separate absolute elements, **which is most of why v2's cell needed 39.18 pt of ink in a 36.00 pt row.** **The end word hung 19.11 pt past the label's cut edge** in every cell of every column, against v1's 125.19 on a 126.00 pt label, 1,160 of 1,180 instances. **And Tail Panel is a ruled table in v1 and had no rules at all in v2** — 76 hairlines per page at 0.5 pt `#DEDEDE`, removed under a code comment that is true of the other three outputs and false of that one.

**CELL-AND-ROW SHIPPED at `2cc6b14`** (12 files, +38 tests). **The structural change: v1's tail cell is a set of FIXED ABSOLUTE SLOTS that overlaps rather than reflows.** Measured on the arbiter: a block cell with no description still sets its `@` at em-top +25.66, exactly where a cell *with* one does; and a wrapped `@` line sets its second line ~8 pt into the next die row **while that row's chip is still stamped at its own origin.** That is the mechanism that makes one number one number. **The ladder statistic, before and after:** Tail Line line chips **5 distinct values → 1** · Tail Line block chips **4 → 1** · Small Tail Block **8 → 1**. The landed value is `+0.75` against v1's `+1.00` and `+0.43`, and **CC reported that honestly rather than reporting the acceptance's number** — Chrome snaps a background rect to its 0.75 pt paint grid, and the 0.57 pt by which v1 distinguishes its two chip classes is not expressible there.

**AND IT WENT TO THE SOURCE THAT MADE THE INK.** §2 asked CC to correlate the stepped and unstepped populations out of the artifacts and report a rule or a halt. It did that first — a clean separator on character count for two sites and **no clean separator at all** for the tint — and then, rather than ship a threshold fitted to one corpus, read v1's own layout definitions in `docs/reference/v1_ddr.xml` (**UTF-16LE; a byte grep finds nothing**). **The type step-down is five `Length()` tests with three different thresholds, each tuned to its own field width.** **The description tint is the description field's own background, on a 12-arm conditional format keyed to `Mult Lines::Line Color`** — a per-line colour, a different column from the cable colour that fills the number chip, which is why nothing in the printout could explain it: **the discriminating datum prints nothing of its own.** The measured separator and the source condition agree exactly wherever both exist, so it stands as a cross-check and not a substitution. **The registered standing rule: go to the source that made the ink.**

**Daniel's two rulings on the return, and the second was a correction to the first ruling's paraphrase.** He ruled a **literal page break at each model section on all three outputs** — and CC had measured that **v1 breaks to a COLUMN on the two Avery sheets** (8/8 and 7/7 markers at row 0 of a column, only 3/8 and 2/7 at a page top) and to a page only on the panel, which has one full-width column. **Daniel ruled the page break anyway, so it is a deliberate divergence by ruling, pedigree (a), and the source comment says so outright** — *do not "fix" this back to a column break by citing the artifact.* And **the line-number chip takes the cable colour alone**: v1's ladder is fifteen `Cable::Color` arms with no line-colour arm, and v2's `?? lineColor` fallback made chip and tint disagree on **63 of the arbiter's 79 tinted cells**. That gap had been recorded as MATCHING in the diff, because the diff sampled cells where the two agree — **CC found it, not Arch.**

**THEN DANIEL PRINTED, AND THE PROJECT GOT ITS FIRST MATCHED PAIR.** Same mult, same nineteen lines, both renderers, one afternoon — every prior v1↔v2 comparison in this project was two different found sets, and **FOUNDSET-NOT-COUNT does not apply to these two files.** It confirmed the unit against v1 rather than against a throwaway: line chip x **54.00 both** · row pitch **36.00 both** · `bDRUMS•1` **67.86 vs 67.85** · the right-hung slot's right edge **144.98 vs 144.95** · **the ladder one value on both sides at 38 chips against 38.**

**And it exposed a defect that was invisible by construction.** `bDRUMS`'s cable colour is black, and v2 was printing the block cell's `Female / Tails` **black on that black chip** — the field did not appear on the page at all. Daniel named it as a contrast rule; **it was worse than contrast.** v1's six-colour white-text condition **is exactly `MINOTAUR_COLORS`' own white-text set**, so the fix was a missing override and not a palette change. **Arch proposed a geometric general form — take the chip's paired colour when the run sits on the chip — and CC refused it with evidence:** v1's mechanism is per-object, the line cell's end-word object carries no conditional format at all, and a geometric rule would have turned it white on a long end name. **Arch was wrong and the artifact says so on one page: the same mult's block-cell `Female` is white and its line-cell `Female` is black.** A 22-run colour audit followed; one run was wrong.

**FENCE-PUSH at `0bf6df3`, and CC hard-stopped in the middle of it, correctly.** Daniel ruled the push fence lifted and `git push` made a natural follow-on to the confirm phrase. **Arch read "break the fence" as a NARROWING rather than a removal** — deleting `"Bash(git push *)"` would have handed over force, `--delete`, `--mirror` and refspec pushes in one step, none of which was ruled. **Then the harness refused CC's writes to `.claude/settings.json` and `.claude/hooks/bash-fence.sh`, and CC stopped rather than routing around the one guard it must not route around**: the unit's core is CC loosening the fence that constrains CC. It built and battery-proved both candidates in a scratchpad so the stop cost one paste, and it argued **against** the alternative — granting `Edit(.claude/**)` is a permanent concession strictly larger than the push lift Daniel actually ruled. **Arch adopted that reasoning whole.**

**The battery is the unit's evidence, and the control run is the part worth keeping.** 34 cases against the installed hook; **the same battery against the hook at HEAD differs in exactly one case — `git push` — and agrees on all 30 others.** That is the narrowing measured rather than asserted. CC added `-d` beyond Arch's ruled list, argued for it, and was upheld: **`-d` is git's real short form for `--delete`, and omitting it because a prose list did not enumerate it is the FENCE-COMMITA hole class exactly.** Arch's one hardening — `"Bash(git push -f *)"` → `"-f*"`, so a bare `git push -f` is caught at the settings layer and not by the hook alone — **was then proven live rather than argued**: `git push` succeeded, `git push --force` and `git push -f` were both REFUSED at the settings layer, all three run **before any commit on a deliberate no-op** so the test could not deploy anything. **And the rest of the fence proved itself unasked** — CC's own gate-log redirect was blocked because the compound ended in `echo`, tripping the redirect whitelist this unit had promised not to disturb.

**CC also declined §2 of that handoff, and Arch's handoff was wrong to ask for it.** Flipping `CLAUDE.md`'s forward-dated push blocks to live while both layers still denied push would have produced a contract asserting an authority CC did not have — **the FENCE-STALE shape, inside the unit whose own text warns about it.** §2 landed with §1 in the second pass, ten sites, each carrying `FENCE-PUSH, 2026-08-30, Daniel's ruling`.

**The `06-settings-light` gate failure, and how it was settled.** Run 1 failed 27/28 at **1451200 px, bbox 0,93–1599,999** — *identical pixel count and bounding box* to the leg-6 theme-race flake `CLAUDE.md` records, but on the **light** half where the entry says dark-only. **Arch ruled it settled by import graph rather than by re-running:** a re-run says whether it recurs; the graph says whether the frame could ever have seen the change, and it gives the same answer either way. **Zero of the eight changed modules is reachable from `06-settings`, against a control of 8 of 8 from the print route that does use them** — and `minotaurColors.ts`, the one module reachable from settings, was untouched with an empty diff. The re-run was green. **Entry (2) is widened a second time: not dark-only, not frame-specific, the invariant is the pixel count and the bbox.** It stays prose and never becomes a `diff-exceptions.json` site — masking 1.79% of a frame would blind that page permanently.

**Two `CLAUDE.md` amendments, both Daniel's, both landed in FENCE-PUSH.** He diagnosed that CC returns control with results living only in terminal scrollback — twice in one day a measurement reached this seat as a photograph of a terminal. **He proposed a 30-second / 10-line trigger; Arch pushed back and proposed the event instead**, because CC cannot check elapsed time against an artifact and this project's own finding (O-12) is that rules checked at discrete events hold while continuous properties do not. **The trigger is RETURNING CONTROL**, the last terminal line is the return file's path alone — Daniel's words, kept — and **one file per unit, appended**, which kills the proliferation objection by changing what gets batched. **His three exclusions are written into the rule, numbered:** waiting on a step the COMMIT BLOCK already documents · being interrupted rather than finished · a clarifying question before any work has been done.

**"Strat invariant 1", asked and answered.** It is **WF-1, Daniel's ruling of 24 July 2026: CC never stages, commits, or pushes.** The finding underneath is that **there is no register of Strat invariants** — no invariant 2, nothing in `Strategy_Prompt_v14.md`, and the phrase appears with content behind it exactly once, in the Ledger's WF-1 entry; every other occurrence quotes the fence's own error string. **And the ruling had already been narrowed twice without the fence noticing** — WF-1a took the stage and commit clauses on Aug 9, Arch v32 took the push clause on Aug 26 pending this unit. **So today's lift is not a Strat retraction; it is the last third of WF-1 catching up with WF-1a.** The fence now cites no authority for the destructive family, deliberately, rather than an invented one; **Arch's ruling is that its real authority is `CLAUDE.md`'s existing hard-stop list — a force-push is a destructive or irreversible operation — and that goes in the fence at its next touch, not into a tested commit at the last moment.**

**FOUR CORRECTIONS TO THIS SEAT'S OWN RECORD, all produced by measurement and all propagated.** **JUSTIFY-PERCELL does not alternate** — the `@` line sits on one of two axes selected by the tail END, 238 of 238 cells, zero exceptions, and the DDR explains it. **ROW-LADDER's quoted v1 baselines were em-tops.** **SKIP-LABELS exists in v2** on four surfaces, where the Roadmap said it had nothing like it. **`FOLDER_CELL`'s em-tops are 8.67 / 30.67**, and the same instrument reproduced four other constants from that file exactly, so the 0.03 is in the constant.

**Arch errors this session, all caught downstream, all recorded.** (1) **The geometric colour rule** — proposed as a general form, refuted at source by CC, and refuted on the artifact. (2) **The yellow bands read as a missing-description highlight** — inferred from a correlation on one page; they are the ordinary line-colour tint over a data convention. (3) **Asking for `CLAUDE.md`'s forward-dated blocks to be flipped in the same pass as the fence lift**, which would have created the stale-document shape the same handoff warned about. (4) The **anatomy diff's own "chip colours match to 1/255"**, true of the cells it sampled and false of the 63 it did not.

**What this session did not touch.** STOCK-IS-DIE, which is now the largest thing standing between these sheets and a usable roll of 5167. FENCE-HEREDOC, consciously left twice with the file open. Everything on Daniel's desk except the three items it created.

**Next concrete action:** install this close → Daniel commits and pushes `Minotaur-Cowork` with the block Arch supplied → per ONE-SESSION-ONE-ARC a **NEW session** opens on a fresh opener, on **STOCK-IS-DIE**.
