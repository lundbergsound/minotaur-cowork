## STATE OF PLAY
- **Shipped:** v2 live at `https://minotaur.app` at **`0bf6df3`**, pushed. **TWO units closed this session — PRINT-PARITY cable pass 4 (CELL-AND-ROW) at `2cc6b14`, and FENCE-PUSH at `0bf6df3`.** Counts measured at `0bf6df3`: suite **2553 tests across 137 test files, ZERO skipped** (from 2506/136 at open) · frozen **3/3 vs `be0769de`** · typecheck 0 · dependency gate 0 · build clean · **visual 28/28 at 0.0000%, all 28 byte-clean at ch0, zero pixels excluded across zero frames.**
- **Count notation (TESTCOUNT):** `2553/137` is **TESTS / TEST FILES**, never passing / skipped.
- **⚠ The migration ledger is 55, unchanged.** No migration rode either unit.
- **⚠ READ `.git/refs/heads/main` FOR HEAD, NEVER `.git/packed-refs`** — it carries `be0769de`, now eleven commits stale, and that value is also the frozen-gate baseline, so a stale read looks plausible. (GIT-CFG.)
- **⚠ FENCE-PUSH IS LIVE, AND IT CHANGES WHAT DANIEL'S PHRASE MEANS.** Both enforcement layers are narrowed: a **bare `git push` is CC's** on the `COMMIT APPROVED:` phrase, and **force in any spelling, a `+` refspec, `--delete`/`-d` and `--mirror` stay human-only** at both layers. **A WF-1a commit is therefore a production DEPLOY** — Vercel builds every push to `main`, so the phrase approves a release. Proven live before any commit, on a deliberate no-op: `git push` → `Everything up-to-date`; `git push --force` and `git push -f` → both REFUSED at the settings layer. **`0bf6df3` is the first commit CC pushed.**
- **⚠ THE ROW LADDER IS CLOSED, AND IT IS THE UNIT OF RECORD.** v1 stamps every cell on its die — line chips at exactly **+1.00 on 249 of 249**, block chips at **+0.43 on 100 of 100 and 17 of 17**, one value each. v2's same statistic took **nine values on Tail Line and fifteen on Small Tail Block**, with a block cell overrunning its 36.00 pt row by 9.75 pt. **After: one value per chip class per sheet** (`+0.75`, Chrome's paint-grid quantisation of v1's `+1.00`), confirmed against v1 on a matched mult at **38 chips against 38**. The tail cell is now a set of **fixed absolute slots that overlap rather than reflow**, which is what v1 does — measured on `Tail Line Labels by Model.pdf` p27 col 0, where a wrapped `@` line runs ~8 pt into the next row and that row's chip is still stamped at its own die origin.
- **⚠ THE FIRST MATCHED PAIR IN THE PROJECT'S HISTORY**, and FOUNDSET-NOT-COUNT does not apply to it. Daniel printed v1 and v2 of the same mult, the same nineteen lines, on 2026-08-30. Filed at `parity-pairs/260830_cable-pass4-matched-pair/`. Measured: line chip x **54.00 both** · row pitch **36.00 both, nine chips running** · `bDRUMS•1` **67.86 vs 67.85** · right-hung `Female` right edge **144.98 vs 144.95**. **A difference between those two files is a renderer difference or a data difference and nothing else.**
- **⚠ STOCK-IS-DIE IS STILL UNBUILT, AND IT IS THE NEXT THING THE PAPER WILL SHOW.** Measured on Daniel's own print: rendered column origins **20.25 · 165.00 · 310.50** (v1's 145.13 die, inherited from pass 3) against Avery 5167's cut lefts **20.25 · 168.75 · 317.25 · 465.75**. Column 2 lands **3.75 pt** left of its cut, column 3 **6.75**, column 4 a projected **~10.15** — ink on the liner rather than the sticker, worsening left to right. **Ruled: where v1 and the die collide the die wins. Nobody has built it.** When a sheet is finally held against a real blank and columns 2–4 read low, **that is this, not a pass-4 regression.**
- **⚠ v1'S TWO UNKNOWN TRIGGERS ARE NOW KNOWN, AND NEITHER WAS INFERRED FROM THE CORPUS.** Both are quoted from `docs/reference/v1_ddr.xml` (**UTF-16LE — a byte-oriented grep finds nothing in it; read via `iconv -f UTF-16LE`**). **The type step-down is five `Length()` tests with THREE different thresholds** — line number `> 10`, Tail Line block name `> 9`, Small Tail Block name `> 11`, panel number `> 9`, panel description `> 14` — each tuned to its own field width, and **one constant would be wrong on two of the three sheets.** **The description tint is the description field's own background**, painted by a 12-arm conditional format on **`Mult Lines::Line Color`** — a per-LINE colour, a different column from the `Cable::Color` that fills the number chip. That is why no artifact-only correlation could find it: the discriminating datum prints nothing of its own.
- **⚠ THE LINE-NUMBER CHIP TAKES THE CABLE COLOUR ALONE.** v1's layout-168 key-5 ladder is fifteen `Cable::Color` arms with **no `Mult Lines::Line Color` arm**. v2 read `lineColor ?? parent.color`, which the anatomy diff recorded as MATCHING because it sampled cells where the two agree — on the arbiter they **disagree on 63 of the 79 tinted cells**. Ruled and built: the chip carries the cable's colour, the tint carries the line's. **A cell shows two facts, not one fact twice.**
- **⚠ TEXT ON A CHIP TAKES THE CHIP'S PAIRED TEXT COLOUR — and v1's rule is PER-OBJECT, not geometric.** v2 printed the block cell's `<End> Tails` field **black on a black chip**, so on a black cable it did not appear on the page at all. v1's six-colour white-text condition (`red · green · blue · purple · black · brown`) **is exactly `MINOTAUR_COLORS`' own white-text set**, so this was a missing override and not a palette change. **The line cell's end-word object carries NO conditional format in v1 and stays black on every cable colour** — a geometric derivation would have turned it white on a long end name and diverged.
- **In flight:** nothing. **CC is IDLE.**
- **Next action:** install this close → per ONE-SESSION-ONE-ARC a **NEW session** opens on a fresh opener. The main line is **STOCK-IS-DIE** — the column drift against the Avery cut, now the last thing between these sheets and a usable roll of 5167 — then **EQUIP-LABELS-2** (Trigger B) to unblock Equipment Labels → **BOX-DOC-PARITY** → **2-MIG-c** without the split arm.
- **RULED this session:** **a model section starts a new PAGE on all three tail outputs** — Arch recommended keeping v2's packing and was overruled; **v1 measured breaks to a COLUMN on the two Avery sheets (8/8 and 7/7 markers at row 0 of a column, only 3/8 and 2/7 at a page top), so this is a DELIBERATE DIVERGENCE by ruling, pedigree (a), and the source comment says so** · **the print date is added where v1 has it, in v2's own Date Format** · **the type step-down's trigger is measured before it is built** · **the `<End> Tails` field follows the database-wide white-versus-black text rules** · **FENCE-PUSH: lift the ordinary push, keep the destructive family human-only** · **the Return Handoff File Protocol's trigger is RETURNING CONTROL**, the last terminal line is the return file's path alone, one file per unit appended — **with three exclusions Daniel named: waiting on a step the COMMIT BLOCK already documents · being interrupted (API error, dropped connection, sleep) · a clarifying question before any work.**
- **ARCH RULED FROM THE RECORD, pedigree (b):** Tail Panel's **74 grey rules per page** reinstated · **model markers restored** on Small Tail Block and Tail Panel with v1's underlined text-width chip · v1's **single wrapped `"<End> Tails"` field** reproduced including the leading-space `" Tails"` form, mechanism left to CC · the **description tint built on a measured condition, not a guessed one** · the **uncoloured-mult chip left alone** as probably data · **`-d` stays blocked** in the fence, beyond the ruled list, because it is git's real short form for `--delete` · **the unasked run-fence refresh accepted**, because `CLAUDE.md`'s own rule says whoever measures updates the fence in the same pass.
- **CLOSED this session:** **cable pass 4's anatomy diff** — nine element classes × four outputs, both sides, sixteen ranked gaps · **CELL-AND-ROW** at `2cc6b14` with two amendments · **FENCE-PUSH** at `0bf6df3` · **the `06-settings-light` gate failure**, settled by import graph rather than by re-running.
- **⚠ FOUR CORRECTIONS THIS SESSION'S MEASUREMENTS PRODUCED, all propagated:** **JUSTIFY-PERCELL does NOT alternate** — the `@` line sits on one of **two axes selected by the tail END** (Female 67.48, Male 65.98, exactly 1.50 pt apart, **238 of 238 cells, zero exceptions**), and the DDR says why: v1 hides the Source object unless `Gender = "source"` and the Destination object unless `= "destination"`, and the two have different left edges · **ROW-LADDER's quoted v1 baselines were EM-TOPS** (74.68 / 76.34 / 81.68 / 89.66 / 97.66; the baselines are 79.43 / 86.43 / 86.43 / 96.00 / 104.00) · **SKIP-LABELS DOES exist in v2**, on four surfaces, inserting exactly-36.00 pt blanks — the Roadmap said "v2 has nothing like it" · **`FOLDER_CELL`'s em-tops are 8.67 / 30.67, not 8.70 / 30.70**, constant on all ten die rows.
- **Riding rulings, unimplemented:** **STOCK-IS-DIE** · **EQUIP-LABELS-2** (Trigger B) · gap 14 the title underline y, taken on Tail Panel only · **FENCE-HEREDOC**, consciously left twice with the file open · MENU-DEPRECATE · PRINT-HINT-EQUIP · APPLY-TIMEOUT · GRANT-TRUNCATE · HARNESS-READ · CONTRAST-PAIR · the `0` vocabulary-row fold.
- **Standing architecture (R-AL):** bulk data never routes through a model context.
- **Standing rules (Arch v34):** a **seat check** gates every session · **one writer at a time** on the repo · **never run git in either repo folder** · **a session closes when the SESSION ends** · **an INSTALLED close ends the session** · every message ends with **one plain-language next step** · **ruled means settled** · the **rollover runs at PRE-OPEN** · **RM-6: six COUNTED bullets** · a governing document's **H1 and filename bump together** · **when a push is due, the close instruction carries the exact commands** · the `COMMIT APPROVED:` phrase now authorises the push **and it is LIVE**.
- **Carried standing rules:** batch rulings, never batch experiments · every identifier gets a gloss on first use · **REPRO-SUSPECT** · **a stale document does not fail quietly — it gives orders** · **DOMAIN-NOT-CHECK** · **TENSE-IS-A-CLAIM** · **DEAD-CODE-IS-NOT-SAFE** · **DEVICE-VM-IS-NOT-CC** · **CLOSE-THEN-CONTINUE** · **GATE-NOGREP** · **ROUNDTRIP-INMEM** · **EXPORT-CARRIES-STATE** · **PDF-CHROME-PATH** · **TEST THE GUARD, DO NOT READ IT** · **A SEARCH RUN WITH THE WRONG INSTRUMENT RETURNS ABSENCE** · **A NUMBER FROM ONE INSTRUMENT IS NOT A VERDICT** · **AN ACCEPTANCE MUST BOUND WHAT THE PASS CAN BREAK** · **NEW (Aug 30) — GO TO THE SOURCE THAT MADE THE INK.** Two triggers that defeated every artifact correlation were quoted verbatim from v1's own layout definitions in one pass. When a rule exists in a system you can read, read it; a threshold fitted to one corpus reproduces that corpus by construction. · **NEW (Aug 30) — AN IMPORT GRAPH BEATS A RE-RUN.** A re-run says whether a failure recurs; the graph says whether the frame could ever have seen the change, and it gives the same answer either way. Two runs is the limit. · **NEW (Aug 30) — A RULE CHECKED AT AN EVENT HOLDS; A RULE CHECKED AGAINST A DURATION DOES NOT.** The return-protocol trigger is *returning control*, not "thirty seconds," because CC cannot check elapsed time against an artifact.
- **`CLAUDE.md`:** **CURRENT at `0bf6df3`** (FENCE-PUSH). Carries the live push authority with its file:line citations re-read at source, the Return Handoff File Protocol's new trigger and Daniel's three exclusions, the twice-widened capture-flake entry (2), FENCE-HEREDOC recorded as still open, and a run fence re-measured on this tree at 2553/137.
- **⚠ The capture-flake registry's entry (2) is WIDENED TWICE and is NOT dark-only.** Three sightings — `04-library-dark` · `06-settings-dark` (leg 6, Δ207) · **`06-settings-light`** (this session, Δ248) — and **the invariant across all three is 1451200 px at bbox 0,93–1599,999**, not the frame and not the half. **It stays PROSE and never becomes a `diff-exceptions.json` site**: DIFF-EXC-BOUND is owed first, a whole-frame diff is not the Δ1 wander that registry exists for, and masking 1.79% of a frame would blind the gate to a real regression there permanently.
- **Install path (Arch v34):** pushing `Minotaur-Cowork` and clicking Sync now does **NOT** put governing docs into project knowledge. **THIS SEAT installs close files to the mount and to claude.ai project knowledge; Daniel does not.** Archives excluded.
- **PDF evidence-class rule:** all v1↔v2 output-parity evidence is **print-to-PDF at 100% scale**, matching paper and orientation on both sides, **through Chrome's own Save-as-PDF destination** (PDF-CHROME-PATH). **And the printed proof is the acceptance, not the gate set.**
- **At Daniel's desk:** **commit + push `Minotaur-Cowork`** (it sits at `c8ae063`, the Aug-30 morning close; this close is not in it) · **a STRAT-record entry retiring WF-1's push clause** — one line: *WF-1's push clause is retired by FENCE-PUSH (Daniel, 2026-08-30); the stage and commit clauses were already retired by WF-1a on Aug 9* · **hold a printed 5167 sheet against a real Avery blank** — the only acceptance a measurement cannot supply · **export a v1 show that actually uses cable SPLITS** · **a fresh v1 Bundle Summary print** · **the fresh-import fixture**, which three separate items wait on · delete BVSC NY's two junk boxes · **BUNDLE-LINK-IMPORT** · **WAITLIST-NOTIFY** (Trigger A) · three **box** print reprints · **METRIC-UNRUN's browser gate** · CAND-4 · **PROJ-CLEAN-2** (**`Visual Gate Fixture` AND `Import Test 5` must NOT be swept**) · GIT-CFG · **a STRAT-record entry for the Pro upgrade.**
- **⚠ `Import Test 5` and `Test Cable` are RLS-INVISIBLE to any harness CC can drive** — both owned by daniel@, and the harness signs in as the TEST user. **The parity corpus cannot be re-measured at CC's desk at all.** Every count in cable pass 4 was taken on a throwaway project seeded to reproduce the corpus, and labelled as a reproduction. Either the test user gets a `project_members` row, or the parity corpus moves to a project it owns, or these counts stay Daniel-side forever. (Rides HARNESS-READ.)
- **Current log:** 6 entries
- **Rollover:** clear. Fires at 10. Last archived 2026-08-27
