# 🗺️ ROADMAP — Minotaur v2 (v4.24)

**Version 4.24 • September 8, 2026 • Retires v4.23 • Forward-only file — history lives in `Minotaur_v2_Ledger_v1_0.md`**

**⚠ STILL SEQUENCED BY ONE PRODUCTION: BUENA VISTA SOCIAL CLUB, MEXICO CITY (Daniel, 2026-09-05). Parity and housekeeping that does not serve that show takes a pin — named as a pin, never dropped.**

**⚠ AND THAT PRODUCTION HAS AN ID, BECAUSE ITS NAME IS NOT UNIQUE. BVSC-MX IS `cf780353-448d-4b15-a054-973f577b215c`.** **STATED (Daniel, 2026-09-06): two productions of the same musical carry the same show name, and supporting that is a MUST-HAVE FEATURE.** The second row is `57874c69-06b6-41b1-91d8-6a00e5255110`, **`Buena Vista Social Club US Tour`** — the 2,591-row import these documents once called "the BVSC Tour." **Mexico City carries 1,024 spine rows deliberately: Daniel started it with cable and boxes blank.** ⚠ **Every citation of a project in this file names an id, and duplicate `projects.name` is never "fixed" by a uniqueness constraint.**

**⚠ WHAT CHANGED IN v4.24:** no structural change — v4.23's three-table Docket (RM-8) stands and every row was carried verbatim again. **FIELD-CONTRACT is now a THREE-LEG unit and two of its legs shipped today** (`44e206d` the census, `1276cc4` FIND-PARITY); its Docket row is updated, not retired. **Five rows are NEW** — BOXBAND-DUPKEY · CAPFLAKE-LIBLIGHT · FINDFLAG-CHECKBOX · CABLEGROUP-EDITOR · CABLE-FINDDEFAULT — and **VERHIST is split out of the LIC-NOV row and promoted**, scoped by a parallel session and awaiting five rulings. **No row retired to the Ledger this session**, so the retirement index is unchanged at 26.

## 🧭 Next Sessions (RM-6 — plain English, and **forward-looking only**: what is being built next and in what order. What just shipped is in the Lineage row; what was learned is in the Ledger. **HARD CAP: six bullets.**)

**⚠ THE FIELD SWEEP IS HALF BUILT AND BOTH HALVES ARE LIVE.** Two deploys today. What is left of it is fixes, and they wait on you rather than on me.

1. **The rest of the field sweep.** The counting and the searching are done and shipped. What remains is the repairs — the ones from your CDMX notes and the ones the count turned up. **It needs two small decisions from you first**, both below.
2. **Then dark mode.** Your eight readability notes all live in one file, so they are one pass rather than eight.
3. **Then a clearing batch:** the small registered fixes that have been waiting for a convenient unit, plus making item duplication quicker.
4. **New, and it is the biggest thing on this list: version history.** A parallel session scoped the replacement for the Dropbox net v1 gave you — automatic hourly saves, and opening any old version as a new production. **Five decisions are waiting on you, plus two I added after measuring.**
5. **Small decisions I owe you, none of them blocking:** which spelling the box Number field should settle on, whether boxes and bundles are held to the field contract, and how a sort band should treat two spellings of one truck name.
6. **Still yours from before:** hold a printed 5167 sheet against a real Avery blank, and reprint the Tail Panel labels. They remain the only unrun checks on things already shipped.

## Lineage (full change-log lives in the Ledger)

| Version | Date | Closed | Retires |
|---|---|---|---|
| **v4.24** | Sep 8 | **FIELD-CONTRACT LEGS 1 AND 2 SHIPPED — two production deploys.** **Leg 1, THE CENSUS, at `44e206d`** — 6 files, +1349/−26: `docs/reference/field-contract-census.md` inventories **83 rendered editable cells across seven surfaces**, one row each with marker, Change All membership, Find membership, Tab-order position and the resolvers that can see it; `src/lib/fieldContract/` holds the instrument and **73 tests that fail when a cell loses a wiring or a marker is renamed**, probed in both directions and restored byte-identically. ⚠ **The scope was larger than Arch measured: SEVEN Change-All registries in three shapes, TEN find maps, THIRTEEN marker attributes** · **Leg 2, FIND-PARITY, at `1276cc4`** — 8 files, +470/−340: **RULED (Daniel, 2026-09-08) — every field a surface's Change All can WRITE is a field its Find can SEARCH**, a SUBSET relation and never a blanket; **25 keys added** (cable's inspector seven · equipment's Labels and four flags · thirteen on boxes), 25 stale exceptions retired, three kept because Change All cannot reach them · **`box_number` rekeyed from `number`, which fixed a live defect** — the focus-aware find default could never resolve that cell · **cable `group` reads the EFFECTIVE group** (Daniel), and the filter, option list and sort bands already did · ⚠ **BOXBAND-DUPKEY found by Daniel's gate, root-caused, and proven NOT this unit's** — the comparator's equality is coarser than the band label's identity · **four Arch §1 claims were wrong and CC caught all four**, one shape: a universal negative asserted from an instrument narrower than the claim · **VERHIST scoped by a parallel and folded in, with its payload estimate corrected from 1–2 MB to a measured 5.3 MB** | v4.23 |
| **v4.23** | Sep 8 | **ROADMAP-REFACTOR CLOSED, carrying CLAUDEMD-ATLIMIT — Arch's own unit, no CC handoff, no commit to the code repo from this seat** · ⚠ **`CLAUDE.md` 149,997 → 105,004 characters, from THREE of headroom to 44,996** — the Visual-Capture Harness relocated VERBATIM to `docs/reference/visual-capture-harness.md` (27,135 chars) on the Guarded Architectures Index precedent, and the 40,156-character run fence retired to the Ledger · ⚠ **AND THE FENCE COULD NOT SIMPLY BE DELETED: EIGHTEEN OF TWENTY STANDING RULES CHECKED LIVED ONLY THERE**, so a new `Standing rules — earned by units, owned by none` section was written to receive them — **which is what finally makes FENCE-ONEUNIT achievable, because a unit's fence can now be discarded whole** · **the Docket given the Sprint Plan's table discipline** — 129 live rows sorted verbatim into ACTIVE / HELD / STANDING, 26 closed rows retired to the Ledger, two older CLOSED-IN narrative sections retired · ⚠ **A SECOND SUPERSEDED DUPLICATE FOUND AND DELETED — LINKGROUP-CORPUS, which the scoping did not know about** · **one Arch error: a saving predicted rather than measured, and stated to Daniel as a number** | v4.22 |
| **v4.22** | Sep 7 (late eve) | **EQUIP-LIST-PARITY SHIPPED at `c525a55`** — 14 files, +2943/−379, **six rounds** (base handoff + AMENDMENTS 1–5), one production deploy · ⚠ **THE 0.750 WAS A UNITS DEFECT, NOT A DIE** — v2 wrote v1's POINT numbers as PIXEL values, which is exactly 0.750 by arithmetic; a v1 point is now **4/3 of a v2 pixel** through one die module and a test that enforces it · ⚠ **THE ESTIMATE PAGINATOR WAS DROPPING 42 BLOCKS IN PRODUCTION AT HEAD** across seven measured configurations and now drops **zero** — a live defect this unit FOUND rather than introduced · **the note wrap column is a greedy wrap over measured glyph advances**, not `ceil(len/52)` in a proportional font — 155 estimated against 155 rendered · **EQUIPLIST-LEAD-QUANTUM** — every baseline advance an exact integer pixel, single-valued on the emitted PDF · **EQUIP-LIST-FULLSIZE**, **CONTCAT-SIZE** and **NOTE-GROUPING** ruled by Daniel · **QTY-ITALIC**, **SUBNOTE-HARDBREAK**, **CATNOTE-FONT-DUP** and the indent ladder closed · **AMEND-DECLARES-PER-ROUND** ruled, discharging AMEND-ROUND-TENSION · **Daniel found three defects on paper across four gate prints; the machine gates found none of them** · **five Arch errors, three of which reached the build** · suite 2768/147 → **2827/149** · **migration ledger UNCHANGED at 57, fifth consecutive unit** · `CLAUDE.md` 132,053 → **149,997, THREE characters under its limit** | v4.21 |
| **v4.21** | Sep 6 (aft) | **CAT-NOTE's PRINT half SHIPPED at `267d477`** and **CAT-NOTE-RULE at `f4192cd`** — the category note prints under its heading at the body item's size in italic, **between v1's two rules**, and the estimate paginator budgets its height · ⚠ **THE EQUIPMENT LIST STILL USES THE ESTIMATE PAGINATOR PRINT-CLIP RETIRED EVERYWHERE ELSE**, into a page with `overflow: hidden` — **a negative control lost SIX of 120 rows off page 2 with no error**, and **the screen probe that proved the cable and box documents is BLIND here** (the clip lives in `@media print`) · ⚠ **CATNOTE-RULE REFUTED, SHIPPED AS REFUTED, THEN REINSTATED when Daniel found it on paper** — the probe filtered `linewidth >= 2.0` because the lead said 2 pt and the rule renders at **1.00 pt**; re-measured at **14 of 14 noted headings, 0 of 33 note-free** · **two productions of one musical sharing a show name RULED a must-have feature**, and **BVSC-8POWER-DRIFT found misattributed in three documents — it is the US Tour, not Mexico City** · **QTY-ITALIC's mechanism found** (quantity italic, description roman, same line) · **two CATNOTE-TYPE claims corrected** (2.00 pt indent, not flush; the heading rule sits between heading and note) · **RECT-VS-FLOW** and **PAGINATE-SLACK** registered · **FENCE-HEREDOC's third and fourth sightings, two binaries, both self-reported, the fence caught neither** · **migration ledger UNCHANGED at 57, fourth consecutive unit** · `CLAUDE.md` 134,764 → **132,053** · **four Arch errors, two of which reached Daniel** | v4.20 |
| **v4.20** | Sep 6 (morning) | **CAT-ADMIN SHIPPED at `1a54c3b`** — the **Categories tab goes live** on v1's own two-field surface and the category note becomes an editable **multi-line** cell that **grows as you type**; **CATNOTE-CELL** and **CATNOTE-AUTOSIZE** ruled · ⚠ **a governing claim CORRECTED — the category screen existed all along as a DISABLED TAB** (`GCMClient.tsx`, `enabled: false`), so CDMX items 1 and 2 merged on a measurement · **v1 layouts 317 `Categories` and 336 `Methods` proved BYTE-IDENTICAL TWINS**, so the copy is pedigree (b) cited by layout and object · **the note's save rule proven on LIVE PRODUCTION DATA by Daniel's own gate** — an edited note stored what the control gave, two untouched notes kept their bare CRs · **GCMTAB-STALE** found and fixed in both admin grids · **declared baseline event, `05-gcm` pair, scope measured identically across THREE runs before adoption** · **`CLAUDE.md` 161,200 → 134,764 chars** and the fence's accretion root-caused as **FENCE-ONEUNIT** · **migration ledger UNCHANGED at 57** · **two Arch instrument errors, both self-caught; one Arch instruction defect that reached CC** | v4.19 |
| **v4.19** | Sep 5 (late eve) | **CHANGEALL-BOXNULL SHIPPED at `6deb411`** — the box arm of the equipment Change All plans off the **TYPED DRAFT** through a new pure `planBoxChangeAll`, suspends on the New Box dialog and writes **NOTHING** until OK; **BOXNULL-TOOLBAR** built on Daniel's option (a) so a toolbar press is no longer an outside click that **swallowed the gesture entirely**; **the equipment-only scope is a machine-checked test, not a comment** · **the negative control reproduced the defect on the OLD tree in SIX cases and found a THIRD failure mode nobody had predicted — an EXISTING box name typed on a boxless row unlinks the whole found set** · visual gate **28/28 TWICE**, no frame moved, no baseline event · **migration ledger UNCHANGED at 57** · **the BVSC Tour imported into `Buena Vista Social Club` and verified sheet by sheet** · **EQUIP-LIST-SCALE MEASURED at exactly 0.750 on every element class** · **CONTCAT-DIVERGE ruled** · **APPLY-TIMEOUT confirmed live on a real import, then PINNED behind the CDMX work by Daniel's ruling** · **FIELD-CONTRACT moved behind the CDMX patches** · **two Arch errors, one of them a wrong claim that reached a governing file before measurement caught it** | v4.18 |
| **v4.18** | Sep 5 | **CHANGEALL-FOUNDSET SHIPPED at `ba0058f`** — all **36** batched call sites moved off the URL onto four body-carried RPCs through ONE helper; **9 deletes, 7 reads and 2 doubled raw filters** included; the two select-then-delete pairs collapsed into single statements; a **guard test proven against the BASELINE tree at 32 real hits**; **GREP-NULBYTE** fixed · **migration ledger 56 → 57**, `changeall_foundset_bulk_write_rpcs`, Arch-applied rehearsal-first with a clean residue check and a POSITIVE control on the rollback · **the Session Log rollover run** (archive 2026-09-01, log reset to 2) · **Daniel's browser gate found THREE things for the SEVENTH time running: CHANGEALL-BOXNULL (live data loss, measured at source), the steck field absent from BOTH Change All and Find, and a clean pass elsewhere** · **the roadmap reprioritised to BUENA VISTA SOCIAL CLUB MEXICO CITY** — CAT-NOTE · CAT-ADMIN · EQUIP-LIST-PARITY registered · **one machine shutdown mid-run, recovered by measurement with nothing lost** · two Arch counting errors, caught by CC | v4.17 |
| **v4.17** | Aug 31 (late eve) | **CHANGEALL-METHODNEW SHIPPED at `e3a336a`** — an unrecognised Method draft in Change All now CREATES the method instead of writing `method_id: null` across the found set; the doctrine and the single `createMethod` primitive **copied from the band editor's bulk arm, not reinvented**; four asymmetric branches including **a failed create that writes NOTHING**; a new pure `planMethodChangeAll` that makes the defect **unrepresentable**, pinned by 32 tests · **proved by a negative control that reproduced the blanking — 400 rows, 11 checks failing** · a stale-closure 409 found during acceptance and closed at source · **`runPlainChangeAll` stops discarding its error**, which paid for itself within the hour · **CHANGEALL-FOUNDSET MEASURED from the Arch seat and RESHAPED — a ~25.0 KB edge URL ceiling, 582 ids, BYTES not rows, and 20+ call sites including 5 deletes** · **`equip.xlsx` re-import verified at 150/57/357 with 1,368 of 1,368 identity rows matched and zero minted** · CHANGEALL-NAALIAS · CHANGEALL-METHODDOCTRINE · RLS-INITPLAN · GATE-BEFORE-COMMIT registered · **one Arch error, caught by CC at source** | v4.16 |
| **v4.16** | Aug 31 (eve) | **EQUIP-LABELS-2 SHIPPED at `374a7c8`** — v1's `Labels` imported, edited and exported as a per-item sticker COUNT; `num_labels` end to end, the row cell a centred Number field on measured DDR pedigree, Change All off `EQUIP_CHECK_FIELDS`, the export emitting BLANK for 0 · **the migration ledger MOVED 55 → 56**, Arch-applied rehearsal-first with a forced rollback and a clean residue check, **GRANT-TRUNCATE riding it at 20 grants not 10** · **HASH-FREEZE-LABELS — 1,368 row hashes bit-for-bit identical**, guard test PROBED by deliberately breaking the freeze · **Daniel's browser gate found three defects, all fixed and demonstrated**, then **two more that were never ours: CHANGEALL-METHODNULL (silent data loss) and CHANGEALL-FOUNDSET, which masks it** · **MARKER-OVERHANG measured against v1 and REVERSED — v1 wraps, we do not** · **SMALLNAME-STEP** found on a shipped output · **OQ-3 · OQ-4 · OQ-5 · BUNDLE-LINK-IMPORT all ruled**, folded in from the parallel and verified against source · **FIELD-CONTRACT ruled at five surfaces** · **five Arch errors, four of them one shape** | v4.15 |
| **v4.15** | Aug 31 | **STOCK-IS-DIE SHIPPED at `9a333cc`** — the three Avery 5167 outputs (Tail Line · Small Tail Block · Small Cable) take `margin.left` **20.25** and `pitch.x` **148.50** from one exported `AVERY_5167` constant, replacing v1's three drifted geometries for one piece of stock · **Small Cable Labels onto the geometry module** at last, the last cable label output deriving its pitch from the page · **CONFIRMED ON DANIEL'S RENDERED PRINT** — four column origins exact, ladder still one value per chip class, ink 15.00 pt clear of the sheet · **the physical-sheet acceptance DEFERRED BY RULING and still OWED — a die match is not a stock match** · the §3.2 checkpoint resolved by **splitting a wrong assertion into three right ones**, the ink bound strictly tighter · **MARKER-OVERHANG** found on the print · **STECK-ZERO DISCHARGED** and the fresh-import fixture found to be one file from complete · **OQ-2 answered** with no measurement · **LINKGROUP-CORPUS**, **DEADEXPORT-GRIDCOL** and **FENCE-COUNTS** registered · the evidence-consolidation parallel folded in · **three Arch errors in one handoff, all caught by CC** | v4.14 |
| **v4.14** | Aug 30 (eve) | **PRINT-PARITY cable pass 4 SHIPPED** — the **anatomy diff** (nine element classes × four outputs, both sides, 16 ranked gaps) then **CELL-AND-ROW at `2cc6b14`**: v1's tail cell rebuilt as **fixed absolute slots that overlap rather than reflow**, the row ladder **9 values → 1**, the `"<End> Tails"` field un-inverted, the 19.11 pt right-edge overhang closed, the panel's 74 grey rules reinstated, `HEADER.panel` wired, model markers restored · **v1's TWO UNKNOWN TRIGGERS QUOTED FROM `v1_ddr.xml`** — five `Length()` step-downs at three thresholds, and the description tint as a `Mult Lines::Line Color` conditional format · **the FIRST MATCHED v1/v2 PAIR** · **CHIP-CABLECOLOUR** and **CHIPTEXT-PAIRED** ruled and built · **PAGEBREAK-MODEL ruled as a deliberate divergence** · **FENCE-PUSH at `0bf6df3` — CC pushes, and a commit is a production DEPLOY** · the Return Handoff File Protocol re-triggered on **returning control** · capture-flake entry (2) **widened a second time** · **ROW-LADDER · JUSTIFY-PERCELL · HEADER-PANEL closed** · **STOCK-IS-DIE promoted to the main line** · **four Arch-recorded facts corrected by measurement** | v4.13 |
| **v4.13** | Aug 30 | **PRINT-PARITY cable pass 3 SHIPPED at `c6bacf7`** — four label outputs rebuilt on v1's measured die, worst element **48.15 → 0.37 pt** · **AMENDMENT 1** — the folder sheet's cell order ruled and applied · **PANEL-CLIP at `702048b`** — pass 3's own regression, **63 of 137 panel labels wrong**, found by Daniel's printed proof; `flexWrap` restored and all three tail outputs paginated by measured height; **36 Tail Line and 18 Small Tail Block cells found silently lost** beside it · **FENCE-STALE at `5488915`** — the run fence **six units stale** and asserting *"Print is CLOSED"*, false since `eaabd03` · **CHECKLIST-GROUPCOUNT CLOSED by measurement, no defect — the "16" was a page-header count** · **THE STOCK IS THE DIE registered**, and **v1 measured off its own 5167 die by up to 12.2 pt** · **ROW-LADDER**, **JUSTIFY-PERCELL**, **HEADER-PANEL**, **DEVICE-FK-LINK**, **SKIP-LABELS**, **CATSORT-NATURAL**, **EQUIPVIEW-UNSAFE**, **BUNDLESUM-MARKS**, **FOUNDSET-NOT-COUNT**, **BROWSER-NOPRINT** registered · **EQUIP-LABELS-2 promoted to a Trigger B unit** · **the remaining-layouts parallel folded in — seven outputs became EIGHT** | v4.12 |
| **v4.12** | Aug 28 | **PRINT-CLIP SHIPPED at `eaabd03`** and **BOX-PRINT-CLIP at `ff82281`** — the estimate paginator retired from all four print documents and DELETED, both proven on paper — both cable checklists paginate against **DOM-measured** heights, all five estimate constants deleted, **proven on paper at 190 of 190 tail lines** · **ORIENT-FIXFAILED CLOSED with NO code change** — a Quartz production path, not a defect; the existing fix was correct and unexercised · **PDF-CHROME-PATH** registered, invalidating every pre-Aug-27 landscape artifact · **GATE-METHODRACE** registered and promoted — the visual gate is blocked at HEAD · **BOX-PRINT-CLIP** found · CLAUDE-HARNESS-STALE and LINKJOIN-FENCE closed · **WF1A-PHRASE** registered · the rollover run at pre-open · **GATE-METHODRACE downgraded from blocking to intermittent** on three clean captures · **BOX-DOC-PARITY**, **BVSCNY-SEED-JUNK**, **CABLE-SPLIT (Trigger A)**, **SPLITEND-DAISY**, **MENU-DEPRECATE** and **CHECKLIST-GROUPCOUNT** registered · **Q-4 RULED — all seven disabled outputs get built, each with a v1 parity round** · the remaining-layouts parallel opened | v4.11 |
| **v4.11** | Aug 27 (aft) | **M0-WIRE SHIPPED at `df42fb0`** — M0-WIN's shared predicate wired into the v1 import, print's inline `'0'` test retired · **FOLDER-NABOX RULED (24) and PROVEN ON PAPER** · **R8-DRILL CLOSED**, restore proven by measurement, plan upgraded to Pro · **PRINT-CLIP and ORIENT-FIXFAILED both confirmed in printed artifacts** · STECK-ZERO's premise CONFIRMED · GRANT-TRUNCATE, CONTRAST-PAIR, WAITLIST-NOTIFY, SPENDCAP-BETA, EXPORT-CARRIES-STATE, BUNDLE-NA-UNTESTED registered · three Arch errors, all caught downstream | v4.10 |
| **v4.10** | Aug 27 | **Arch Prompt v33 installed (retires v32)** · `Architecture_Provenance.md` created · RM6-CAP + ONE-SESSION-ONE-ARC registered · the v15–v26 prompt lineage recovered and filed · governance session, no build unit | v4.9 |
| **v4.9** | Aug 27 | **PORT-COV LEG 2 CLOSED — 2-IMP-c shipped at `0468a5d`** · CLONE-FLAG-DIVERGE closed against a UTF-16 DDR read · APPLY-TIMEOUT · EXPORT-HISTID-DROP · ROUNDTRIP-INMEM · ORIENT-FIXFAILED · both parallels folded in · the palette closed by Daniel's rulings | v4.8 |
| **v4.8** | Aug 26 (eve) | **PRINT-PARITY unit 1 CLOSED at `8c6b2e0`** · DEVICE-VM-IS-NOT-CC and CLOSE-THEN-CONTINUE registered · the Inspector-vs-print finding · the cable-parity evidence parallel opened · an Arch gate found unrunnable by any CC session | v4.7 |
| v4.7 | Aug 26 | **2-MIG-b APPLIED** (ledger 54→55) · **2-IMP-b closed at `e17fd97`** · **R-7 ruled** · CABLE-IDENTITY-GAP's mult-line half · BRANCH-NO-ARM · LINES-PRINTLIST-NOEXPORT · LEN-INPUT-SUFFIX · LABELCOUNT-BOOL (→R-7) · **Arch Prompt v32** · **§2.2 withdrawn — the delete universe does NOT widen** · SPLITOF-NO-ARM, FENCE-HEREDOC, FENCE-PUSH, GATE-DEVCHECK, GATE-PORT, LINECOLOR-ZERO, TENSE-IS-A-CLAIM registered · PRINT-PARITY unit 1 issued | v4.6 |
| v4.6 | Aug 24 | **2-MIG APPLIED** (ledger 52→54) · **2-IMP-a closed at `af04251`** · **FENCE-COMMITA at `9a140ca`** · DEFTAILS-OVERBROAD · CC-BASHWRITE closed harness-side · DOMAIN-NOT-CHECK registered · the worktree question closed NO | v4.5 |
| v4.5 | Aug 23 | PORT-COV leg 2 SCOPED (5 units) · 2-EXP closed at `9c41b37` · eight rulings · two dangle figures struck · OQ1: re-import is a product requirement | v4.4 |
| v4.4 | Aug 22 (evening) | MIRROR-SYSMETHOD + METHOD-DUP/CAT-DUP at `d39179a` · HISTID-UUID + FENCE-STALE at `41d1002` · the first complete BVSC NY mirror import · GATE-NEST-UPGRADE ruled | v4.3 |

## Current Status (September 8, 2026)

Live at `https://minotaur.app` at **`1276cc4`**, pushed — `.git/refs/heads/main` and `.git/refs/remotes/origin/main` read identical, no `index.lock` beyond the two known `ORPHANED-` files. **Production READY confirmed at `1b23223`** by Daniel's pre-open check; the two deploys since are `44e206d` and `1276cc4`. **`Minotaur-Cowork` carries the previous close at `0242174`**, HEAD and origin identical.

Counts re-verified at this close on the final tree: suite **2908 tests / 151 test files, zero skipped** (from 2827/149 — **+81 tests in three new files**, all FIELD-CONTRACT's) · frozen **3/3 vs `be0769de`** · typecheck 0 · dependency gate **0 lines — `FindFieldType` is untouched, still `'text' | 'number'`** · build clean · visual **28/28 at 0.0000%**, no baseline event, load 3.62, finished 11:36:47 against a newest source edit of 11:29:53 so it covers the committed tree **by mtime rather than by assumption** · **migration ledger 57, live-read and byte-matched, UNCHANGED for seven consecutive units.**

⚠ **`CLAUDE.md` IS AT 109,135 CHARACTERS, 40,865 UNDER ITS LIMIT.** Two fence replacements today, both in the same pass as the counts they changed (FENCE-COUNTS), and leg 1's two earned rules — **SCAN-NONZERO** and **A SELECTOR IS NOT AN EMISSION** — lifted into `Standing rules` before the fence body was discarded. **Whether to relocate the 34,808-character rulings list remains Daniel's open question and is not yet pressing.**

⚠ **THE FIELD CONTRACT IS NOW A MEASURED OBJECT RATHER THAN AN INTENTION.** Before today nobody had put the four structures side by side. Measured: **seven Change-All registries in three different shapes** — two in shared testable modules (`cableChangeAll.ts`, `equipmentChangeAll.ts`) and **five declared inline inside multi-thousand-line clients** — **ten find maps**, **thirteen marker attributes**, and **83 editable cells**. The asymmetry is registered, not repaired.

⚠ **AND THE HAZARD THE UNIT LEAVES LIVE, NAMED BY CC AND CARRIED HERE RATHER THAN SOFTENED: FIND-PARITY IS PROVEN BY STATIC ANALYSIS ONLY.** The test reads source and proves the four structures agree. **It does not prove that any of the 25 new fields matches anything when typed.** Daniel's gate covered four checks on one project. **Twenty-five fields are live on seven surfaces with four spot-checked** — a wrong accessor returns an empty find, which an operator reads as missing data. **A short browser sweep of the remainder is owed before CDMX leans on it.** (FINDPARITY-UNTYPED.)

⚠ **READ `.git/refs/heads/main` FOR HEAD, NEVER `.git/packed-refs`** — it still carries `be0769de`, now **twenty-two** commits stale, and that value is also the frozen-gate baseline, so a stale read looks plausible. (GIT-CFG.)

**Next on the main line: FIELD-CONTRACT leg 3**, which owes Daniel two small rulings first. Then **DARKMODE-PASS**, then **BATCH-HOUSEKEEPING**. **VERHIST is scoped and waiting on five rulings** and, once ruled, Arch's recommendation places it after DARKMODE-PASS.

## 🏗 Sprint Plan (RM-7) — **IN ORDER**

**⚠ THIS SECTION IS ORDERED, AND THE ORDER IS THE POINT.** The first table is what gets built and in what sequence; the second is everything held behind it; the third is history. **A unit does not move up from PINNED without a ruling.**

### ▶ NEXT — the CDMX sequence is DISCHARGED and the rulebook blocker is cleared; this is what follows

⚠ **CDMX-SEQUENCE IS FINISHED (Daniel, 2026-09-05, closed 2026-09-07).** *"Category note management, category note printing, and print parity. Nothing else before those are finished."* All three shipped: CAT-ADMIN at `1a54c3b`, CAT-NOTE's print half at `267d477` with CAT-NOTE-RULE at `f4192cd`, and EQUIP-LIST-PARITY at `c525a55`.

⚠ **AND FIELD-CONTRACT'S FIRST TWO LEGS SHIPPED 2026-09-08** — the census at `44e206d` and FIND-PARITY at `1276cc4`, two production deploys, both gated. **Leg 3 is the repairs, and it opens on two rulings.**

| # | Unit | What it is | State |
|---|---|---|---|
| **1** | **FIELD-CONTRACT leg 3 — the repairs** | Legs 1 and 2 are shipped. **This leg is the fixing, and every item in it was found by measurement rather than recalled.** ⚠ **THE MODEL GAP, and it is one attribute in one array, not the redesign Arch first described:** the model band editor wears `data-find-field="model"` (`EquipmentListClient.tsx:3453`) and **three of the four equipment resolvers already read it. Exactly one cannot — `captureFocusedField` (`:1244`, `:1259`)** — which is why ⌥⇧D from Model lands nowhere. ⚠ **`data-equip-field` is NOT the fix**: `AM-3` (`:373-376`) kept it off the band editors deliberately, because Enter-advance reads it. **Also riding:** AUTOCOMPLETE-NOPORTAL (six equipment call sites against 24 elsewhere) · **CABLE-FINDDEFAULT** · the steck marker/column split · the library's missing Tab order · the two uncited Change All refusals · the four **CDMX-DEFERRED** cable findings · **CABLEGROUP-EDITOR**. ⚠ **OPENS ON TWO RULINGS: the `box_number`/`number` spelling, and whether boxes and bundles are held to the contract** | **NEXT** |
| **2** | **DARKMODE-PASS** | ⚠ **ONE unit rather than eight.** Eight of Daniel's sprint notes are dark-mode readability, and **all of them live in `src/theme/hcTheme.ts`'s `buildHCEquipPalette`**, which derives the whole palette from the project trim by hue. One file, one declared baseline event, one gate round. **CONTRAST-PAIR is partly discharged by it** | After FIELD-CONTRACT |
| **3** | **BATCH-HOUSEKEEPING, and `duplicate_item_v1` with the next migration window** | The registered small fixes that have been waiting for a convenient unit. ⚠ **`duplicate_item_v1` is a MIGRATION — Trigger B, Arch's, rehearsal-first — and must not ride a UI batch.** Its ⌥⇧D handler's wait on `lastCommitRef` is **load-bearing and STAYS**: it fixed a real race (recon 260717-2217) | After DARKMODE-PASS |

### ⏸ PINNED — held behind the three above

| Unit | Status | Ref |
|---|---|---|
| **MMM-CAT — the category sub-band editable under every sort** | **NEVER-RAN, and Daniel HIT IT LIVE 2026-09-08** | ⚠ **This is what his CDMX note "category band not editable unless sorted by category" actually is — a missing feature, not a defect, and NOT a FIELD-CONTRACT item.** Measured at source: the nested category band is a bare `<div>` (`EquipmentListClient.tsx:3393`) whose own comment says *"display-only here — the edit surface stays the category-sort top-level band"*; in category sort the category IS the outer band, which is editable. **Already ruled: MMM-CAT-R1** (⌥⇧I toggles Model ↔ Category in every sort mode; the outer method/box/group band stays click-only) and **MMM-CAT-R2** (a sub-band edit cascades to that visible sub-band's records only). **Handoff written 2026-08-11 at `to-cc/260811-0216_mmm-cat.md` and STALE** — it cites the sub-band at `:3122`, which has moved 271 lines. **Re-measure, do not re-design** |
| **2-MIG-c — the second gap migration** | QUEUED | **MIGRATION.** SPLITOF-NO-ARM **+ APPLY-TIMEOUT**. Trigger B; rehearsal-first; Daniel confirms before the apply |
| **EXPORT-HISTID-DROP — the cable export's identity projection** | QUEUED, and it blocks round-trip acceptance | 852 of 1,119 `HistoryID`s emitted against 1,119 identity rows live · 2-EXP / 2-IMP-b's, not 2-IMP-c's |
| **PRINT-PARITY unit 2 — the box residuals** | **STAGED, UNISSUED** | `handoffs/_staging/260826-1845_print-parity-box-ADDENDUM-1_DRAFT.md` · Stack underline · dashed grey cut rules (OQ-4) · Box-in-Box suppresses the Stack · revert `MC.BUNDLE_FLAG` |
| **GATE-THEMEGLYPH — the diagnosis** | QUEUED | its own unit; **never register it** |
| **FENCE-PUSH — lift the two push layers** | QUEUED | Strat-tier ruling; + `chmod +x`, + a battery proving bulk staging STILL blocked |
| **PRINT-PARITY — the CABLE family** | **UNBLOCKED and SCOPED** | **no fresh v1 print owed** · selectors → orientation → geometry · 4 content defects, ORIENT-FIXFAILED, 11 measured grids |
| S4-mid: D-2b + BASE-EVT (FOCUS-MP in headroom) | QUEUED | |
| **GATE-METHODRACE — the intermittent visual harness** | QUEUED, small, **no longer blocking** | **DID NOT REPRODUCE on 2026-08-28** — frame 03 captured cleanly on all three runs. Fired 4× on Aug-27 at loads 3.70–4.27 and NOT at 4.91 today, so **load is not the variable**. `visual-capture.mjs:425` queries with no settle wait. Rides the small clearing unit with **PRINT-HINT-EQUIP** and the stale run fence |
| **BOX-DOC-PARITY — the two box DOCUMENTS** | QUEUED | **NEW.** Print Box List and Print Contents Lists have NEVER had a parity round — unit 1 was the box LABEL family and the staged addendum is labels too. **Cannot run against BVSC NY** (BVSCNY-SEED-JUNK) |
| **THE EIGHT QUEUED LAYOUTS** — Group Parts · Device Patch · Equipment Labels · Bundle Summary · Line List · Steck Labels · Tail Folder Labels · **Bundle Labels** | QUEUED, **Q-4 RULED: all built, each with a v1 parity round** | **DESIGNED — the parallel is CLOSED and folded in.** Seven became eight (**Q-2: Bundle Labels ruled IN at v1 geometry**, the strictest bar of the eight) · **all three equipment-side v1 prints have LANDED** at `~/Minotaur_v1_exports/equipment/` · **Equipment Labels is BLOCKED behind EQUIP-LABELS-2**, a Trigger B unit · Steck Labels is 5167 and gets built on the die from the start |
| **CABLE-SPLIT — the split feature** | **BLOCKED on evidence. TRIGGER A.** | v2 has two columns and **no feature**; the only Split UI is the equipment quantity split. **Needs a v1 export from a show that uses splits** — the corpus has 3 rows in one show |
| **The four MEASURED-but-UNBUILT cable outputs** | QUEUED | Cable Checklist (**bottom margin 15.80 vs v1's 45.52 — 29.72 pt per page, which is why 54 v1 pages become 38**) · Bundle Sheets (right margin 27.00 vs 61.04) · Small Cable Labels (die 144.43 vs v2's 141.25) · Tail Checklist (row pitch 24.75 vs 26.00). Numbers banked in `cableGeometry.ts` |
| S4-late: GS-C → LINE-STECK (+STECK-COLOR) → S3-G-2 (+UNFLAG-WARN) → ALLOC | QUEUED | deferred by the Aug-21 sequencing ruling |
| S4-close: GBD | QUEUED | |
| Sprint-4 close reviews: WF1A-SUNSET · auto-mode rider EXPIRES · COWORK-PORT · PRINT-REDO if opened | QUEUED | |
| KKK-PASS (narrow-window responsive) | after Sprint 4 | |
| **Pre-beta: SHARE build → ADM-1 · AUTH-DOMAIN · 3TL · DEPS · CL-HILITE · GCM (R-AH)** | QUEUED | BATCH-PREBETA |
| **AC pass (BATCH-AC)** | after private beta STARTS (R-AG) | own trigger |
| Post-beta: BATCH-BOX · Maintenance · Spreadsheet Export · **RF/Walkies/Circuits (BVSC-RF)** · IAS Import · real-time collab · licensing ~Nov · PWA | QUEUED | |

### ✅ SHIPPED — history, retained for citation

| Unit | Status | Ref |
|---|---|---|
| S4-1 decks-clear · S4-2 S4-AUTO | ✅ | Aug 10–11 · `1f06092` |
| S4-3 legs 0–6 (box family) | ✅ | `8b078b2` |
| PORT-COV leg 1 — box EXPORT half | ✅ | `a243867` |
| ARCH-MIG-BOX — the migration | ✅ | ledger 52, rehearsal-first |
| PORT-COV leg 1b — box IMPORT half | ✅ | `4c234dd` |
| MIRROR-SYSMETHOD + METHOD-DUP / CAT-DUP | ✅ | `d39179a` |
| HISTID-UUID + FENCE-STALE | ✅ | `41d1002` |
| PORT-COV leg 2 — 2-EXP, the cable EXPORT half | ✅ | `9c41b37` · 281 columns sha256-pinned |
| PORT-COV leg 2 — 2-MIG, the RPC arms | ✅ | ledger 53 + 54, rehearsal-first |
| PORT-COV leg 2 — 2-IMP-a (types + bundles) | ✅ | `af04251` · round-trip 100% |
| FENCE-COMMITA — the bulk-staging guard | ✅ | `9a140ca` · a false NEGATIVE open since the fence was written |
| **PORT-COV leg 2 — 2-MIG-b, the gap migration** | ✅ **APPLIED** | **ledger 55**, rehearsal-first, pre-flight carried INSIDE the migration · five arms, all Arch's |
| **PORT-COV leg 2 — 2-IMP-b (cables + lines + tails)** | ✅ **CLOSED** | `e17fd97` · 33 files · **162 columns** · round-trip **100% on all three corpora** · own insert/update-only partitions, delete universe unchanged |
| **PRINT-PARITY unit 1 — the box label family** | ✅ **CLOSED** | `8c6b2e0` · worst element 51.35 pt → **0.63 pt** · geometry measured off the v1 PDFs, not derived from page ÷ grid |
| **CABLE-PARITY EVIDENCE — the parallel** | ✅ **CLOSED** | three returns · all 17 outputs data-derived · 11 grids measured in points · 3 sorts recovered · 8 v2 pairs measured · no repo writes, no CC handoff |
| **PORT-COV leg 2 — 2-IMP-c (clone links, by recompute)** | ✅ **CLOSED** | `0468a5d` · 31 files · 913 groups / 2036 members on BVSC NY · mult-line and bundle vocabularies ship COMPLETE and GATED · CLONE-TOKEN's export half landed · **leg 2 closed entire** |
| HARNESS H1 · H2 · H4 | ✅ | `e55269c` · `ddcf929` · `13a8233` |
| DOOR + LANDING | ✅ | `e92f2c9` |
| **PRINT-PARITY cable pass 1 — M0-WIRE (records)** | ✅ | `df42fb0` · 28 files · suite 2374/132 → 2398/133 |
| **PRINT-PARITY cable pass 2 — the PAGE CONTAINER** (ORIENT-FIXFAILED → PRINT-CLIP) | ✅ **CLOSED** | `eaabd03` · 8 files · checkpoint 1 closed with **no code change** (PDF-CHROME-PATH); checkpoint 2 shipped DOM-measured pagination, five constants deleted, **190/190 on paper** · **visual gate owed** |
| **BOX-PRINT-CLIP — the box family's own clipping** | ✅ **CLOSED** | `ff82281` · 7 files · **15 of 395 Contents blocks and 2 of 60 Box List rows** were being lost; zero after · sectioned measured pagination, the truck break now STRUCTURAL · `paginateByHeight` **deleted** · **136/136 on paper** |
| **PRINT-PARITY cable pass 3 — the COLUMN DIE** | ✅ **CLOSED** | `c6bacf7` · 5 files · four outputs rebuilt on v1's measured die, worst element **48.15 → 0.37 pt** · **AMENDMENT 1** applied the folder cell-order ruling · **checkpoint 1 CHECKLIST-GROUPCOUNT closed by measurement with no code change** · **it set COLUMN x only — row y was never touched** |
| **PANEL-CLIP — pass 3's own regression** | ✅ **CLOSED** | `702048b` · 3 files · **63 of 137 tail panel labels wrong** (56 off the sheet, 7 cut mid-token) from a dropped `flexWrap`; found by Daniel's printed proof, not by four green gate runs · **36 Tail Line and 18 Small Tail Block cells found silently lost** in the same sweep · all three now paginate by measured height · **PROVEN ON PAPER at 137** |
| **PRINT-PARITY cable pass 4 — FULL LAYOUT PARITY** | ✅ **CLOSED** | **The anatomy diff** (`pdfruns.py` built outside the repo; nine element classes × four outputs, both sides, per-cell variation recorded; 16 ranked gaps) then **CELL-AND-ROW at `2cc6b14`**, 12 files, +38 tests · ladder **9 values → 1** · the `"<End> Tails"` field un-inverted and the **19.11 pt** right-edge overhang closed · five `Length()` step-downs and the `Line Color` tint built **from v1's own layout definitions** · 74 panel rules · `HEADER.panel` wired · model markers restored · **0 content metrics down** |
| **FENCE-PUSH — the push lift** | ✅ **CLOSED** | `0bf6df3`, 3 files · a **narrowing**, not a removal: bare `git push` is CC's, force/`+`refspec/`--delete`/`-d`/`--mirror` human-only at both layers · **34-case battery on the installed hook; the same battery at HEAD differs in exactly one case** · live two-layer test on a deliberate no-op · CC **hard-stopped** rather than editing its own permission file, and Daniel applied both candidates |
| **STOCK-IS-DIE — the 5167 columns onto the Avery cut** | ✅ **CLOSED** | `9a333cc` · 3 files, +491/−66 · three grids onto one `AVERY_5167` constant · Small Cable Labels onto the module · **114-metric before/after acceptance, 6 changed and all 6 column origins, nothing down** · confirmed on Daniel's rendered print, four columns exact · **the physical sheet DEFERRED and still owed** |
| **CHANGEALL-METHODNEW — the Method create-missing fix** | ✅ **CLOSED** | `e3a336a` · 4 files, +284/−29 · the doctrine and the one `createMethod` copied from the band editor's bulk arm · a new pure `planMethodChangeAll` making `method_id: null` **unrepresentable** from a non-empty draft, 32 tests · **negative control reproduced the defect at 400 blanked rows** · a stale-closure 409 found in the fix itself and closed at source · gate **28/28, all byte-clean, no baseline event** |
| **CHANGEALL-FOUNDSET — the batched-write ceiling** | ✅ **CLOSED** | `ba0058f` · 14 files, +1160/−137 · **36 call sites onto four SECURITY INVOKER RPCs through ONE helper** — 19 updates · 9 deletes · 7 reads chunked with errors that THROW · 2 doubled raw filters at half chunk with de-dupe · the two select-then-delete pairs collapsed to single statements · guard test proven against the baseline tree at **32 hits** · GREP-NULBYTE fixed · migration ledger **57**, Arch-applied |
| **CHANGEALL-BOXNULL — the Box arm's PAYLOAD defect** | ✅ **CLOSED** | `6deb411` · 8 files, +640/−34 · a pure `planBoxChangeAll` making the stored id **unrepresentable**, 33 tests · the New Box dialog suspend/resume, nothing written until OK, Cancel writes nothing at all · **BOXNULL-TOOLBAR** on Daniel's option (a), equipment surface only, **enforced by `changeAllInvokeScope.test.ts` rather than by a comment** · negative control reproduced **SIX** pre-fix cases including one nobody predicted · gate 28/28 TWICE |
| **CAT-ADMIN — the Categories tab, with the category note** | ✅ **CLOSED** | `1a54c3b` · 14 files, +1313/−580 · **the tab existed all along as `enabled: false`** — the unit switched it on and filled it · `CategoriesAdminClient.tsx` copied from `MethodsAdminClient.tsx` on **v1 layouts 317/336 proved byte-identical twins** · **CATNOTE-CELL held by a byte-reading test that was TIGHTENED, not loosened, to admit the autosize handler** · **CATNOTE-AUTOSIZE** · the note's save rule **proven on live production data by Daniel's gate** · **GCMTAB-STALE** found and fixed in both grids · declared `05-gcm` event, blast radius proven at exactly 2 files · **`CLAUDE.md` 161,200 → 134,764 chars** |
| **EQUIP-LIST-PARITY — the list and the cover letter at v1's size** | ✅ **CLOSED** | `c525a55` · 14 files, +2943/−379, four new · **six rounds, one deploy** · **the 0.750 was a UNITS defect** and a v1 point is now 4/3 of a v2 pixel through one die module · **the estimate paginator was dropping 42 blocks in production and now drops zero**, proven at every round against a like-for-like control · the note wrap column is a greedy wrap over **measured glyph advances**, 155 estimated against 155 rendered · every baseline advance an exact integer pixel, single-valued on the emitted PDF · **QTY-ITALIC · SUBNOTE-HARDBREAK · CATNOTE-FONT-DUP · the indent ladder · the header block · the rule weights · NOTE-GROUPING** · CommonMark escapes · **Daniel's gate found three defects on paper that no machine gate saw** |

## 📌 Docket

**⚠ THREE TABLES, AND THE DIVISION IS THE POINT** (RM-8, new in v4.23, on the Sprint Plan's model). **▶ ACTIVE** is what is being worked, dated, or owed now. **⏸ HELD** is real and registered, waiting on a named unit, batch or trigger. **📐 STANDING** binds indefinitely and never closes — a rule or a permanent caution, and a row here is never "done." **Closed items do not live here at all**; they are indexed at the end of the Docket and their text is in the Ledger. **Every row below was carried verbatim from v4.22 — the sort moved rows, it did not rewrite them.**

### ▶ ACTIVE — being worked, dated, or owed now

| ID | One line | When / trigger |
|---|---|---|
| **CLAUDEMD-ATLIMIT** | ⚠ **NEW, and it BLOCKS EVERY BUILD UNIT.** `CLAUDE.md` closed EQUIP-LIST-PARITY at **149,997 characters — THREE under its 150,000 limit**, from 17,947 under three closes ago. The run fence is REPLACED not appended (FENCE-ONEUNIT) and **still grows per unit**; the hard-stop fired once inside the unit at 150,582 and was honoured. **Three of that unit's findings are recorded as NOT in the fence**, including one load-bearing constant that survives only because a test asserts it. ⚠ **A file over the limit loses its TAIL, and the tail of this one is the Return Handoff File Protocol** — the same failure FENCE-ONEUNIT was written to fix. **The next unit cannot write its first finding.** Folded into ROADMAP-REFACTOR as its first item on Arch's recommendation | **⚠ IMMEDIATE. No build unit runs before it** |
| **BOXBAND-DUPKEY** | **⚠ NEW, found by Daniel's browser gate 2026-09-08, root-caused, and PROVEN not FIND-PARITY's.** Two bands both labelled `Truck: Truck 2` on the Box List, one box drawn in both, and a React duplicate-key error at `BoxListClient.tsx:1854`. **Cause: the comparator's equality is COARSER than the band label's identity.** `banded` (`:1562-1573`) is a **run-length** grouping on the raw trimmed truck string and keys React on that string; `makeBoxComparator('truck')` (`boxSortChains.ts:89`) orders by `compareLabels` — `localeCompare` with **`sensitivity: 'base'`** and **`numeric: true`**. Variants compare EQUAL, so their order is arbitrary and they interleave, while producing DIFFERENT labels — one label opens a band twice. **CC reproduced it executably against the real comparator; Arch corroborated it in live data, which CC cannot read (HARNESS-RLS-CORPUS): on `Cable Parity Fixture` (`dbf56057-0447-41a9-81b1-a8842a83b958`) `Truck 2` = 10 rows (hex `547275636b2032`) and `truck 2` = 1 row (hex `747275636b2032`), the lowercase row being `More Hardware`, box 409.** No other case or padding variant there. **NOT introduced by this unit:** `boxSortChains.ts` is byte-identical at `44e206d` and no line of the diff touches banding, sorting, nesting or truck. ⚠ **THE FIX IS THE KEY, NOT THE DATA, AND NOT A UNIQUE KEY** — `${label}-${i}` silences the warning and leaves two bands the sort believes are one. **A band's identity must be the COMPARATOR'S EQUALITY CLASS, not the raw string it displays.** That merges two bands and decides which spelling shows, so it is UX-affecting with no pedigree. ⚠ **DO NOT correct `More Hardware`'s truck string** — plausibly genuine v1 data, the only known live reproducer, and editing the parity corpus is what produced BVSC-8POWER-DRIFT. ⚠ **Whether the equipment list bands with the same asymmetry is UNMEASURED and is not asserted** | **Daniel's ruling, then its own small unit** |
| **FINDPARITY-UNTYPED** | **⚠ NEW, and it is the hazard FIND-PARITY leaves live — named by CC rather than discovered later.** The contract test is a **static analysis**: it proves the four structures agree and **proves nothing about what happens when a field is typed into the Find panel.** Daniel's gate covered four checks on one project; **25 fields are live across seven surfaces.** A wrong accessor returns an empty find, which an operator reads as missing data rather than as a defect | **A browser sweep of the remaining fields, before CDMX leans on it** |
| **FINDFLAG-CHECKBOX** | **⚠ NEW — RULED (Daniel, 2026-09-08) and it SUPERSEDES a declared choice.** A flag criterion in the Find panel becomes a **pre-checked checkbox** rather than `contains` plus a text field; **unchecking finds the unchecked rows.** CC's `Yes`/blank face ships as the INTERIM and is not wrong, only clumsy. **Its own unit: it needs a third `FindFieldType`, which FIND-PARITY's handoff explicitly forbade.** ⚠ **FOUR FLAGS ONLY when built — `num_labels` is an INTEGER and a checkbox would collapse a `2` to a tick, which is EQUIP-LABELS-2's own stated reason for keeping it out of `EQUIP_CHECK_FIELDS`. Labels stays a value field** | Its own unit, after FIELD-CONTRACT leg 3 |
| **CABLEGROUP-EDITOR** | **⚠ NEW — RULED (Daniel, 2026-09-08).** *"A bundled cable shouldn't have a group field — its group should be text instead, inherited from the bundle."* **The FIND half shipped** in FIND-PARITY's amendment 2 (the accessor reads `effective_group`, which the filter, the option list and the sort bands already did). **This row is the EDITOR half: a cell LEAVING the editable set** — which the census and the contract test now have an opinion about, so it lands where the machinery can see it | **FIELD-CONTRACT leg 3** |
| **CABLE-FINDDEFAULT** | **NEW, and FIND-PARITY created it by succeeding.** `CableListClient.tsx:476` reads `[data-b2-field]` only. That was coherent while no inspector field was findable; **the seven this unit made findable cannot seed ⌥⇧F from focus.** CC deliberately did not fix it — a document `focusin` listener is a different mechanism from the Change-All focus capture, and touching it changes keyboard behaviour. The box equivalent WAS widened, because there the gate is one line inside a handler the unit was already correcting | **FIELD-CONTRACT leg 3** |
| **CAPFLAKE-LIBLIGHT** | **⚠ NEW, and it is a MEASUREMENT PARKED FOR H3 rather than a defect to chase.** FIND-PARITY's visual run 1 returned **27/28**: `04-library-light` at **110357/1600000 px judged (6.8973%), 293871 px measured @ch0, max Δ248, bbox 0,48–1599,996, load 4.44** — every other frame byte-clean **including `04-library-dark`**. Runs 3 and 5 were 28/28; runs 2 and 4 REFUSED on load, which is GATE-LOAD-AUDIO working. **GRAPH-BEATS-RERUN settled it before run 3 existed: 0 of 4 changed modules reachable from the library route, against a positive control of 2 of 4 from equipment.** ⚠ **It does NOT match the registered theme-race invariant** (1451200 px at bbox 0,93–1599,999) — **a new signature, on a frame whose DARK half is already registered.** ⚠ **It must NOT enter `screenshots/diff-exceptions.json`** — that registry masks pixels and DIFF-EXC-BOUND is owed first; the prose capture-flake list is where it belongs. **Free data point beside it:** the Δ1 registry site `subpixel-wander/topbar-theme-toggle-glyph` fired on run 5 in both halves of frames 02 and 03 and **not at all** on runs 1 and 3 — presence wandering on an identical tree. ⚠ **That site is NOT GATE-THEMEGLYPH and the names invite the error: the registered one is bbox 1582,18–1583,27 at Δ≤1; GATE-THEMEGLYPH's undiagnosed one is bbox 1273,14–1307,32 at max Δ42 and is unregistered.** All four registered sites remain at Δ1, so DIFF-EXC-BOUND's trigger is still untripped | **H3** |
| **VERHIST — durable per-production version history** | **⚠ PROMOTED out of the LIC-NOV row and SCOPED by a parallel session 2026-09-08 (`handoffs/parallel/260908-1551_verhist-scope_RETURN.md`, copy in `drafts/`).** **Ruling of record (Daniel, 2026-09-08): it is SCOPED, no Strat pass** — v1 users rely on Dropbox to take a whole `.mino` back, for reference, for side-by-side, and to undo; v2 removed that net. **Proposed shape: one table (`project_snapshots`, DATA-shaped, not the print-shaped `equipment_list_revisions`), one function (`snapshot_project_v1`, skip-if-unchanged by content hash), one timer (`pg_cron`, hourly), one gesture ("Open as new production" — a fork, never in place), which answers all three use cases with one piece of UI.** ⚠ **ARCH VERIFIED THREE OF THE PARALLEL'S MEASUREMENTS AND CORRECTED A FOURTH.** Confirmed: the five tables with no `updated_at` (`cable_details · tail_details · box_details · link_groups · link_group_members`) — and **more than five lack it, which strengthens "compare content, not clocks"** · `pg_cron` **1.6.4 available, `installed_version` null**. ⚠ **CORRECTED: the payload is NOT "1–2 MB". Measured on the US Tour (`57874c69-06b6-41b1-91d8-6a00e5255110`, 2,591 rows): 5,390,551 bytes — 5.3 MB, ~3×, and a FLOOR** because thirteen tables were included and the definitive list is unsettled. **That is JSON text length; the `jsonb` on-disk cost after TOAST compression is UNMEASURED and is the number retention actually needs.** ⚠ **AND THE RISK IS ON THE WRONG HALF: the parallel names APPLY-TIMEOUT as a prerequisite of the RESTORE. The SNAPSHOT has the same exposure and it is more dangerous** — `authenticated` carries an 8-second statement timeout, and the proposal puts `snapshot_project_v1` in front of Change All, Delete Found and import apply. **Ruling owed that is not on the parallel's list: if the snapshot fails, does the destructive gesture proceed? Arch's position is NO — refuse the gesture and say why; a net the operator believes is there and is not is worse than none** | **FIVE RULINGS OWED (R-V1…R-V5), plus two Arch added. Then VERHIST-MIG (Arch, Trigger B) → VERHIST-UI (CC)** |
| **CDMX-SPRINT** | ⚠ **NEW — seventeen notes from Daniel running the CDMX list, 2026-09-07, all queued behind sending it and none critical.** Triaged against the codebase in `drafts/260907-2330_cdmx-sprint-notes_triage.md`, every file:line checked at source. **Six are FIELD-CONTRACT's acceptance criteria arriving from real use** (duplicate landing back in the Model field · focus lost when a category change re-sorts the last row · ⌘J out of a find · the category band · the split dialog's initial selection · the find panel not re-running on edit). **Eight are dark-mode readability and become DARKMODE-PASS.** Three are chrome (tab title · project/theatre in the header · the model field's narrow-window cut-off, which is probably KKK-PASS's first real finding). One is `duplicate_item_v1`. ⚠ **TWO CARRY QUESTIONS ONLY THE GESTURE CAN ANSWER and they are recorded as OWED AT THE UNIT rather than left to be inferred** — whether the list was sorted by category when focus was lost, and whether the band problem reproduces on a NAMED category or only on `Uncategorized`. **ASK THEM; do not derive them from the code** | Behind ROADMAP-REFACTOR |
| **AUTOCOMPLETE-NOPORTAL** | ⚠ **NEW, measured at source, and it is the FIELD-CONTRACT shape a THIRD time.** `AutocompleteInput` has two branches: `portalDropdown` renders into `document.body`, computes geometry from the input rect, **flips upward when there is no room below** and clamps `maxHeight` (`:314-332`, `:640-645`); the fallback is `absolute top-full max-h-56` with no viewport awareness (`:658`). **`portalDropdown` is passed on tail, line, cable, bundle, box, cable-type and inspector surfaces — 24 call sites — and on ZERO equipment ones** (`EquipmentItemRow.tsx:745`, `:805`; `EquipmentListClient.tsx:2808`, `:2860`, `:3315`, `:3457`). That is Daniel's *"auto fill is cut off by bottom of screen"*, and **the component's own comment says the portal exists for exactly this.** Equipment is again the surface that missed a wiring every other surface has | Rides FIELD-CONTRACT or BATCH-HOUSEKEEPING |
| **DARKMODE-PASS** | ⚠ **NEW, and it is ONE unit rather than eight notes.** Every dark-mode item Daniel raised resolves to a named token in **`src/theme/hcTheme.ts`'s `buildHCEquipPalette(trimHex, 'dark')`**, which derives the palette from the project trim by hue: filter labels `filterText ch(hue,14,72)` · project/theatre `headerText ch(hue,22,84)` · category drop-down `filterInputText`/`selInputText ch(hue,10,82)` · selected row `rowSelected ch(hue,20,16)` · category band `groupBorderBottom ch(hue,28,26)`. **Two are NOT colour values and want their own fix:** the found-count bar staying light after a find is a **missed theme wiring**, and the orange default button is not derived from `hue` at all — **CONTRAST-PAIR already ruled its mechanism** (a hex→text pairing plus a luminance fallback), so building it discharges part of CONTRAST-PAIR rather than adding work. ⚠ **The equipment list and project menu are captured gate frames — this is a DECLARED BASELINE EVENT; CC measures which frames move, Arch does not predict** | After FIELD-CONTRACT |
| **EQUIPLIST-WHOEST** | ⚠ **NEW, measured BOTH directions and deliberately NOT acted on.** The cover letter's `estimateWhoHeight` **models a layout the renderer no longer draws** — it charges a second line whenever a row has an email or phone, a two-line-per-person shape, where the renderer now puts role, name, email and phone in ONE four-column row. **Over by 119 px on a six-row block.** ⚠ **And the safe-looking correction is the one that CLIPS:** a flat row height would be exact on this shape and under-count every wrapping row — measured, a row whose role wraps renders 48 px and **the estimator does not move at all, because it has no wrap model.** Safe today because it over-counts. Wants the treatment `textMetrics.ts` gave the sub-note column: a measured wrap model for the roman face across four column widths | **Its own scoped round. Arch's** |
| **REVGROUP-SPLIT** | ⚠ **NEW, and it is a KNOWN divergence on ONE surface rather than an oversight.** NOTE-GROUPING splits the plain-list print by `(model, note)`; **revision-mode print still groups by model alone and stacks a model's differing notes under one row.** `groupByModel` was left byte-identical because two of its five callers are not the print render: **`pairRevision` matches old rows by `row.model` ALONE**, so two current rows sharing a model would each claim the same old quantity and double-count silently, and **`buildRevisionSnapshot` WRITES `equipment_list_revisions` JSON**, whose recovery rules are documented against that grouping. Bringing revision mode onto the same key means deciding **what a new (model, note) row pairs against in an OLD snapshot written model-only** — a question about historical data, not an implementation detail. **No regression: revision mode keeps today's behaviour.** ⚠ **Arch's §1 said "print only" and CC's census found this at source and narrowed rather than building it — the guard working** | **Its own unit. Changing a persisted artifact's shape is not a print round's** |
| **EQUIPLIST-DOTRULE** | ⚠ **NEW, and it is both a parity divergence and a performance question.** v1 draws **ONE solid 1.00 pt grey rule per row** at `x 25.27 → 586.27`; v2 draws a **dotted line of 372 filled rects per row — 176,527 objects in a 24-page PDF against v1's 690**, a factor of 256. Style, weight (0.75 pt against 1.00) and span (27.00→584.25 against 25.27→586.27) all differ. **Dotted-vs-solid is UX-affecting with no pedigree either way, so it is unruled and was fenced out of EQUIP-LIST-PARITY.** Plausible cause of the slow print preview | **Daniel's ruling; pairs with PREVIEW-CLIP** |
| **EQUIPLIST-CLMARGIN** | **NEW.** v1's cover-letter body starts at `x0` **37.91** against v2's **27.00**, because v1's letter is a standalone Word document with its own 36.00 pt margins while v2's shares the list's page chrome. **Matching it would diverge the two halves of one printed document.** Measured, registered, not built | Daniel's desk |
| **FENCE-FORMS** | **⚠ NEW, and it is the escalation of FENCE-HEREDOC rather than a repeat of it.** Two consecutive units self-reported a single-file stream edit the fence did not match: **`perl -0pi`** in CAT-NOTE (the matcher was tightened for `perl -pi` on 2026-07-31) and a **`python3` heredoc** in CAT-NOTE-RULE. **Three sightings in four days across two binaries, and the fence caught neither.** Both were disclosed in full with the verbatim command, both files reverted and proven byte-identical by `diff`, the frozen gate run **immediately** rather than deferred, and each change redone through the Edit tool — **the disclosure discipline is working; the guard is not.** ⚠ **The ruled list of forms to block is a DESCRIPTION, not a specification, and it is being patched one spelling at a time by READING rather than by TESTING.** It wants a battery the way FENCE-PUSH was proved at 34 cases. **TEST THE GUARD, DO NOT READ IT** | **Arch's. Its own small unit, after EQUIP-LIST-PARITY** |
| **GATE-REPORTING** | **⚠ NEW, process, and the guard is unobservable from the seat that enforces it.** Daniel's browser gate runs in his browser and is reported to **Arch, in Cowork**; the confirm phrase is typed at **CC**. **Both of this session's units therefore recorded the gate as never run when it had run and passed** — and CC was right to refuse to infer a gate from silence, having just been wrong in the other direction on `267d477`. **A gate reported where CC cannot see it is, to CC, indistinguishable from a gate that did not happen.** GATE-BEFORE-COMMIT needs a **reporting channel**, not more emphasis — O-12's shape: a rule checked at an event holds only if the event is visible where it is checked | **Next handoff's commit block** |
| **EQUIPLIST-ESTIMATE-PAGINATOR** | **⚠ NEW, and it is PRINT-CLIP's mechanism still live on a document nobody converted.** PRINT-CLIP and BOX-PRINT-CLIP retired the estimate paginator from the four cable and box documents and **deleted `paginateByHeight`. The equipment list was never converted.** `paginateBlocks` accumulates `usedH` from `SECTION_HDR_H`/`METHOD_HDR_H`/`DETAIL_ROW_H` against `CONTENT_H`, into a fixed `height: PAGE_H` container whose body is `flex: 1, overflow: hidden`. **Proven live by negative control inside CAT-NOTE: reverting one line lost SIX of 120 rows off page 2 with no error, no warning and no gap in the numbering.** Every variable-height element added to this flow is a clip risk until it is converted. **Converting it is its own unit with its own printed proof — do not attempt it inside a feature unit** | Its own later unit; not in the CDMX sequence |
| **CATNOTE-EXPORT-LF** | **⚠ NEW, and it went LIVE on 2026-09-06 rather than staying hypothetical.** An edited category note now stores **LF** where the imported one had **CR**, and `buildSheet.ts` emits `notes` to v1's `categories.xlsx` through `reverseText` **verbatim**. **Whether v1 reads an LF-separated note as two lines is UNMEASURED.** Nothing was coerced on write to paper over it, deliberately. PORT-COV-REIMPORT makes round-trip the acceptance, so this is in scope for the product requirement | Before the next export unit |
| **CAT-MERGE** | **⚠ NEW.** `merge_methods` exists as an RPC; **there is no `merge_categories`**. CAT-ADMIN therefore **refuses and reverts** on a rename collision, naming the category already using that name — nothing was stubbed and `merge_methods` is not called. Building it is a new database function: **Arch's, Trigger B, rehearsal-first** | Its own later unit |
| **CATCOUNT-COLUMN** | **NEW, Daniel's, and deliberately undecided.** Should the Categories tab show an item count? **v1's category form is two fields and shows none**, so a count column has no pedigree; CAT-ADMIN renders none and counts internally for the delete guard only. ⚠ **The trap if it ever is built:** the count is unfiltered by `type` — the spine is polymorphic — so `13.0 Cable` on BVSC reads **1,480 spine rows**, not 1,480 pieces of equipment. The EQUIPVIEW-UNSAFE shape one table over | Daniel's desk; not blocking |
| **ROADMAP-REFACTOR** | **⚠ NEW (Daniel, 2026-09-06), queued behind the CDMX sprint.** Three observations from reading v4.19 whole, offered as scoping evidence rather than a design: **(1)** the file does not obey its own *"Forward-only — history lives in the Ledger"* header, carrying closed items inline as `~~strikethrough~~` rows in both docket tables; **(2)** a **superseded duplicate is live inside the Docket** — APPLY-TIMEOUT appears twice, the EVENT copy marked *"SUPERSEDED by the DATED row above"*, which is the exact shape that has produced wrong claims here before; **(3)** the single unified Docket lacks the three-table discipline the Sprint Plan above it now has. **Not in scope unless Daniel says so:** the 🧭 block, which RM-6 bounds at six bullets and which works | **After the CDMX sprint (Daniel's ruling)** |
| **APPLY-TIMEOUT — the import's 8-second ceiling** | **⚠ PROMOTED 2026-09-05: CONFIRMED LIVE on a real user importing a real show.** A full BVSC Tour apply returns `canceling statement due to statement timeout` and **writes nothing** — the single transaction holds, which is the design working. Measured at source: `authenticated` carries `statement_timeout=8s`, `anon` 3s, and `import_apply_v1` is SECURITY INVOKER with `search_path=public` and **no timeout override**. Daniel's workaround shipped the show: **several smaller imports, each its own transaction**, verified complete sheet by sheet. ⚠ **REHEARSE, DO NOT PATCH: Postgres arms the statement timeout when the statement BEGINS, so setting it inside the function may not lift a timer already running.** The reliable lever is role-level and raises the ceiling for every request the app makes — a real tradeoff, and Daniel rules it on a measurement, not an opinion. **Chunking the apply is NOT a candidate: it destroys the atomicity that made a failed import harmless** | ⚠ **THE PIN MAY HAVE LAPSED AND IT IS DANIEL'S ONE WORD.** It was pinned *behind the CDMX work*; **CDMX-SEQUENCE is DISCHARGED — all three units shipped.** If the pin is spent this unblocks **2-MIG-c AND VERHIST's restore path** in one answer. Trigger B. Arch's, rehearsal-first, Daniel confirms before the apply |
| **CDMX-DEFERRED — the four cable findings from the 2026-09-05 gate** | **⚠ NEW, and DEFERRED BY DANIEL'S EXPLICIT INSTRUCTION 2026-09-05:** *"I would like to log remaining fixes from this unit until after our production patches for BVSC CDMX."* All four are on the CABLE list and **none is a regression from `6deb411`** — the Change All invoke marker exists on exactly one component, machine-checked, so the autocomplete behaves on cable byte-identically to before. **CABLE-NAME-COMMIT** — a cable NAME commit is the only cable arm doing work after its write: `maybePropagateCableName` fetches the project's **entire mult-line list** for any branch or mult cable **before it knows whether anything needs propagating**, and the plan is often empty; separately the repaint Daniel saw (old value, then a blink to the new one) is UNPROVEN — the local patch IS applied first, and the lead is that `writeIdsDetails` fires `cancelQueries` without awaiting it, so an in-flight read can land after the optimistic patch. **Measure it on the running app before writing it down as a cause.** · **CABLE-CHANGEALL-BOX** — the cable Change All confirm and the New Box dialog are two independent overlays with nothing clearing or ordering them, so a pending confirm for one field stacks on top of the New Box dialog for another (observed: *"replace the Description field of these 1121 records with 'for eating'?"* over *Create box "Ruben"*). **The cable twin of what `6deb411` closed on equipment** · **CABLE-TAB-BUNDLE-METHOD** — Tab does not move from the Bundle pull-down to the Method pull-down; the ruled tab order, the same family as the steck exclusion · **BOXBAND-DOCTRINE** — the sort-band editor's box arm still silently no-op-reverts an unmatched name (`EquipmentListClient.tsx:1903`), left untouched deliberately because ruling 1 says every Box **field** and a sort band is not one | **All four ride FIELD-CONTRACT, AFTER the CDMX patches** |
| **METHOD-SORT-ORDER** | **NEW (Daniel, 2026-09-05), from the v2 print.** Methods **without** a sort override should appear **after** methods that have one. Today they do not | After the CDMX patches |
| **PREVIEW-CLIP** | **NEW (Daniel, 2026-09-05), from the v2 print.** Content is cut off in the **print preview** that is NOT cut off in the produced PDF. The PRINT-CLIP / BOX-PRINT-CLIP family's mechanism on the on-screen surface only — those two were fixed in the printed documents and **the preview was never measured** | After the CDMX patches |
| **EVIDENCE-UNTRACKED** | **NEW, small, CC's OQ4.** `docs/reference/evidence/equipment/` is untracked — four PNGs dated Aug 21, from S4-AUTO. Correctly excluded from CHANGEALL-FOUNDSET's commit. **Wants a deliberate keep-or-drop, not drift** — the DEADEXPORT-GRIDCOL shape one folder over | Daniel's desk; not blocking |
| **BUNDLE-LINK-IMPORT** | **NEW, and it is the one decision 2-IMP-c deferred.** v1's bundle-field link groups exist and were USED — **19 / 42 / 26** groups across BVSC NY / BVSC / JOY. `BUNDLE_LINK_VOCABULARY` ships complete with `enroll: false` on all eight and a DO-NOT-ENROLL test. Import them, or leave them out permanently? Flipping is 8 flags. **Arch's read: it is a PRODUCT question — v2 has no bundle lockstep surface, so a group imported today is a row nothing reads and nothing maintains** | Daniel's desk; not blocking |
| **OQ-1 COLOUR-TABLES** | **NEW, and it blocks work.** v1 has TWO colour tables that agree on four values and disagree on two — Green **#66B132** (cable) vs **#008F00** (box labels), Red **#D90B00** vs **#FF2712**. v2 has one. Does v2 keep one (which?) or gain a second keyed to record type? **Blocks every `minotaurColors.ts` edit** | Before PRINT-PARITY's palette work |
| **SPENDCAP-BETA** | **RULED (Daniel, Aug 27): KEEP the Supabase spend cap ON.** With it on, exceeding a covered quota **stops that item** rather than billing. Measured headroom: database **34 MB / 8 GB** · storage **2.82 MB / 100 GB** · **12** users / 100k MAU — nothing is close. **Rider: PITR is EXCLUDED from the cap and bills regardless if enabled.** Revisit at the same trigger as R8's re-verify | **Before public beta (October), or if any covered item crosses half its quota** |
| **CONTRAST-PAIR** | **RULED (Daniel, Aug 27): no white text on yellow.** Root cause measured: `minotaurColors.ts` ALREADY pairs each colour with readable text (Yellow → `#000000`), and `ImportV1Client.tsx:1105` hardcodes `color:'#fff'` over `backgroundColor: projectColor` — one style object, three buttons. **Not a yellow problem: EIGHT of fourteen palette colours want black text.** `paperwork_color` is a HEX and one default (`#4f52e7`) is off-palette, so the fix needs a hex→text pairing **plus a luminance fallback**. Gate frames measured: none renders the import page, so probably zero frames move — **CC measures, Arch does not predict**. **Rider: `BundleListClient.tsx` reads as BINARY to grep and would be missed by a text sweep** | Small unit; Daniel's ruling is the pedigree |
| **WAITLIST-NOTIFY** | Email Daniel when someone submits the waitlist form. **Send from the existing route handler (`src/app/api/waitlist/route.ts`) after the write succeeds — CONFIRMED, but NOT for the reason the raising session gave.** Measured: the route uses the ANON key calling SECURITY DEFINER `waitlist_join_v1`; **there is no service-role client in `src/`** (grep = 0). **Three success-shaped exits, only ONE may email** — the bot honeypot returns ok and writes nothing. **`waitlist_join_v1` returns `void` and carries ON CONFLICT, so the route cannot tell a NEW signup from a repeat**: notify-on-every-accept needs no migration; new-signups-only is a signature change, **Trigger B**. **TRIGGER A — decision block before code, in a new session** | Daniel's desk; provider chosen for the beta-invite horizon |
| **GRANT-TRUNCATE** | **⚠ NEW, and it rides the next migration.** `anon` holds **TRUNCATE on 10 objects** (`bundles · cable_details · cable_mult_lines · cable_types · global_cable_types · link_group_members · link_groups · tail_details · v_bundle_list · v_equipment_list`) and **SELECT on zero**; `service_role` holds TRUNCATE on **35** including `projects`, `profiles`, `waitlist_signups` — and SELECT on **2**. **TRUNCATE is NOT filtered by RLS.** Reachability NOT measured and not asserted (no REST verb exposes it). Grant residue of the class Engineering Standards names. **Pairs with the read-only role below** | **Trigger B. Before public beta** |
| **HARNESS-READ** | A dedicated **READ-ONLY role** with SELECT on the read surface — not a widening of `service_role` — plus a **parity FIXTURE PROJECT** the harness seeds and owns. **The keystone for any automated verification: today no harness can read the database at all.** CC's OQ5 from M0-WIRE | Trigger B; rides GRANT-TRUNCATE |
| CAND-4 | Four personal texts to the named beta candidates | This week |
| HERO-PLURAL | `HERO_BID_TO_OPENING` goes plural | When Vape! Chicago opens |
| COMMS-REWRITE | R-W: Comms Prompt v9 deleted, rewritten from scratch | Before next Comms deliverable |
| LEGAL-SEP | September attorney engagement; addendum §F scope | September 2026 |
| COMMS-Q | October announcement to the v1 list | October |
| TOK-VC | Vercel token rotation | Private-beta start; hard ~Nov 8 |
| LIC-NOV | Licensing subsystem (+ ownership transfer). **VERHIST split out and promoted — see its own ACTIVE row** | ~November |
| BRIEF-12 | Brief v1.2: terms-before-third-party-access · signup-state history · Communications section | Strat scheduling |
| GIT-CFG | git identity · Next 16 `middleware`→`proxy` · stray trees · the two `ORPHANED-index.lock.*` files in `Minotaur/.git/` | Housekeeping |
| **PROJ-CLEAN-2** | **⚠ COUNTS CORRECTED 2026-08-31 by live measurement: there are **29** projects, not 27, and **20** genuine leftovers, not ~21** — the Docket's own enumeration named 22, but two of those are the parity fixtures. **⚠ `Visual Gate Fixture`, `Import Test 5` AND `Test Cable` must NOT be swept.** **The first action is a RENAME, not a delete, and it is the only irreversible risk on Daniel's desk:** `Import Test 5` → `Cable Parity Fixture`, `Test Cable` → `Matched Pair Fixture`. The cable parity corpus took four passes to build, carries the only real v1 identity and the whole steck population, and is sitting in a sweep list under two names that read like test junk. **A name protects it; a docket row does not.** Six real shows: Our Town in Space · JOY · Matchbook Festival · Vape! · BVSC NY · Comet Fall '26. These are ROWS, not Supabase projects: **there is ONE Supabase project**, and no compute is leaking. | Daniel's desk — the rename FIRST |
| **CHANGEALL-NAALIAS** | **⚠ NEW, and it wants Daniel's ruling.** v1's `0` is the N/A input alias: `isNaAlias`/`naSystem.ts` (P12) makes a typed `0` mean the seeded `N/A - Labels Only` on the ROW COMMIT path, explicitly so it is not vivified as a junk method. **Neither the Change All arm NOR the band editor's bulk arm consults it.** Before `e3a336a` a `0` draft + Change All blanked the found set; after it, a `0` draft creates a method literally **named `0`**. Both wrong; the new one is recoverable and visible. **Not hypothetical — `Cable Parity Fixture` already carries a method named `0` (0 rows) and one named `i`, both from the import.** The band bulk path (`:1889-1894`) has the identical gap and wants the same fix in the same pass. A fifth branch the handoff did not authorise, so CC correctly did not build it | Daniel's desk; rides FIELD-CONTRACT or its own small pass |
| **RLS-INITPLAN** | **⚠ NEW, found while exonerating the database, and it is real even though it was NOT the cause.** `equipment_items_read` and `equipment_items_write` both call `auth.uid()` **unwrapped**, so it re-evaluates per row; Supabase's own linter flags it `auth_rls_initplan` (WARN) on **53 tables project-wide**. The documented remedy is `(select auth.<fn>())`. **Measured impact at today's scale is small — 1,368 rows in 62.6 ms — so this is scale hygiene, not a live defect**, and it must not be cited as CHANGEALL-FOUNDSET's cause. Also standing beside it: **38 unindexed foreign keys** and **16 multiple-permissive-policies** from the same lint run | Before public beta; pairs with GRANT-TRUNCATE / HARNESS-READ |
| **GATE-BEFORE-COMMIT** | **⚠ NEW, process, and it cost nothing THIS time.** The WF-1a approval phrase for `e3a336a` was typed at 23:05; Daniel's browser gate ran at 23:29 and again at 23:38. **The gate is what closes a handoff, and a WF-1a push is a production DEPLOY** — so a defect the gate caught would have been a defect already live. It came out clean (and the 1,368-row run was CHANGEALL-FOUNDSET, not this unit). **The CLOSE.txt itself recorded the gate as owed, which is the guard working; the ordering is the gap.** Wants the commit block to state the gate as a precondition rather than a follow-up | Next handoff's commit block |
| **FIELD-CONTRACT** | **⚠ WIDENED 2026-09-05 BY DANIEL'S INSTRUCTION: *"Look at every field in every layout to verify search works and change all works, like v1."* FIND JOINS CHANGE ALL IN THE REGISTRY.** Ruled 2026-08-31 as ALL FIVE list surfaces in ONE unit (equipment · cable · lines · tails · library): every editable cell and the behaviours each must wire — Enter-advance · ⌥⇧ record movement · the ruled Tab order · Change-All draft capture · Escape-reverts · display-and-edit-agree — **plus, now, presence in the FIND vocabulary**, with **a test that fails when a rendered editable cell is absent from the registry or missing a wiring.** **⚠ THE STECK FIELD IS THE PROVING CASE AND ITS MECHANISM IS MEASURED:** `CHANGE_ALL_FIELD_LABELS` holds fifteen fields and neither steck, the gate at `CableListClient:3112` drops the cell "naturally" in its own words, and the marker (`source_steck`) does not share a name with the column (`source_steck_count`) — **two vocabularies, one field.** An exclusion from the ruled TAB order (`CableRow.tsx:62`, click-entry only) carried unexamined into Change All and Find. **v1 is the arbiter and Daniel says v1 does both; it still gets checked against the DDR, because the source states intent and the artifact states behaviour.** Three findings still banked: `data-equip-field` overloaded across four unrelated concerns · `captureFocusedField` and `captureInFlightDraft` read DIFFERENT attribute sets · a sibling-branch editor defeats any marker on the wrapper. **It also owns `num_labels`'s real Tab position**  | **AFTER the CDMX patches (Daniel, 2026-09-05)** |
| **CORPUSBOUND-AUDIT** | **NEW, and CC raised it.** The mapping doc concluded `Labels` was "boolean-shaped … no other values" on the strength of **BVSC + JOY alone**. It was true of those two and false of the world — **BVSC NY brought `2` and 300 stickers were lost for it.** The five-corpus census is now in the doc so the next reader sees evidence rather than a conclusion. **How many OTHER "resolved by corpus scan" dispositions rest on the same two shows?** Not audited. **Arch's, not CC's** | Before the next mapping decision cites a corpus scan |
| **MARKER-OVERHANG** | **⚠ NEW, found on Daniel's 2026-08-31 print, and it is the one thing on the sheet that lands on a NEIGHBOURING sticker.** The model-section marker `CAT5-1xXLRF+1xXLRM Tail Labels` is a **text-width** chip — v1's own shape, pedigree (b) — 165.00 pt wide from x 26.25, **ending at 191.25, i.e. 22.50 pt inside column 2's sticker**. Three things settled before registering rather than fixing: **it is pre-existing and STOCK-IS-DIE improved it** (26.12 pt onto column 2 at v1's old pitch) · **it collides with nothing on this artifact**, because column 2's first row is empty — it is one data value away from printing a model name across a real label · **and it is not a defect until v1 is measured**, because a text-width marker in v1 would overhang identically. **First step is the v1 measurement, not a fix.** | **With the v1 marker measurement; before any 5167 output ships to real stock** |
| **FENCE-COUNTS** | **⚠ NEW, and it is Arch's fault rather than CC's.** `CLAUDE.md`'s run fence reads **2553/137 measured at `2cc6b14`** and names CELL-AND-ROW as HEAD; both went stale the moment `9a333cc` landed. The project's own rule is that **whoever measures writes the fence in the same pass** — CC measured and did not, **because the handoff never asked**. That is the FENCE-STALE mechanism one turn earlier in the loop. **Nothing it asserts is false about scope**, so it rides the NEXT CC handoff as step 1 rather than spending a production deploy on a docs-only commit. **Every future handoff that changes a measured count asks for the fence in the same pass.** | **Next CC handoff, step 1** |
| **BUNDLESUM-MARKS** | **⚠ NEW, and it blocks a fair parity round.** v1's `bundle summary.pdf` (7/11/2026) shows **`Bundled: 1` on 71 of 71 rows and `Tested: 1` on 71 of 71**; the Aug-20 workbook carries `Bundled` on **1** and `Tested` on **0**, and v2 matches the workbook exactly. Both cannot describe the same data. Either the fields were cleared between the two dates — Q-1's shape, which would widen Q-1's scope — or the layout prints a static mark. **A parity round run today would score v2 wrong on 70 rows for a reason that is not v2's.** One fresh v1 print settles it | Daniel's desk |
| **BVSCNY-SEED-JUNK** | **⚠ NEW, and it disqualifies an arbiter.** BVSC NY holds **136 boxes against the v1 export's 134** — two are named **`In Box`** and **`Box Name`**, column headings that became records. It is also missing box types and weights (607/608/609 carry no type; 603 carries no weight where v1 prints 301 lbs). Same root as STECK-ZERO: **seeded by script, never run through the import engine.** **BVSC NY is not a valid arbiter for anything box-shaped, and its completeness is suspect generally.** `Import Test 5` is the engine-built equivalent — 134 boxes, 237 cables carrying steck, 2,030 real v1 identity rows | Daniel's desk: a two-row delete |
| **CABLE-SPLIT** | **⚠ NEW, TRIGGER A, and it reorders the import queue.** v2 carries `cable_details.split_of_id` and `split_end` and **no feature** — nothing in the app writes either; the only Split UI is the EQUIPMENT quantity split (`SplitDialog`/`OverSplitDialog`). The repo names the gap itself: `lib/cable/linkFields.ts:79-80` marks both `enroll: false`, queued to a *"split-linkage unit"* and a *"split unit"* nobody scheduled, and `LineBands.tsx:9` records an earlier ruling fencing off a band `Split?` affordance pending it. **SPLITOF-NO-ARM therefore comes OFF 2-MIG-c** — importing into a column nothing reads is the BUNDLE-LINK-IMPORT shape. **BLOCKED on a v1 export from a show that actually uses splits** | Daniel's desk |
| **GATE-METHODRACE** | **⚠ NEW, and it BLOCKS EVERY UNIT.** `visual-capture.mjs --gate` fail-hards mid-capture at *"[light] equipment list: Method sort button not found"*. Reproduced 3× on CC's tree at loads 4.27 / 4.01 / 3.88, then **at HEAD `df42fb0` with the tree stashed at load 3.70** — and, sharper, **the gate ran 28/28 at `df42fb0` hours earlier the same day**, so the cause is environmental and the tree is eliminated rather than exonerated. The button exists (45 `data-equip-field` nodes on a direct probe); the harness queries it with **no settle wait** at `visual-capture.mjs:425`. `--force-load` NOT used, `GATE_MAX_LOAD` NOT raised. **Promoted out of BATCH-HARNESS** | **Next session, first action** |
| **BOX-PRINT-CLIP** | **⚠ NEW, and it is PRINT-CLIP's defect class one family over.** `BoxContentsPrintClient.tsx:53` and `BoxListPrintClient.tsx:50` still import the estimate-based `paginateByHeight` from `docData.ts`, each with its own `PAGE_CAPACITY`, rendering into the same fixed `overflow: hidden` box. **UNMEASURED and UNTOUCHED** — out of PRINT-CLIP's scope by its own §0 — but the mechanism is identical, so the Box Contents List and Box List are likely losing rows today. PRINT-PARITY unit 1 closed the box *labels*, never these two. Verified at source by Arch | After GATE-METHODRACE |
| **PRINT-HINT-EQUIP** | **NEW, one ruled line, blocked only by the gate.** `PrintEquipmentListClient.tsx:951` still carries the old ambiguous hint. CC deliberately did NOT change it: §4.3 required measuring the visual gate before and after, and **frame 07 navigates to `/equipment/print`** (`visual-capture.mjs:455`), so the surface may move. Verbatim text ruled by Daniel: *`Choose "Save as PDF" in the print dialog — not "Open PDF in Preview," which prints landscape pages sideways`* | Rides GATE-METHODRACE |
| **CLAUDE-HARNESS-STALE** | **NEW.** `CLAUDE.md`'s harness section still describes SUBPIXEL-WANDER site 4 as `provisional: true` with a `refineBy`; GATE-NEST-UPGRADE dropped both on the second observation and `screenshots/diff-exceptions.json` already carries the corrected reasoning. **Arch's stewardship; CC correctly refused to fix it inside a clone-links commit** | Rides the next handoff |
| **GATE-THEMEGLYPH** | Δ42 at the topbar theme-toggle glyph, **19 judged px, 172/3706 measured, bbox `1273,14–1307,32`**, light-only. Reproduced to the digit Aug 21 → Aug 26 on different trees; the same tree fails then passes. **NEVER register it** — Δ42 is not the Δ1 SUBPIXEL-WANDER the registry exists for, and DIFF-EXC-BOUND's bound is owed first | **Its own diagnosis unit** |
| **METRIC-UNRUN** | All 27 live projects are `imperial`. The per-row metric path has **never met real data**. **The input half is now testable: a typed `150m` on an imperial show commits metres** (LEN-INPUT-SUFFIX, closed) | Daniel's browser gate |
| **EQUIP-LABELS-2** | **⚠ PROMOTED 2026-08-30 out of the unchanged one-liner it had ridden since v4.4, and it now BLOCKS an output.** **RULED (Daniel, 2026-08-28): equipment keeps its real per-item label count** — v2 stores the integer, as the box family already does (BOX-LABEL-COUNTS). **This rules on equipment labels and nothing else; R-7 still governs cables, lines and tails.** Today `Labels` maps to `labels_done` with coercion `fm-bool` (`spec.ts:120`), `coerceFmBool` **fails** on `"2"` (`normalize.ts:21-28`), `map.ts:336` resolves that to `null` and the column is `NOT NULL DEFAULT false` — so a `2` becomes *not a label*: on BVSC NY's 207 items, **`2` on 150 and `1` on 57 — 357 stickers — collapse to 57**, and `buildSheet.ts:144` writes a blank back to v1, wiping the box. **150 of 207 items, 72% of the population, drop out of the output that exists to serve them.** Cost beyond the import: the row renders it as a flag checkbox (`EquipmentItemRow.tsx:707`), Change All carries it as boolean (`equipmentChangeAll.ts:74`), the import client projects it (`ImportV1Client.tsx:326`, `:534`). **An integer column is a MIGRATION — Trigger B.** Precedent for every piece: the box family for the column shape, `LineRow.tsx:406-410` for an integer cell where a flag was | **Trigger B. Blocks Equipment Labels** |
| DIFF-EXC-BOUND | Bound registry sites by area × maxPixels | **Owed before ANY Δ>1 site is admitted — GATE-THEMEGLYPH is why** |

### ⏸ HELD — real and registered, waiting on a named unit, batch or trigger

| ID | One line | When / trigger |
|---|---|---|
| **EQUIPLIST-QTYCOL** | **NEW.** ⚠ **v1 has NO fixed quantity column at all** — it right-aligns the number hard against the description, `x0` 30.52 / 37.18 / 43.83 pt for three / two / one digits, a constant 6.65 pt per digit with the right edge at 50.48 on all three. v2 has a fixed cell, **widened inside EQUIP-LIST-PARITY to stop a measured clip**: `"888"` measures 26.60 px in a 19 px box and the block is the first child of an `overflow: hidden` container, so it was cut at the page edge rather than spilling. **The clip is stopped; the cell is not rebuilt** | With a later print unit |
| **RENAME-HASHDRIFT** | **⚠ NEW, real, and NOT introduced by CAT-ADMIN.** `category_name` and `method_name` are both among `computeRowHash`'s FROZEN fourteen (`labelsHashFreeze.test.ts:39-42`), but `computeRowHash` is called **only from the import mappers** (`map.ts`, `workbook/workbookMap.ts`), never from v2's live rows — **so renaming in v2 recomputes nothing and orphans nothing today.** The exposure is a **v2 export re-imported into v2**: the row hashes differently and a null-HistoryID row mints a duplicate instead of matching. `Vape!` (214 rows) and `Matchbook Festival` (171) carry **zero v1 HistoryIDs — the hash is their only identity.** ⚠ **The same door has been open on the Methods admin since it shipped**; CAT-ADMIN opens no new class | Pairs with any export-integrity unit |
| **ADMIN-NEWDUP** | **NEW, small, pattern-level rather than CAT-ADMIN's.** `addCategory` inserts the literal name `'New Category'`, so pressing ⌥⇧N twice without renaming **silently no-ops the second time** — the UNIQUE constraint rejects it and `data` comes back undefined. **Copy-faithful: the Methods admin does exactly the same with `'New Method'`**, and inventing a de-duplicating name scheme would be UX without a pedigree. Registered, not fixed | BATCH-HOUSEKEEPING |
| **CHANGEALL-SPEED** | **⚠ NEW — and its pedigree is CC's MEASUREMENT, not a gate finding. Recorded that way deliberately.** `applyModelChangeAll` is a per-record `await` loop (the v1 script-694 cascade, deliberately left off CHANGEALL-FOUNDSET's bulk RPCs): **measured ≈2.8 rows/second on 220 rows ≈ 80 seconds**. Arch then extrapolated a full show at roughly six minutes and **put that projection in front of Daniel in the tense of a finding** — he read it as an observed defect and ruled on it. **He never ran a slow Change All.** The ruling stands on its merits: **RULED (Daniel, 2026-09-05) — fix it around private beta.** ⚠ **Model is the ONLY slow field: every other Change All field goes through a single bulk RPC call** | **Private beta (Daniel's ruling)** |
| **CAT-NOTE — the PRINT half** | **⚠ THE DATA AND EDITING HALF IS CLOSED at `1a54c3b`; this row is the print half and nothing else.** A category note rendered between the category heading and its first item. **Type spec at CATNOTE-TYPE — MEASURED, do not re-derive: 12.00 pt ITALIC (body size); item sub-notes 10.00 pt italic. Two levels, two sizes. Flush with the heading's left edge, three indent levels. Does NOT repeat on a continued category; DOES repeat when the category appears in another LIST.** ⚠ **IMPORT-LINEBREAK-CR applies here too — `PrintEquipmentListClient.tsx` already calls `normalizeLineBreaks`, so the mechanism exists; the note just has to use it.** ⚠ **CATNOTE-RULE is a LEAD to check against the v1 print before building, not a finding.** **RULED (Daniel, 2026-09-05): ONE note per category.** ⚠ **NOT the existing item-level notes** — a different level and a different size | **NEXT. BVSC-MX** |
| **LINT-DEAD** | **NEW, CC's OQ5.** `npm run lint` is dead on Next 16 — `next lint` was removed and the script errors with *"Invalid project directory provided, no such directory: …/lint"*. **Not in the Tier-A standing gate set, so nothing was gated on it**, but the `package.json` script is stale and `package.json` is dependency-gate territory. Rides **BATCH-HOUSEKEEPING** beside the existing LINT member | BATCH-HOUSEKEEPING |
| **PRINT-FOUNDSET** | **PREMISE WITHDRAWN 2026-08-27 — all seventeen v1 cable outputs are DATA-DERIVED and none is Find-driven, so the July-11 PDFs are valid arbiters and NO fresh v1 cable print is owed. The two optional re-prints buy exactness, not access, and one needs Q-2 first. Previously read:** **AMENDED BY R-7.** Fresh v1 cable print runs with a **stated found set**, print-to-PDF at 100%, **and label counts normalised to ONE SET** — otherwise the comparison reports a ruled deprecation as a defect. The July-11 run is 794 of 1,119 cables and is unreconstructable | Before PRINT-PARITY's cable family |
| **CHANGEALL-METHODDOCTRINE** | **⚠ NEW, and it is a divergence `e3a336a` CREATED — named rather than left to be found.** `CableListClient.tsx:3167` runs `resolveDraftOption` on `method_name`, so **the cable list's Change All REVERTS an unmatched method draft**. Equipment now **CREATES** one. Same gesture, same field, two behaviours across two surfaces. Neither is wrong on its own pedigree — equipment follows its band editor, cable follows its own resolver — but **nothing has ruled that they should differ.** **FIELD-CONTRACT's to rule; do not "harmonise" either one before it does** | With FIELD-CONTRACT |
| **CREATEMETHOD-BLINDERR** | **NEW, small, CC's OQ3.** `createMethod` (`EquipmentListClient.tsx:793-803`) swallows its own error and returns `null` on `!data`, so METHODNEW's abort branch **cannot tell a 409 from a permission failure or a dropped connection.** That is why the acceptance-time 409 had to be fixed by not *causing* it rather than by handling it. Distinguishing causes means changing the signature of a primitive the band editor shares — out of scope there, its own pass if branch 4 ever needs to react per cause | BATCH-HOUSEKEEPING |
| **SMALLNAME-STEP** | **⚠ NEW, a live parity defect on a SHIPPED output.** v2 steps the Small box name down at **`> 14` characters to 34 pt**; v1 steps at **`> 17` to 36 pt**. Re-measured independently on `Small Box Labels.pdf`: **all 20 printed names are 48.00 pt, and the document contains no 34 pt and no 36 pt text at all** — sizes are 12 · 18 · 24 · 48 · 72. **Six** distinct names sit in v1's dead 12–17 window, not the four the parallel reported. v1's condition 1 (`17 ≥ Length(name) ≥ 12`) is a **chained comparison that can never be true**, so it has never fired. Today v2 prints `FX+Util IO Rack` (15 chars) at 34 pt where v1 prints 48 — **one of ten names wrong at HEAD, at a size v1 never uses.** Build the BEHAVIOUR (48 to 17, 36 from 18), not the intent. `boxLabelGeometry.ts:253-254`; the parallel's `fitPt(...)` citation names a function that does not exist — the mechanism is `stepSlot` at `:106-109` | With the box residuals |
| **BOXLABEL-ZERO** | **⚠ NEW, and it is the equipment ruling's mirror image, unruled.** `Number of Small Labels` writes **`0` into 124 of 134 BVSC NY box cells and 23 of 23 JOY ones** where v1 wrote blank, on a shipped output. `box_details.num_large_labels`/`num_small_labels` are `NOT NULL DEFAULT 0` and `buildSheet.ts:371-372` emits both through `reverseNumeric`. **Recorded as a divergence in a code comment, which is not a ruling.** Daniel ruled it OUT of EQUIP-LABELS-2 deliberately: a second unit on a surface that has already had its parity round. **`reverseNumeric` was left untouched so the box columns are bit-for-bit unaffected** | Its own small unit |
| **DEADEXPORT-GRIDCOL** | **NEW, small, and it caused an Arch error.** `gridColumnWidthPx`, `gridColumnHeightPx` and `GridColumns` in `cableSheet.tsx` have **ZERO callers** anywhere in the repo — dead since pass 4 replaced the flowing tail columns with `TailDieCells`. Their doc comments still read as live guidance, and Arch quoted one into a handoff as a reason. **DEAD-CODE-IS-NOT-SAFE, walked into a second time by the seat the rule was registered against.** Wants a deliberate **keep-or-delete**, not drift; CC correctly declined to delete inside STOCK-IS-DIE's scope. | BATCH-HOUSEKEEPING |
| **DEVICE-FK-LINK** | **NEW, from the parallel.** The "smarter way of linking to a device" Q-3 deferred: resolving `source_device_id` / `destination_device_id` on `cable_details` and `cable_mult_lines` — real foreign keys to `equipment_items`, **0-populated on every project that exists** (0 of 1,119 cables and 0 of 552 lines on BVSC NY; 0 of 461 and 0 of 636 on JOY). Until then Device Patch reproduces **v1's own text join**, exactly — normalising buys two devices and would be an unpedigreed divergence | **Post-public-beta, beside DOOR-V1LINK** |
| **SKIP-LABELS** | **⚠ CORRECTED 2026-08-30 — the previous entry said "v2 has nothing like it" and that is FALSE at HEAD.** `SkipLabelsDialog.tsx` exists and is mounted on **four surfaces** (`TailLabelsPrintClient:574`, `CableLabelsPrintClient:318`, `BoxLabelsPrintClient:372`), and `applySkip` prepends blanks of exactly **36.00 pt** — the tail die row. **The blank honours the die; the rows around it did not**, so before pass 4 it landed the right COUNT of blanks in the wrong PLACE. v1's script 83 prompts *"Skip Labels?"* ahead of Tail Folder · Equipment · Steck · Bundle Labels. **Re-verify the gating against v1 when the first 5167 output pass runs** | With the first 5167 output pass |
| **CATSORT-NATURAL** | **NEW, from the parallel, and it wants a test.** v1 sorts Group Parts by a **natural** decomposition of the category name — the printed headings read `1.0 Consoles` · `1.1 Processing` · `2.0 Speakers` · `10.0 Miscellaneous` · `13.0 Cable` · `14.6 Tools And Test Equipment`, so **`10.0` sorts AFTER `9.0`**, not between `1.0` and `2.0`. v2's `categories` table carries a single integer `sort_order` and **whether it reproduces that is unverified** | With Group Parts |
| **EQUIPVIEW-UNSAFE** | **⚠ NEW, and it must not be discovered mid-build.** `v_equipment_list` **does not filter `type`** — the spine is polymorphic (BVSC NY: generic 1,368 · cable 1,119 · tail 190 · box 136). **Group Parts WANTS all four families** minus method-0 (974 predicted / 976 printed); **Equipment Labels wants generic only.** Separately the view **exposes `ip_address` and hides `address`** — `address` is populated on **261** rows, `ip_address` on **1**, and v1's Equipment Labels prints Address, so the output would silently print blanks | With whichever equipment output lands first |
| **BOX-DOC-PARITY** | **⚠ NEW.** The **Print Box List** and **Print Contents Lists** have never had a v1 parity round. PRINT-PARITY unit 1 was the box **LABEL** family; the staged ADDENDUM-1 is labels too (Stack underline, cut rules, Box-in-Box, palette, geometry). Candidates already visible: v1's header block is three lines with the FULL show name where v2 prints two with the short name · v1 wraps a long Notes cell to its own line, v2 does not · v1's Box List opens with an unnamed-truck section v2 does not emit. **Candidates, not findings** | After cable parity |
| **SPLITEND-DAISY** | **NEW, small, and it contradicts a claim in the code.** `runExport.ts:420` emits a variance saying *"v1 knows Source/Destination only."* **All three of v1's real splits carry `Split End = Daisy Chain`** (BVSC NY: `paDAUTO→paDAUTOx`, `paX20DELAYd→paX20DELAYdx`, `uFX-CPU→uSD TECH`), and a fourth row carries a Split End with no parent. Rides CABLE-SPLIT's decision block | With CABLE-SPLIT |
| **MENU-DEPRECATE** | **NEW, RULED (Daniel, 2026-08-28): remove `Wire Types` and `Wiring Schedule` from the main menu.** Both are disabled entries (`MenuPageLoader.tsx:189` and `:199`) for features R-12 and the Aug-21 WIRE-TYPE ruling already deprecated. **The project menu IS a captured gate frame (`01-project-menu`, `13-boxes-menu-tab`), so this is likely a DECLARED BASELINE EVENT — CC measures which frames move, Arch does not predict** | Rides a UX unit with a working gate |
| **WF1A-PHRASE** | **NEW.** Daniel's approval arrived as `OMMIT APPROVED: fix(print): PRINT-CLIP` — a dropped leading character. **CC accepted it and flagged it** rather than hiding it, on the reasoning that the subject-binding (the half that guards against a stale approval) was present and correct. **Arch agrees on the substance; the lesson is for the guard, not for CC: if the phrase is a mechanism, a matcher should check it rather than CC's judgment** | Pairs with FENCE-PUSH's battery |
| **BUNDLE-NA-UNTESTED** | **NEW.** `cableDocs.ts:279` excludes N/A-method cables from Bundle Sheet member rows — **a filter that had never fired**, because nothing stored the marker. M0-WIRE turns it on. Measured on BVSC NY: **349 bundled cables, ZERO method-`0`, so 71/71 cannot move** — safe **by corpus, not by construction.** On a show with a bundled method-`0` cable it vanishes from its Bundle Sheet, and the exclusion has never been checked against a v1 bundle print | Before another corpus exercises it |
| **EXPORT-CARRIES-STATE** | **NEW standing rule, and a hole M0-WIRE CREATES.** The export writes neither marker back: **no `no_box_needed` emission** (grep = 0 in `runExport.ts`) and it emits `N/A - Labels Only` where v1 expects `0`. Before M0-WIRE the flag was never set so nothing was lost; **now 562 cables carry a state the export drops.** PORT-COV-REIMPORT makes round-trip the acceptance, so this is in scope for the product requirement. **The rule: a unit that creates state must prove the export can carry it** — sibling to ROUNDTRIP-INMEM | **Pair with EXPORT-HISTID-DROP as one export-integrity unit** |
| **EXPORT-HISTID-DROP** | **NEW.** `cable.xlsx` emits `HistoryID` on **852 of 1,119** cables **⚠ ITS PREMISE IS NOW CONTRADICTED AND MUST BE RE-MEASURED BEFORE THIS UNIT IS DESIGNED: the carried figure "all 1,119 carry an identity row live (1,112 v1 + 7 assigned)" is measured 2026-08-27 as 0 v1 / 2,811 assigned.** The "join that misses" attribution rests on that figure and is now unproven rather than wrong; blanks scattered over 203 runs, so a join that misses. Round trip drops **913 → 585 groups** and **2,036 → 1,276 memberships**. `cable clone links.xlsx` is faithful and blanks exactly those 267. **v1's `Import Show` is 28 `Method: Add` steps, so NO duplicates in v1 — the loss is identity continuity** | Its own unit; 2-EXP / 2-IMP-b's |
| **APPLY-PROMISE-GAP** | **NEW.** Dry run promised 913 groups / 2,036 memberships; the database holds **912 / 2,034**. One group and two memberships short, silently — the class the unit's own rule 6 was written against. One measurement, no guess | With EXPORT-HISTID-DROP |
| **ROUNDTRIP-INMEM** | **NEW standing rule.** The three-corpus round trip runs in memory against the same identity map and never calls the RPC or writes a byte of `.xlsx`. It proves the mappers, not the product. **Round-trip through the files and a fresh project.** Third gate this week found measuring something adjacent to what ships | BATCH-HARNESS |
| **LINKJOIN-FENCE** | **NEW.** `historyJoinKey` carries a comment saying it must never be used for identity. `MIRROR_SCOPED_TYPE`'s precedent says that wants a test whose only job is to fail if someone uses it | Next touch of the import mappers |
| **AUTH-REFRESH-HANG** | **NEW.** A failed Supabase refresh blocks every matched route for **~90 seconds** before falling through, because `src/middleware.ts` calls `getUser()` on everything but static assets. The user sees *"the app is broken"* where the truth is *"please sign in"* — and these users work in dark rooms under time pressure | BATCH-PREBETA |
| **SPLITOF-NO-ARM** | **NEW.** `cable_details.split_of_id` exists and the export emits `Split of` as a NAME, but `import_apply_v1` has no insert column and no second pass for it. Refused on import rather than emitting a key the RPC ignores; the loss is reported. **3 rows corpus-wide, all BVSC NY, all resolvable** | **Rides 2-MIG-c** |
| **FENCE-HEREDOC** | **NEW, and it is the SECOND hole in this guard in three days.** `bash-fence.sh` blocks `sed -i` and redirects but does NOT match `python3 - <<'EOF'` opening and rewriting a file. The Aug-13 scope widening covers the mechanism in words; the matcher does not cover it in fact. **Test the guard, do not read it** | Arch's, with FENCE-PUSH |
| **GATE-DEVCHECK** · **GATE-PORT** | **NEW.** The harness calls any listener on `:3000` a "dev server" without checking, and `BASE_URL` has no port override. Together they spent two fail-harded gate runs measuring a production `npm start` | BATCH-HARNESS |
| **LINECOLOR-ZERO** | **NEW.** `lines.xlsx`.`Line Color` carries v1's null-marker `'0'` on 59 BVSC NY and 27 JOY rows — 86 total. Not one of the fourteen colours, so it imports blank AND emits a variance line. Same idiom already ruled for `def tails`.`Method` | 2-IMP-c or BATCH-HOUSEKEEPING |
| **LINK-ORPHAN** | `link_group_members.item_id` has no FK and is **100% orphaned live** — 210 members, 0 resolving. 2-MIG defends it at WRITE | BATCH-BOX or 2-IMP-c |
| **CABLE-AUDIT-TIME** | `_Create Time` / `_Modify Time` are real Excel **datetime** cells at second resolution. `*Person` has no v2 counterpart | Next export touch |
| **EXPRPT-STALE** | The export report lists **4 filenames of 14**, hardcoded at `settings/page.tsx:478`. Its at-rest hint text sits inside gate frame 06 | Its own small pass |
| **BOX-REPARENT** · **IMPRPT-COPY** · **BUILDSHEET-CMT** · **BOX-NAME-UNIQ** · **BOX-TYPES-DIM-DROP** · **BOX-ALPHA-UNSAMPLED** · **BVSC-RF** · **HISTID-HASH-COST** · **SMOKE-LOGIN-HYDRATION** | unchanged from v4.6 | |
| **GATE-LOAD-AUDIO** · **REPRO-SUSPECT** · **SENT-BUNDLE** | unchanged from v4.6 | |
| **GATE-PDF** | **BUILT, and it stays OUT of the repo for now.** `handoffs/parallel/tools/pdfgeom2.py` — mode 1 (geometry) valid on any print; mode 2 (pixel diff) needs matched found sets and is not built. **It imports `pdfplumber`, so porting it into the repo is a DEPENDENCY CHANGE** — a CC hard-stop — and is its own unit. Carries a real fix: FileMaker and Chrome store font size differently, so a naive read of rotated output gives fractional garbage | Its own port unit |
| **BOX-TYPE-VOL** · **BOX-LABEL-COUNTS** · **BOX-EXPORT-ORDER** · **SHOP-ORDER-NOTES** · **BUNDLE-FIND** · **SED-GUARD** · **DEV-SERVER-AGE** · **REPO-WEIGHT** | unchanged from v4.6 | |
| METHOD-FIX · PRINT-REDO · PARITY-PAIR · GATE-OL25WR · GATE-C4 · S504 · CONCEPT-A · UNALLOC · EFFGRP · DOOR-V1LINK · CABLE-LOADER | unchanged from v4.6 — **PRINT-REDO and PARITY-PAIR fold into PRINT-PARITY** | |
| RULINGS-WANTED | REV-STAMP · (ff) tab strip · (dddd) `#` headings · IMP-SORT · BAND-EDITOR sequencing · `no_box_needed` vs the yellow | Icebox sweeps |

### 📐 STANDING — binds indefinitely; a row here never closes

| ID | One line | When / trigger |
|---|---|---|
| **EQUIPLIST-ROWH-BIMODAL** | **NEW, measured and unexplained, and recorded as unexplained rather than guessed.** v1's body row takes **TWO heights, 19.97 and 20.97, exactly 1.00 pt apart**, confirmed on the printed separator rules and not merely on baselines. 19.97 is the only value on pages 1–22 and 30–31 and composes as `19.97 + 12.00 × n` for sub-notes; 20.97 appears only on pages 23–29, **mixed within single pages and single categories**. Indent, quantity digit count, description length, right-edge extent, descenders, apostrophes, commas, parentheses and slashes were all tested and **none separates them**. **19.97 is taken** — the base, 73% of the population, the only value on 24 of 31 pages | Standing; blocks nothing |
| **BVSC-MX** | **⚠ THE DRIVER (Daniel, 2026-09-05). Buena Vista Social Club, Mexico City.** Everything in this Roadmap is now sequenced by whether it serves that production. Parity and housekeeping that does not takes a pin — **named as a pin, not dropped**, so nothing is quietly lost | **Standing, until the show is out** |
| **PROJECT-NAME-NOT-UNIQUE** | **⚠ NEW, and it is a PRODUCT REQUIREMENT rather than a defect. STATED (Daniel, 2026-09-06): two productions of the same musical carry the same show name, and supporting that is a MUST-HAVE FEATURE.** Two live rows share `Buena Vista Social Club`: **`cf780353…` is MEXICO CITY, the BVSC-MX driver** (1,024 spine rows, deliberately started with cable and boxes blank) and **`57874c69…` is the 2,591-row US Tour**, renamed by him this session. **Swept: every `projects` access in `src/` is keyed on `id`, and there is no uniqueness constraint on `projects.name` — the app is already correct, which is why the feature works.** ⚠ **ONE EXPOSURE: `scripts/visual-capture.mjs:122` finds its fixture project BY NAME**, takes `.first()`, and matches on Playwright's `hasText`, which is a **substring** match — so a second project whose name merely CONTAINS `Visual Gate Fixture` would silently hijack the gate. Unreachable today; **the comment directly above that line records the team fixing this same class once already.** ⚠ **NEVER "fix" this by making the name unique — the duplicate IS the feature.** **Every future citation of a project names an id** | Harness fix rides BATCH-HARNESS; the naming rule is standing |
| **PRINTPROBE-MEDIA · RECT-VS-FLOW · PAGINATE-SLACK** | **⚠ THREE NEW PROBE HAZARDS ON THE EQUIPMENT LIST, and the first one already made a probe report a false clean.** **(1) PRINTPROBE-MEDIA:** on screen `.print-page` carries an inline `height: 1056` and flex items refuse to shrink below their content, so an over-budget page **GROWS**; the scissoring `overflow: hidden` is in the `@media print` block. **The screen-only probe that proved PRINT-CLIP and BOX-PRINT-CLIP reports zero blocks lost here while rows are being lost.** Every clip probe on this document runs under `emulateMedia({media:'print'})` at a true 816×1056 viewport. **(2) RECT-VS-FLOW:** `getBoundingClientRect().height` excludes the last child's collapsed bottom margin, so CAT-NOTE-RULE's growth reads **+3.25 by rect against +3.99 by flow**; a probe comparing block rects to `CONTENT_H` under-counts **every** block on this document. **(3) PAGINATE-SLACK:** `CONTENT_H` is 880 against a measured container of **910** by-category and **890** with a method band — conservative, so slack rather than defect, but the estimate and the box disagree and anyone tuning these constants must know it | Standing, binds every probe on this document |
| **ARCH-OUTPUTS-MIRROR** | **⚠ NEW, and it breached the one-writer backstop.** Files written through the Cowork seat's output path are **mirrored into the FIRST connected folder, which is `~/Developer/Minotaur` — the code repo.** A `Claude outputs/` directory therefore appeared in the code repo's working tree **while a CC session was live**, and CC's `git status` correctly flagged it as untracked and not its own. Untracked and excluded from both commits; files moved to `Minotaur-Cowork/_to_delete/260906_arch-stray-outputs/`. **Intermediates belong in the session scratchpad; only a file about to be committed goes through the output path** | Standing, binds this seat |
| **BVSC-8POWER-DRIFT** | **⚠ NEW, small, and it is a CONSEQUENCE rather than a defect.** `Buena Vista Social Club`'s `8.0 Power` category note went **153 chars / 1 CR / 0 LF → 154 / 0 CR / 2 LF** during Daniel's 2026-09-06 browser gate — he typed a blank line into it while finding CATNOTE-AUTOSIZE. **It no longer matches its v1 source byte-for-byte.** `9.0 Hardware and Rigging` and `13.0 Cable` are untouched and still carry their bare CRs. **Named so a future round-trip comparison on BVSC does not report it as a defect** | Standing caution |
| **FENCE-ONEUNIT** | **⚠ NEW STANDING RULE, and CC found its root cause.** **The run fence carries ONE unit, not a history** — `git log` already is the closed-and-pushed list and it cannot go stale. That list had run ~30 units deep while **its own header read *"read off `git log` rather than copied forward"* — for weeks, while being copied forward underneath**, which is a large part of how the fence reached 51,359 chars. **Retired fence text goes to the Ledger; the Ledger is Arch's, not CC's to write.** ⚠ **And the limit is in CHARACTERS: `wc -c` reports 136,136 bytes for the 134,764-char file** | Standing, binds both tiers |
| **CONTCAT-DIVERGE** | **⚠ NEW, and it is a DELIBERATE DIVERGENCE by ruling — pedigree (a), not (b).** **RULED (Daniel, 2026-09-05): v2's handling of a CONTINUED category — a separate line reading `4.0 Microphones (continued)` rather than v1's overlap with the method chip — is BETTER THAN v1 AND STAYS.** It is also the one element off EQUIP-LIST-SCALE's 0.750: v1 sets the continued-category name at 10.00 pt and v2 at 9.00, a ratio of **0.900**. Daniel's ruling and the measurement landed on the same element from opposite directions. **Do not "fix" this back to v1 by citing the artifact** — the PAGEBREAK-MODEL protection, applied here | **Standing. Binds EQUIP-LIST-PARITY** |
| **CATNOTE-TYPE** | **⚠ NEW, MEASURED off the v1 artifact 2026-09-05 — CAT-NOTE's type spec, do not re-derive.** A v1 category note prints at **12.00 pt ITALIC** — the same size as a body item line — and an item sub-note at **10.00 pt italic**. **Two note levels at two different sizes; they cannot share a style.** The note sits between the category heading and the first item, **flush with the heading's left edge** while items are indented and sub-notes indented further — three levels, not two. **It does NOT repeat when a category continues onto a following page**, and it DOES print again when the same category appears in a different LIST (`8.0 Power` carries its note in both `Main List` and `Production Only`) | **CAT-NOTE. BVSC-MX** |
| **IMPORT-LINEBREAK-CR** | **⚠ NEW, measured, and it is a REQUIREMENT for CAT-NOTE rather than a defect.** Stored v1 text arrives **carriage-return separated** — FileMaker's line endings — on the BVSC Tour import: **210 equipment public notes, 15 library notes and 3 of the 6 category notes carry CR, and ZERO carry LF.** A bare CR is not a line break in a browser, so text split on `\n` renders run-on. v2 already knows this: the equipment print calls `normalizeLineBreaks` at display time, deliberately never writing it back so the frozen row hash is untouched (`src/lib/utils.ts`, and `revisionSnapshot.ts` records the same finding on JOY's `Meyer UPM-1P`). **Nothing is broken today. CAT-NOTE's renderer needs the same one-line call, written down now so it is not discovered mid-build** | **CAT-NOTE** |
| **LABELSDONE-RESIDUE** | **NEW.** `labels_done` is now product-dead — `num_labels` is the truth — but it **STAYS**, because it is one of `computeRowHash`'s frozen fourteen and HASH-FREEZE-LABELS keeps it producing today's value. Dropping it is **destructive and a ruled event**, and it would change every stored row hash. **Do not "tidy" the two values into one; the code says so in its own comments** | Post-beta at the earliest; needs its own ruling |
| **CHIP-CABLECOLOUR** | **NEW, ruled and BUILT at `2cc6b14`.** v1 fills the line-number chip from **`Cable::Color` alone** — layout-168 key-5 is fifteen cable-colour arms with **no `Mult Lines::Line Color` arm.** v2 read `lineColor ?? parent.color`, which the anatomy diff recorded as MATCHING because it sampled cells where the two agree; on the arbiter they **disagree on 63 of the 79 tinted cells.** Ruled (Daniel, 2026-08-30): the chip carries the cable colour, the tint carries the line colour — **a cell shows two facts, not one fact twice.** The `?? lineColor` idiom existed in exactly one place and is gone; an 8-site audit proved it | closed |
| **CHIPTEXT-PAIRED** | **NEW, ruled and BUILT at `2cc6b14`, and the defect was INVISIBLE rather than merely wrong.** v2 printed the block cell's `<End> Tails` **black on a black chip**, so on a black cable the field did not appear at all — found on Daniel's printed pair. **v1's rule is PER-OBJECT, not geometric:** its six-colour white-text condition (`red · green · blue · purple · black · brown`) **is exactly `MINOTAUR_COLORS`' own white-text set**, so this was a missing override and not a palette change — and **the LINE cell's end-word object carries no conditional format at all and stays black on every cable colour**, so a geometric derivation would have turned it white on a long end name and diverged. Arch proposed the geometric form; CC refuted it at source and on the artifact. 22-run colour audit, one run wrong | closed |
| **PAGEBREAK-MODEL** | **NEW, and it is a DELIBERATE DIVERGENCE by ruling — pedigree (a), not (b).** **RULED (Daniel, 2026-08-30): a model section starts a new PAGE on all three tail outputs.** v1 measured breaks to a **COLUMN** on the two Avery sheets (8/8 and 7/7 markers at row 0 of a column; only 3/8 and 2/7 at a page top) and to a page only on Tail Panel, which has one full-width column. Arch recommended keeping v2's packing and was **overruled**. The MULT column break is untouched and remains parity (96/96). **The source comment states v1's measured behaviour, names the ruling and its date, and says outright: do not "fix" this back to a column break by citing the artifact** | closed |
| **DDR-IS-THE-SOURCE** | **NEW STANDING RULE — go to the source that made the ink.** Two v1 behaviours that defeated every artifact correlation were quoted verbatim from v1's own layout definitions in one pass: the type step-down (**five `Length()` tests at THREE thresholds** — >10, >9, >11, >9, >14, each tuned to its own field width, so one constant would be wrong on two of the three sheets) and the description tint (**a 12-arm conditional format on `Mult Lines::Line Color`** — the discriminating datum prints nothing of its own, which is why no artifact-only rule could exist). **A threshold fitted to one corpus reproduces that corpus by construction.** ⚠ `docs/reference/v1_ddr.xml` is **UTF-16LE — a byte-oriented grep finds nothing in it; read via `iconv -f UTF-16LE`** | Standing, binds both tiers |
| **GRAPH-BEATS-RERUN** | **NEW STANDING RULE.** A re-run says whether a failure recurs; **an import graph says whether the frame could ever have seen the change, and gives the same answer either way.** Earned on the `06-settings-light` gate failure: **0 of 8 changed modules reachable from that route, against a control of 8 of 8** from the route that does use them, plus an empty diff on the one shared module. **Two runs is the limit** — three identical failures is what taught this project that a reproducing flake is staging, not noise. **And keep the two registries apart:** `CLAUDE.md`'s capture-flake list is PROSE and masks nothing; `screenshots/diff-exceptions.json` excludes pixels from a verdict and DIFF-EXC-BOUND is owed before any Δ>1 site enters it | Standing, binds both tiers |
| **RETURN-ON-CONTROL** | **NEW, ruled and BUILT into `CLAUDE.md` at `0bf6df3`.** The Return Handoff File Protocol's trigger is **RETURNING CONTROL** — an event, not a judgment — and **the last terminal line is the return file's path alone** (Daniel's words). **One file per unit, APPENDED**, which changes what gets batched: the file, never the writing. **Daniel's three exclusions, written into the rule:** waiting on a step the COMMIT BLOCK already documents · being interrupted (API error, dropped connection, sleep) · a clarifying question before any work. **Daniel proposed a 30-second / 10-line trigger; Arch pushed back** — CC cannot check elapsed time against an artifact, and O-12 says rules checked at discrete events hold while continuous properties do not | closed |
| **HARNESS-RLS-CORPUS** | **⚠ NEW, and every future parity pass has it.** `Import Test 5` **and** `Test Cable` are owned by daniel@ and the harness signs in as the TEST user, so **no harness CC drives can read the parity corpus at all.** Every pass-4 count was taken on a throwaway seeded to reproduce it, and labelled as a reproduction. Either the test user gets a `project_members` row, or the corpus moves to a project it owns, or these counts stay Daniel-side forever | **Rides HARNESS-READ** |
| **FOUNDSET-NOT-COUNT** | **NEW standing caution, from the parallel.** `Tail Folder Labels.pdf` carries 8 labels and both corpora produce 8 distinct parent-cable models — **but different eights.** The July-11 print's set matches `BVSC_NY/2026-08-20_0120/` exactly and `BVSC/2026-07-12_1608/` on **none** of the eight. **A count that matches is not a found set that matches** | Standing, binds every print↔corpus pairing |
| **BROWSER-NOPRINT** | **NEW, measured, and it closes a line of inquiry.** Cowork's built-in browser (Chromium 148 in its own profile) can navigate, click, read the DOM, run JavaScript and read console and network — **and has no print and no Save-as-PDF.** `window.print()` would raise a modal that freezes the automation. **It cannot take over the print-parity gate.** Two further caveats: while hidden the pane reports a **0 × 0 viewport**, and its screenshots return **800 × 452 against a 1280 × 720 page**, so they are not pixel-faithful. Useful for the *behavioural* half of Daniel's browser gate, useless for the printed half | Standing |
| **PDF-CHROME-PATH** | **⚠ NEW STANDING RULE, and it invalidates evidence.** A v1↔v2 parity print is valid **only** when produced through **Chrome's own Save-as-PDF destination**. Quartz-produced artifacts measure the operating system, not the app. **Every landscape artifact in the corpus taken before 2026-08-27 is invalid as an orientation or geometry arbiter**; the box family is unaffected because every box output is portrait, so PRINT-PARITY unit 1's 0.63 pt stands. The on-screen hint that caused it is fixed on the cable side (PRINT-HINT) | Standing, binds every parity measurement |
| **GATE-NOGREP** | **NEW standing rule.** A visual-gate run piped through `grep` lost its verdict detail and its diff PNGs were overwritten, costing the only characterisation of a 26/28 failure. **Never pipe a gate run through a filter — write it to a file and read the file.** The Raw Output Rule's operational form | Standing, binds both tiers |
| **HIST-BLIND** | **NEW.** v1's field-level audit lives entirely in OnObjectSave triggers, which `Replace Field Contents` does not fire. **Any scripted mass edit is invisible to `history`.** This bounds what that table can ever prove — and it is why Q-1's intent can never be closed from data | Standing caution |
| **CLONELINK-ROWSET** | **NEW, and named so it is not "fixed back."** v2 emits one clone-link row per cable and one per mult line — 1,671 for BVSC NY against v1's 596 — because **what selects v1's 596 is still unexplained** (not all cables, not all lines, and 28 of the 284 cable rows are in no group). A ruled divergence, not a defect | Standing |
| **PARALLEL-CLOCK** | **NEW, minor.** A parallel return named `260827-0020` and headed EDT was written at 20:04 EDT the previous evening, so it sorts ahead of openers written after it. The `YYMMDD-hhmm` convention is the ordering key | Standing, binds parallel openers |
| **PORT-COV-REIMPORT** | A v1 re-import path is a product requirement (Daniel, Aug 23): *if something doesn't work in v2, the user reverts to v1 immediately.* Acceptance is round-trip, not column order. **2-IMP-b met it: 100% on all three corpora**, with two ruled non-round-trips named rather than failed | Standing, binds every export unit |
| **TENSE-IS-A-CLAIM** | **NEW standing rule.** An expectation written in the past tense is indistinguishable from a measurement to whoever reads it next. Earned when a `28/28` that had not happened reached four documents including `CLAUDE.md`'s run fence | Standing, binds both tiers |
| **DEAD-CODE-IS-NOT-SAFE** | **NEW standing rule.** A symbol with no production callers is not "low blast radius" — it is dead code. Reading `MIRROR_SCOPED_TYPE` as authoritative produced an Arch instruction to widen a delete universe the module did not govern | Standing, binds Arch |
| **R-7 LABEL SETS** | **RULED Aug 26.** v1's label counts are SETS; v2 prints one. The count collapses to a boolean. 32 corpus rows, reported as a deprecation | ✅ applied in 2-IMP-b |
| **LEN-SCALE** | `cable_details.length_value` 853 rows at scale 1 vs 7 at scale 0; `bundles` 97 vs 2. **MECHANISM CORRECTED Aug 26:** the carried claim that PostgREST hides it is right in conclusion, wrong in mechanism — `to_jsonb(150.0::numeric)::text` is `"150.0"`, so the scale IS on the wire and JS `JSON.parse` is what normalises it. A non-JS consumer or a raw-text comparison WOULD see it | Standing caution |
| **HISTENTRY-NOTID** | `History Entry ID` is 100% non-UUID and **not unique** (279 distinct over 1,084). A timestamp despite the name. Never map it to an id column | Standing caution |
| **JOY-NAME-HYGIENE** | JOY carries 29 blank `cable.Name` rows and 40 with leading/trailing whitespace. **Do not trim cable names** — `coerceTextEmpty` was found TRIMMING them in 2-IMP-b and a verbatim coercion was added for that one column | ✅ defended in 2-IMP-b |
| **LINES-NOFK** | `lines.sourceID` / `destinationID` are 0-populated in all three corpora; the DDR confirms no relationship joins on them | Standing |

### ✅ RETIRED TO THE LEDGER — index only, so a citation still resolves

**These 26 rows left the Docket in v4.23 under the forward-only header. Their full text is in `Minotaur_v2_Ledger_v1_0.md`.** ⚠ **Checked rather than assumed: the Ledger held 24 of the 26. `CLAUDEMD-OVERSIZE` and `HEADER-PANEL` were in NO append, and are carried verbatim into the EIGHTH append (§5) so their retirement is real.** Cite by ID; the append is named beside each.

| ID | Closed at | Ledger |
|---|---|---|
| **EQUIP-LIST-PARITY** · **EQUIP-LIST-SCALE** · **QTY-ITALIC** · **CATNOTE-FONT-DUP** · **AMEND-ROUND-TENSION** | `c525a55`, 2026-09-07 | seventh append |
| **CATNOTE-RULE** | `f4192cd`, 2026-09-06 | sixth append |
| **CAT-ADMIN** · **CAT-NOTE's data and editing half** · **GCMTAB-STALE** · **CLAUDEMD-OVERSIZE** | `1a54c3b`, 2026-09-06 | fifth append |
| **CHANGEALL-BOXNULL** | `6deb411`, 2026-09-05 | fourth append |
| **CHANGEALL-FOUNDSET** · **CHANGEALL-METHODNULL** (closed as CHANGEALL-METHODNEW at `e3a336a`) · **GREP-NULBYTE** | `ba0058f` / `e3a336a` | third and fourth appends |
| **LINKGROUP-CORPUS** (discharged — it was ATTRIBUTION, and the attribution was a RENAME) | 2026-09-05, no commit | fourth append |
| **STOCK-IS-DIE** · **FENCE-PUSH** | `9a333cc` / `0bf6df3` | second append |
| **PRINT-CLIP** · **CHECKLIST-GROUPCOUNT** · **ORIENT-FIXFAILED** | `eaabd03` / by measurement / no code change | first and second appends |
| **ROW-LADDER** · **JUSTIFY-PERCELL** · **HEADER-PANEL** | `2cc6b14` | second append |
| **CLONE-FLAG-DIVERGE** · **CLONE-TOKEN** · **STECK-ZERO** | `0468a5d` / by measurement | first and second appends |
| ~~**APPLY-TIMEOUT** (EVENT copy)~~ | **DELETED, not retired** — a superseded duplicate of the live DATED row, which stays in ACTIVE. It is the duplicate ROADMAP-REFACTOR was scoped to remove | — |

⚠ **AND A SECOND ONE THE SCOPING DID NOT KNOW ABOUT: `LINKGROUP-CORPUS` ALSO APPEARED TWICE** — a struck DATED row recording it as discharged 2026-09-05, and a **live, unstruck EVENT row still asserting the contradicted premise** (*"a figure of record does not match live data … BVSC NY holds ZERO link groups"*). The EVENT copy was **DELETED**, not sorted. **The same shape as APPLY-TIMEOUT, one table over, and unmarked — which is worse, because nothing signalled that it was stale.**

### CLOSED THIS SESSION

**FIELD-CONTRACT legs 1 and 2 — two units, two production deploys, both gated, `1b23223..1276cc4`.** This Roadmap installed as **v4.24, retiring v4.23**.

**LEG 1 — THE CENSUS, `44e206d`.** 6 files, +1349/−26. The field contract stopped being an intention and became a measured object: **83 rendered editable cells across seven surfaces**, one row each carrying its marker and `file:line`, its Change All membership, its Find membership, its Tab-order position, and which resolvers can see it. **Seven Change-All registries in three shapes — two in shared testable modules, five inline in multi-thousand-line clients — ten find maps, thirteen marker attributes.** All larger than the scoping handoff measured. ⚠ **The test could not render a component:** the repo carries `vitest` and `playwright` and no DOM, so it is a **static source analysis** — which is the better instrument, since it runs in `npm test` and sees a marker emitted on a branch a render test would have to reach. **Adding a DOM package was named as a hard-stop in the handoff and was not taken.** **73 tests, probed both directions** — a renamed cell value fails, a renamed attribute fails LOUDLY through the non-zero guard rather than passing vacuously — **both probe files restored byte-identically, proven by sha256.** Tab order measured for every surface: **all have one except the equipment library, which declares none anywhere**, an absence now pinned by its own test.

**LEG 2 — FIND-PARITY, `1276cc4`.** 8 files, +470/−340. ⚠ **RULED (Daniel, 2026-09-08): every field a surface's Change All can WRITE is a field that surface's Find can SEARCH.** A **subset relation** — `changeAll ⊆ find` — never a blanket; the two steck columns and `in_box` stay out because Change All cannot reach them. His reasoning, kept because a bare rule gets rationalised past: **v1 finds on any field, and a tool that can rewrite a thousand records on a field the operator cannot search for is one an operator stops trusting.** **The gap that prompted it: seven of the cable inspector's eight fields were Change All targets and none was findable.** **25 keys added.** ⚠ **The work was driven by the instrument, not by a list Arch typed:** the subset assertion was written FIRST, run against the unmodified tree, and it named the missing keys. **No engine change** — `FindFieldType` is still `'text' | 'number'`; the boolean face copies `flagFace` and `colorDisplay` from bundles, the only surface that already found flags. ⚠ **`box_number` rekeyed from `number` fixed a LIVE defect nobody had reported:** the focus-aware find default looks the focused marker up in the find map, the marker said `box_number` and the map said `number`, so focusing that cell and pressing ⌥⇧F never defaulted to Number.

**⚠ BOXBAND-DUPKEY — DANIEL'S GATE FOUND IT, AND THE CONTROL THAT SETTLED IT WAS NOT THE ONE ARCH ASKED FOR.** Arch instructed a browser negative control. CC ran a **static** control instead — `boxSortChains.ts` byte-identical at `44e206d`, no line of the diff touching band, sort, nesting or truck — then **reproduced the mechanism executably against the real comparator**, which is stronger and more precise than the browser repro would have been, and said so rather than quietly substituting. **Arch then corroborated it in live data, which CC cannot read (HARNESS-RLS-CORPUS).** Full mechanism and the do-not-fix lines are in the Docket row and in `drafts/260908-1150_boxband-dupkey_measurement.md`.

**⚠ FOUR ARCH CLAIMS WERE WRONG IN ONE HANDOFF, AND CC CAUGHT ALL FOUR.** Equipment's Change All registry is **16 fields, not 17** · `data-equip-field="model"` **does exist**, twice, in the LIBRARY · **the model band editor is NOT unmarked** — it wears `data-find-field="model"` and three of four resolvers read it, so the fix is one attribute in one array rather than a redesign · `resolveNavField` is in `recordNav.ts`, not where the handoff placed it. **Three of the four were universal negatives — "does not exist anywhere" — asserted from a grep for TWO attribute names, on a surface the census then showed carries THIRTEEN.** ⚠ **An instrument must not inherit the claim's parameters: census the class, then narrow.** The same shape as the CATNOTE-RULE `linewidth >= 2.0` probe that reported a rule's absence because it filtered for the wrong width. **Errata written into the spent handoff so no future reader inherits them.**

**⚠ THE GATE DISCIPLINE HELD IN BOTH DIRECTIONS, WHICH IS THE POINT OF HAVING IT.** On leg 1 the browser gate was answered *"n/a, no rendering surface"* by Daniel and recorded verbatim — **CC asked rather than inferring, and Arch's waiver was ratified by him rather than assumed.** On leg 2 **CC refused twice to infer the gate — once from silence and once from a green machine gate** — and Arch relayed Daniel's four answers verbatim into the close. **GATE-REPORTING's whole problem is that the gate runs where CC cannot see it; the relay is the mechanism, not friction.**

**Method, recorded because it is what made two 100,000-character governing files safe to edit twice in a day:** every move was a read-modify-write by script with an assertion per anchor, whole lines moved and never retyped, and every count taken rather than carried.

**DOCKET SWEEP.** *Touched this session:* FIELD-CONTRACT (**legs 1 and 2 closed; leg 3 opened and scoped**) · CDMX-SPRINT (**four of six criteria dispatched: the duplicate-from-Model gap root-caused, the category band reassigned to MMM-CAT, the cable group ruled, the box Number default fixed**) · MMM-CAT (**re-scoped from Daniel's live report; established as a missing feature, not a defect**) · APPLY-TIMEOUT (**pin questioned — CDMX-SEQUENCE is discharged**) · AUTOCOMPLETE-NOPORTAL · CHANGEALL-METHODDOCTRINE · CDMX-DEFERRED (**all three carried to leg 3, untouched**) · LIC-NOV (**VERHIST split out and promoted**). *New rows:* **BOXBAND-DUPKEY · FINDPARITY-UNTYPED · FINDFLAG-CHECKBOX · CABLEGROUP-EDITOR · CABLE-FINDDEFAULT · CAPFLAKE-LIBLIGHT · VERHIST.** *Retired to the Ledger:* **none — the retirement index is unchanged at 26.** *Triggers that went live:* the **field-contract test** now fails any new editable cell that misses a wiring, and **`changeAll ⊆ find`** binds every future registry edit. *Nearest three dated items:* **CAND-4** (overdue) · **LEGAL-SEP** (September 2026 — now, and **VERHIST's retention window belongs in it**) · **SPENDCAP-BETA** review (before public beta, October).

### BATCH

| Batch | Members |
|---|---|
| **BATCH-HARNESS** | H3 · GATE-THEMEGLYPH *(promoted out — its own diagnosis unit)* · **GATE-DEVCHECK** and **GATE-PORT** *(promoted — they have now cost THREE runs; a listener check and a port override are both small)* · **ROUNDTRIP-INMEM** · **GATE-NOGREP** · GATE-COUNTWAIT · GATE-DLGRACE · GATE-FIXDIRTY · GATE-LOADPROXY · VIS-ENVFILE · DEV-SERVER-AGE · GATE-LOAD-AUDIO |
| **BATCH-AC** (after private beta starts, R-AG) | unchanged from v4.6 |
| **BATCH-BOX** (post-private-beta) | unchanged · BOX-NAME-UNIQ · BOX-REPARENT · **LINK-ORPHAN** |
| **BATCH-EXPORT** | ~~leg 1~~ ✅ · ~~ARCH-MIG-BOX~~ ✅ · ~~leg 1b~~ ✅ · ~~MIRROR-SYSMETHOD~~ ✅ · ~~HISTID-UUID~~ ✅ · ~~**2-EXP**~~ ✅ · ~~**2-MIG**~~ ✅ · ~~**2-IMP-a**~~ ✅ · ~~**2-MIG-b**~~ ✅ · ~~**2-IMP-b**~~ ✅ · **2-IMP-c** · **2-MIG-c** |
| **BATCH-PREBETA** | SHARE (build-prep DONE) · (ppp) settings header · ADM-1 · AUTH-DOMAIN · 3TL · DEPS · CL-HILITE · GCM · **EXPRPT-STALE** · **AUTH-REFRESH-HANG** · **IMPRPT-COPY** |
| **BATCH-HOUSEKEEPING** | **LINT** · **FENCE-HEREDOC** · **FENCE-PUSH** · **LINECOLOR-ZERO** · SRGRANT sweep · ST-RACE · CTF-SEED · SLOT-AUDIT residue · REPO-WEIGHT · BOX-TYPES-DIM-DROP · PROJ-CLEAN-2 |

### ICEBOX

Unchanged from v4.6.

## ❓ Open Questions (live only)

- **PRINT-PARITY's remaining, with OQ-1 now DISCHARGED:** ~~OQ-1 the two colour tables~~ — **CLOSED by Daniel's rulings; the palette is the parity target, not v1's render, and `minotaurColors.ts` is unblocked** · ~~**OQ-2** — Black has no measured hex on either side~~ — **ANSWERED 2026-08-31 with no measurement needed: OQ-1 ruled the v2 palette the parity target and v1's printed swatches drift, and `minotaurColors.ts:14` states Black explicitly as `#000000` with white text. There is nothing to look up** · **OQ-3** — does the SMALL box layout's Box Name carry a Conditional Formatting rule? A screenshot settles it, **and note the Inspector disagreed with FileMaker's own print: a rendered STYLE value the engine ignores is not evidence of what prints; a BEHAVIOURAL rule still is** · **OQ-4** — are the dashed grey cut rules wanted in v2? **Now fully specified**: `x 22.00 → 590.00` at `y 395.50`, 1.0 pt, `#ADADAD`, dash `[3.005291, 3.005291]`, phase `0.5` · **OQ-5** — which cell edge is the parity target · **OQ-B** — does Box-in-Box suppress fields other than the Stack? One more card answers it.
- ~~**How should the CABLE FOLDER LABELS be ORDERED?**~~ **RULED (Daniel, 2026-08-30, on the measured v1 sheet): DOWN THEN ACROSS, NO-BOX CELLS FIRST.** Applied and **proven on paper** — 24 cells, 10 / 10 / 4, `No Box` then `N/A - No Box Needed` leading column 1. **Arch's recommendation to keep them last is overruled and closed.** SKIP-LABELS and Bundle Labels' own column-major fill inherit this ruling. Superseded text: The Parity Rule names ordering as UX-affecting and **neither choice has a pedigree.** v1 fills **column-major** with the two no-box cells **FIRST** (slot 1 blank, slot 2 `Box`/`0`), 10 rows × 3. v2 today fills **row-major** with them **LAST**, 8 rows × 3. **Count is right (24); order is unruled.** On peel-and-stick sheets position determines which physical label a box gets. **Arch recommends keeping v2's LAST** — v1 wastes its first label on a blank — **ruled as a deliberate divergence, not left as an accident.**
- **Should un-boxing round-trip?** A blank box name does not clear an existing link on either the equipment or the bundle side, so a row removed from its box in v1 stays boxed in v2. Consistent across both sides deliberately; Daniel's ruling to change it.
- **Should "blank means clear" be ONE rule?** `branch_cable_type_id` and `tail_method_id` clear on an explicit blank; `box_id` on both sides does not. Each follows the precedent beside it in the same statement.
- ~~What actually drives the over-asserting clone flags?~~ **ANSWERED** — INVERT and mult-line cross-sums, cited from the DDR. Six columns, not four. (CLONE-FLAG-DIVERGE, closed.)
- **Do v1's bundle-field link groups come into v2, or never?** (BUNDLE-LINK-IMPORT.) Built, gated, awaiting Daniel.
- ~~Do the five disabled v2 cable outputs get built, or are they deprecated?~~ **RULED (Daniel, 2026-08-28): ALL SEVEN disabled outputs get built** — the five cable ones plus Group Parts, Device Patch and Equipment Labels — **each with a meticulous v1 print-parity comparison of the kind the box label family got.** (Q-4, closed, and widened past what it asked.)
- **What selects the 596 rows in v1's `cable clone links.xlsx`?** Not all cables, not all lines, not "items with a link." (CLONELINK-ROWSET — divergence named, not chased.)
- **What zeroed the three v1 count fields between July 11 and Aug 20?** (Q-1.) v2's booleans match v1's counts on all seven label columns, so v2's empty Big and Balun outputs are faithful. **HIST-BLIND means only Daniel's memory can ever close intent.**
- **Do the 39 boxes whose types BOX-TYPE-LIB created want dimensions?** Not blocking.
- **Does the operator need the report to say a system row was protected?** Not blocking.
- **Should the HISTID-HASH-COST wording be sharpened?**
- **What actually moves the theme-toggle glyph?** Now reproduced twice, five days apart, on different trees — and still undiagnosed.
- The own-truck vs resolve-up reading on box labels is unobservable by construction.
- `box_default_large_labels` 5 (OQ4 ruled) vs the v1 panel's `#Big Labels 2`.
- v1 continuation header · cover-page body size → **PRINT-PARITY**.
- Script 551 extraction · cable Change All checkbox eligibility · house-wide Cancel semantics · Used∩Ignored · empty-string note · link `field_name` CHECK backstop · cable-type RENAME affordance · CL a-1 uploads · d2_01 red Members count · CL multi-letter-per-project · stale-category healing · CC credit-budget policy · OQ#2b own-band pendency · band-cascade harmonization.

**CLOSED this session:** ~~What drives the over-asserting clone flags?~~ **INVERT + mult-line cross-sums, cited from the DDR — six columns, not four, and 17 of 23 agree, not 19.** ~~Is the cable print family blocked on an unreconstructable found set?~~ **NO — all seventeen v1 outputs are data-derived; no fresh v1 print is owed.** ~~Does v2 keep one colour table or gain a second?~~ **One. The product palette is the parity target and v1's printed swatches are drift.** ~~Which sorts did the July-11 operator use where the filename does not say?~~ **All three recovered by measurement.**

---

*End of Roadmap v4.24 — September 8, 2026. **Retires Roadmap v4.23**. Units of record: **FIELD-CONTRACT leg 1 (the census) at `44e206d`** and **leg 2 (FIND-PARITY) at `1276cc4`** — two production deploys, both gated, `1b23223..1276cc4`. **83 editable cells censused across seven surfaces; seven Change-All registries, ten find maps, thirteen marker attributes — all larger than the scoping measured.** **RULED (Daniel): `changeAll ⊆ find`**, 25 keys added, three exceptions kept. **BOXBAND-DUPKEY found by Daniel's gate, root-caused, and proven not this work's.** **Four Arch claims wrong in one handoff, all four caught by CC, one shape: a universal negative from an instrument narrower than the claim.** **VERHIST scoped by a parallel, folded in, its payload estimate corrected from 1–2 MB to a measured 5.3 MB, and its APPLY-TIMEOUT risk moved onto the snapshot as well as the restore.** HEAD **`1276cc4`**, pushed, origin identical. **Migration ledger 57 — UNCHANGED for seven units.** Suite **2908 / 151**. Visual gate **28/28 at 0.0000%**. `CLAUDE.md` **109,135 of 150,000**. **Session Log at 9 entries — rollover CLEAR and NOT due at the next open: it fires when the live log stands at 10, so the NEXT close trips it and the open after that runs it.** Last archived 2026-09-01. In flight: nothing. CC is IDLE. ⚠ **Next: FIELD-CONTRACT leg 3, which opens on two rulings — and VERHIST is waiting on five more.***
