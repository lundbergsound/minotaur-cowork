# 🗺️ ROADMAP SYNC SHEET #2 — vs v2.46 · 2026-08-09 (AUG-9 STRAT CLOSE)

Sheet 2 of ≤2 under the sync convention — the cap is now REACHED. Reads WITH
Roadmap v2.46 AND Sheet #1 (the S3-G close); where they conflict, the later
sheet wins. **Fold path of record (Daniel-CONFIRMED, Aug 9):
the next roadmap delivery is the v3.0 SPLIT (forward Roadmap + Ledger v1.0),
folding v2.46 + Sheets #1–2 directly — no v2.47 is produced.**

---

## 1 · THE WORKFLOW-HOUSEKEEPING PROGRAM (Aug-9 Strat retrospective — all Daniel-confirmed)

- **GitHub connector — INSTALLED** (claude.ai project ← repo `.md` files +
  CLAUDE.md; evidence media deliberately excluded after the full-folder
  selection overran capacity 1631% — images remain per-gate uploads).
  **"Sync now" joins the pre-open protocol** (rides Arch Prompt v28).
- **Cowork Phase 2** (post-S3-G, pre-Sprint-4): Cowork project mounted on
  governing docs + `docs/reference/` ONLY; canonical governing-doc home =
  **`~/Developer/Minotaur-Cowork/`, its own git repository** (`git init` at
  creation; `git diff` before acceptance = the verbatim-immutability proof);
  CLAUDE.md stays upload-and-return; doc-production/close-package sessions
  run in Cowork, design gates and rulings stay in claude.ai chat; pilot =
  2–3 doc sessions, then a keep/expand/revert review. **ONE-WRITER-ON-THE-
  REPO rule (standing):** Cowork touches repo paths only when no CC session
  is open and the tree is clean; otherwise it writes to a non-repo staging
  folder and placement waits.
- **WF-1a — COMMIT PILOT (supersedes WF-1's no-commit clause for the pilot
  window):** CC stages by explicit filename and commits ONLY on Daniel's
  explicit terminal confirm phrase **`COMMIT APPROVED: <first words of the
  subject>`** (subject-bound — a bare phrase approves whatever is staged,
  the stale-approval class). `git push` remains Daniel's paste. No-trailer
  rule moves to the tested `commit-msg` hook (drafting-session deliverable);
  wiring = `core.hooksPath → .claude/hooks` after an empty-`.git/hooks`
  check; the chmod standing instruction widens to `.claude/hooks/*`. The
  bash-fence and the settings deny list lift `git commit` in the SAME
  execution pass and keep `git push` blocked in both — half-lifted is
  dead-on-arrival, asserted checkable in the housekeeping handoff. **Sunset
  review at Sprint 4 close, alongside the auto-mode rider.**
- **Multi-Leg Run Pattern — memorialized** (fold text drafted): Step-0
  disjoint-footprint map · full machine gate set per leg · per-leg boundary
  files · serial commits · the context-budget handover license stands ·
  Daniel browser gates batch by risk class (plumbing legs → end of run;
  UX-surface legs → before the next leg builds on them).
- **Evidence rules:** the Evidence Intake Protocol (Daniel uploads raw; Arch
  renames per convention + states destination in one line) · the **PDF
  evidence-class rule** — all v1↔v2 OUTPUT-parity evidence is print-to-PDF,
  100% scale, matching paper size/orientation both sides; screenshots valid
  for on-screen UX anatomy only. (Independently convergent with Sheet #1's
  renderer-of-record doctrine; the two rules read together.)
- **Prompt slate:** plain-language-first (>~4 sentences leads with a plain
  summary) + bullets for 3+ enumerations, all prompts · Model & Effort Check
  softens to state-and-proceed · every close package gains a **Docket Sweep**
  line (items touched · triggers gone live · nearest 3 dated items) ·
  **Strategy Prompt v14 DELIVERED at this close** · Arch Prompt v28 draft
  held for housekeeping install (with a WF-1a Commit-authority touch-up at
  install, not v29) · Comms v9→v10 rides the registered "Comms Prompt
  structure" open question.
- **Roadmap v3.0 split:** forward-only Roadmap (status ≤10 lines · sprint
  plan · ONE unified docket · open questions) + **Ledger v1.0** (own version
  line; cross-reference note at the split). Docket schema: ID · Status ·
  Owner · Trigger (+ description). **Status vocabulary confirmed:** OPEN ·
  GATED · AWAITING-RULING · IN-BUILD · BLOCKED — deliberately no DONE;
  completed items EXIT to the Ledger (the migration procedure states this in
  one line). UX Reference Catalog → Ledger, pointer stays forward.
- **CLAUDE.md relocation:** completed-unit architecture blocks →
  `docs/reference/arch-record/<unit>.md`; a compact **Guarded Architectures
  Index** (unit · commit · "do not undo or fork" · pointer) stays ambient;
  byte-accounting proof that content moved, never dropped. Its own gated CC
  errand inside the housekeeping session.
- **Project-knowledge prune — EXECUTED** (12 files Aug-9; + the two Aug-5
  S3-G draft files at this close, discharging the Sheet #1 §3 row).

## 2 · REGISTERED THIS SESSION

- **⌥⇧K COMMAND PALETTE unit** (Daniel-approved; unscheduled, Sprint-4-map
  candidate): ⌥⇧K trigger + top-bar keyboard-shortcut icon. Riders: Arch
  verifies ⌥⇧K against the chord registry pre-design; palette CONTENTS are
  each Parity-governed — the unit opens with its own ruling block. **The
  other four Aug-9 UI proposals (print-list drawer · selection action bar ·
  collapsible inspector · saved find sets) DENIED of record.**
- **Latency/optimistic unit — scope amendment:** mandatory first leg = a
  MEASUREMENT RECON (where time goes: database vs network vs rendering);
  the candidate menu behind it, purchased in evidence order: query collapse
  into RPCs · prefetch expansion ((kk) idiom) · index/view audit + the
  Vercel↔Supabase region pairing check (fold these two into one session) ·
  row virtualization (HIGH-CAUTION: fights sticky bands/focus/scroll
  machinery — recon-first) · payload trimming · cache persistence across
  reloads (TanStack persist) · bundle audit/code-splitting (cheapest
  first-pass) · streaming shell/skeletons (baseline-event interaction
  noted) · write coalescing (only if measurement shows chatter) · compute
  tier (after the audit, never instead of it). **Realtime push deliberately
  SHELVED to the real-time-collaboration unit** (its payoff is multi-user).
- **Aug-25 — RETIRED (Daniel-confirmed Aug 9):** the port date retires (Daniel,
  Aug-9: shop runs v1; import "when v2 is in a good spot"); the port gains a
  named criterion at the Sprint-4 map discussion, WHERE Sheet #1's
  cable-data-gap escalation is ruled in the same breath (equipment-only port
  + hand re-entry vs a cable-import extension unit). R8's deadline detaches
  from shop prep and re-pegs to the September attorney window (F4 feeds the
  addendum).

## 3 · SEQUENCE OF RECORD (post-S3-G)

1. **Owed-gates close-out session** (Arch principal; opener delivered at the
   Aug-9 Strat close): pre-open w/ push-verified `1055e76` → Sheet #1 §2
   owed gates (visual 16/16 FIRST) → the CLAUDE.md fold errand (§5 queue) →
   the three-PDF packet adjudication → closes into the housekeeping opener.
2. **Housekeeping session:** v3.0 migration (v2.46 + Sheets #1–2 folded
   directly) · CLAUDE.md relocation · WF-1a mechanics (hook + hooksPath +
   the paired fence/deny lift) · Arch Prompt v28 install (+ the WF-1a
   touch-up) · inputs = the drafting session's five files, Daniel-uploaded.
3. **Cowork Phase 2 pilot** (runbook executes; `~/Developer/Minotaur-Cowork/`
   seeded from confirmed-canonical copies).
4. **Sprint 4 planning, written ONCE into the v3.0 docket** (Daniel-confirmed),
   then Sprint 4 opens. OL25WR stock re-gate slots
   whenever Daniel reaches a printer, independent of this sequence.

## 4 · CORRECTIONS OF RECORD (this session)

- **Strat (Aug-9, owned):** the roadmap-restructure need (v2.46's four
  overlapping registers, the 2,000-word Status paragraph) was flagged by
  Daniel before Strat raised it — register hygiene was Strat's to own
  earlier; the v3.0 split + the Docket Sweep are the structural fix.
- **Strat (Aug-9, self-caught):** the S3-G close rider retired before
  delivery when Daniel serialized the close through the Strat session — the
  rider would have injected scope into a mid-build session against the
  compression instinct.
- **Not corrections:** the drafting session's fold-in surfaced an 11th open
  question (Ledger versioning) beyond its own numbered list — caught at
  Strat adjudication; the hook shipped pre-tested (four cases).

## 5 · ONE-WORD CONFIRMS — ALL FIVE CONFIRMED (Daniel, Aug 9 in-session)

1. **S3-G-2** → Sprint-4 basket. CONFIRMED (closes Sheet #1's twice-flagged ask).
2. **R8** → deferred-to-desk; deadline pre-September (attorney F4). CONFIRMED.
3. **Aug-25** → formally retired per §2. CONFIRMED.
4. **Fold path** → v3.0 folds v2.46 + Sheets #1–2 directly; no v2.47. CONFIRMED.
5. **Sprint-4 planning** → after housekeeping, into the v3.0 docket
   (amends Sheet #1 §6.2). CONFIRMED.

## 6 · NEXT ACTIONS

1. **Daniel in the air:** install this sheet + the Session Log + Strategy
   Prompt v14 · `mkdir` + `git init` + seed `~/Developer/Minotaur-Cowork/`.
   (Prune executed; five words confirmed.) **On the ground:** `git push` →
   `ls-remote` confirms `1055e76` (verified not-landed airborne; remote at
   `5616b89`).
2. **Next Arch open:** per §3.1 (the delivered opener).
3. **Standing:** ADM-1 waits on the sharing unit · attorney window September
   (now also R8's deadline) · the port at its named criterion.

*End of Sync Sheet #2 — the sheet cap is reached; the next roadmap delivery
is the v3.0 split.*
